
## Итог съёмки (2026-09-04)

Снято и смонтировано в режиме A: `~/Projects/Builds/demo-tools/ChooseReply-demo-2026-09-04.mp4` (1920×1080, 3:30,
английские подписи для ревьюера; сырые клипы и конвейер монтажа — в той же папке, README внутри). Тестовый номер
владельца подключён по QR, «Вместе» и «Авто» сняты на живых сообщениях, «Сводка» — на реальном исходе «Нужен
владелец», пейволл — с переходом в Safari на Terms и «Восстановить покупки». Бот «Автодеталь» после съёмки выключен
(тумблер WhatsApp off → Сохранить; workflow деактивирован). Осталось: выложить MP4 непубличной ссылкой и вписать её
в `app-review-notes.md` (`<VIDEO URL>`).

Ловушки симулятора на Intel-маке, чтобы не переоткрывать: плагин `unity.webp` без x86_64-среза (сборка libwebp 1.6.0
из исходников + fat-архивы в сгенерированном проекте), `ibtool` виснет на storyboard (перезапуск), симуляторный плеер
только development-вариант + URP MSAA 8× не поддерживается (`SimulatorRenderingGuard.cs` + define `DEMO_SIMULATOR`),
HID-ввод панели — только ASCII и прячет экранную клавиатуру (`ConnectHardwareKeyboard=false`), кириллица через
`LANG=en_US.UTF-8 simctl pbcopy` + «Вставить», DerivedData держать ВНЕ папки сборки (Unity обходит её после экспорта).
