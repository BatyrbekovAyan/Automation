---
phase: 01-polished-suggestions-panel-on-mock-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Chat/ISuggestionsProvider.cs
  - Assets/Scripts/Chat/SuggestionRequest.cs
  - Assets/Scripts/Chat/SuggestionResult.cs
  - Assets/Scripts/Chat/SuggestionItem.cs
  - Assets/Scripts/Chat/SuggestionStatus.cs
  - Assets/Scripts/Chat/SuggestionSequenceGuard.cs
  - Assets/Scripts/Chat/MockSuggestionsProvider.cs
  - Assets/Tests/Editor/Chat/SuggestionSequenceGuardTests.cs
  - Assets/Tests/Editor/Chat/MockSuggestionsProviderTests.cs
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "ISuggestionsProvider exists as a pure C# interface with no Unity/UWR/n8n references"
    - "MockSuggestionsProvider returns 4 ranked Russian replies best-first, including one deliberately long reply"
    - "MockSuggestionsProvider supports a steered re-cluster (different set given a picked reply)"
    - "MockSuggestionsProvider can emit an error result and an out-of-order/superseded result"
    - "SuggestionSequenceGuard discards stale-seq and chat-switched results, keeps current-seq same-chat results"
  artifacts:
    - path: "Assets/Scripts/Chat/ISuggestionsProvider.cs"
      provides: "DATA-01 provider seam"
      contains: "void Request("
    - path: "Assets/Scripts/Chat/MockSuggestionsProvider.cs"
      provides: "DATA-02 mock data source"
      contains: "ISuggestionsProvider"
    - path: "Assets/Scripts/Chat/SuggestionSequenceGuard.cs"
      provides: "DATA-03 monotonic-seq + chat-capture discard predicate"
      contains: "public static bool IsCurrent("
    - path: "Assets/Tests/Editor/Chat/SuggestionSequenceGuardTests.cs"
      provides: "guard discard/keep coverage"
      contains: "[Test]"
    - path: "Assets/Tests/Editor/Chat/MockSuggestionsProviderTests.cs"
      provides: "ranking/steer/error logic coverage"
      contains: "[Test]"
  key_links:
    - from: "Assets/Scripts/Chat/MockSuggestionsProvider.cs"
      to: "Assets/Scripts/Chat/ISuggestionsProvider.cs"
      via: "implements interface"
      pattern: "class MockSuggestionsProvider\\s*:\\s*ISuggestionsProvider"
    - from: "Assets/Scripts/Chat/MockSuggestionsProvider.cs"
      to: "callback"
      via: "WaitForSeconds latency then Invoke"
      pattern: "WaitForSeconds"
---

<objective>
Build the pure-C# heart of the feature: the `ISuggestionsProvider` seam (DATA-01), its value objects, a `MockSuggestionsProvider` carrying the entire Phase-1 demo (DATA-02 — Russian replies, simulated latency, steered re-cluster, adversarial out-of-order + error paths), and the `SuggestionSequenceGuard` discard predicate (DATA-03). Everything in this plan is headless-EditMode-testable logic — NO Unity UI, NO MonoBehaviour view code, and NO reference to n8n / UnityWebRequest / Wappi anywhere (the seam contract that lets Phase 2 swap in `N8nSuggestionsProvider` with zero UI edits).

Purpose: This is the load-bearing seam. The UI (Plan 03) and the controller (Plan 04) build against these types; the guard is the proven concurrency discipline reused from QuoteResolve/CrossChatResponseGuard. Keeping the source logic pure (not a MonoBehaviour) is what makes ranking/steer/error/guard behavior verifiable headlessly.
Output: 7 new source files + 2 EditMode test files, all compiling and the two test classes green.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-polished-suggestions-panel-on-mock-data/01-CONTEXT.md
@.planning/phases/01-polished-suggestions-panel-on-mock-data/01-RESEARCH.md
@.planning/phases/01-polished-suggestions-panel-on-mock-data/01-PATTERNS.md

<interfaces>
<!-- Contracts this plan CREATES. Plans 03 and 04 build against exactly these. Executor: produce these signatures verbatim. -->

ISuggestionsProvider (the seam — NO Unity/UWR/n8n types):
```csharp
public interface ISuggestionsProvider
{
    // steerTowardText null = fresh refresh; set = re-cluster (INT-04/D-01).
    // request.requestSeq rides through to result.requestSeq so the controller can reject stale (DATA-03).
    void Request(SuggestionRequest request, System.Action<SuggestionResult> callback);
}

[System.Serializable]
public class SuggestionRequest
{
    public string chatId;            // captured active chat (scoping)
    public string lastIncomingText;  // trigger message (INT-02) or null (manual/pick)
    public string steerTowardText;   // picked reply for re-cluster (INT-04/D-01); null = fresh set
    public long   requestSeq;        // monotonic; echoed back for the guard (DATA-03)
}

public class SuggestionResult
{
    public System.Collections.Generic.List<SuggestionItem> items; // ranked best-first (PANEL-03)
    public long requestSeq;          // echoed correlation id
    public SuggestionStatus status;  // Ok | Empty | Error (drives PANEL-04 states)
}

public class SuggestionItem { public string text; public string intentLabel; } // matches Phase-2 { text, label }
public enum SuggestionStatus { Ok, Empty, Error }
```

SuggestionSequenceGuard (pure static predicate, consumed by Plan 04 controller):
```csharp
public static bool IsCurrent(long resultSeq, long currentSeq, string capturedChatId, string currentChatId);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seam interface, value objects, and the sequence guard (with guard tests)</name>
  <files>Assets/Scripts/Chat/ISuggestionsProvider.cs, Assets/Scripts/Chat/SuggestionRequest.cs, Assets/Scripts/Chat/SuggestionResult.cs, Assets/Scripts/Chat/SuggestionItem.cs, Assets/Scripts/Chat/SuggestionStatus.cs, Assets/Scripts/Chat/SuggestionSequenceGuard.cs, Assets/Tests/Editor/Chat/SuggestionSequenceGuardTests.cs</files>
  <read_first>
    - Assets/Scripts/Chat/CrossChatResponseGuard.cs (L10-38 — the EXACT static-class single-bool-predicate shape to mirror; class declared `public static class CrossChatResponseGuard` at L10, predicate `public static bool IsForDifferentChat(...)` at L19, conservative "never discard on missing data" stance)
    - Assets/Tests/Editor/Chat/CrossChatResponseGuardTests.cs (the test-class shape to mirror: `using NUnit.Framework;`, `public class ...Tests`, `[Test]` methods, expression-bodied asserts)
    - Assets/Scripts/Chat/OutboxStore.cs (L1-70 — `[System.Serializable]` public-field DTO convention for value objects; the `OutboxEntry` nested DTO shape)
    - .planning/phases/01-polished-suggestions-panel-on-mock-data/01-PATTERNS.md (sections "ISuggestionsProvider.cs", "SuggestionRequest/Result/Item", "SuggestionSequenceGuard.cs")
    - .claude/rules/networking.md (the `FetchData(param, Action<T>)` callback convention — the seam follows it)
  </read_first>
  <behavior>
    SuggestionSequenceGuard.IsCurrent:
    - Test: newest seq + same chat → returns true. `IsCurrent(5, 5, "chatA@c.us", "chatA@c.us")` == true.
    - Test: older/out-of-order seq → returns false. `IsCurrent(4, 5, "chatA@c.us", "chatA@c.us")` == false.
    - Test: chat switched under the request → returns false. `IsCurrent(5, 5, "chatA@c.us", "chatB@c.us")` == false.
    - Test: both stale seq AND chat switched → returns false.
    - Test: equal seq with both chatIds null → returns true (conservative: don't discard on missing data, mirroring CrossChatResponseGuard's "anyForeign && !anyMatch" conservatism — null == null is "same chat").
  </behavior>
  <action>
    Create each class in its OWN .cs file (CONVENTIONS.md "each class lives in its own .cs file").

    1. `ISuggestionsProvider.cs` — copy the interface verbatim from the `<interfaces>` block above. The file MUST contain `void Request(` and MUST NOT contain any of: `UnityWebRequest`, `UnityEngine.Networking`, `wappi`, `n8n`, `X-N8N`. Add a top XML-doc comment stating "Pure C# seam. Nothing above this seam may reference n8n/Wappi/UnityWebRequest (ROADMAP §Phase 1 SC-5)."

    2. `SuggestionRequest.cs` — `[System.Serializable] public class SuggestionRequest` with exactly the four public fields from `<interfaces>`: `string chatId; string lastIncomingText; string steerTowardText; long requestSeq;`. Public fields, no behavior (DTO; JsonConvert-friendly for the Phase-2 swap).

    3. `SuggestionResult.cs` — `public class SuggestionResult` with `System.Collections.Generic.List<SuggestionItem> items; long requestSeq; SuggestionStatus status;`.

    4. `SuggestionItem.cs` — `public class SuggestionItem { public string text; public string intentLabel; }`. The `{ text, intentLabel }` shape mirrors Phase-2's `{ text, label }[]` contract (N8N-01).

    5. `SuggestionStatus.cs` — `public enum SuggestionStatus { Ok, Empty, Error }`.

    6. `SuggestionSequenceGuard.cs` — model EXACTLY on CrossChatResponseGuard.cs (static class, single pure bool predicate, no Unity types). Signature:
       ```csharp
       public static class SuggestionSequenceGuard
       {
           /// True only when this result is the newest request AND the chat hasn't changed.
           public static bool IsCurrent(long resultSeq, long currentSeq, string capturedChatId, string currentChatId)
           {
               if (resultSeq != currentSeq) return false;       // superseded / out-of-order
               if (capturedChatId != currentChatId) return false; // chat switched under us (string.Equals; null==null is same)
               return true;
           }
       }
       ```
       Use `!string.Equals(capturedChatId, currentChatId, System.StringComparison.Ordinal)` for the chat compare so `null == null` is treated as same-chat (conservative). NO `using UnityEngine;`.

    7. `SuggestionSequenceGuardTests.cs` — model on CrossChatResponseGuardTests.cs. `using NUnit.Framework; public class SuggestionSequenceGuardTests { ... }`. One `[Test]` per behavior bullet above; expression-bodied where it reads clean (e.g. `[Test] public void OlderSeq_IsDiscarded() => Assert.IsFalse(SuggestionSequenceGuard.IsCurrent(4, 5, "chatA@c.us", "chatA@c.us"));`).

    Per .claude/rules/csharp-quality + unity-general: methods <30 lines, explicit null handling, no async/await. NO `UnityEngine.Networking` in any of these files.
  </action>
  <verify>
    <automated>grep -L "UnityWebRequest\|UnityEngine.Networking\|wappi\|n8n\|X-N8N" Assets/Scripts/Chat/ISuggestionsProvider.cs Assets/Scripts/Chat/SuggestionSequenceGuard.cs | wc -l | grep -q 2 && grep -q "void Request(" Assets/Scripts/Chat/ISuggestionsProvider.cs && grep -q "public static bool IsCurrent(" Assets/Scripts/Chat/SuggestionSequenceGuard.cs && echo OK</automated>
    <automated>Tools/run-tests-headless.sh "SuggestionSequenceGuardTests" (exit 0 = green) — OR if Editor open: echo "SuggestionSequenceGuardTests" > Temp/claude/run-tests.trigger then read Temp/claude/test-summary.json for "overall":"Passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "void Request(" Assets/Scripts/Chat/ISuggestionsProvider.cs` succeeds AND `grep -L "UnityWebRequest" Assets/Scripts/Chat/ISuggestionsProvider.cs` returns the path (forbidden token absent).
    - `grep -q "public static bool IsCurrent(" Assets/Scripts/Chat/SuggestionSequenceGuard.cs` succeeds.
    - `grep -q "\[System.Serializable\]" Assets/Scripts/Chat/SuggestionRequest.cs` succeeds.
    - `grep -q "enum SuggestionStatus" Assets/Scripts/Chat/SuggestionStatus.cs` succeeds and it contains `Ok`, `Empty`, `Error`.
    - `SuggestionSequenceGuardTests` runs exit 0 (green) via the project headless runner or test bridge.
  </acceptance_criteria>
  <done>Seam interface, 4 value-object files, the static guard, and a green guard test suite all exist; no forbidden tokens above the seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MockSuggestionsProvider (RU content, latency, steered re-cluster, adversarial + error paths) + tests</name>
  <files>Assets/Scripts/Chat/MockSuggestionsProvider.cs, Assets/Tests/Editor/Chat/MockSuggestionsProviderTests.cs</files>
  <read_first>
    - Assets/Scripts/Chat/ISuggestionsProvider.cs (the seam created in Task 1 — implement it)
    - Assets/Scripts/Main/ChatManager.QuoteResolve.cs (L82-153 — the serial coroutine fetch + `yield return new WaitForSeconds`/capture pattern to mirror for LATENCY ONLY; do NOT copy any UnityWebRequest call)
    - Assets/Tests/Editor/Chat/OutboxStoreTests.cs (L1-30 — the `[SetUp]`/factory test shape; assert pure logic)
    - .planning/phases/01-polished-suggestions-panel-on-mock-data/01-RESEARCH.md (§"Mock Provider" / Pattern about plain-C# + runner, A4; Pitfall 2 — never touch `_chatFetchesInFlight`)
    - .planning/phases/01-polished-suggestions-panel-on-mock-data/01-PATTERNS.md (section "MockSuggestionsProvider.cs")
    - .planning/phases/01-polished-suggestions-panel-on-mock-data/01-UI-SPEC.md (Copywriting Contract — RU intent label set; PANEL-06 long-reply requirement)
  </read_first>
  <behavior>
    Pure logic under test (latency excluded — the test calls BuildResult directly, NOT the coroutine):
    - Test: a fresh request (`steerTowardText == null`) BuildResult returns `status == Ok` with exactly 4 items, ordered best-first, item[0] is the "recommended" lead.
    - Test: one of the 4 items has reply `text.Length > 120` (the deliberately-long RU reply for PANEL-06 truncation).
    - Test: every item has a non-empty `intentLabel` drawn from the RU set {«Приветствие», «Цена», «Наличие», «Запись», «Вежливый отказ»}.
    - Test: a steered request (`steerTowardText` set to a card's reply) returns a DIFFERENT ordered set than the unsteered request (re-cluster toward the pick) — assert the two `items[0].text` differ OR the ordering differs.
    - Test: the error trigger produces `status == Error` (and items null/empty); the empty trigger produces `status == Empty`.
    - Test: the echoed `result.requestSeq` equals the `request.requestSeq` passed in (correlation echo for DATA-03).
    - Test: the adversarial/out-of-order toggle, when enabled, can produce a result whose `requestSeq` is LOWER than the latest issued (simulating a late superseded reply) — assert the provider is capable of emitting a stale seq so the controller guard is exercised.
  </behavior>
  <action>
    Create `MockSuggestionsProvider.cs` as a PLAIN C# class (NOT a MonoBehaviour) implementing `ISuggestionsProvider` — `public class MockSuggestionsProvider : ISuggestionsProvider`. This keeps ranking/steer/error logic EditMode-testable (RESEARCH A4; PATTERNS anti-pattern: never a MonoBehaviour with hard-coded logic).

    Structure:
    - Constructor takes a `UnityEngine.MonoBehaviour runner` and a `float latencySeconds = 1.0f`: `public MockSuggestionsProvider(MonoBehaviour runner, float latencySeconds = 1.0f)`. The runner starts the latency coroutine; the runner is the ONLY Unity dependency and is irrelevant to the pure logic tests.
    - `public void Request(SuggestionRequest request, System.Action<SuggestionResult> callback)` → calls `_runner.StartCoroutine(RespondAfterLatency(request, callback))`.
    - `private IEnumerator RespondAfterLatency(SuggestionRequest req, System.Action<SuggestionResult> cb)`:
      ```csharp
      yield return new UnityEngine.WaitForSeconds(_latencySeconds);  // skeleton genuinely exercised (D-15)
      cb?.Invoke(BuildResult(req));                                   // BuildResult = pure, testable
      ```
      NEVER touch `_chatFetchesInFlight` (Pitfall 2 — the mock makes no Wappi call). NO `UnityWebRequest`/`n8n`/`wappi`.
    - `public SuggestionResult BuildResult(SuggestionRequest req)` — PUBLIC so tests call it directly without latency. Pure: builds the ranked RU set, applies steer, echoes `req.requestSeq`, returns Ok/Empty/Error per the active mock mode.

    Mock content (D-14, RU verbatim — small-business CIS chat). Provide at least these intents, with item[0] as the recommended lead:
    - «Приветствие»: e.g. `"Здравствуйте! Спасибо за обращение. Чем могу помочь?"`
    - «Цена»: e.g. `"Стоимость зависит от объёма заказа. Подскажите, что именно вас интересует?"`
    - «Наличие»: e.g. `"Да, товар есть в наличии. Могу оформить для вас прямо сейчас."`
    - «Запись»: the DELIBERATELY LONG reply (>120 chars, PANEL-06), e.g. `"Конечно, давайте подберём удобное для вас время. У нас есть свободные слоты на этой неделе в первой половине дня и ближе к вечеру — подскажите, какой день вам подходит, и я сразу забронирую запись на ваше имя."`
    - «Вежливый отказ»: e.g. `"К сожалению, сейчас это направление мы не обслуживаем, но буду рад помочь с другими вопросами."`

    Steered re-cluster (INT-04/D-01): when `req.steerTowardText` is non-null, return a re-ranked/re-worded set biased toward that pick (a deterministic transform is fine for the mock — e.g. promote the same-intent variants and reorder so `items[0].text` differs from the unsteered lead). Document with a comment that Phase-2 replaces this with the n8n "steer toward" field (N8N-03).

    Adversarial + error modes (D-15) — expose simple controls the controller/tests can flip:
    - `public bool simulateError;` → BuildResult returns `status = SuggestionStatus.Error`, `items = null`.
    - `public bool simulateEmpty;` → returns `status = SuggestionStatus.Empty`, `items = new List<SuggestionItem>()`.
    - `public bool simulateOutOfOrder;` → expose a method or behavior that can deliver a callback echoing a LOWER `requestSeq` than the latest (the deliberately-superseded path). Simplest: a `public void RequestStale(SuggestionRequest request, long staleSeqOverride, System.Action<SuggestionResult> callback)` test hook, OR have `BuildResult` honor a settable `_forcedEchoSeq`. Keep it test-reachable.

    Create `MockSuggestionsProviderTests.cs` — model on OutboxStoreTests shape, `using NUnit.Framework;`. Construct `new MockSuggestionsProvider(null)` (runner unused by BuildResult) and assert against `BuildResult(...)` per every behavior bullet. Do NOT call the coroutine (no latency in tests). Use a factory `private static SuggestionRequest Req(long seq = 1, string steer = null) => new SuggestionRequest { chatId = "c1@c.us", requestSeq = seq, steerTowardText = steer };`.

    Per csharp-quality: methods <30 lines (split BuildResult into a `BuildRankedSet()` + `ApplySteer()` if needed), string interpolation, explicit null handling.
  </action>
  <verify>
    <automated>grep -q "class MockSuggestionsProvider" Assets/Scripts/Chat/MockSuggestionsProvider.cs && grep -q "ISuggestionsProvider" Assets/Scripts/Chat/MockSuggestionsProvider.cs && grep -q "WaitForSeconds" Assets/Scripts/Chat/MockSuggestionsProvider.cs && ! grep -q "UnityWebRequest\|_chatFetchesInFlight\|wappi\|n8n" Assets/Scripts/Chat/MockSuggestionsProvider.cs && echo OK</automated>
    <automated>Tools/run-tests-headless.sh "MockSuggestionsProviderTests" (exit 0 = green) — OR Editor-open bridge</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class MockSuggestionsProvider\s*:\s*ISuggestionsProvider" Assets/Scripts/Chat/MockSuggestionsProvider.cs` (implements seam; whitespace-tolerant).
    - `grep -q "WaitForSeconds" Assets/Scripts/Chat/MockSuggestionsProvider.cs` (latency sim present).
    - `grep -c "_chatFetchesInFlight\|UnityWebRequest\|wappi\|n8n" Assets/Scripts/Chat/MockSuggestionsProvider.cs` returns 0 (Pitfall 2 + seam contract).
    - The mock file contains at least one RU reply string with `.Length > 120` (long-reply for PANEL-06) — verify by reading the file; one Cyrillic reply string must visibly exceed ~120 chars.
    - `MockSuggestionsProviderTests` runs exit 0 (green), covering: 4-item Ok set, best-first, long reply present, steered set differs, error path, empty path, seq echo.
  </acceptance_criteria>
  <done>The mock implements the seam with coroutine latency, ranked RU content (incl. one long reply), steered re-cluster, and error/empty/out-of-order paths; its pure logic is green under EditMode tests; nothing references the gate, Wappi, n8n, or UnityWebRequest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan adds no network, no auth, no secret, no untrusted-input parser. The mock data is hard-coded in-repo Russian strings; the seam is a pure in-process C# interface. No data crosses a process or trust boundary in Phase 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | MockSuggestionsProvider hard-coded content | accept | Content is static developer-authored RU strings compiled into the assembly; not user-supplied, not persisted, not network-sourced. No tampering surface in Phase 1. (Phase 2's N8N-04 schema/injection hardening covers the live path — explicitly out of this phase.) |
| T-01-02 | Information Disclosure | seam value objects | accept | No PII, no secrets, no tokens flow through `SuggestionRequest`/`SuggestionResult`; `lastIncomingText` is chat text already resident in memory. No new disclosure path. |
| T-01-03 | Denial of Service | latency coroutine | accept | `WaitForSeconds` runs on the runner the controller owns; bounded single-shot, no unbounded loop, no external resource. Low-value local-only target. |

Honest assessment: backend-free, no-network, no-secrets, local-only logic. No threat warrants a mitigation beyond accepting the minimal surface. ASVS L1 has no applicable control for a hard-coded in-memory mock with no boundary crossing.
</threat_model>

<verification>
- `grep -rL "UnityWebRequest\|UnityEngine.Networking\|wappi\|n8n\|X-N8N" Assets/Scripts/Chat/ISuggestionsProvider.cs Assets/Scripts/Chat/MockSuggestionsProvider.cs Assets/Scripts/Chat/SuggestionSequenceGuard.cs` — all three files free of forbidden tokens (seam contract, ROADMAP SC-5).
- `Tools/run-tests-headless.sh "Suggestion|MockSuggestions"` exits 0 (both new test classes green). If a brand-new test type is "not found" (Unity new-file import quirk, RESEARCH Pitfall 4) → delete the `.cs` + `.meta`, let Unity register the deletion, recreate, re-run.
- All files compile into `Assembly-CSharp` (source) / `Assembly-CSharp-Editor` (tests) with no errors; `validate-cs.sh` passes.
</verification>

<success_criteria>
- DATA-01: `ISuggestionsProvider` exists as a pure C# callback seam with no n8n/Wappi/UWR reference.
- DATA-02: `MockSuggestionsProvider` returns realistic ranked RU stub data with simulated latency, steered re-cluster, and adversarial/error paths.
- DATA-03 (logic half): `SuggestionSequenceGuard.IsCurrent` correctly discards stale-seq and chat-switched results; covered green by tests. (The controller that drives it lands in Plan 04.)
- Both EditMode test classes are green via the project test harness.
</success_criteria>

<output>
After completion, create `.planning/phases/01-polished-suggestions-panel-on-mock-data/01-01-SUMMARY.md`
</output>
