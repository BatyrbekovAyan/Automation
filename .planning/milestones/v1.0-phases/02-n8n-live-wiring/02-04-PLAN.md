---
phase: 02-n8n-live-wiring
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified: []
autonomous: false
requirements: [N8N-01, N8N-02, N8N-03, N8N-04]
# files_modified empty: verification + human checkpoint only, no production code changes.
# autonomous false: contains a checkpoint:human-verify (device pass — the milestone gate).

must_haves:
  truths:
    - "The seam is live: SuggestionsController instantiates N8nSuggestionsProvider and no Phase-1 UI file changed except that one line"
    - "The dev workflow is active and reachable at the base URL the app is pointed at"
    - "On device: toggling «Вместе» shows the skeleton then 4 real LLM cards (distinct moves, ranked, Recommended badge on card 1)"
    - "On device: an incoming customer message refreshes the cards without overwriting an in-progress composer draft"
    - "On device: tapping a card loads it into the composer (never auto-sends) and produces a fresh steered set of 4"
    - "On device: airplane mode → error state; disabling airplane mode + refresh recovers"
  artifacts:
    - path: ".planning/phases/02-n8n-live-wiring/02-04-SUMMARY.md"
      provides: "Device UAT record + seam-invariant proof"
  key_links:
    - from: "app DevN8nBaseUrl (runtime-editable)"
      to: "dev n8n /webhook/SuggestReplies (active)"
      via: "Profile → Edit dev n8n URL / tunnel"
      pattern: "SuggestReplies"
---

<objective>
Prove the live path end-to-end from the real app and close the milestone gate. First an autonomous pre-flight: re-assert the zero-Phase-1-UI-edit invariant, confirm the swap is in place, and smoke the committed dev workflow. Then the human device pass (the final gate) — the owner toggles «Вместе» in a real chat and sees real LLM suggestions, incoming-refresh, pick→composer+steer, and airplane-mode error→recover.

Purpose: Roadmap SC-1 requires the owner to see real n8n/LLM suggestions with no Phase-1 UI touched; SC-2/3/4 require the steer loop, the correlation/drain guards, and graceful malformed/injection handling — all verified through the actual client, not just curl.
Output: A device UAT record (`02-04-SUMMARY.md`) with the owner's GREEN, or a gap list to route to `/gsd-plan-phase --gaps`.
Depends on Plan 02 (client + swap) and Plan 03 (hardened workflow).
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-n8n-live-wiring/02-CONTEXT.md
@docs/superpowers/specs/2026-07-10-live-reply-suggestions-design.md
@.planning/phases/02-n8n-live-wiring/02-01-SUMMARY.md
@.planning/phases/02-n8n-live-wiring/02-02-SUMMARY.md
@.planning/phases/02-n8n-live-wiring/02-03-SUMMARY.md
# How the app resolves/overrides the n8n base URL at runtime (dev tunnel):
@docs/superpowers/specs/2026-06-30-runtime-editable-n8n-url-design.md

<interfaces>
<!-- The zero-edit Phase-1 set (N8N-02). Among these, ONLY SuggestionsController.cs may change, and by exactly one line (L31). -->
Assets/Scripts/Chat/ISuggestionsProvider.cs
Assets/Scripts/Chat/SuggestionRequest.cs
Assets/Scripts/Chat/SuggestionResult.cs
Assets/Scripts/Chat/SuggestionItem.cs
Assets/Scripts/Chat/SuggestionStatus.cs
Assets/Scripts/Chat/SuggestionSequenceGuard.cs
Assets/Scripts/Chat/SemiAutoStore.cs
Assets/Scripts/Chat/MockSuggestionsProvider.cs
Assets/Scripts/UI/SemiAutoToggle.cs
Assets/Scripts/UI/SuggestionCard.cs
Assets/Scripts/UI/SuggestionsPanel.cs
Assets/Scripts/Main/ChatManager.Suggestions.cs
Assets/Editor/SuggestionsControllerWirer.cs
Assets/Editor/SuggestionsPanelBuilder.cs
Assets/Scripts/Chat/SuggestionsController.cs   # only L31 may differ (the provider swap)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — seam-invariant proof + live workflow smoke + device test script</name>
  <files>(read-only verification: git diff over the zero-edit set; curl to dev; writes only the device script into 02-04-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/02-n8n-live-wiring/02-01-SUMMARY.md and 02-03-SUMMARY.md (dev webhook URL, active status, RAG-on-dev vs deferred)
    - .planning/phases/02-n8n-live-wiring/02-02-SUMMARY.md (confirmation the SuggestionsController diff was a single line)
    - docs/superpowers/specs/2026-06-30-runtime-editable-n8n-url-design.md (how to point the app at the dev tunnel via Profile → Edit dev n8n URL, or Tools/n8n/rotate-tunnel.py)
    - docs/superpowers/specs/2026-07-10-live-reply-suggestions-design.md (§Testing → Device pass steps; §Failure & edge behavior)
  </read_first>
  <action>
    Autonomous pre-flight before handing the device to the owner:

    1. **Seam-invariant proof (N8N-02).** Run `git diff --name-only` for the phase and confirm that among the zero-edit Phase-1 set (see &lt;interfaces&gt;), ONLY `Assets/Scripts/Chat/SuggestionsController.cs` appears, and its diff is exactly one line (the L31 provider swap). Concretely: assert `SuggestionsController.cs` contains `_provider = new N8nSuggestionsProvider();` and NOT `new MockSuggestionsProvider(this`; and that none of the other listed files appear in the phase's changed-files set. If any other Phase-1 UI/seam file changed → STOP and report a seam-breach defect (do NOT proceed to the device pass).

    2. **Live workflow smoke.** Resolve the dev base URL from the SUMMARYs and confirm the workflow is active + reachable: POST a minimal valid request to `${N8N_DEV}/webhook/SuggestReplies` and confirm a 200 with a 4-item distinct-enum success payload (echoing requestSeq). This proves the app's target endpoint is live before the owner tests.

    3. **Point the app at dev.** Ensure the app's runtime n8n base URL (`DevN8nBaseUrl`) is set to the reachable dev tunnel URL so the device build hits the dev workflow (Profile → Edit dev n8n field, or the tunnel rotation tool). Record the exact URL used in the SUMMARY.

    4. **Produce the exact device test script** for Task 2 (the owner follows it verbatim) — the 4 device scenarios in Task 2 with expected outcomes.
  </action>
  <verify>
    <automated>grep -q "new N8nSuggestionsProvider()" Assets/Scripts/Chat/SuggestionsController.cs && ! grep -q "new MockSuggestionsProvider(this" Assets/Scripts/Chat/SuggestionsController.cs && curl -sS -X POST "${N8N_DEV:-http://localhost:5678}/webhook/SuggestReplies" -H 'Content-Type: application/json' -d '{"v":1,"requestSeq":900,"chatId":"7701@c.us","botWaId":"-1","businessTypeId":"","businessName":"Тест","ownerPrompt":"","catalog":"• Товар — 100 ₸","steerTowardText":null,"lastIncomingText":"привет","messages":[{"role":"client","text":"привет","ts":1720600000}]}' | head -c 300</automated>
  </verify>
  <acceptance_criteria>
    - `SuggestionsController.cs` contains `new N8nSuggestionsProvider()` and no `new MockSuggestionsProvider(this`.
    - No Phase-1 file from the zero-edit &lt;interfaces&gt; set (other than SuggestionsController.cs) appears in the phase's `git diff --name-only`.
    - The live smoke POST returns 200 with `"v":1`, `"requestSeq":900`, and 4 distinct enum-labeled suggestions.
    - The exact dev base URL the app is pointed at is recorded in the SUMMARY.
    - The device test script (4 scenarios below) is written out for the owner.
  </acceptance_criteria>
  <done>Seam invariant proven, the app's target dev workflow is live and returns a valid set, and the device script is ready.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Device pass — live suggestions UAT (milestone gate)</name>
  <files>(none — human verification on device; the owner exercises the running app)</files>
  <action>
    Hand the owner the device test script from Task 1 and pause for their verification. This is a human-only checkpoint — Claude performs no code changes here; it presents what was built and the exact steps, then waits for the resume signal. Present the &lt;what-built&gt; and &lt;how-to-verify&gt; below; do not proceed until the owner responds.
  </action>
  <what-built>
    The Phase-1 «Вместе» suggestions panel now sources 4 ranked reply cards LIVE from the n8n Suggest Replies workflow (gpt-4o-mini), swapped in behind the seam with zero UI changes. The provider runs a serial-guarded POST on ChatManager.Instance, drains in-flight chat fetches first, and the Phase-1 seq/chat guard discards stale results. The workflow is hardened against prompt injection and grounds prices only in the catalog/RAG.
  </what-built>
  <how-to-verify>
    Build/run on your device (or the Editor Play mode with a real bot + open chat). The app's dev n8n URL is pointed at the active Suggest Replies workflow (see the pre-flight SUMMARY). Perform:

    1. **Toggle → live cards.** Open a WhatsApp chat on an authed bot, flip «Вместе» ON. Expect: the skeleton loading state, then 4 cards within ~3–4 s — each a DIFFERENT move (labels from «Ответ»/«Уточнить»/«Вариант»/«К заказу»/«Отложить»/«Отказ»), ranked best-first, "Recommended" badge on card 1 only, no numeric %. Text should read like a real owner (RU/KZ), grounded in your bot's catalog where relevant.
    2. **Incoming refresh.** Have the customer send a new message (or simulate one) while the panel is open. Expect: the cards refresh to fit the new message. Then type a draft in the composer and trigger another incoming — your in-progress draft must NOT be overwritten.
    3. **Pick → composer + steer.** Tap a card. Expect: its text loads into the composer to edit (it does NOT auto-send), and a FRESH set of 4 appears re-clustered toward your pick. Edit and send via the normal Send button to confirm the hand-off.
    4. **Airplane mode → error → recover.** Turn on airplane mode, trigger a refresh. Expect: the panel's error state (no raw JSON, no crash). Turn airplane mode off and manually refresh. Expect: cards return.
    5. **(Optional) Rapid picks / chat switch.** Pick several cards quickly, then switch chats mid-load. Expect: no stale or crossed cards ever render in the wrong chat.

    Report any card that shows an invented price not in your catalog, any raw error text, any auto-send, any stale/crossed set, or any jank.
  </how-to-verify>
  <verify>Owner exercises the running app per the 4 (+1 optional) scenarios; the resume signal captures the result.</verify>
  <done>Owner replies "approved" (all scenarios pass) — or a precise gap list is captured for --gaps closure.</done>
  <resume-signal>Type "approved" if all scenarios pass, or describe the specific failures (which scenario, what you saw vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live n8n response → running app UI | The real client renders live LLM output; transport/malformed/injected cases must degrade gracefully, never raw. |
| owner action → send | Only the owner's explicit Send delivers a message; suggestions never auto-send. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering (seam breach) | Phase-1 UI integrity | mitigate | Pre-flight `git diff` invariant (Task 1) blocks the device pass unless only SuggestionsController L31 changed. |
| T-02-13 | Tampering | transport failure surfaces raw / crashes | mitigate | Device scenario 4 (airplane mode) confirms the Error state renders (no raw JSON) and refresh recovers. |
| T-02-14 | Elevation / trust core | a suggestion auto-sends | mitigate | Device scenario 3 confirms tap → composer only; only the owner's Send delivers (owner-in-the-loop by design). |
| T-02-09 | Tampering | stale/crossed cards across rapid picks + chat switch | mitigate | Device scenario 5 exercises the Phase-1 drain + seq/chat guard end-to-end on the live path. |
</threat_model>

<verification>
- Autonomous: seam invariant proven + live workflow smoke returns a valid set (Task 1).
- Human: all device scenarios pass with the owner's GREEN (Task 2); no invented prices, no raw errors, no auto-send, no stale/crossed sets.
</verification>

<success_criteria>
- Roadmap SC-1: owner sees real n8n/LLM suggestions with zero Phase-1 UI code touched (invariant proven).
- SC-2: pick → fresh steered set of 4.
- SC-3: drain + correlation/chat guards hold on the live path (rapid picks / chat switch never cross).
- SC-4: malformed/injection/transport handled gracefully (error state, refresh recovers) — never raw.
</success_criteria>

<output>
After completion, create `.planning/phases/02-n8n-live-wiring/02-04-SUMMARY.md` (summary template) with the device UAT record (per-scenario pass/fail + the owner's verbatim signal) and the dev URL used. If the owner reports gaps, list them precisely so `/gsd-plan-phase --gaps` can close them.
</output>
