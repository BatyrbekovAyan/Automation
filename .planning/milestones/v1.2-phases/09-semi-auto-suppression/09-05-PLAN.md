---
phase: 09-semi-auto-suppression
plan: 05
type: execute
wave: 3
depends_on: [09-03, 09-04]
files_modified:
  - .planning/phases/09-semi-auto-suppression/09-HUMAN-UAT.md
autonomous: false
requirements: [SUP-03, SUP-05]
must_haves:
  truths:
    - "On WhatsApp, a «Вместе» chat gets NO auto-reply and stays unread while the suggestions panel still populates; «Авто» restores auto-replies"
    - "A bot-wide «Вместе» default suppresses a never-opened chat via the '*' row"
    - "The same suppress/restore behavior holds on a Telegram-authed bot"
    - "A never-toggled chat replies normally (absence -> reply); the «Бот работает/пауза» switch is unchanged"
  artifacts:
    - path: ".planning/phases/09-semi-auto-suppression/09-HUMAN-UAT.md"
      provides: "the 5-scenario owner e2e runbook + recorded verdicts"
      contains: "Вместе"
  key_links:
    - from: "app «Вместе» toggle"
      to: "no auto-reply, stays unread"
      via: "SetReplyMode write -> gate suppression, both channels"
      pattern: "unread"
---

<objective>
The phase-closing HUMAN-UAT gate: the owner exercises the whole suppression feature end-to-end on ONE build across both channels (the 5-scenario checklist from the approved design), proving the observable behavior — a «Вместе» chat stands down (no reply, stays unread) while suggestions still work, «Авто» restores replies, the bot-wide '*' default suppresses a never-opened chat, and a never-toggled chat replies normally. This is the only verification Claude cannot run (live dev n8n + tunnel + real WhatsApp/Telegram profiles + a device build). Structural + curl + runData checks already passed in 09-04; this is behavioral confirmation.

Purpose: Success criteria 1-2 of the ROADMAP Phase 9 goal are user-observable and can only be confirmed with a real customer message on a real device.
Output: `.planning/phases/09-semi-auto-suppression/09-HUMAN-UAT.md` with recorded pass/fail verdicts; on all-PASS the phase closes.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Design (Testing section = the e2e scenarios) + the breakdown's owner checklist (Task 7 Step 2)
@docs/superpowers/specs/2026-07-13-semi-auto-suppression-flag-design.md
@docs/superpowers/plans/2026-07-13-semi-auto-suppression-flag.md
@.planning/phases/09-semi-auto-suppression/09-CONTEXT.md

<environment_notes>
- USER-ASSISTED: dev n8n (localhost:5678) + tunnel (Tools/n8n/rotate-tunnel.py) + a real authorized WhatsApp profile AND a real authorized Telegram profile. `secrets.json` deny-ruled for Claude.
- The per-bot reply-workflow clone must be ACTIVE only during the test window and DEACTIVATED immediately after (real contacts). Confirm the 09-04 test clones were already deactivated; deactivate any clone activated here.
- Prod bagkz stays DORMANT — this whole phase folds into the future one-shot bulk copy (SUP-05).
- The «Бот работает/пауза» activation switch is the real n8n activate/deactivate and must stay UNTOUCHED — verify it still works independently of «Авто/Вместе».
</environment_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 09-HUMAN-UAT.md owner e2e runbook</name>
  <files>.planning/phases/09-semi-auto-suppression/09-HUMAN-UAT.md</files>
  <read_first>
    - docs/superpowers/plans/2026-07-13-semi-auto-suppression-flag.md (Task 7 Step 2 — the 5 owner scenarios to transcribe)
    - docs/superpowers/specs/2026-07-13-semi-auto-suppression-flag-design.md (Testing section — "Human dev e2e")
  </read_first>
  <action>
Write `.planning/phases/09-semi-auto-suppression/09-HUMAN-UAT.md` as an owner-run checklist. Include a pre-flight block (dev n8n up, tunnel up, both templates redeployed with the gate per 09-04, the test bot's reply-workflow clone ACTIVATED for the window, real WhatsApp + Telegram profiles authed) and a post-run block (DEACTIVATE the clone; note prod stays dormant). Each scenario has an explicit expected result and a PASS/FAIL line to fill:

1. **WhatsApp suppress** — flip a specific chat to «Вместе» → have the customer send a message. EXPECT: bot does NOT auto-reply; the chat stays UNREAD (unread badge shows); the suggestions panel still populates in that chat. Sub-check (re-assert heal): close and re-open the chat → it is still «Вместе»/suppressed (a lost "back to Авто" write self-heals on open).
2. **WhatsApp restore** — flip the same chat back to «Авто» → customer sends again. EXPECT: bot auto-replies normally.
3. **Bot-wide default** — set the BOT default to «Вместе» (the toggle on the bots-list card) → a chat that has NEVER been opened receives an incoming message. EXPECT: NOT auto-replied (the `'*'` default row suppresses it).
4. **Telegram suppress/restore** — repeat scenarios 1-2 on a Telegram-authed bot (same behavior; the gate keys on the Telegram profile id).
5. **Fail-closed / absence sanity** — with the flag DB reachable, a never-toggled chat on a «Авто»-default bot receives a message. EXPECT: bot replies normally (absence of any row → `suppressed=false` → reply; the never-toggled common case is never silenced). Also confirm the «Бот работает/пауза» switch still pauses/resumes the bot independently of «Авто/Вместе» (it must be untouched).

Add a verdict table (scenario | channel | expected | result | notes) and a final disposition line (ALL PASS → phase closes → `/gsd-secure-phase 09`; any FAIL → gap round).
  </action>
  <verify>
    <automated>test -f .planning/phases/09-semi-auto-suppression/09-HUMAN-UAT.md && grep -qi "Вместе" .planning/phases/09-semi-auto-suppression/09-HUMAN-UAT.md && grep -qi "unread\|непрочит" .planning/phases/09-semi-auto-suppression/09-HUMAN-UAT.md && grep -qi "Telegram" .planning/phases/09-semi-auto-suppression/09-HUMAN-UAT.md && echo UAT_DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/09-semi-auto-suppression/09-HUMAN-UAT.md` exists
    - It contains all 5 scenarios, each with an explicit EXPECT and a PASS/FAIL field
    - It includes the re-assert-on-open sub-check (scenario 1) and the "«Бот работает/пауза» untouched" sub-check (scenario 5)
    - It has a pre-flight (clone ACTIVE for window) + post-run (clone DEACTIVATED; prod dormant) block
    - It names both channels (WhatsApp + Telegram) and the bot-wide '*' default case
  </acceptance_criteria>
  <done>The owner runbook exists with all 5 scenarios, expected results, the heal + activation-switch sub-checks, and the clone activate/deactivate discipline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner runs the 5-scenario e2e on one build (SUP-03, SUP-05)</name>
  <files>.planning/phases/09-semi-auto-suppression/09-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/09-semi-auto-suppression/09-HUMAN-UAT.md (the runbook authored in Task 1)
    - .planning/phases/09-semi-auto-suppression/09-04-SUMMARY.md (confirm the live server side + gate passed structurally before behavioral e2e)
  </read_first>
  <what-built>
Everything: the `reply_mode_flags` table (live, 09-04), the Set Reply Mode webhook (deployed, 09-04), the fail-closed gate in both templates (runData-verified, 09-04), and the Unity client that writes the flag on toggle/default/re-open (09-03, in this build). This gate confirms the user-observable behavior a customer would experience.
  </what-built>
  <action>
Owner installs the build (with the 09-03 client changes), brings dev n8n + tunnel up, activates the test bot's reply-workflow clone for the window, and runs all 5 scenarios in `09-HUMAN-UAT.md` on BOTH channels. Fill in each PASS/FAIL + notes. Verify the suggestions panel still populates in a suppressed chat (the whole point — «Вместе» = no auto-reply BUT suggestions work). Confirm re-opening a suppressed chat keeps it suppressed (heal). Confirm the «Бот работает/пауза» switch still works independently. DEACTIVATE the clone when done.
  </action>
  <how-to-verify>
Scenario 1: no reply + unread badge + suggestions populate. Scenario 2: reply returns. Scenario 3: never-opened chat not replied (the '*' default). Scenario 4: same on Telegram. Scenario 5: never-toggled chat replies (absence→reply) and the activation switch pauses/resumes independently. All 5 PASS → Gate closes.
  </how-to-verify>
  <verify>Owner-run on-device e2e across both channels; verdicts recorded in 09-HUMAN-UAT.md.</verify>
  <acceptance_criteria>
    - All 5 scenarios recorded PASS in `09-HUMAN-UAT.md` (or gaps filed with the failing scenario + observed behavior)
    - Scenario 1 confirms BOTH "no auto-reply / stays unread" AND "suggestions panel still populates"
    - Scenario 4 confirms the same suppress/restore on Telegram
    - Scenario 5 confirms absence→reply AND the untouched «Бот работает/пауза» switch
    - The test reply-workflow clone was DEACTIVATED after the window (real-contacts constraint); prod bagkz untouched
  </acceptance_criteria>
  <done>The owner confirms the full suppression behavior on both channels on one build; on all-PASS the phase is behaviorally complete and ready for `/gsd-secure-phase 09`.</done>
  <resume-signal>Type "UAT pass" (or list failing scenarios + observed behavior). Confirm the test clone is deactivated.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real customer message -> live gated bot (dev clone, active for the window) | the behavioral suppression is exercised with real contacts |
| owner device build -> /webhook/SetReplyMode | the client write path is exercised end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | Elevation of Privilege | test reply-workflow clone left ACTIVE | mitigate | Clone ACTIVE only for the e2e window, DEACTIVATED in the same run (real contacts) — acceptance requires it; pre-flight/post-run blocks in the runbook enforce it |
| T-09-17 | Repudiation / Safety | «Вместе» chat silently auto-answered | mitigate | Scenario 1/4 directly observe no-reply + stays-unread; scenario 5 confirms the never-toggled case still replies (fail-open-on-absence is intended, fail-closed-on-error is not user-triggerable in this pass) |
| T-09-18 | Tampering | prod bagkz accidental target | accept/mitigate | Prod stays DORMANT; all e2e is dev-only; SUP-05 folds the gate into the future one-shot bulk copy (documented, not executed this phase) |
</threat_model>

<verification>
- 09-HUMAN-UAT.md authored with all 5 scenarios + heal + activation-switch sub-checks (Claude-verifiable grep).
- Owner e2e: all 5 scenarios PASS on both channels; suggestions still work in a suppressed chat; absence→reply; activation switch untouched; test clone deactivated (owner-recorded).
</verification>

<success_criteria>
- The owner confirms behaviorally: «Вместе» → no auto-reply + stays unread + suggestions still populate; «Авто» → replies; '*' default suppresses a never-opened chat; identical on WhatsApp and Telegram; absence→reply; activation switch unchanged (ROADMAP Phase 9 success criteria 1-2, SUP-03).
- No dev clone left active; prod dormant (SUP-05).
</success_criteria>

<output>
After completion, create `.planning/phases/09-semi-auto-suppression/09-05-SUMMARY.md` with the recorded UAT verdicts and the phase disposition.
</output>
