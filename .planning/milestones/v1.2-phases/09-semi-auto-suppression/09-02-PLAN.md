---
phase: 09-semi-auto-suppression
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json
  - Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json
autonomous: true
requirements: [SUP-03, SUP-04]
must_haves:
  truths:
    - "Each bot template reads a suppression flag after the group-chat If and dead-ends the reply when suppressed"
    - "A suppressed message is never marked read (Mark Read is downstream of Input type, which the dead-end skips)"
    - "The gate fails closed: a Postgres read error halts the execution (no reply)"
  artifacts:
    - path: "Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json"
      provides: "Read Reply Mode + Suppressed? gate on If.main[0]"
      contains: "reply_mode_flags"
    - path: "Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json"
      provides: "identical gate on If.main[0]"
      contains: "reply_mode_flags"
  key_links:
    - from: "group-chat If (main[0], 1:1)"
      to: "Read Reply Mode"
      via: "rewired connection"
      pattern: "Read Reply Mode"
    - from: "Read Reply Mode"
      to: "reply_mode_flags"
      via: "coalesce resolve query, cred 1H5xlpFSESU4w6JH, no continueOnFail"
      pattern: "coalesce"
    - from: "Suppressed? (false)"
      to: "Input type"
      via: "existing reply path unchanged"
      pattern: "Input type"
---

<objective>
Insert the fail-closed suppression gate into BOTH bot templates (SUP-03) — two nodes (`Read Reply Mode` Postgres + `Suppressed?` If) spliced onto the group-chat `If` TRUE output, before `Input type`. The gate is authored once and pasted byte-identically into both templates (they are structurally identical at the insertion point). This is pure workflow-JSON editing; the live runData verification is 09-04 (owner gate).

Purpose: This is the actual behavior change — a «Вместе» chat gets no auto-reply and stays unread. Fail-closed (no error wiring) is the load-bearing safety property (SUP-04).
Output: gate nodes + rewired connections in `4wYitz5ek30SVNlT-WhatsApp_Bot.json` and `4VN3gsFaC2HUYmcc-Telegram_Bot.json`.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Canonical design + corrected breakdown (read before editing)
@docs/superpowers/specs/2026-07-13-semi-auto-suppression-flag-design.md
@.planning/phases/09-semi-auto-suppression/09-RESEARCH.md
@.planning/phases/09-semi-auto-suppression/09-PATTERNS.md

# The two templates being edited + the node-shape analogs
@Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json
@Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json
@Tools/n8n/workflows/ZTqpumOpL1rNDOp6-Delete_File.json

<interfaces>
<!-- Exact insertion anchors, VERIFIED against the live JSON this session. Do not re-derive. -->

The group-chat If node (WhatsApp @ name "If" ~line 641; Telegram @ ~line 655 — IDENTICAL params):
  compares  leftValue "={{ $json.body.messages[0].from }}"  ==  rightValue "={{ $json.body.messages[0].chatId }}"
  operator string/equals, typeVersion 2.2

Current output wiring (the exact splice point) — ONE output array only:
  "If": { "main": [ [ { "node": "Input type", "type": "main", "index": 0 } ] ] }
  main[1] (group/FALSE) is ABSENT -> groups dead-end BEFORE the gate.
  => THE GATE ONLY EVER RUNS ON 1:1 CHATS, where from == chatId == CurrentChatId (RESEARCH Pitfall 1, verified).

The Telegram reply-entry node is ALSO named "Input type" (VERIFIED — the plan's "confirm its name" resolves).

Both templates' existing Chat Memory Postgres node already binds cred id 1H5xlpFSESU4w6JH (WhatsApp ~line 605, Telegram ~line 619) — reuse the SAME cred for the gate so it reads the DB the table lives on.

The webhook already exposes both params the query needs:
  $('Webhook').item.json.body.messages[0].profile_id   and   .messages[0].from

Postgres executeQuery node shape (from Delete_File.json): resource:"database", operation:"executeQuery",
  typeVersion 2.6, options.queryReplacement (positional, comma-separated). Mark Read is downstream of Input type.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WhatsApp template reply-mode gate (SUP-03, SUP-04)</name>
  <files>Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json</files>
  <read_first>
    - Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json (the group-chat "If" node params + its `"If": { "main": [...] }` connection block + the existing Chat Memory Postgres node's credentials block for cred 1H5xlpFSESU4w6JH)
    - Tools/n8n/workflows/ZTqpumOpL1rNDOp6-Delete_File.json (Postgres executeQuery node shape to copy)
    - .planning/phases/09-semi-auto-suppression/09-PATTERNS.md (section: WhatsApp_Bot.json + Telegram_Bot.json gate — the exact query, params, cred, and rewire)
  </read_first>
  <action>
Add TWO nodes to `4wYitz5ek30SVNlT-WhatsApp_Bot.json` and rewire the group-chat `If` TRUE output.

**Node A — "Read Reply Mode"** (Postgres, `n8n-nodes-base.postgres` typeVersion 2.6, `resource:"database"`, `operation:"executeQuery"`, credential id `1H5xlpFSESU4w6JH` name "Postgres" — the SAME block the Chat Memory node uses). Query (the LOCKED resolve query — the always-one-row `coalesce` shape is load-bearing for fail-closed SUP-04):
```sql
select coalesce(
  (select suppressed from reply_mode_flags
   where profile_id = $1 and chat_id in ($2, '*')
   order by (chat_id = '*')   -- specific chat_id sorts before the '*' default
   limit 1),
  false
) as suppressed;
```
`options.queryReplacement` (positional; mirrors how Chat Memory references the webhook):
```
={{ $('Webhook').item.json.body.messages[0].profile_id }},{{ $('Webhook').item.json.body.messages[0].from }}
```
**CRITICAL (SUP-04 fail-closed):** do NOT set `continueOnFail`, `onError`, `retryOnFail`, or `alwaysOutputData` on this node. A genuine Postgres error MUST throw and halt the execution -> no reply. Any error-tolerance here inverts the safety property.

**Node B — "Suppressed?"** (If, `n8n-nodes-base.if` typeVersion 2.2) — a single boolean condition reading `{{ $json.suppressed }}`, operator `type:"boolean", operation:"true", singleValue:true`, `options.typeValidation:"loose"` (copy the exact shape from Suggest_Replies "If invalid?"; `loose` lets n8n coerce a string `"true"` — RESEARCH A1/Pitfall 4; if 09-04 runData shows a string, the fallback is to cast `suppressed::boolean` in Node A's query).

**Rewire** the `"If"` connections block:
- Change `If.main[0]` from `[ { "node": "Input type", ... } ]` to `[ { "node": "Read Reply Mode", "type": "main", "index": 0 } ]`.
- Add `"Read Reply Mode": { "main": [ [ { "node": "Suppressed?", "type": "main", "index": 0 } ] ] }`.
- Add `"Suppressed?": { "main": [ [], [ { "node": "Input type", "type": "main", "index": 0 } ] ] }` — i.e. output 0 (TRUE / suppressed) = **empty array (dead-end: no Mark Read, stays unread)**; output 1 (FALSE / not suppressed) -> `Input type` (the existing reply path, unchanged).
  > n8n If node: main[0] = TRUE branch, main[1] = FALSE branch. Suppressed==true must dead-end; not-suppressed must reach Input type. Double-check the branch order in runData at 09-04.
- Leave `Webhook.main[0] -> If` and everything downstream of `Input type` (Mark Read, typing, agent, send) UNTOUCHED.

Give the two new nodes distinct `id` values and reasonable `position` coords so the graph stays readable. Do not touch any other node.
  </action>
  <verify>
    <automated>python3 -c "import json; json.load(open('Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json')); print('JSON_OK')" && grep -q "Read Reply Mode" Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json && grep -q "reply_mode_flags" Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json && grep -q "Suppressed?" Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json && grep -q "order by (chat_id = '\*')" Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json && ! grep -q "continueOnFail" Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json && echo WA_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - File is valid JSON (`json.load` exits 0)
    - `grep -c "reply_mode_flags"` >= 1 AND the `coalesce(... order by (chat_id = '*') ... limit 1), false)` query text is present verbatim
    - Nodes named `Read Reply Mode` and `Suppressed?` both exist
    - `Read Reply Mode` binds credential id `1H5xlpFSESU4w6JH` (grep the id near the new node's credentials block)
    - `options.queryReplacement` contains `messages[0].profile_id` and `messages[0].from`
    - The connections block routes `If.main[0] -> Read Reply Mode -> Suppressed?`; `Suppressed?` FALSE branch -> `Input type`; TRUE branch is an empty array (dead-end). Confirm by reading the `"connections"` object.
    - `grep -c "continueOnFail"` on the whole file is 0 (no error-tolerance was introduced anywhere; fail-closed preserved) — and the `Read Reply Mode` node has no `onError`/`retryOnFail`/`alwaysOutputData`
    - `git diff` touches ONLY the two new nodes + the `If`/`Read Reply Mode`/`Suppressed?` connection entries (nothing downstream of `Input type`)
  </acceptance_criteria>
  <done>WhatsApp_Bot.json has a fail-closed gate on If.main[0] that reads reply_mode_flags via cred 1H5xlpFSESU4w6JH and dead-ends the reply when suppressed; nothing else changed.</done>
</task>

<task type="auto">
  <name>Task 2: Telegram template reply-mode gate (identical paste) (SUP-03, SUP-04)</name>
  <files>Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json</files>
  <read_first>
    - Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json (its group-chat "If" node + `"If"` connection block + existing Chat Memory Postgres cred 1H5xlpFSESU4w6JH; confirm the reply-entry node is named "Input type")
    - Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json (the gate authored in Task 1 — paste the SAME two nodes + rewiring)
  </read_first>
  <action>
Paste the IDENTICAL gate authored in Task 1 into `4VN3gsFaC2HUYmcc-Telegram_Bot.json`. The templates are byte-identical at the insertion point (same `If` params comparing `from == chatId`, same Chat Memory cred `1H5xlpFSESU4w6JH`, same single-output `If.main[0] -> Input type` wiring), and the Telegram reply-entry node is ALSO named `Input type` (VERIFIED this session).

- Add the same `Read Reply Mode` (Postgres, cred `1H5xlpFSESU4w6JH`, the same locked `coalesce` query + `queryReplacement` params) and `Suppressed?` (boolean If) nodes.
- Rewire the Telegram template's `"If"` connections identically: `If.main[0] -> Read Reply Mode -> Suppressed?`; `Suppressed?` TRUE (main[0]) = empty dead-end (no Mark Read); FALSE (main[1]) -> `Input type`.
- Same fail-closed rule: NO `continueOnFail`/`onError`/`retryOnFail`/`alwaysOutputData` on `Read Reply Mode`.
- The table is channel-agnostic (keyed by `profile_id`, which is per-channel), so the query + params are unchanged — the Telegram workflow reads by its own Telegram profile id. Use fresh node `id`s (unique within this file) and readable `position` coords.

Touch nothing downstream of `Input type` (the tapi'd Mark Read / typing / send from Phase 4 stay byte-identical).
  </action>
  <verify>
    <automated>python3 -c "import json; json.load(open('Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json')); print('JSON_OK')" && grep -q "Read Reply Mode" Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json && grep -q "reply_mode_flags" Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json && grep -q "order by (chat_id = '\*')" Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json && grep -q "Input type" Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json && ! grep -q "continueOnFail" Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json && echo TG_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - File is valid JSON
    - `Read Reply Mode` + `Suppressed?` nodes exist; the same locked `coalesce(... order by (chat_id = '*') ... limit 1), false)` query is present
    - `Read Reply Mode` binds credential id `1H5xlpFSESU4w6JH`
    - Connections route `If.main[0] -> Read Reply Mode -> Suppressed?`; FALSE -> `Input type`; TRUE -> empty dead-end
    - The gate node params (query text + queryReplacement) are IDENTICAL to the WhatsApp gate (diff the two node bodies — only node `id`/`position` may differ)
    - `grep -c "continueOnFail"` is 0; no `onError`/`retryOnFail` on `Read Reply Mode`
    - Nothing downstream of `Input type` changed (`git diff` scoped to the gate + connections)
  </acceptance_criteria>
  <done>Telegram_Bot.json carries the byte-identical fail-closed gate; both templates now stand down for a suppressed 1:1 chat on their own channel's profile id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| incoming customer message (wappi/tapi -> n8n Webhook) | untrusted `messages[0].from`/`profile_id` enter the resolve query |
| bot template -> Supabase Postgres | Read Reply Mode reads reply_mode_flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | Read Reply Mode query | mitigate | Parameterized `$1/$2` via `options.queryReplacement`; `from`/`profile_id` never string-concatenated into the SQL |
| T-09-06 | Repudiation / Safety | Read Reply Mode error handling | mitigate | Fail-CLOSED by design — NO `continueOnFail`/`onError`; a Postgres error halts the execution so a «Вместе» chat is never silently auto-answered. Confirmed by acceptance grep `continueOnFail == 0` |
| T-09-07 | Denial of Service (availability) | gate on the reply hot path | accept | A Postgres outage halts ALL replies (fail-closed availability tradeoff, deliberate per SUP-04). No NEW point of failure: the workflow already depends on this same Postgres for Chat Memory |
| T-09-08 | Tampering | Suppressed? boolean coercion (A1) | mitigate | `typeValidation:"loose"` boolean-true condition; 09-04 runData confirms the branch; fallback = cast `suppressed::boolean` in the read |
</threat_model>

<verification>
- Both templates are valid JSON with the gate spliced onto If.main[0], reading reply_mode_flags via cred 1H5xlpFSESU4w6JH.
- The locked coalesce/order-by/limit/false resolve query is present verbatim in both; queryReplacement binds profile_id + from.
- No continueOnFail/onError anywhere (fail-closed preserved).
- Suppressed? TRUE dead-ends (no Mark Read); FALSE reaches Input type.
- NOTE: live runData branch confirmation + fresh-bot propagation are 09-04 (owner gate) — NOT this plan.
</verification>

<success_criteria>
- Both bot templates carry the identical fail-closed suppression gate on the 1:1 branch (SUP-03).
- The always-one-row coalesce query encodes per-chat-beats-default precedence + absence->false (SUP-04).
- WhatsApp behavior outside the gate is byte-identical; the tapi'd Telegram reply path is byte-identical.
</success_criteria>

<output>
After completion, create `.planning/phases/09-semi-auto-suppression/09-02-SUMMARY.md`.
</output>
