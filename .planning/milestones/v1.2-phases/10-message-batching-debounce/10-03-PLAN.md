---
phase: 10-message-batching-debounce
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json
  - Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json
  - Tools/n8n/apply-message-batching.py
autonomous: false
requirements: [BATCH-01, BATCH-02]
must_haves:
  truths:
    - "Both edited templates run live on dev n8n; any existing frozen dev clone is recreated so it carries the debounce"
    - "Two text fragments ~1s apart produce exactly ONE reply — runData shows the first fragment aborts at Is Latest?, the second combines the concatenation"
    - "A single complete message still gets one reply after the window; a bot/owner reply between fragments bounds the combined run"
    - "The webhook messages[0].id string-equals the messages/get id for the same message on BOTH channels (id-equality, Pitfall 2/A3)"
    - "A freshly created bot's cloned workflow contains the 4 debounce nodes after the Suppressed? gate"
  artifacts:
    - path: "Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json"
      provides: "the WhatsApp template as deployed + runData-verified live"
      contains: "Latest+Combine"
  key_links:
    - from: "dev n8n bot clone"
      to: "one combined reply"
      via: "Debounce Wait -> Fetch Recent -> Latest+Combine (abort earlier fragments)"
      pattern: "combinedText"
    - from: "Create orchestrator clone"
      to: "debounce nodes"
      via: "Get Sample Workflow -> Create Workflow (verbatim template clone)"
      pattern: "Debounce Wait"
---

<objective>
Owner-run live bring-up + structural verification of the auto-reply half (all live n8n work — `secrets.json` is deny-ruled for Claude; dev n8n + tunnel + real WA/TG profiles required). Redeploy both debounce-spliced templates to dev, recreate any frozen existing clone, then prove via execution `runData` that a multi-fragment message produces exactly ONE combined reply (earlier fragments abort at `Is Latest?`), a single message still replies once after the window, a bot-reply-between bounds the run, and the webhook/sync id-equality holds on BOTH channels. Finally confirm a freshly created bot inherits the debounce. Behavioral device e2e (owner watches one reply arrive; suggestions coalesce) is the separate closing gate 10-04.

Purpose: The structural splice (10-01) cannot prove abort-vs-combine, id-equality, message-propagation-within-window, or clone propagation — those need a running dev n8n with real fragments. A JSON grep would false-positive on all four.
Output: both templates live on dev, recorded runData verdicts (2-fragment combine / single / bot-boundary / id-equality) on both channels, fresh-bot propagation confirmed, any test clone deactivated.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Design + research + the artifacts produced upstream
@docs/superpowers/specs/2026-07-15-message-batching-debounce-design.md
@.planning/phases/10-message-batching-debounce/10-RESEARCH.md
@.planning/phases/10-message-batching-debounce/10-CONTEXT.md
@Tools/n8n/apply-message-batching.py
@Tools/n8n/verify-message-batching.py
@Tools/n8n/build-suggest-replies.py

# The Phase-9 owner-gate precedent (deploy/runData/propagation pattern to mirror)
@.planning/phases/09-semi-auto-suppression/09-04-PLAN.md

<environment_notes>
- `secrets.json` (n8nAPIKey, DB creds) is DENY-RULED for Claude — the OWNER runs the deployer + tunnel + curl/runData. Dev n8n at localhost:5678; tunnel via Tools/n8n/rotate-tunnel.py.
- Prod bagkz stays DORMANT — do NOT target it. The debounce folds into the future one-shot bulk copy.
- INACTIVE-clone constraint: a per-bot template clone may be ACTIVE only during the test window, then DEACTIVATED in the same task (real contacts).
- Deploy pattern: the templates keep their literal ids (4wYitz5ek30SVNlT / 4VN3gsFaC2HUYmcc) — re-import by literal id (UI download strips the top-level id; never rename). Existing per-bot clones are FROZEN copies of the pre-debounce template and must be recreated (Pitfall 6).
- runData introspection precedent: 09-04 (Suppressed? branch); the Fetch Recent within the ~8s window depends on messages/get returning the just-arrived fragment (A2 — confirm at this gate).
</environment_notes>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Redeploy both debounce templates + recreate frozen clones (BATCH-01)</name>
  <files>Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json, Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json, Tools/n8n/apply-message-batching.py</files>
  <read_first>
    - Tools/n8n/verify-message-batching.py (run it first — must be green before deploying)
    - Tools/n8n/build-suggest-replies.py (the REST deploy/update-by-id pattern the owner mirrors)
    - .planning/phases/10-message-batching-debounce/10-RESEARCH.md (Runtime State Inventory — existing clones are frozen; Pitfall 6 — recreate them)
    - .planning/phases/09-semi-auto-suppression/09-04-PLAN.md (Task 3 — the template-redeploy + clone-recreate discipline)
  </read_first>
  <what-built>
Claude spliced the debounce stage into both committed templates (10-01) and a structural verifier greens them. The dev n8n instance still runs the pre-debounce templates on any existing clone — nothing is live yet.
  </what-built>
  <action>
Owner, with dev n8n up (`localhost:5678`) + the n8n API key available:
1. Run `python3 Tools/n8n/verify-message-batching.py` (must exit 0 / "ALL BATCHING ASSERTS PASSED") as a pre-deploy gate.
2. Re-import BOTH edited templates to dev by their LITERAL ids (`4wYitz5ek30SVNlT` WhatsApp, `4VN3gsFaC2HUYmcc` Telegram) — REST update-by-id, mirroring the deployer pattern. Do NOT rename or let a UI round-trip strip the top-level id.
3. Recreate any EXISTING dev bot clone (frozen copies of the old debounce-less template) so it carries the 4 new nodes — via the app's create-bot flow or by driving the Create orchestrator on dev. A clone is ACTIVE only during the test window (Task 2) and DEACTIVATED immediately after (real contacts).
4. Confirm the two templates on dev now contain `Debounce Wait`/`Fetch Recent`/`Latest+Combine`/`Is Latest?` (fetch each workflow JSON via the n8n REST API and grep the node names).

If a live template's Fetch Recent lost its base or a UI import stripped a node, re-run `apply-message-batching.py` against the committed JSON and re-import (do NOT hand-edit the live workflow).
  </action>
  <how-to-verify>
Both live templates (fetched via REST) contain the 4 debounce node names wired after `Suppressed?`. The verifier greened pre-deploy. Any recreated clone is present and INACTIVE until Task 2.
  </how-to-verify>
  <verify>Owner-run: verify-message-batching.py green -> re-import both by literal id -> REST-grep the 4 node names live -> recreate frozen clones (INACTIVE).</verify>
  <acceptance_criteria>
    - `verify-message-batching.py` exited 0 before the deploy
    - Both dev templates (fetched via REST) contain `Debounce Wait`, `Fetch Recent`, `Latest+Combine`, `Is Latest?` after the `Suppressed?` gate
    - Any existing dev bot clone was recreated (not hand-edited) so it carries the debounce
    - The template ids on dev are still `4wYitz5ek30SVNlT` / `4VN3gsFaC2HUYmcc` (literal-id preserved)
    - No clone is left ACTIVE at the end of this task (it activates only for Task 2's window)
  </acceptance_criteria>
  <done>Both debounce templates are live on dev by literal id, the verifier greened them, and frozen clones are recreated — ready for the runData matrix.</done>
  <resume-signal>Type "deployed" (note whether an existing clone was recreated), or report the import/verify error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: runData matrix (2-fragment combine / single / bot-boundary / id-equality) both channels + fresh-bot propagation (BATCH-01, BATCH-02)</name>
  <files>(no repo file — live runData introspection)</files>
  <read_first>
    - .planning/phases/10-message-batching-debounce/10-CONTEXT.md (Specific Ideas — the worked example + the n8n curl matrix scenarios)
    - .planning/phases/10-message-batching-debounce/10-RESEARCH.md (Pitfall 2 / A3 — id-equality is the highest-risk assumption; A2 — propagation within the window; Pitfall 6 — fresh-bot propagation)
    - .planning/phases/09-semi-auto-suppression/09-04-PLAN.md (Task 3 — the runData introspection + fresh-bot propagation pattern)
  </read_first>
  <what-built>
Both templates carry the debounce stage live (Task 1). This gate proves the mechanism via execution `runData`: earlier fragments abort at `Is Latest?`, the winning fragment combines the concatenation, and the id-equality the whole scheme rests on holds on both channels.
  </what-built>
  <action>
Owner, dev n8n + tunnel up, the test bot's reply-workflow clone ACTIVE for the window (DEACTIVATE it immediately after). Run each scenario on BOTH WhatsApp and Telegram and record the runData verdict:

**(A) Two fragments -> ONE combined reply.** Send «есть колодки» then «на камри 70?» ~1s apart to the test bot. EXPECT (from the two executions' runData):
- Fragment 1's execution: `Debounce Wait` -> `Fetch Recent` -> `Latest+Combine` (`abort:true`) -> `Is Latest?` takes the TRUE/main[0] dead-end — NO `Input type`/`AI Agent`/`send` (this fragment never generates).
- Fragment 2's execution: `Latest+Combine` `abort:false` + `combinedText` == "есть колодки\nна камри 70?"; `Is Latest?` FALSE/main[1] -> `Input type` -> `Text` (value resolves to the combinedText) -> `AI Agent` -> one reply delivered.
- Net: exactly ONE message arrives in the chat.

**(B) Single complete message -> ONE reply after the window.** Send one message, wait. EXPECT: `Latest+Combine` `abort:false`, `combinedText` == that single message's body (or the fallback resolves it), full reply path runs once ~8s later.

**(C) Bot/owner reply between fragments bounds the run.** Fragment A -> let the bot reply (or the owner replies) -> fragment B. EXPECT: fragment B's combine walk STOPS at the `fromMe` reply — `combinedText` == only fragment B's text (the earlier fragment is not re-answered).

**(D) id-equality (Pitfall 2/A3) — HIGHEST RISK.** In fragment 2's runData, confirm the trigger's `$('Webhook').first().json.body.messages[0].id` STRING-equals the newest incoming id in the `Fetch Recent` response for the SAME message — on BOTH channels (WhatsApp jid-form; Telegram bare numeric like `"2910"`). If they diverge: EITHER every fragment aborts (zero replies) OR none abort (duplicate replies) — record the exact id forms and STOP (this is a design assumption failure, not a tuning issue).

**(E) Fresh-bot propagation (Pitfall 6).** Create a throwaway bot via the app (or drive the Create orchestrator on dev), fetch its cloned workflow JSON via REST, and grep the node names — confirm `Debounce Wait`/`Fetch Recent`/`Latest+Combine`/`Is Latest?` exist wired after the `Suppressed?` gate. If ABSENT, the orchestrator's "Get Sample Workflow" fetches a stale template — fix the fetch/id, do NOT hand-edit clones. Deactivate/delete the throwaway.

DEACTIVATE every clone activated for testing. If A2 shows the fetch missing the just-arrived fragment (propagation lag > window), note it and raise the `Debounce Wait` amount as the tuning knob (re-run `apply-message-batching.py` with a larger `amount`, re-import). If the window feels too long/short for perceived single-message latency, tune the `amount` here (Claude's discretion range).
  </action>
  <how-to-verify>
(A) runData: fragment 1 dead-ends at `Is Latest?` (no reply nodes), fragment 2 combines + replies; exactly one message arrives. (B) one reply after the window. (C) combinedText is only the post-fromMe fragment. (D) trigger id == fetched newest incoming id (string) on both channels. (E) fresh clone JSON contains all 4 node names. All clones deactivated.
  </how-to-verify>
  <verify>Owner-run runData matrix (A-E) on both channels; verdicts recorded; clones deactivated.</verify>
  <acceptance_criteria>
    - (A) Two fragments -> exactly ONE reply; runData shows fragment 1 `abort:true` dead-ending at `Is Latest?` and fragment 2 `abort:false` with `combinedText` == the two-line concatenation — on BOTH channels
    - (B) A single message -> one reply after the window; `combinedText` resolves to that message
    - (C) A `fromMe` reply between fragments bounds the run (`combinedText` == only the trailing fragment)
    - (D) The webhook `messages[0].id` string-equals the `messages/get` newest-incoming id for the same message on BOTH WhatsApp and Telegram (exact id forms recorded); if it diverged, that is filed as a blocking design failure
    - (E) A freshly created bot's cloned workflow JSON contains all 4 debounce node names wired after `Suppressed?`
    - Every clone activated for testing was DEACTIVATED afterward (real-contacts constraint); prod bagkz untouched
    - If the `Debounce Wait amount` was tuned (propagation/latency), the committed template + `apply-message-batching.py` default were updated to match and re-verified
  </acceptance_criteria>
  <done>runData proves multi-fragment -> one combined reply (earlier fragments abort), single/boundary cases hold, id-equality holds on both channels, and a fresh bot inherits the debounce; no clone left active.</done>
  <resume-signal>Type "runData pass" (paste the abort/combine + id-equality verdicts for both channels), or report which scenario failed + the observed runData.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner tooling -> dev n8n REST | template re-import by literal id + clone recreate |
| real customer message -> live debounce (dev clone, active for the window) | the abort/combine mechanism is exercised with real fragments |
| Fetch Recent -> Wappi/tapi messages/get | the live fetch reads recent messages during the wait |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Elevation / Safety | test clone left ACTIVE against real contacts | mitigate | Clone ACTIVE only for the runData window, DEACTIVATED in the same task (acceptance requires it); pre-flight/post-run discipline mirrors Phase 9 |
| T-10-03-02 | Tampering / correctness | webhook vs sync id divergence (Pitfall 2/A3) | mitigate | id-equality is an explicit first-class runData assertion on BOTH channels; a divergence is filed as blocking (zero-reply or duplicate-reply failure) before any device UAT |
| T-10-03-03 | Information Disclosure / side-effect | Fetch Recent marking read early | mitigate | Verifier (10-01) already asserts no `mark_all`; runData confirms the chat is marked read only by the downstream humanizer `Mark Read` after the reply decision |
| T-10-03-04 | Tampering | prod bagkz accidental target | accept/mitigate | Prod stays DORMANT; all deploy/runData is dev-only; the debounce folds into the future one-shot bulk copy |
</threat_model>

<verification>
- [BLOCKING] Both templates re-imported live by literal id; verifier greened pre-deploy; frozen clones recreated.
- runData: 2-fragment combine (earlier aborts), single-message one reply, bot-reply boundary, id-equality on both channels; fresh bot inherits the debounce.
- Every test clone deactivated; prod untouched.
- NOTE: behavioral device e2e (one reply arrives; suggestions coalesce) is 10-04 (closing gate) — NOT this plan.
</verification>

<success_criteria>
- Both debounce templates run live on dev; a multi-fragment message yields exactly ONE combined reply with earlier fragments aborting (BATCH-01), verified via runData on both channels.
- One fetch serves is-latest + combine; single/boundary cases hold; id-equality holds (BATCH-02).
- A fresh bot inherits the debounce; no clone left active; prod dormant.
</success_criteria>

<output>
After completion, create `.planning/phases/10-message-batching-debounce/10-03-SUMMARY.md` recording the runData verdicts + the id-equality forms + any window tuning.
</output>
