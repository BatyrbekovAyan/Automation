---
phase: 11-first-run-onboarding-flow
plan: 05
type: execute
wave: 5
depends_on: [11-03, 11-04]
files_modified:
  - Assets/Editor/OnboardingAuthBlocksBuilder.cs
  - Assets/Scenes/Main.unity
autonomous: true
requirements: [ONB-02, ONB-03, ONB-05]

must_haves:
  truths:
    - "The WhatsApp code panel and the Telegram phone/code/2FA panel each show a green «Это безопасно» trust card with channel-specific copy and a lock icon (no QR references)"
    - "The trust card is appended as the LAST child so Manager's hardcoded GetChild(3/4/5) / GetChild(3) indices do not shift"
    - "The WhatsApp AND Telegram success panels each host their OWN independent «Загрузить прайс-лист»/«Позже» cluster + title/body labels (two clusters, one per SuccessOverlay)"
    - "The WhatsApp cluster is stamped onto Manager's wa* success-moment fields and the Telegram cluster onto the tg* fields"
    - "The green check animates DOScale 0.9→1 OutBack"
  artifacts:
    - path: "Assets/Editor/OnboardingAuthBlocksBuilder.cs"
      provides: "Idempotent builder injecting trust cards + PER-CHANNEL success CTAs into the existing auth panels"
      contains: "Headless build + save complete"
  key_links:
    - from: "Assets/Editor/OnboardingAuthBlocksBuilder.cs"
      to: "Manager wa*/tg* success-moment fields"
      via: "SerializedObject stamp of waSuccess*/tgSuccess* button+label props (two clusters)"
      pattern: "waSuccessPrimaryButton"
    - from: "trust card"
      to: "WhatsappCodePanel / TelegramCodePanel"
      via: "appended as LAST child (index-safe)"
      pattern: "DestroyAllByName.*TrustBlock"
---

<objective>
Deliver ONB-02 (both-channel «Это безопасно» trust blocks) and the SCENE half of ONB-03 (success-panel CTA buttons + labels). A new idempotent builder injects a green trust card as the LAST child of the WhatsApp code panel AND the Telegram phone/code/2FA panel (preserving Manager's hardcoded `GetChild` indices — Pitfall 2), and builds TWO independent «Загрузить прайс-лист»/«Позже» clusters + title/body labels + animated check — one into each of the separate `WhatsappAuthSuccessPanel` / `TelegramAuthSuccessPanel` GameObjects — stamping them onto the PER-CHANNEL Manager success-moment fields declared in Plan 04. Second scene-mutating plan — run headless, commit `Main.unity` immediately.

Purpose: Address the #1 owner fear at the scariest step (linking a personal account) on BOTH channels, and give the Plan-04 success moment real buttons to drive on WHICHEVER channel authed last.
Output: `OnboardingAuthBlocksBuilder.cs` + rebuilt `Main.unity` (trust cards + two per-channel success CTA clusters wired to Manager).
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-first-run-onboarding-flow/11-CONTEXT.md
@.planning/phases/11-first-run-onboarding-flow/11-RESEARCH.md
@.planning/phases/11-first-run-onboarding-flow/11-PATTERNS.md
@docs/superpowers/specs/2026-07-17-first-run-onboarding-design.md

<interfaces>
<!-- Injection targets + Manager fields to stamp. Read the panel hierarchies before building. -->

From Assets/Scripts/Main/Manager.cs (Pitfall 2 — DO NOT shift these indices):
```csharp
// ShowWhatsappAuth (1653+): WhatsappCodePanel.transform.GetChild(3)/(4)/(5) + GetChild(4).GetChild(0)
// ShowTelegramAuth (1696+): TelegramCodePanel.transform.GetChild(3)
// Success panels: WhatsappAuthSuccessPanel, TelegramAuthSuccessPanel (fields ~55/57) — SEPARATE
//   GameObjects nested inside WhatsappAuth / TelegramAuth (they are NOT the same object).
// Plan 04 declared PER-CHANNEL success-moment fields to stamp (SerializedObject property names):
//   WhatsApp cluster: waSuccessTitleLabel, waSuccessBodyLabel, waSuccessPrimaryButton, waSuccessPrimaryLabel, waSuccessLaterButton
//   Telegram cluster: tgSuccessTitleLabel, tgSuccessBodyLabel, tgSuccessPrimaryButton, tgSuccessPrimaryLabel, tgSuccessLaterButton
```
Builder helpers to copy verbatim from Assets/Editor/NavRestructureBuilder.cs (552-659):
Hex, NewChild, SetAnchors, StretchFill, SetPreferredSize, AddText, AddIconImage (Image+sprite),
AddRounded/RefreshRounded (deferred bake), DestroyAllByName, FindDeepChild, LoadFont, LoadSprite.
Idempotent inject-into-existing-panel analog: NavRestructureBuilder.BuildOverlayChrome (264-337).

Trust-card tokens (spec): bg #F2F8F2, border #DCEDDD, title 50-55, body 38-40, caption 28-32.
Copy deck (verbatim):
- Trust title (both): «Это безопасно»
- Trust body (WhatsApp): «Работает через официальные «Связанные устройства» WhatsApp. Переписка остаётся у вас, отключить бота можно в любой момент.»
- Trust body (Telegram): «Официальный вход Telegram: код приходит в само приложение. Переписка остаётся у вас, отключить бота можно в любой момент.»
- Success title «Бот подключён!»; body «Осталось научить бота вашим ценам — загрузите прайс-лист, и он будет отвечать по вашим товарам»; buttons «Загрузить прайс-лист» · «Позже».
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OnboardingAuthBlocksBuilder — trust cards (both channels) + TWO per-channel success clusters, index-safe, stamped</name>
  <read_first>
    - Assets/Editor/NavRestructureBuilder.cs (BuildOverlayChrome 264-337 inject-into-existing-panel; helpers 552-659; DestroyAllByName 640-648)
    - Assets/Scripts/Main/Manager.cs — ShowWhatsappAuth/ShowTelegramAuth (1653-1736) to see the exact GetChild usage; the two SEPARATE success panel fields; the Plan-04 per-channel success-moment fields
    - Assets/Editor/ChannelSwitcherBuilder.cs (idiom for locating panels + SerializedObject-stamping Manager refs — analog headless builder)
    - docs/superpowers/specs/2026-07-17-first-run-onboarding-design.md (§Auth trust blocks, §Success moment, §Copy deck)
    - Tools/run-editor-builder.sh (sentinel contract)
  </read_first>
  <action>
Create `Assets/Editor/OnboardingAuthBlocksBuilder.cs` (`public static`, `#if UNITY_EDITOR`, clone NavRestructureBuilder envelope + helpers + font GUIDs verbatim). Entry points mirror the other builders:
```csharp
[MenuItem("Tools/Onboarding/Build Auth Blocks")]
public static void Build() { BuildInternal(); EditorSceneManager.MarkAllScenesDirty(); Debug.Log("[OnboardingAuthBlocksBuilder] Built — SAVE THE SCENE"); }
public static void BuildHeadless()
{
    var scene = EditorSceneManager.OpenScene("Assets/Scenes/Main.unity");
    BuildInternal();
    EditorSceneManager.SaveScene(scene);
    Debug.Log("[OnboardingAuthBlocksBuilder] Headless build + save complete"); // EXACT sentinel
}
```

`BuildInternal()`:

A) **Trust cards (ONB-02)** — for BOTH `WhatsappCodePanel` and `TelegramCodePanel` (locate via the Manager's serialized refs / by name under the auth screens):
- `DestroyAllByName(panel.transform, "TrustBlock")` first (idempotent), then build a fresh `TrustBlock` and **append it as the LAST child** (`transform.SetAsLastSibling()`). This keeps `GetChild(3/4/5)` (WhatsApp) and `GetChild(3)` (Telegram) valid — DO NOT insert at a low index.
- Card: `AddRounded` rounded rect, bg #F2F8F2, 1px border #DCEDDD; a lock icon via `AddIconImage` (Image + sprite — NEVER a TMP glyph); title «Это безопасно» (semibold, Ink); channel-specific body (verbatim from the copy deck above — WhatsApp body for the WhatsApp panel, Telegram body for the Telegram panel). NO QR text anywhere.

B) **Success CTAs (ONB-03 scene) — TWO INDEPENDENT CLUSTERS (BLOCKER fix):** build ONE success cluster into `WhatsappAuthSuccessPanel` AND a SECOND, independent cluster into `TelegramAuthSuccessPanel`. These are separate GameObjects in separate hierarchies, so a shared label/button cannot child both — Plan 04 declared per-channel field sets specifically for this.
- Each cluster: a green check circle (Image+sprite) tagged so Plan 04 can DOScale 0.9→1 OutBack it; title label «Бот подключён!»; body label (verbatim); a full-width primary button (`AddRounded`, Primary #1B7CEB, white label «Загрузить прайс-лист») and a ghost «Позже» button. Build each with `DestroyAllByName(panel.transform, "SuccessCta")` first (idempotent).
- Stamp BOTH clusters onto the Plan-04 per-channel Manager fields via `SerializedObject`:
```csharp
var so = new SerializedObject(manager);
// WhatsApp cluster → wa* fields
so.FindProperty("waSuccessTitleLabel").objectReferenceValue    = waTitleTmp;
so.FindProperty("waSuccessBodyLabel").objectReferenceValue     = waBodyTmp;
so.FindProperty("waSuccessPrimaryButton").objectReferenceValue = waPrimaryBtn;
so.FindProperty("waSuccessPrimaryLabel").objectReferenceValue  = waPrimaryLabelTmp;
so.FindProperty("waSuccessLaterButton").objectReferenceValue   = waLaterBtn;
// Telegram cluster → tg* fields
so.FindProperty("tgSuccessTitleLabel").objectReferenceValue    = tgTitleTmp;
so.FindProperty("tgSuccessBodyLabel").objectReferenceValue     = tgBodyTmp;
so.FindProperty("tgSuccessPrimaryButton").objectReferenceValue = tgPrimaryBtn;
so.FindProperty("tgSuccessPrimaryLabel").objectReferenceValue  = tgPrimaryLabelTmp;
so.FindProperty("tgSuccessLaterButton").objectReferenceValue   = tgLaterBtn;
so.ApplyModifiedPropertiesWithoutUndo();
EditorUtility.SetDirty(manager);
```
The field NAMES are the builder↔component contract — they must match Plan 04's declared per-channel fields exactly.

C) Deferred RoundedCorners bake after the whole tree: `Canvas.ForceUpdateCanvases(); foreach (var r in _roundedToRefresh) RefreshRounded(r);`.

Fonts assigned explicitly via `LoadFont(guid)`. All icons Image+sprite.
  </action>
  <verify>
    <automated>grep -n "Headless build + save complete\|DestroyAllByName.*TrustBlock\|SetAsLastSibling\|waSuccessPrimaryButton\|tgSuccessPrimaryButton\|Это безопасно" Assets/Editor/OnboardingAuthBlocksBuilder.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Headless build + save complete" Assets/Editor/OnboardingAuthBlocksBuilder.cs` (sentinel `[OnboardingAuthBlocksBuilder] Headless build + save complete`)
    - `grep -q 'DestroyAllByName' Assets/Editor/OnboardingAuthBlocksBuilder.cs` and the trust block is appended via `SetAsLastSibling()` (index-safe)
    - Both verbatim trust bodies present: `grep -q "официальные «Связанные устройства» WhatsApp"` AND `grep -q "Официальный вход Telegram: код приходит в само приложение"`
    - Trust title `«Это безопасно»` present; NO «QR»/«qr» string anywhere in the builder
    - Lock + check icons built via `AddIconImage` (Image+sprite) — no TMP glyph icon
    - Builder stamps the WhatsApp cluster (`waSuccessTitleLabel`, `waSuccessBodyLabel`, `waSuccessPrimaryButton`, `waSuccessPrimaryLabel`, `waSuccessLaterButton`) AND the Telegram cluster (`tgSuccess…` five fields) via SerializedObject — grep confirms both `waSuccessPrimaryButton` and `tgSuccessPrimaryButton` stamps
    - Two independent success clusters are built (one into each of `WhatsappAuthSuccessPanel` and `TelegramAuthSuccessPanel`) — no single shared cluster
    - `#if UNITY_EDITOR` wraps the file
  </acceptance_criteria>
  <done>Builder injects index-safe trust cards on both channels + TWO independent per-channel success clusters stamped to Manager's wa*/tg* fields.</done>
</task>

<task type="auto">
  <name>Task 2: Run headless + verify GetChild indices intact + commit Main.unity immediately + suite green</name>
  <read_first>
    - Tools/run-editor-builder.sh (lock guard + sentinel)
    - Tools/run-tests-headless.sh
    - Assets/Scripts/Main/Manager.cs (ShowWhatsappAuth/ShowTelegramAuth GetChild lines — the regression check)
    - CLAUDE.md (parallel-scene-clobber rule)
  </read_first>
  <action>
Run the builder and commit immediately.

Primary (Editor CLOSED): `Tools/run-editor-builder.sh OnboardingAuthBlocksBuilder.BuildHeadless` — assert the exact sentinel `"[OnboardingAuthBlocksBuilder] Headless build + save complete"`.
Fallback (Editor OPEN): owner runs `Tools/Onboarding/Build Auth Blocks` from the menu, saves the scene, confirms; then commit. Do NOT run headless against a held lock.

After GREEN:
1. **Index-safety regression check (Pitfall 2):** confirm `Manager.cs` STILL uses `WhatsappCodePanel.transform.GetChild(3)/(4)/(5)` and `TelegramCodePanel.transform.GetChild(3)` unchanged (this plan did not edit Manager.cs), and that in the saved scene the `TrustBlock` is the LAST child under each code panel (grep/inspect the panel's child order — TrustBlock must have the highest sibling index).
2. Stage + commit immediately: `Assets/Scenes/Main.unity` + `Assets/Editor/OnboardingAuthBlocksBuilder.cs(.meta)`:
   `docs(11-05): auth trust blocks + per-channel success CTAs into auth panels (ONB-02, ONB-03 scene)`
3. Run the EditMode suite — baseline still green (ONB-05).
  </action>
  <verify>
    <automated>grep -c "TrustBlock" Assets/Scenes/Main.unity ; grep -c "SuccessCta" Assets/Scenes/Main.unity ; grep -n "GetChild(3)" Assets/Scripts/Main/Manager.cs ; git log --oneline -1</automated>
  </verify>
  <acceptance_criteria>
    - `run-editor-builder.sh` GREEN with the exact sentinel (or owner menu build confirmed)
    - `grep -c "TrustBlock" Assets/Scenes/Main.unity` >= 2 (one per code panel)
    - `grep -c "SuccessCta" Assets/Scenes/Main.unity` >= 2 (one cluster per success panel — WhatsApp + Telegram)
    - `Manager.cs` unchanged in this plan — `GetChild(3)`, `GetChild(4)`, `GetChild(5)` (WhatsApp) and `GetChild(3)` (Telegram) all still present
    - TrustBlock is the LAST child of each code panel (verified by sibling order in the scene)
    - `Main.unity` + `OnboardingAuthBlocksBuilder.cs` committed in one commit whose subject contains `11-05`
    - EditMode suite baseline still green — 0 new failures (ONB-05)
  </acceptance_criteria>
  <done>Auth blocks + two per-channel success clusters built into the scene, GetChild indices provably intact, committed immediately, suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builder → auth panels | Injects children into hand-built panels addressed by hardcoded sibling index in Manager |
| trust copy → user | Static reassurance copy shown at the account-linking step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Tampering | GetChild index shift breaks the real auth code flow | mitigate | Trust card appended as LAST child (SetAsLastSibling); Manager's GetChild(3/4/5)/(3) untouched; acceptance asserts both the child order and the Manager lines; suite green |
| T-11-05-02 | Repudiation/trust | over-promising security copy | mitigate | Owner-approved verbatim copy only («официальные Связанные устройства» / «официальный вход Telegram»); no end-to-end guarantees claimed; no QR references |
| T-11-05-03 | Repudiation | uncommitted scene clobber | mitigate | Commit Main.unity in the same task as the builder run |
| T-11-05-04 | Tampering | success buttons unwired / blank on one channel (Telegram parity) | mitigate | Builder builds TWO clusters and stamps all 5 wa* AND all 5 tg* Plan-04 fields by exact property name; acceptance greps both channel stamps |
</threat_model>

<verification>
- `run-editor-builder.sh` GREEN sentinel (or owner menu confirmation).
- Scene grep: TrustBlock present twice + last-child; SuccessCta cluster present per success panel; Manager GetChild lines intact.
- EditMode suite green (ONB-05).
</verification>

<success_criteria>
- «Это безопасно» trust card on both auth code panels with channel-specific verbatim copy, lock icon (sprite), no QR.
- Both success panels host their OWN «Загрузить прайс-лист»/«Позже» cluster stamped onto Manager's wa*/tg* fields; checks ready to animate.
- Auth code flow byte-identical (indices intact); scene committed immediately; suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-first-run-onboarding-flow/11-05-SUMMARY.md`.
</output>
