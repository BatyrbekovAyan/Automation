---
phase: 11-first-run-onboarding-flow
plan: 10
type: execute
wave: 3
depends_on: [11-08, 11-09]
files_modified:
  - .planning/phases/11-first-run-onboarding-flow/11-HUMAN-UAT.md
autonomous: true
gap_closure: true
requirements: [ONB-03, ONB-04, ONB-05]
user_setup: []

must_haves:
  truths:
    - "11-HUMAN-UAT.md carries a focused «Round 2 re-verify (D1–D3)» section the owner can run to close the phase — NOT a brand-new full UAT"
    - "Each of D1/D2/D3 has a PASS/FAIL re-verify item tied to its requirement and its expected-vs-actual, plus a zero-regression re-check of the auth flows + suite count"
    - "The existing Round-1 defect table and Overall line are preserved; the addendum is appended, and authoring it is autonomous while RUNNING it stays the owner gate"
  artifacts:
    - path: ".planning/phases/11-first-run-onboarding-flow/11-HUMAN-UAT.md"
      provides: "Appended «Round 2 re-verify (D1–D3)» runbook section with per-defect PASS/FAIL items + a Round-2 overall line"
      contains: "Round 2 re-verify"
  key_links:
    - from: ".planning/phases/11-first-run-onboarding-flow/11-HUMAN-UAT.md"
      to: "gap plans 11-08 / 11-09"
      via: "each re-verify item names the fix it validates (D2→11-08 standalone overlay; D1/D3→11-09 card)"
      pattern: "11-08|11-09"
---

<objective>
Reopen the Phase-11 owner gate with a FOCUSED «Round 2 re-verify (D1–D3)» section appended to
the existing `11-HUMAN-UAT.md` — do NOT author a new full UAT. The three gap plans (11-08
standalone success overlay; 11-09 checklist visibility + live refresh) close D1/D2/D3; this
runbook section is how the owner confirms the three specific fixes on a device/Game view and
re-checks zero regression before flipping the phase to PASS.

Purpose: keep the human gate traceable — one runbook, Round-1 history intact, a tight Round-2
re-verify that only re-tests what changed plus the byte-identical auth safety net.
Output: an appended re-verify section + a Round-2 overall line; the phase-close instruction
already in the doc still applies.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-first-run-onboarding-flow/11-HUMAN-UAT.md
@.planning/phases/11-first-run-onboarding-flow/11-08-PLAN.md
@.planning/phases/11-first-run-onboarding-flow/11-09-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the «Round 2 re-verify (D1–D3)» section to 11-HUMAN-UAT.md</name>
  <read_first>
    - .planning/phases/11-first-run-onboarding-flow/11-HUMAN-UAT.md (whole file — preserve the
      Round-1 «Defects found» table + «Overall result»; append below them).
    - 11-08-PLAN.md / 11-09-PLAN.md must_haves (the exact expected behaviours to re-verify).
  </read_first>
  <files>.planning/phases/11-first-run-onboarding-flow/11-HUMAN-UAT.md</files>
  <action>
    Append (do not rewrite) a new section titled `## Round 2 re-verify (D1–D3)` after the existing
    «Overall result». Keep every existing line intact. The section must:

    - Open with a one-line note: authoring is autonomous; RUNNING it is the owner gate; blank
      checkboxes ship. Reset to a true first run the sanctioned way (Профиль → Аккаунт → «Удалить
      все данные», or fresh install) before the D1/D3 checks.

    - **D2 re-verify (ONB-03 — standalone success overlay, 11-08):** blank PASS/FAIL items —
      1) on a successful auth (creation AND settings re-auth), «Бот подключён!» shows on a clean
         standalone FULL-SCREEN overlay with NOTHING of the code UI (title/code field/buttons/trust
         card) visible beneath it;
      2) «Загрузить прайс-лист» deep-links into that bot's «Прайс-листы» tab; «Позже» → Bots;
      3) the «Открыть чаты» files-exist fallback still appears on a re-auth of a bot that already has
         a price list;
      4) the green check still pops (DOScale 0.9→1 OutBack);
      5) a «both»-channel creation shows the moment exactly ONCE.

    - **D1 re-verify (ONB-04/05 — zero-bot card visibility, 11-09):** blank PASS/FAIL items —
      1) with ZERO bots (finish the carousel → wizard opens → press back), ONLY the EmptyState
         renders — the «Первые шаги» card is NOT visible (no overlap);
      2) the card first appears once ≥1 bot exists.

    - **D3 re-verify (ONB-04 — live derived state, 11-09):** blank PASS/FAIL items —
      1) immediately after creating a bot (no navigate-away-and-back), the card shows «2 из 4» with
         rows 1-2 checked;
      2) uploading a price list flips «Загрузить прайс-лист» to done and increments «N из 4» without
         a tab bounce;
      3) after the first outgoing bot reply at 4/4 the card hides and stays hidden on relaunch.

    - **Zero regression (ONB-05) re-check:** blank PASS/FAIL items —
      1) WhatsApp pairing-code + Telegram phone→code→(2FA) auth flows behave exactly as before (no
         shift/break from the relocation);
      2) the Bots EmptyState + AddBotPanel auto-open behave as before;
      3) record the EditMode suite count (expected 1169/1169, up from 1165 by the D1 visibility tests): ___ / ___.

    - Close with a **Round 2 Overall** line (`☐ PASS  ☐ ISSUES`) and the instruction: on a green
      Round-2 (D1–D3 all PASS), Phase 11 is complete — reply "approved" to the executor checkpoint;
      on ISSUES, paste the residual defects to spin another `/gsd-plan-phase 11 --gaps` round.

    Use verbatim RU strings from the existing Copy deck. Do not tick anything.
  </action>
  <acceptance_criteria>
    - `grep -c "## Round 2 re-verify" .planning/phases/11-first-run-onboarding-flow/11-HUMAN-UAT.md` == 1
    - The Round-1 «Defects found» table + original «Overall result» are still present (`grep -c "| D1 |" ...` == 1 unchanged; `grep -c "Round 1 run 2026-07-18" ...` == 1)
    - The addendum references each fix plan: `grep -c "11-08\|11-09" .planning/phases/11-first-run-onboarding-flow/11-HUMAN-UAT.md` >= 1
    - No checkbox in the new section is pre-ticked: `grep -c "\[x\]" .planning/phases/11-first-run-onboarding-flow/11-HUMAN-UAT.md` unchanged from before the append (all new boxes blank `[ ]` / `☐`)
    - Expected suite count 1169 recorded as the Round-2 regression target
  </acceptance_criteria>
  <verify>
    <automated>grep -c "## Round 2 re-verify" .planning/phases/11-first-run-onboarding-flow/11-HUMAN-UAT.md returns 1 and the file still contains the Round-1 table</automated>
  </verify>
  <done>11-HUMAN-UAT.md carries a focused, blank Round-2 re-verify (D1–D3) section + Round-2 overall line; Round-1 history preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Documentation-only append to a planning runbook. No code, network, or scene surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10-01 | Tampering | Round-1 history overwritten instead of appended | mitigate | Append-only; acceptance greps assert the Round-1 defect table + original Overall line still present |
| T-11-10-02 | Repudiation | Owner verdict ambiguous | accept | Each item is a blank PASS/FAIL tied to a requirement + expected-vs-actual; a Round-2 overall line records the verdict |
</threat_model>

<verification>
- The «Round 2 re-verify (D1–D3)» section exists; Round-1 content intact; each fix plan referenced.
- No pre-ticked boxes; suite target 1169 recorded.
</verification>

<success_criteria>
- A focused owner-runnable re-verify section covering D1/D2/D3 + zero-regression is appended to the existing gate.
- The phase can be closed on a green Round-2 pass via the existing "approved" checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/11-first-run-onboarding-flow/11-10-SUMMARY.md`
</output>
