---
phase: 04-n8n-telegram-template-parity-dev
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json
  - Tools/n8n/workflows/Uz6HBBUpAiUqVysB-CreateTelegramWorkflow.json
  - Tools/n8n/workflows/XuvOp7TxOImOAmlj-CreateWhatsappWorkflow.json
  - Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json
  - Tools/n8n/verify-telegram-parity.py
autonomous: true
requirements: [TPL-01, TPL-02, TPL-03, TPL-04, TPL-05]

must_haves:
  truths:
    - "Telegram_Bot outbound HTTP nodes POST to tapi bases (message/send, message/mark/read, chats/typing/start) — zero remaining api/sync outbound URLs"
    - "Telegram text (type:\"text\") routes through the AI agent, not the 'Ask to Send Text' fallback (both Input type Switch nodes match text AND chat)"
    - "Voice humanizer 'Listening Pause' resolves via length_seconds fallback and never yields undefined/NaN"
    - "Chat Memory sessionKey keys on profile_id + ':' + chatId (not from)"
    - "Creating a Telegram workflow re-stamps pre-auth RAG chunks (botTgId '-1' -> new id, keyed by botWaId) and the webhook still returns { id } to Unity"
    - "Creating a WhatsApp workflow mirror-re-stamps (botWaId '-1' -> new id, keyed by botTgId) and still returns { id }"
    - "Suggest_Replies accepts an additive channel field and routes RAG retrieval to the botTgId metadata key for telegram; absent channel behaves byte-identically to v1.0"
    - "verify-telegram-parity.py exits 0 on the edited files"
  artifacts:
    - path: "Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json"
      provides: "tapi-correct Telegram bot clone source"
      contains: "tapi/sync/message/send"
    - path: "Tools/n8n/workflows/Uz6HBBUpAiUqVysB-CreateTelegramWorkflow.json"
      provides: "Create Telegram orchestrator with RAG re-stamp"
      contains: "Restamp RAG Chunks"
    - path: "Tools/n8n/workflows/XuvOp7TxOImOAmlj-CreateWhatsappWorkflow.json"
      provides: "Create WhatsApp orchestrator with mirror RAG re-stamp"
      contains: "Restamp RAG Chunks"
    - path: "Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json"
      provides: "channel-branched RAG retrieval (botWaId | botTgId)"
      contains: "Retrieve RAG TG"
    - path: "Tools/n8n/verify-telegram-parity.py"
      provides: "structural-assert verifier for the 4 edited workflows"
      min_lines: 80
  key_links:
    - from: "Set Wappi Webhook Types"
      to: "Restamp RAG Chunks"
      via: "n8n connection, before Send New Workflows Id"
      pattern: "Restamp RAG Chunks"
    - from: "Suggest_Replies If channel TG?"
      to: "Retrieve RAG TG"
      via: "channel === 'telegram' branch"
      pattern: "botTgId"
---

<objective>
Make the four canonical n8n workflow JSONs Telegram-correct at the FILE level: fix the Telegram_Bot clone source to run on tapi bases + route Telegram text through the AI agent + key session memory stably (TPL-01..04); add a RAG re-stamp node to BOTH Create orchestrators so files uploaded before a channel was authed become retrievable once that channel's workflow is created (TPL-05, server half); add the additive channel-branched RAG retrieval to Suggest_Replies (D3 server half, verified in Phase 7); and ship a repeatable structural-assert verifier the owner runs before deploying.

These are surgical edits guided by exact line references — modify existing nodes, add new nodes, rewire connections. Node ORDER in Telegram_Bot.json is load-bearing (Set Fields in the Create orchestrator indexes nodes[0]/nodes[5]) — do NOT reorder or delete nodes in the bot template.

Purpose: A Telegram-authed bot converses (replies arrive in Telegram, text routes to the agent, memory stays coherent) and its pre-auth price-list files become RAG-retrievable.
Output: 4 edited workflow JSONs + verify-telegram-parity.py.

NOT in this plan: the Unity form-field change (04-02) and the live dev deploy/e2e (04-02's HUMAN-UAT). No client suggestions payload change (Phase 7). WhatsApp_Bot.json is NOT touched (locked: session-continuity risk).
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-n8n-telegram-template-parity-dev/04-CONTEXT.md

# Read before editing (exact node lines + parity checklist + tapi endpoint names):
@.planning/research/telegram-parity/n8n-templates.md
@.planning/research/telegram-parity/tapi-shapes.md
@.planning/research/telegram-parity/suggestions-vmeste.md
@Tools/n8n/README.md

<invariants>
- Bot template node ORDER is load-bearing: Set Fields in the Create orchestrator patches nodes[0].parameters.path and nodes[5].parameters.options.systemMessage by INDEX. URL/param edits and Switch-rule additions do NOT reorder nodes — never insert/delete/reorder nodes inside 4VN3gsFaC2HUYmcc-Telegram_Bot.json.
- Never change the literal workflow ids (they are referenced by literal id inside the Create handlers) and keep templates active:false.
- Both bot templates must stay byte-identical EXCEPT the documented channel deltas (name, botTgId/botWaId filter key, and these 6 tapi fixes) — future diff-audits rely on this.
- n8n Postgres queryReplacement comma-splits list values; single scalar values (workflow ids, no commas) are safe. SQL MUST be parameterized ($1/$2), NEVER string-interpolated.
- Suggest_Replies frozen v1 request is ADDITIVE-only: a client sending no `channel`/`botTgId` must behave exactly as today.
</invariants>

<interfaces>
Existing Postgres credential for executeQuery nodes (reuse verbatim — do NOT create a new one). This exact block is used by ALL executeQuery Postgres nodes in the canonical set: 2htWSV5IHO8E2CgB-Dashboard_Outcomes.json (×4), ZTqpumOpL1rNDOp6-Delete_File.json, and lmjYsdNcQA2IE5rl-Delete_Bot_Files.json:
```
"credentials": { "postgres": { "id": "vvRrFiEXzLVqKjOx", "name": "Postgres" } }
```
CAUTION: the bot templates' memoryPostgresChat "Chat Memory" nodes use a DIFFERENT credential id (`1H5xlpFSESU4w6JH`) — that one is attached to the memory node type only; do NOT copy it onto the executeQuery re-stamp node.

CreateTelegramWorkflow connection chain (Uz6HBBUpAiUqVysB), insert the re-stamp between the last two:
```
Unity Webhook -> Vertical Prompt -> Get Sample Workflow -> Set Fields -> Create Workflow
  -> Get Created Workflow Id -> Activate Created Workflow -> Set Wappi Webhook
  -> Set Wappi Webhook Types -> Send New Workflows Id   (Unity Webhook responseMode: lastNode)
```
"Send New Workflows Id" is a Set node that re-emits `={{ $('Get Created Workflow Id').item.json.id }}` BY NODE REFERENCE — so any passthrough item content is safe as long as it stays the last node.

CreateWhatsappWorkflow (XuvOp7TxOImOAmlj) has the SAME node names and the SAME `Set Wappi Webhook Types -> Send New Workflows Id` tail (confirmed) — mirror the insertion there.

The webhook Unity form field is read the same way the Vertical Prompt node reads BusinessTypeId: `$('Unity Webhook').first().json.body.<FieldName>`. 04-02 adds `WhatsappWorkflowId` to the CreateTelegram form and `TelegramWorkflowId` to the CreateWhatsapp form; this plan reads them from `body`.

Suggest_Replies (9PTyYcelRQI7bGDb) node chain and the two channel-relevant nodes:
```
Webhook -> Prep (jsCode) -> If invalid? -> If skipRag? -> [true -> Assemble | false -> Retrieve RAG (+Embeddings) -> Assemble] -> LLM -> ...
```
Current Retrieve RAG (vectorStoreSupabase, load mode) metadata filter:
```
{ "metadataValues": [ { "name": "botWaId", "value": "={{ $json.botWaId }}" } ] }
```
Prep currently computes: `const skipRag = (botWaId === '' || botWaId === '-1' || !queryText);`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Telegram_Bot.json onto tapi (TPL-01..04)</name>
  <read_first>
    - .planning/research/telegram-parity/n8n-templates.md §1 + §6 (the confirmed bugs)
    - Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json lines 17-263 (send/Switch), 265-343 (Mark Read/Typing), 577 (Listening Pause), 592 (sessionKey)
  </read_first>
  <files>Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json</files>
  <action>
    Six surgical edits (modify existing nodes only — never add/remove/reorder nodes). Use exact-string Edit ops:

    (a) TPL-01 send URL — "HTTP Request" node:
        `https://wappi.pro/api/sync/message/send` -> `https://wappi.pro/tapi/sync/message/send`

    (b) TPL-01 mark-read URL + remove mark_all — "Mark Read" node:
        URL `https://wappi.pro/api/sync/message/mark/read` -> `https://wappi.pro/tapi/sync/message/mark/read`
        Then delete the `mark_all` query parameter object entirely so `queryParameters.parameters` keeps ONLY the `profile_id` entry (tapi mark/read does not document mark_all). Remove the trailing comma after the profile_id block so the array stays valid JSON.

    (c) TPL-01 typing URL — "Typing" node (base AND path change):
        `https://wappi.pro/api/sync/chat/typing/start` -> `https://wappi.pro/tapi/sync/chats/typing/start`
        (tapi path is plural "chats"; tapi has no typing/stop — do not add one.)

    (d) TPL-02 text routing — BOTH Switch nodes "Input type" (~line 151) and "Input type2" (~line 414). In each, the FIRST rule (outputKey "Text") currently matches only `type == "chat"`. Change that rule's `conditions.combinator` from `"and"` to `"or"` and ADD a second condition to that same rule's `conditions.conditions` array matching `type == "text"` (same operator string/equals, same leftValue expression as the existing chat condition in that node, give the new condition a fresh unique id). Keep the "chat" condition (harmless). This routes both "text" and "chat" to the single existing "Text" output — no new output port, no node insertion. Leave the Audio/Image/Document rules untouched.

    (e) TPL-03 voice pause — "Listening Pause" node amount (line 577). Replace:
        `={{ $('Webhook').item.json.body.messages[0].media_info.duration + 2 }}`
        with a fallback chain that also reads the flat webhook `length_seconds` and keeps the same +2 pause math:
        `={{ ($('Webhook').item.json.body.messages[0].media_info?.duration ?? $('Webhook').item.json.body.messages[0].length_seconds ?? 0) + 2 }}`

    (f) TPL-04 sessionKey — "Chat Memory" node (line 592). Replace the trailing `+ ...messages[0].from` with `+ ...messages[0].chatId`:
        `={{ $('Webhook').item.json.body.messages[0].profile_id + ':' + $('Webhook').item.json.body.messages[0].chatId }}`

    Do NOT touch: the send "recipient" (messages[0].from is correct — numeric tapi ids work), the "Download Audio" node (channel-neutral file_link), the group guard If (from == chatId — stays per CONTEXT pending Phase 3 verdicts), the botTgId vector-store filter key.
  </action>
  <verify>
    <automated>cd /Users/ayan/Projects/Automation && python3 -c "import json; json.load(open('Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json'))" && ! grep -Eq 'api/sync/(message/send|message/mark/read|chat/typing/start)' Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json && grep -q 'tapi/sync/chats/typing/start' Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json && grep -q 'length_seconds' Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json && grep -q "profile_id + ':' + \$('Webhook').item.json.body.messages\[0\].chatId" Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'tapi/sync/message/send' <file>` == 1 and no `api/sync/message/send` remains
    - `grep -c 'tapi/sync/message/mark/read' <file>` == 1 and no `mark_all` query param remains in the Mark Read node
    - `grep -c 'tapi/sync/chats/typing/start' <file>` == 1
    - Both Switch nodes contain a `"rightValue": "text"` condition with combinator `"or"` in the Text rule; count of `"rightValue": "text"` == 2
    - `grep 'length_seconds' <file>` present; no `media_info.duration + 2` remains
    - sessionKey ends with `messages[0].chatId` (not `messages[0].from`)
    - File parses as valid JSON (python json.load succeeds)
  </acceptance_criteria>
  <done>Telegram_Bot.json posts to tapi bases, routes text to the agent, uses length_seconds fallback + chatId sessionKey; node count/order unchanged; valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Add RAG re-stamp (both Create orchestrators) + Suggest_Replies channel branch (TPL-05 server + D3)</name>
  <read_first>
    - 04-CONTEXT.md > Create orchestrators — RAG re-stamp (exact SQL semantics, skip semantics, placement invariant)
    - .planning/research/telegram-parity/suggestions-vmeste.md §2 + §4 (Suggest_Replies node chain, single-key filter invariant)
    - Tools/n8n/workflows/Uz6HBBUpAiUqVysB-CreateTelegramWorkflow.json lines 124-270 (Get Created Workflow Id / Send New Workflows Id) + 285-385 (connections)
    - Tools/n8n/workflows/2htWSV5IHO8E2CgB-Dashboard_Outcomes.json (any executeQuery Postgres node — copy its credentials block shape verbatim)
    - Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json lines 23 (Prep jsCode), 108-141 (Retrieve RAG), 164 (Assemble jsCode), 321-410 (connections)
    - .claude/skills/supabase-postgres-best-practices/SKILL.md (parameterized query pattern)
  </read_first>
  <files>Tools/n8n/workflows/Uz6HBBUpAiUqVysB-CreateTelegramWorkflow.json, Tools/n8n/workflows/XuvOp7TxOImOAmlj-CreateWhatsappWorkflow.json, Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json</files>
  <action>
    ADD nodes + rewire connections (three files). All three are "insert node(s) + rewire connection" edits — no existing node is deleted or reordered.

    (1) CreateTelegramWorkflow (Uz6HBBUpAiUqVysB) — RAG re-stamp (TPL-05):
        Add a Postgres node named `Restamp RAG Chunks` (type `n8n-nodes-base.postgres`, executeQuery operation, credential id `vvRrFiEXzLVqKjOx` name `Postgres` — the executeQuery Postgres credential used by Dashboard_Outcomes/Delete_File/Delete_Bot_Files; copy the exact credentials block shape from a Dashboard_Outcomes executeQuery node. NOT the Chat Memory cred `1H5xlpFSESU4w6JH`, which is memoryPostgresChat-only. Unique node id/position.) Parameterized SQL (queryReplacement, NOT interpolated):
          UPDATE documents
          SET metadata = jsonb_set(metadata, '{botTgId}', to_jsonb($1::text))
          WHERE metadata->>'botWaId' = $2 AND metadata->>'botTgId' = '-1';
        Query parameters (queryReplacement): $1 = `={{ $('Get Created Workflow Id').item.json.id }}` (the new TG workflow id), $2 = `={{ $('Unity Webhook').first().json.body.WhatsappWorkflowId }}` (the bot's WhatsApp workflow id, sent by 04-02; when it is "" / "-1" the WHERE simply matches nothing — no If-node needed).
        Robustness (REQUIRED so the response chain never breaks): set the node's `alwaysOutputData: true` AND `onError: "continueRegularOutput"` — a 0-row UPDATE or a DB error must still pass an item downstream WITHOUT surfacing in the HTTP response.
        Rewire: change the `Set Wappi Webhook Types` connection target from `Send New Workflows Id` to `Restamp RAG Chunks`, and add `Restamp RAG Chunks -> Send New Workflows Id`. `Send New Workflows Id` stays the last (response) node and still re-emits the id by node reference, so Unity's `{ id }` contract is preserved.

    (2) CreateWhatsappWorkflow (XuvOp7TxOImOAmlj) — mirror re-stamp (TPL-05):
        Same as (1) but reversed keys:
          UPDATE documents
          SET metadata = jsonb_set(metadata, '{botWaId}', to_jsonb($1::text))
          WHERE metadata->>'botTgId' = $2 AND metadata->>'botWaId' = '-1';
        $1 = `={{ $('Get Created Workflow Id').item.json.id }}` (new WA workflow id), $2 = `={{ $('Unity Webhook').first().json.body.TelegramWorkflowId }}`. Same credential (`vvRrFiEXzLVqKjOx`), same alwaysOutputData/onError settings, and the same `Set Wappi Webhook Types -> Restamp RAG Chunks -> Send New Workflows Id` rewire. Do NOT touch the pre-existing trailing-space activate URL (Phase 8 prod-pass item, out of scope).

    (3) Suggest_Replies (9PTyYcelRQI7bGDb) — additive channel branch (D3 server half):
        - Prep jsCode: accept `const channel = (b.channel === 'telegram') ? 'telegram' : 'whatsapp';` (default whatsapp when absent) and `const botTgId = (typeof b.botTgId === 'string') ? b.botTgId : '';`. Compute the channel-appropriate workflow id: `const wfId = (channel === 'telegram') ? botTgId : botWaId;` and change skipRag to `const skipRag = (wfId === '' || wfId === '-1' || !queryText);`. Echo `channel` and `botTgId` through in the returned json object (keep every existing field).
        - Add a second vectorStoreSupabase node `Retrieve RAG TG` identical to `Retrieve RAG` EXCEPT the single metadata filter key is `botTgId` with value `={{ $json.botTgId }}` (single-key filter invariant — never OR two keys in one node). Give it an embeddings input (connect the existing `Embeddings` node to it via ai_embedding, or add an equivalent embeddings node — executor discretion).
        - Add an If node `If channel TG?` testing `={{ $json.channel }}` equals `telegram`. Rewire the false-branch of `If skipRag?` (currently -> Retrieve RAG) to go to `If channel TG?` instead; `If channel TG?` true -> `Retrieve RAG TG`, false -> `Retrieve RAG`; both `Retrieve RAG` and `Retrieve RAG TG` -> `Assemble` (Assemble reads `$input.all()`, tolerates whichever ran; only one branch executes).
        - Assemble prompt string (cosmetic, keep everything else byte-identical): change «со своего WhatsApp» to «из этого чата» in the first pushed line so the copy is channel-neutral.
        - ADDITIVE guarantee: with no `channel`/`botTgId` in the request, channel resolves to 'whatsapp', wfId = botWaId, `If channel TG?` false -> `Retrieve RAG` (botWaId) — byte-identical routing to v1.0.
  </action>
  <verify>
    <automated>cd /Users/ayan/Projects/Automation && for f in Uz6HBBUpAiUqVysB-CreateTelegramWorkflow XuvOp7TxOImOAmlj-CreateWhatsappWorkflow 9PTyYcelRQI7bGDb-Suggest_Replies; do python3 -c "import json; json.load(open('Tools/n8n/workflows/$f.json'))" || exit 1; done && grep -q "Restamp RAG Chunks" Tools/n8n/workflows/Uz6HBBUpAiUqVysB-CreateTelegramWorkflow.json && grep -q "jsonb_set(metadata, '{botTgId}'" Tools/n8n/workflows/Uz6HBBUpAiUqVysB-CreateTelegramWorkflow.json && grep -q "vvRrFiEXzLVqKjOx" Tools/n8n/workflows/Uz6HBBUpAiUqVysB-CreateTelegramWorkflow.json && grep -q "Restamp RAG Chunks" Tools/n8n/workflows/XuvOp7TxOImOAmlj-CreateWhatsappWorkflow.json && grep -q "jsonb_set(metadata, '{botWaId}'" Tools/n8n/workflows/XuvOp7TxOImOAmlj-CreateWhatsappWorkflow.json && grep -q "vvRrFiEXzLVqKjOx" Tools/n8n/workflows/XuvOp7TxOImOAmlj-CreateWhatsappWorkflow.json && grep -q "Retrieve RAG TG" Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json && grep -q "botTgId" Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both Create orchestrators parse as valid JSON and contain a node named `Restamp RAG Chunks` with credential id `vvRrFiEXzLVqKjOx` (name `Postgres`) — NOT `1H5xlpFSESU4w6JH` (that id is the memoryPostgresChat cred)
    - CreateTelegram SQL uses `jsonb_set(metadata, '{botTgId}'` with `$1`/`$2` placeholders (parameterized) and NO workflow-id string is interpolated inside the SQL text
    - CreateWhatsapp SQL uses `jsonb_set(metadata, '{botWaId}'` with `$1`/`$2` placeholders
    - Both re-stamp nodes have `alwaysOutputData` true and `onError` continueRegularOutput; connections read `Set Wappi Webhook Types -> Restamp RAG Chunks -> Send New Workflows Id`; `Send New Workflows Id` is still the last node
    - Suggest_Replies parses as valid JSON, contains `Retrieve RAG TG` (metadata key `botTgId`), an `If channel TG?` node, Prep computes skipRag from the channel-appropriate `wfId`, and Assemble no longer contains the literal «со своего WhatsApp»
    - Grep for `channel` and `botTgId` present in Prep jsCode; grep for `co своего WhatsApp` returns nothing
  </acceptance_criteria>
  <done>Both Create orchestrators re-stamp the "-1" sentinel via a parameterized UPDATE while preserving the {id} response; Suggest_Replies routes RAG by channel (botWaId | botTgId) additively.</done>
</task>

<task type="auto">
  <name>Task 3: Structural-assert verifier verify-telegram-parity.py</name>
  <read_first>
    - Tools/n8n/verify_rag.py (existing verifier style — assert-and-exit-nonzero)
    - 04-CONTEXT.md > Validation (the exact assert list)
  </read_first>
  <files>Tools/n8n/verify-telegram-parity.py</files>
  <action>
    Write a python3 script (no external deps beyond stdlib json) that loads the 4 edited workflow JSONs by their filenames in Tools/n8n/workflows/ and asserts, exiting non-zero with a clear message on the FIRST violation (mirror verify_rag.py's print-and-sys.exit(1) style):

    Telegram_Bot.json (4VN3gsFaC2HUYmcc): (i) no `api/sync/` outbound URL remains in the send/mark-read/typing nodes and the three tapi URLs are present (message/send, message/mark/read, chats/typing/start); (ii) no `mark_all` query param in the Mark Read node; (iii) both Switch nodes' first rule match a `text` value; (iv) `length_seconds` present in Listening Pause; (v) sessionKey ends with `chatId`; (vi) node COUNT unchanged (assert the node array length equals the pre-edit count) and nodes[0].name == "Webhook" and nodes[5].name == "AI Agent" (order invariant); (vii) vector-store filter key still `botTgId`.

    CreateTelegramWorkflow + CreateWhatsappWorkflow: (i) a `Restamp RAG Chunks` Postgres node exists with credential id `vvRrFiEXzLVqKjOx` (the executeQuery Postgres credential — explicitly assert the id is NOT `1H5xlpFSESU4w6JH`, the memory-node cred); (ii) its SQL contains the expected jsonb_set target (`{botTgId}` for TG, `{botWaId}` for WA) and uses `$1`/`$2` placeholders; (iii) assert NO workflow-id expression is interpolated directly inside the SQL string (guard against injection regressions — the SQL must contain `$1` and `$2`, not `{{`); (iv) connections place `Restamp RAG Chunks` between `Set Wappi Webhook Types` and `Send New Workflows Id`, and `Send New Workflows Id` is a terminal node (no outgoing main connection); (v) the Unity Webhook responseMode is still lastNode.

    Suggest_Replies (9PTyYcelRQI7bGDb): (i) `Retrieve RAG TG` node exists with a single metadata filter key `botTgId`; (ii) the original `Retrieve RAG` still filters `botWaId` (single-key each — assert neither node ORs two keys); (iii) an `If channel TG?` node exists on the RAG path; (iv) Prep jsCode references `channel` and `botTgId`; (v) Assemble no longer contains «со своего WhatsApp».

    Print a final "ALL PARITY ASSERTS PASSED" and exit 0 when everything holds. Make it runnable as `python3 Tools/n8n/verify-telegram-parity.py` from repo root (resolve paths relative to the script's own location so cwd doesn't matter).
  </action>
  <verify>
    <automated>cd /Users/ayan/Projects/Automation && python3 Tools/n8n/verify-telegram-parity.py && echo VERIFIER_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `python3 Tools/n8n/verify-telegram-parity.py` exits 0 and prints "ALL PARITY ASSERTS PASSED" against the Task 1 + Task 2 edits
    - The verifier asserts the re-stamp nodes' credential id == `vvRrFiEXzLVqKjOx` and fails if it is `1H5xlpFSESU4w6JH`
    - Temporarily reverting any single edit (e.g. restoring an api/sync URL) makes the script exit non-zero with a message naming the failed assert (spot-check at least one revert during development, then restore)
    - Script has no non-stdlib imports and resolves workflow paths from its own __file__ location
  </acceptance_criteria>
  <done>A repeatable verifier proves all four workflows carry the Telegram-parity edits and the injection-safe re-stamp shape; the owner runs it before deploying (04-02 HUMAN-UAT step).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unity client → n8n Create webhook | Client-sent workflow-id form fields (WhatsappWorkflowId / TelegramWorkflowId) cross into a Postgres UPDATE |
| Unauthenticated client → Suggest_Replies webhook | Client `channel` field influences which RAG metadata key is used |
| n8n → Supabase Postgres | Re-stamp UPDATE on documents.metadata + vector retrieval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (SQLi) | Restamp RAG Chunks node (both Create orchestrators) | mitigate | SQL is parameterized via n8n queryReplacement (`$1`/`$2`); workflow-id values are NEVER string-interpolated into the query. jsonb_set target path is a static literal (`{botTgId}`/`{botWaId}`), not client-derived. Verifier Task 3 asserts `$1`/`$2` present and no `{{` inside the SQL string. |
| T-04-02 | Information Disclosure | Webhook response to Unity | mitigate | Restamp node sets `onError: continueRegularOutput` + `alwaysOutputData: true`; `Send New Workflows Id` re-emits `{ id }` by node reference — a Postgres error or 0-row result never surfaces in the HTTP body. |
| T-04-03 | Tampering (filter-key injection) | Suggest_Replies RAG filter key | mitigate | The metadata filter KEY is chosen by a server-side branch (`channel === 'telegram'` → `botTgId`, else `botWaId`); the client `channel` field only selects between two hardcoded keys and never becomes the key or value. Single-key filter invariant preserved (Retrieve RAG / Retrieve RAG TG each filter exactly one key). |
| T-04-04 | Elevation / cross-tenant RAG | Suggest_Replies botTgId value | accept | botTgId carries the bot's own workflow id — same trust model as botWaId in the v1.0 contract that closed 14/14 threats; the additive field does not widen scope. skipRag sentinel (`''`/`'-1'`) still gates empty ids; absent channel = byte-identical v1 behavior. |
</threat_model>

<verification>
- All 4 workflow JSONs parse (`python3 -c "import json; json.load(...)"`).
- `python3 Tools/n8n/verify-telegram-parity.py` exits 0.
- No `api/sync/` outbound URL remains in Telegram_Bot.json; three tapi URLs present.
- Both Create orchestrators contain a parameterized `Restamp RAG Chunks` node (credential `vvRrFiEXzLVqKjOx`) preserving the `{ id }` response.
- Suggest_Replies routes RAG additively by channel with single-key filters.
- Live deploy + e2e (TPL-06) is the owner gate written in 04-02's 04-HUMAN-UAT.md — NOT run here (dev n8n is not running; its API key is in deny-ruled secrets.json).
</verification>

<success_criteria>
- Telegram_Bot.json is tapi-correct (TPL-01), routes text to the agent (TPL-02), uses length_seconds fallback (TPL-03), keys memory on chatId (TPL-04) — node count/order unchanged.
- Both Create orchestrators re-stamp pre-auth RAG chunks via a parameterized UPDATE and still return { id } to Unity (TPL-05 server half).
- Suggest_Replies carries an additive channel-branched RAG retrieval (D3 server half, injection-hardened; verified live in Phase 7).
- verify-telegram-parity.py is the repeatable pre-deploy check.
</success_criteria>

<output>
After completion, create `.planning/phases/04-n8n-telegram-template-parity-dev/04-01-SUMMARY.md`.
</output>
