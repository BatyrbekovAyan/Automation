---
phase: 05-channel-aware-chatmanager-core
plan: 06
type: execute
wave: 5
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified:
  - Assets/Scripts/Main/ChatManager.cs
  - Assets/Scripts/Chat/ChatDialog.cs
  - Assets/Scripts/Chat/ReplyParser.cs
  - Assets/Tests/Editor/Chat/TelegramMediaNormalizeTests.cs
  - Assets/Tests/Editor/Chat/TelegramMessageTypeTests.cs
autonomous: false
user_setup:
  - service: "Wappi tapi (dev Telegram profile)"
    why: "Live shape capture is a user-assisted gate — secrets.json is deny-ruled for Claude, so the owner must run the capture script against an authorized dev Telegram profile before the media/Normalize parser work can be grounded in real JSON."
    dashboard_config:
      - task: "Run Tools/tapi/capture-shapes.sh and record the PENDING CAPTURE verdicts in Tools/tapi/SHAPES.md (tracked in 03-HUMAN-UAT.md)"
        location: "Local machine with an authorized dev Telegram Wappi profile"
requirements: [CHAT-03, CHAT-07]

must_haves:
  truths:
    - "The owner has run Tools/tapi/capture-shapes.sh and every PENDING CAPTURE verdict in Tools/tapi/SHAPES.md is resolved to confirmed shape / divergence / not-observed before any implementation task runs"
    - "Telegram media messages (image/video/voice/document, plus sticker/GIF per the recorded verdicts) Normalize into MessageViewModels with thumbnails/durations/downloads per the captured shapes"
    - "Telegram-only media type strings map correctly in ParseMessageType (no silent drop to Unknown for observed types)"
    - "Incoming Telegram replies render quoted cards (snapshot + messages/id/get recovery) with the echo-bug guard applied only if the capture shows tapi shares the WhatsApp echo quirk"
    - "The reactions-receive go/no-go decision is honored: receive-side reactions are built only if a viable tapi transport was observed, otherwise an explicit v2 deferral (TG-REACT-RECV) note is recorded"
    - "Dialog name/thumbnail fallbacks are applied per verdict (backfill from user.FirstName / last_message_sender.pushname if chats/filter name is empty)"
    - "Every existing WhatsApp behavior is byte-identical and the full EditMode suite is green"
  artifacts:
    - path: "Assets/Scripts/Main/ChatManager.cs"
      provides: "Telegram media Normalize branches + type-string mapping per captured verdicts"
      contains: "MessageType"
  key_links:
    - from: "Assets/Scripts/Main/ChatManager.cs Normalize"
      to: "captured tapi media shapes (body / s3Info / attaches / length_seconds)"
      via: "channel-aware media parse per SHAPES.md verdicts"
      pattern: "MessageType"
---

<objective>
Close every tapi divergence that REQUIRES live-captured media samples. This plan is BLOCKED on the Phase-3 owner capture run: `Tools/tapi/SHAPES.md` currently records `PENDING CAPTURE` for the media-body shape, sticker/video-note/GIF type strings, reactions-receive transport, dialog name/thumbnail presence, and the reply echo-bug. Its FIRST task is a human-action checkpoint. If the verdicts are still `PENDING CAPTURE`, the executor returns at the checkpoint and the phase closes as `human_needed` (the established pattern) — the implementation tasks do NOT run against guesses.

Once verdicts exist, port the media Normalize branches (currently WhatsApp `body`-as-JObject / `s3Info.url` / `media_info.duration` at ChatManager.cs:1414-1530), add the observed sticker/video-note/GIF type strings + voice duration source, decide reactions-receive (build or record the v2 deferral), apply the reply-snapshot echo-bug guard only if the capture shows tapi shares it, and add dialog name/thumbnail fallbacks.

Purpose: Telegram media + incoming replies render at WhatsApp parity, grounded in real JSON.
Output: capture-gated Normalize/type/reply/name-fallback work + tests — OR a clean human_needed return.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-channel-aware-chatmanager-core/05-CONTEXT.md
@.planning/phases/05-channel-aware-chatmanager-core/05-03-SUMMARY.md

# MANDATORY skill read before matching work:
# - .claude/skills/chat-data-flow/SKILL.md (RawMessage -> NormalizedMessage -> MessageViewModel pipeline — REQUIRED)

# The verdict checklist THIS plan consumes — check verdict status FIRST:
@Tools/tapi/SHAPES.md
@.planning/research/telegram-parity/tapi-shapes.md   # §4 message shape, §5 replies/reactions, §10 parser impact list

<interfaces>
<!-- From 05-01..05-04 (committed): ChatChannel, WappiEndpoints, ChatIdFormat, ActiveChannel,
     ParseMessageType with the "text" case, ChatDialog.last_time/type, channel-aware media/quote URLs. -->

<!-- The capture-gated touchpoints (verified line refs; implement per verdicts): -->
// ChatManager.cs:1414-1530  Normalize media branches — WA: body-as-JObject {JPEGThumbnail,url,fileName,
//   mimetype,fileLength,pageCount,width,height}, s3Info.url, media_info.duration. TG shape per SHAPES.md Q1.
// ChatManager.cs ParseMessageType — add observed TG media type strings (sticker/video-note/GIF) per Q2;
//   "text" already maps to Chat (05-03).
// ChatManager.cs voice duration — WA reads media_info.duration; TG source per Q1 (webhook uses length_seconds).
// ReplyParser.cs FromSnapshot (reply echo guard) — apply only if Q8 shows tapi echoes the replying body.
// ChatManager.QuoteResolve.cs / QuotedMessageCache — messages/id/get recovery already channel-aware (05-03);
//   deepen TG-specific handling only if Q3/Q8 verdicts demand it (CHAT-07).
// ParseChatsJson name/thumbnail fallback — backfill from user.FirstName / last_message_sender.pushname per Q5.
// isDeleted stickiness — ParseChatsJson isDeleted skip already honors it; adjust per Q7 verdict.
// Reactions-receive — ChatManager.cs:1532 + ReactionStore pipeline; build ONLY if Q3 go/no-go == GO.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: CHECKPOINT — capture live tapi shapes and record SHAPES.md verdicts</name>
  <files>Tools/tapi/SHAPES.md</files>
  <what-built>Nothing yet — this gate confirms the parser work has real JSON to build against.</what-built>
  <action>
    BLOCKING human-action gate — the executor makes NO code changes in this task; it only checks the capture state and either proceeds or returns human_needed.
    1. Read `Tools/tapi/SHAPES.md`. If every required verdict (Q1, Q2, Q3 + reactions-receive go/no-go, Q5, Q7, Q8) is resolved (confirmed shape / divergence / not-observed), record that the gate is open and continue to Task 2.
    2. If ANY required verdict is still `PENDING CAPTURE`, STOP: return `human_needed` with the owner instructions in &lt;how-to-verify&gt; and do NOT run Tasks 2-3 (the phase closes as human_needed; resume once capture is done). Never implement the media/Normalize branches against unresolved verdicts.
  </action>
  <how-to-verify>
    1. On a machine with an authorized dev Telegram Wappi profile, run `Tools/tapi/capture-shapes.sh` (read-only; token never leaves the machine). See `Tools/tapi/README.md`.
    2. Open the sanitized samples in the gitignored `Tools/tapi/samples/` and set each `PENDING CAPTURE` verdict in `Tools/tapi/SHAPES.md` to `confirmed shape` / `divergence` / `not-observed` (structural verdicts + redacted excerpts only — NO raw PII). Track the run in `03-HUMAN-UAT.md`.
    3. Confirm at minimum these verdicts are resolved: Q1 (media body shape), Q2 (sticker/video-note/GIF type strings), Q3 + Reactions-receive go/no-go, Q5 (name/thumbnail presence), Q7 (isDeleted), Q8 (reply echo bug).

    GATE: If any of those are still `PENDING CAPTURE`, STOP and return `human_needed` — do NOT run Tasks 2-3 against guesses.
  </how-to-verify>
  <verify>Read Tools/tapi/SHAPES.md — the Q1/Q2/Q3/Q5/Q7/Q8 verdicts are no longer `PENDING CAPTURE`.</verify>
  <done>Every required SHAPES.md verdict is resolved (gate open → proceed) OR the executor returned human_needed with capture instructions (gate closed).</done>
  <resume-signal>Type "captured" once SHAPES.md verdicts are recorded (or "blocked" if capture can't run — the phase closes as human_needed).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Telegram media Normalize branches + type strings + voice duration (per verdicts)</name>
  <files>Assets/Scripts/Main/ChatManager.cs, Assets/Scripts/Chat/ChatDialog.cs, Assets/Tests/Editor/Chat/TelegramMediaNormalizeTests.cs, Assets/Tests/Editor/Chat/TelegramMessageTypeTests.cs</files>
  <read_first>.claude/skills/chat-data-flow/SKILL.md</read_first>
  <behavior>
    - Each observed TG media type (image/video/voice/document + any sticker/video-note/GIF the capture recorded) Normalizes to the correct MessageType with the media url/thumbnail/duration sourced per the verdict
    - An unobserved / unknown type still maps to Unknown (dropped, never crashes)
    - Voice duration is sourced from the field the capture confirmed (e.g. length_seconds) — TG voice bubbles no longer show duration 0 when the source exists
    - The parse is null-tolerant: a missing s3Info/attaches/thumbnail degrades gracefully, never throws
  </behavior>
  <action>
    ONLY after Task 1 verdicts are recorded. Read the resolved `Tools/tapi/SHAPES.md` (Q1/Q2) + the sanitized excerpts. Then, gated on `ActiveChannel == ChatChannel.Telegram` (WhatsApp branches stay byte-identical):
    - Extend the Normalize media branches (ChatManager.cs:1414-1530) to read the TG media shape the capture confirmed (body-as-JObject vs `s3Info.url` vs `attaches` vs flat fields; thumbnail; dimensions). Keep the WhatsApp path untouched.
    - Add the observed TG media `type` strings to `ParseMessageType` (sticker / video-note / GIF handling per Q2; respect `isGif` if the verdict says GIFs arrive as `type:"video"`). Unknown/unobserved types remain Unknown.
    - Source TG voice duration from the confirmed field (Q1; webhook evidence points at `length_seconds`) — add the field to the wire model if needed.
    Prefer extracting the type/shape decisions into pure testable seams (MessageTypeParser from 05-03, plus a small media-shape resolver) so the branches are unit-testable without a live server.

    Add `TelegramMessageTypeTests.cs` (observed type strings → correct MessageType; unknown → Unknown) and `TelegramMediaNormalizeTests.cs` (redacted sample-shaped inputs → expected media url/duration/thumbnail; missing fields degrade, don't throw).
  </action>
  <verify>
    <automated>Tools/run-tests-headless.sh "TelegramMedia|TelegramMessageType"</automated>
  </verify>
  <done>TG media Normalizes per captured shapes; observed types map; voice duration sourced correctly; WhatsApp byte-identical; targeted tests green.</done>
  <acceptance_criteria>
    - `test -f Assets/Tests/Editor/Chat/TelegramMediaNormalizeTests.cs && test -f Assets/Tests/Editor/Chat/TelegramMessageTypeTests.cs`
    - `grep -q "ChatChannel.Telegram" Assets/Scripts/Main/ChatManager.cs`
    - `Tools/run-tests-headless.sh "TelegramMedia|TelegramMessageType"` reports 0 failures
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Reply echo guard + reactions-receive go/no-go + name/thumbnail + isDeleted; full suite green</name>
  <files>Assets/Scripts/Chat/ReplyParser.cs, Assets/Scripts/Main/ChatManager.cs</files>
  <read_first>.claude/skills/chat-data-flow/SKILL.md</read_first>
  <action>
    ONLY after Task 1 verdicts. Apply the remaining capture-gated divergences per SHAPES.md:
    - Reply echo bug (Q8): apply the `ReplyParser.FromSnapshot` snapshot-echo blanking to the Telegram path ONLY if the capture shows tapi echoes the replying message's own body; if the verdict says it does NOT, leave the TG snapshot as-is (the messages/id/get recovery from 05-03 already handles missing bodies). Record the decision in the SUMMARY (CHAT-07).
    - Reactions-receive (Q3 go/no-go): if GO (a viable tapi receive transport was observed), implement the receive-side ingestion following the existing ReactionStore pipeline; if NO-GO, do NOT build it — record the explicit v2 `TG-REACT-RECV` deferral note in the SUMMARY (send-side CHAT-08 already ships in 05-04, unaffected).
    - Dialog name/thumbnail (Q5): if chats/filter leaves `name` empty for TG user dialogs, backfill the display name from `last_message_sender.pushname` (already parsed) or add a minimal nested user model to reach `user.FirstName` — per the verdict. Keep the WhatsApp fallback unchanged.
    - isDeleted (Q7): confirm ParseChatsJson's `isDeleted` skip matches the observed TG stickiness/revival semantics; adjust only if the verdict diverges.

    Run the FULL suite headless; require green. Commit new .cs + .meta together. In the SUMMARY, record each Q verdict's disposition and any v2 deferrals.
  </action>
  <verify>
    <automated>Tools/run-tests-headless.sh</automated>
  </verify>
  <done>Echo guard applied per verdict; reactions-receive built or explicitly deferred; name/thumbnail + isDeleted handled per verdicts; WhatsApp byte-identical; full suite green.</done>
  <acceptance_criteria>
    - `Tools/run-tests-headless.sh` reports 0 failures
    - SUMMARY records the Q3/Q7/Q8 dispositions and any TG-REACT-RECV deferral
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wappi tapi media JSON → Normalize | Untrusted, previously-uncaptured media/reply shapes (base64 body, s3Info, attaches, nested user objects) are parsed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0506-01 | Denial of Service | Normalize media branches | mitigate | Parse per captured shape but stay null-tolerant — missing s3Info/attaches/thumbnail/duration degrade to a renderable bubble, unknown types map to Unknown; no field access throws. Tested with missing-field inputs. |
| T-0506-02 | Information Disclosure | SHAPES.md excerpts | mitigate | Committed verdicts carry structural notes + redacted excerpts only; raw samples stay in the gitignored Tools/tapi/samples/ (owner-run capture, token never leaves the machine). |
| T-0506-03 | Tampering | reply snapshot echo | mitigate | Echo-blanking applied only where the capture confirms the quirk; otherwise the messages/id/get recovery path (null-tolerant) fills the quoted card — no fabricated quote text. |
</threat_model>

<verification>
- Task 1 gate honored: no implementation runs while SHAPES.md verdicts are PENDING CAPTURE.
- `Tools/run-tests-headless.sh` full suite green (when Tasks 2-3 run).
- WhatsApp regression: WhatsApp Normalize/reply/name paths byte-identical (TG branches gated on ActiveChannel==Telegram).
</verification>

<success_criteria>
- SHAPES.md verdicts recorded (or a clean human_needed return at the checkpoint).
- Telegram media renders per captured shapes (CHAT-03); incoming replies render quoted cards (CHAT-07).
- Reactions-receive built or explicitly deferred to v2; name/thumbnail + isDeleted handled per verdicts.
- WhatsApp byte-identical; suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-channel-aware-chatmanager-core/05-06-SUMMARY.md` (record each Q-verdict disposition + any TG-REACT-RECV v2 deferral). If the phase closes at the checkpoint, note human_needed and the pending capture in the SUMMARY.
</output>
