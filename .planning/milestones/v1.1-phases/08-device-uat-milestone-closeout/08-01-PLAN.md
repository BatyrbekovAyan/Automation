---
phase: 08-device-uat-milestone-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
autonomous: false
requirements: []   # closeout phase owns no v1.1 REQ-ID; this plan closes carried uat_gap/verification_gap items across the milestone (see ROADMAP Phase 8: "none new")
user_setup: []

must_haves:
  truths:
    - "The owner can run one ordered on-device pass that covers EVERY still-open milestone gate (auth+2FA, chat list/history/media incl. the 05-07/08 treatments, the 05-09 fixes, the vthumb id-ambiguity probe, switcher, auto-reply e2e, live «Вместе»+dashboard, and the carried v1.0 scenarios)."
    - "Every runbook item states expected / how-to / PASS·FAIL·N/A and cites the source doc it came from, so a FAIL is unambiguous and traceable to the fix that must reopen."
    - "A 'Defects found' section captures any FAIL so it can spin its own gap-closure plan; the phase stays human_needed until the owner records results."
  artifacts:
    - path: ".planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md"
      provides: "Consolidated owner-run device-UAT runbook aggregating every open gate with source refs"
      contains: "05-06-REVIEW WR-02"
  key_links:
    - from: "08-DEVICE-UAT.md auth section"
      to: "05-VERIFICATION.md #3 (2FA live round-trip)"
      via: "source-ref citation"
      pattern: "05-VERIFICATION"
    - from: "08-DEVICE-UAT.md media section"
      to: "05-HUMAN-UAT.md (.tgs card / кружок float / GIF badge / 05-09 number+chip)"
      via: "source-ref citation"
      pattern: "05-HUMAN-UAT"
    - from: "08-DEVICE-UAT.md n8n-dependent section"
      to: "04-HUMAN-UAT.md (auto-reply e2e) + 07-HUMAN-UAT.md (live «Вместе» + dashboard)"
      via: "shared dev-n8n session note"
      pattern: "0[47]-HUMAN-UAT"
    - from: "08-DEVICE-UAT.md carried-v1.0 section"
      to: "01-HUMAN-UAT.md + 02-HUMAN-UAT.md + 01-VERIFICATION.md"
      via: "run-or-re-defer citation"
      pattern: "0[12]-HUMAN-UAT"
---

<objective>
Author `08-DEVICE-UAT.md`: ONE consolidated, ordered, owner-run device-UAT runbook that aggregates EVERY still-open device-verify gate across the whole v1.1 milestone (plus the carried v1.0 deferred UAT) into a single pass the owner runs on a real build. This runbook is the single source of truth for "is v1.1 shippable" — it must be complete enough that any FAIL is unambiguous and traceable to the exact fix/source it reopens.

Purpose: Phases 3-7 are all code-complete + green, but their device/live confirmations are scattered across seven separate gate docs. Consolidating them into one traceable checklist is what lets the milestone close cleanly and lets a FAIL become its own gap-closure plan.

Output: `08-DEVICE-UAT.md` (the runbook). Writing it is autonomous; RUNNING it is the owner gate (Task 2) — the phase stays `human_needed` until the owner records results.

Read first (authoritative enumeration of what the runbook must cover): `08-CONTEXT.md` §Decisions → "08-01". Do NOT invent new scope — aggregate the enumerated gates verbatim, each with its source ref.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-CONTEXT.md

<!-- Source gate docs to aggregate — this runbook is BUILT from these; the executor
     needs their exact PASS/FAIL items + source anchors for traceability fidelity. -->
@.planning/phases/05-channel-aware-chatmanager-core/05-HUMAN-UAT.md
@.planning/phases/05-channel-aware-chatmanager-core/05-VERIFICATION.md
@.planning/phases/06-channel-switcher-ui/06-HUMAN-UAT.md
@.planning/phases/04-n8n-telegram-template-parity-dev/04-HUMAN-UAT.md
@.planning/phases/07-vmeste-suggestions-dashboard-on-telegram/07-HUMAN-UAT.md
@.planning/milestones/v1.0-phases/01-polished-suggestions-panel-on-mock-data/01-HUMAN-UAT.md
@.planning/milestones/v1.0-phases/01-polished-suggestions-panel-on-mock-data/01-VERIFICATION.md
@.planning/milestones/v1.0-phases/02-n8n-live-wiring/02-HUMAN-UAT.md

<constraints>
- This is a DOC-ONLY plan. No app code changes. If authoring surfaces a trivially-obvious pre-device fix, FLAG it in the "Defects found / pre-device notes" section — do NOT fold a code change into this plan.
- Do NOT run any device test, capture script, deployer, or verifier yourself. Do NOT tick any owner checkbox on the owner's behalf. Every item ships blank (`☐ PASS ☐ FAIL ☐ N/A`).
- No secrets, tokens, or API keys anywhere in the doc (`secrets.json` is deny-ruled). Reference credentials by name only.
- Unity Editor may be open; you are only WRITING a markdown doc, so no Editor/test-bridge interaction is involved.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the consolidated 08-DEVICE-UAT.md runbook (all open gates, grouped + source-ref'd)</name>
  <files>.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md</files>
  <action>
Create `08-DEVICE-UAT.md`. Structure: a short **Preamble** (status OPEN/owner-run; environment: a real device build, an authorized dev Telegram profile, a 2FA-enabled Telegram account for the auth group, and the dev n8n at localhost:5678 + cloudflared tunnel for the n8n-dependent groups G/H — those two ride ONE shared dev-n8n session), then one **grouped section per gate**, then a **Defects found** section, then an **Overall result** line.

Each item MUST use this exact shape:
`**expected:** … | **how-to:** … | **verdict:** ☐ PASS ☐ FAIL ☐ N/A | **source:** <doc-ref>`

Author these groups verbatim from `08-CONTEXT.md §Decisions 08-01`, pulling the specific items from the @-referenced source docs:

- **A. Auth — Telegram phone/code + 2FA cloud-password (both code AND QR flows).** Correct password → `ShowAuthSuccess`; wrong password → «Неверный пароль» re-prompt without loop/crash; `TelegramCodeInput` empty afterward on every path. **source:** 05-VERIFICATION.md #3 (+ 05-05 SUMMARY).
- **B. Chat client — list / history / media render + send.** List (names, avatars, unread, correct time via `last_time` fallback); paginated history (text via `type:"text"`, numeric ids never slice/crash); media render for **image / video / voice(ptt) / document**; the **05-07/08 treatments**: `.tgs` → sticker **CARD** («Стикер», 396² neutral rounded card, NOT a document card), video note (кружок) → **bubble-free** floating circle + duration badge, GIF → **"GIF"** corner badge; static webp sticker renders via the existing Sticker path (05-HUMAN-UAT Notes); send text / send media / send quoted-reply; send + remove reaction (pill toggles through the server echo); incoming reply → quoted card; opening an unread chat marks it read (no `mark_all`); swipe-to-delete hidden on Telegram, WhatsApp unchanged. **source:** 05-VERIFICATION.md #1/#2, 05-HUMAN-UAT.md #1-3 + the 05-08 note-float/sticker-card refinements. Also record the "not-a-bug if seen" observations from 05-VERIFICATION #2 (no chat-list "X reacted…" preview on TG; own-reaction-removal one-cycle flicker) so they are not mis-filed as defects.
- **C. 05-09 field/UI fixes.** Bot-settings Telegram number shows a **clean phone (or hides, then repopulates on the next status check)** — NEVER the raw JSON blob; channel-switcher chip labels have comfortable side padding; opening a **healthy authorized** Telegram bot's settings does NOT trip outside-app de-auth (number stays, `isOnTelegram` stays on, Wappi profile not deleted). **source:** 05-HUMAN-UAT.md #4/#5/#6.
- **D. vthumb id-ambiguity probe.** Send/receive videos with overlapping short numeric message ids across TWO Telegram dialogs on the SAME profile (e.g. a channel post + a private chat) → each video's downloaded bytes + thumbnail match its own source dialog. If it crosses, note the follow-up: check whether tapi accepts a `chat_id` param on `message/media/download`. **source:** 05-VERIFICATION.md #4 / **05-06-REVIEW WR-02** / STATE blocker.
- **E. Video-note `is_round` re-capture (optional, minor).** Confirm whether a genuine кружок ever reports `is_round:true` on re-capture; heuristic detection is the shipped path either way. **source:** 05-08 / 05-HUMAN-UAT #2.
- **F. Switcher (Phase 6).** Segmented flip mid-flight-safe (no crossed lists / no flicker); re-tap selected chip = no-op; muted chip is tappable → connect empty state (both WA-only and TG-only bots); single-connected-channel bot auto-selects; last-used channel persists per bot across restart; bottom bar shows exactly 4 tabs, tab 0 reads «Чаты», no reachable pink Telegram screen. Also carry the two 06 deferred-polish decisions (per-row swipe-delete affordance on TG; RU-localization sweep of English empty-state copy IN-09) as **record-owner-decision** lines. **source:** 06-HUMAN-UAT.md.
- **G. Auto-reply e2e (Phase 4 TPL-06).** Create a Telegram bot from the app (fresh clone off the fixed template) → text reply arrives in Telegram; voice → transcription + humanized pause; multi-turn memory stays coherent; **pre-auth-file RAG re-stamp** — upload a price-list BEFORE authing Telegram, then ask a price question in Telegram → grounded answer; recreate any stale `api/sync` clone; DEACTIVATE the clone after the window; prod bagkz untouched. **source:** 04-HUMAN-UAT.md. (Rides the dev-n8n session.)
- **H. Live «Вместе» + Dashboard live-data (Phase 7).** Suggestions populate in a real Telegram chat and are RAG-grounded via `botTgId`; «Сводка» counts/lists that real Telegram conversation, a dual-channel bot shows exactly ONE chip covering both profiles, and a Telegram outcome row deep-links straight into that Telegram chat. **source:** 07-HUMAN-UAT.md (rides the SAME dev-n8n session as G).
- **I. Carried v1.0 (Phases 01/02).** Enumerate the 4 pending 01 scenarios, the 5 pending 02 scenarios, and the 01-VERIFICATION device confirmation — each as **run OR explicitly re-defer with a reason** (a re-defer is a valid disposition and rolls forward via 08-03). **source:** 01-HUMAN-UAT.md, 02-HUMAN-UAT.md, 01-VERIFICATION.md.

End with **Defects found** (table: item / group / severity / source-anchor / → gap-closure plan?) and an **Overall: ☐ PASS ☐ ISSUES** line. Add a one-line note that any FAIL spins its own gap-closure plan (`/gsd-plan-phase 08 --gaps`) and is out of scope to pre-plan here.
  </action>
  <verify>
    <automated>D=.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md; test -f "$D" && grep -q "2FA" "$D" && grep -q "05-VERIFICATION" "$D" && grep -q "05-06-REVIEW WR-02" "$D" && grep -qi "vthumb" "$D" && grep -q "\.tgs" "$D" && grep -qi "GIF" "$D" && grep -q "05-HUMAN-UAT" "$D" && grep -q "06-HUMAN-UAT" "$D" && grep -q "04-HUMAN-UAT" "$D" && grep -q "07-HUMAN-UAT" "$D" && grep -q "01-HUMAN-UAT" "$D" && grep -q "02-HUMAN-UAT" "$D" && grep -qi "defects" "$D" && grep -qi "PASS" "$D" && grep -qi "FAIL" "$D" && echo OK</automated>
  </verify>
  <done>`08-DEVICE-UAT.md` exists; contains every group A-I with per-item expected/how-to/PASS·FAIL·N/A + a cited source ref; the vthumb probe cites 05-06-REVIEW WR-02; the n8n-dependent groups note the shared dev-n8n session; a Defects section + Overall line are present; every checkbox is blank.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner runs the consolidated device pass and records results</name>
  <action>Owner-run gate (not executor work): run `08-DEVICE-UAT.md` end-to-end on a real device build and record PASS/FAIL/N/A per item. Do not tick on the owner's behalf.</action>
  <what-built>The consolidated device-UAT runbook `08-DEVICE-UAT.md` — every open v1.1 gate + the carried v1.0 scenarios, grouped and source-ref'd, in one ordered owner pass.</what-built>
  <how-to-verify>
1. Open `.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md`.
2. Produce a real device build (Android primary). For groups G + H, start the dev n8n (localhost:5678) + cloudflared tunnel and run `Tools/n8n/rotate-tunnel.py` first (do NOT touch prod bagkz).
3. Work the runbook top-to-bottom; record `PASS` / `FAIL` / `N/A` on each item and fill the Defects table for anything that fails. Re-defer any carried v1.0 scenario you choose not to run, with a one-line reason.
4. Set the Overall line. On any FAIL, run `/gsd-plan-phase 08 --gaps` to spin a gap-closure plan; do not hand-patch here.
  </how-to-verify>
  <resume-signal>Type "approved" once results are recorded (Overall = PASS, or FAILs filed as gaps), or describe the FAILs to spin gap-closure plans.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device build → Wappi/tapi live servers | The device pass drives the owner's real Wappi token against live WhatsApp/Telegram infra (token is deny-ruled from Claude; never in this doc). |
| planning doc → repo | The runbook is a committed markdown artifact; its only sensitive content risk is embedded secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `08-DEVICE-UAT.md` content | mitigate | No tokens/keys/profile ids/phone numbers in the doc; reference `secrets.json` + credentials by name only; how-to steps describe actions, not values. |
| T-08-02 | Tampering | owner ticking on Claude's behalf | mitigate | Task 2 is a blocking human gate; the auto task ships every checkbox blank and explicitly forbids self-ticking. |
| T-08-03 | Repudiation | untraceable FAIL | mitigate | Every item cites a source-doc anchor; the Defects table maps each FAIL to its origin so a gap-closure plan is unambiguous. |
| T-08-04 | Denial of Service / blast radius | live bot clone during the e2e pass | accept | Groups G/H run against a dev clone that is deactivated after the window (runbook step); prod stays dormant — no new attack surface, existing bot-activation policy governs. |
</threat_model>

<verification>
- `08-DEVICE-UAT.md` exists and the Task-1 automated grep chain returns OK (every gate group + source ref + Defects/PASS/FAIL present).
- No app code touched: `git status --porcelain Assets/` is empty for this plan.
- No secret material in the doc: a scan for token/key/JWT-shaped strings finds nothing.
</verification>

<success_criteria>
- One consolidated runbook covers every gate enumerated in 08-CONTEXT §08-01, each item traceable to its source doc.
- The plan is `autonomous: false`; the phase remains `human_needed` until the owner records results at the Task-2 gate.
- Any FAIL is filed in the Defects section for a follow-up gap-closure plan (not patched here).
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-01-SUMMARY.md`.
</output>
