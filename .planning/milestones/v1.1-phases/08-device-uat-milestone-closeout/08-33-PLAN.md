---
phase: 08-device-uat-milestone-closeout
plan: 33
type: execute
wave: 16
depends_on: [30, 31, 32]
gap_closure: true
autonomous: false
requirements: []          # closeout phase — no new v1.1 REQ ids (per ROADMAP Phase 8 + gap_closure)
files_modified:
  - .planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
  - .planning/phases/08-device-uat-milestone-closeout/08-33-SUMMARY.md

must_haves:
  truths:
    - "The owner verifies the round-6 fixes and records a verbatim verdict for each: D2-view (rapid own-reaction change in the Telegram app repaints every time), WR-02 regression (an own WhatsApp reaction removed in-app STAYS removed), D17 (late WhatsApp auth shows the sync cover), D15 disposition (the probe / response.txt confirms no reaction-state key), plus the WhatsApp/Telegram invariants."
    - "The fix items are Editor-reproducible — Editor Play-Mode verification is sufficient for D2-view + WR-02; ONE Android build is run only for the final Gate A confirmation sweep."
    - "On all-PASS: Gate A flips to PASS, I.3 #10 re-aggregates, Gates B/C unblock; any FAIL keeps Gate A = ISSUES and spins round 7 with the captured logs as the discriminator."
    - "G6 is NOT carried (resolved post-08-25); the D2-ext echo-hex is CLOSED (captured round 5) — neither is a checkpoint line item."
  artifacts:
    - path: ".planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md"
      provides: "round-6 verdicts appended (D2-view / WR-02 / D17 / D15 disposition + WA/TG invariants), Overall + Gate A updated, Defects rows updated"
      contains: "Round 6"
  key_links:
    - from: "owner Editor/device pass"
      to: "08-DEVICE-UAT.md Defects table + Overall + Gate A"
      via: "verbatim verdict transcription per item, mapped to its source anchor, with any FAIL log captured"
      pattern: "D2-view|WR-02|D17|D15"
---

<objective>
Round-6 consolidated owner device re-verify — the Gate A gate for milestone v1.1. The owner verifies the
round-6 fixes (08-30 D2-view CR-01 + WR-01 / 08-31 WR-02 revert + D15 disposition / 08-32 D17 late-WA cover)
and records a verbatim verdict per item.

**KEY CHANGE from prior rounds:** D2-view and the WR-02 regression are EDITOR-reproducible (round-5 proved
both repro in the Unity Editor Console). So the checklist says **Editor Play-Mode verification is sufficient
for the fix items**, with **ONE Android build only for the final Gate A confirmation sweep** (WA/TG
invariants + D17 cover). This plan writes the runbook autonomously; RUNNING it is the owner gate. The phase
stays open until the owner records verdicts. **Do NOT tick these on the owner's behalf.** No app code changes
here.

Carried notes for this pass:
- **G6 (deactivate the dev test clone) is RESOLVED** (post-08-25) — do NOT carry it.
- **D2-ext echo-hex is CLOSED** (captured round 5) — do NOT carry it.
- **New capture asks that make a FAIL discriminable:** the `[D2-merge]` Editor log (08-30) fires only when a
  DIFFERING echo is suppressed within the grace — for a genuine external own-change it should NOT fire (the
  change should just apply); if D2-view still FAILS, its presence/absence tells whether the suppression path
  is still eating the change. The `[D15-probe]` log (08-31) / an Editor `response.txt` grep confirms whether
  a WhatsApp payload carries any reaction-state key (the D15 platform-limit disposition).

Purpose: prove Gate A can close (or spin round 7 with discriminating logs) so the milestone-closeout
mechanics (Gates B/C) proceed. Output: round-6 verdicts in 08-DEVICE-UAT.md + a Gate A disposition + the D15
close/round-7 decision.

**Standing rule (shared live tree):** parallel sessions (phases 9/10/11) are committing to this working
tree. This plan touches ONLY the two planning docs above — stage ONLY those; never `git add -A`.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.planning/phases/08-device-uat-milestone-closeout/08-29-SUMMARY.md
@.planning/phases/08-device-uat-milestone-closeout/08-30-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-31-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-32-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Owner re-verify — D2-view / WR-02 / D17 / D15 disposition (round 6)</name>
  <files>.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md, .planning/phases/08-device-uat-milestone-closeout/08-33-SUMMARY.md</files>
  <read_first>
    - 08-DEVICE-UAT.md §Defects table (D2-view, D15, D17 rows) + §Round 5 re-verify block + §Overall (mirror its structure for a new §Round 6 re-verify block)
    - 08-30-PLAN.md / 08-31-PLAN.md / 08-32-PLAN.md (what each fix does + the log tokens to capture: [D2-merge], [D15-probe])
    - 08-29-SUMMARY.md (round-5 owner-verbatim transcription format to mirror; G6 resolved + echo-hex closed — no carry)
  </read_first>
  <action>Owner-run gate (not executor work). After the pre-build test gate passes, verify the fix items in
  the Unity Editor Play Mode (D2-view + WR-02 are Editor-reproducible), capture the D15 probe signal, then
  build ONE Android device build (08-30/08-31/08-32 merged) for the final Gate A confirmation sweep (WA/TG
  invariants + the D17 cover). Record exactly one verdict (PASS/FAIL/N/A) per item VERBATIM. Do NOT tick on
  the owner's behalf. G6 is resolved and echo-hex is closed (no carry). On all-PASS: flip Gate A → PASS,
  re-aggregate I.3 #10, unblock Gates B/C, and close D15 as platform-limited (keep the CLAUDE.md note). On any
  FAIL: keep Gate A = ISSUES, capture the [D2-merge]/[D15-probe] logs into the Defects row, and spin round 7
  via /gsd-plan-phase 08 --gaps. No app code changes in this plan.</action>

  <pre-build-gate>
    Before verifying, confirm the EditMode suite is GREEN on the tree the owner will verify/build from — read
    the total FRESH from the bridge (`Temp/claude/test-summary.json`, or `Tools/run-tests-headless.sh` if the
    Editor is closed). Do NOT assert an absolute total: the suite grows concurrently from parallel phases
    (~1181 as of round 5) and prior rounds carried stale hardcoded numbers that had to be corrected mid-run.
    The gate is "completed/Passed, 0 failures at the current baseline". Round-6 fixes add: **+3** (08-30 CR-01
    distilled + CR-01 differ-guard + WR-01 re-add; 08-30 also RENAMES 2 tombstone tests, count flat) / 08-31
    **+0** (WR-02 revert + Editor-only probe + doc) / 08-32 **+0** (PlayerPrefs glue). Read it fresh, never
    hardcode.
  </pre-build-gate>

  <what-built>
    Round-6 fixes:
    - **D2-view (08-30):** `TelegramReactionMerge` now ENDS the optimistic grace on server confirmation — a
      same-emoji echo adopts the server entry (grace consumed) so a subsequent own-reaction change made IN the
      Telegram app applies and repaints; a differing echo within the window is still suppressed. WR-01: a
      removal tombstone is dropped once the server confirms the absence, so an external re-add applies. A new
      Editor-only `[D2-merge]` log fires only on a differing-echo suppress.
    - **WR-02 (08-31):** the harmful 08-27 candidate-(a) re-process is reverted — an own WhatsApp reaction
      removed/changed in-app is no longer resurrected/reverted by the re-delivered add raw on the next poll.
      The `[D15]` shape log is retained; a secret-free Editor `[D15-probe]` checks whether a WhatsApp payload
      carries reaction state; CLAUDE.md documents the WhatsApp reaction-removal platform limit (D15).
    - **D17 (08-32):** a late WhatsApp auth on an existing (e.g. Telegram-first) bot now stamps
      `{bot}WhatsappSyncUntil`, so the WhatsApp post-creation sync cover fires when the WhatsApp channel is
      opened — cover parity for both channels on every late-add.
  </what-built>

  <how-to-verify>
    Record EXACTLY ONE verdict per item (PASS / FAIL / N/A), transcribed VERBATIM (do not paraphrase, do not
    tick on the owner's behalf). Any FAIL → add/append a row in 08-DEVICE-UAT.md §Defects with its source
    anchor AND paste the relevant captured log line(s).

    **Editor Play-Mode is sufficient for items 1-2 (Editor-reproducible). Item 5 (D17) + items 3-4/6-7
    (invariants) ride ONE Android build — the final Gate A sweep.**

    1. **D2-view — rapid own-reaction change in the Telegram app repaints EVERY time.**
       expected: on a Telegram bot, tap a reaction on a message IN OUR app (arms the optimistic grace), then
       within 90 s change your OWN reaction on that message IN the Telegram app 2-3 times (e.g. 👍 → 😁 → 👌).
       Each change repaints the in-app bubble pill to the new emoji — no change is dropped after the first.
       source: 08-DEVICE-UAT.md D2-view / B9–B13.
       IF FAIL: capture any `[D2-merge] suppressed server-me '…' by fresh local '…' age=…s` lines (a genuine
       external change should NOT log this — its presence means the suppression path is still eating the
       change) plus the `[D2-view]`/`[TG reaction echo]` timeline — paste into the Defects row.

    2. **WR-02 regression — an own WhatsApp reaction removed in-app STAYS removed across polls.**
       expected: on a WhatsApp bot, add a reaction in OUR app (or from the WhatsApp phone app), then REMOVE it
       in OUR app — the pill clears and STAYS cleared across the next several poll cycles (it does not
       resurrect within one poll as it did before the revert).
       source: 08-DEVICE-UAT.md D15 / 08-REVIEW WR-02.

    3. **D2-view — WhatsApp add/change unaffected (invariant).**
       expected: on a WhatsApp bot, add then change a reaction — the pill repaints exactly as before.
       source: WhatsApp byte-identical invariant (08-30/08-31).

    4. **Telegram add/change/remove still correct (invariant).**
       expected: on a Telegram bot, add / change / remove your own reaction in OUR app — count is 1 (not «2»),
       a change leaves ONE pill, a removal clears and stays cleared (the round-2/round-4 fixes still hold).
       source: WhatsApp/Telegram reaction invariants (08-06/08-11/08-17/08-30).

    5. **D17 — late WhatsApp auth on an existing bot shows the WhatsApp sync cover.**
       expected: on a bot that already has Telegram, authorize WhatsApp from settings; when you open the bot's
       WhatsApp channel, the ~5-min post-creation sync cover (spinner + progress + countdown) shows over the
       chat list — mirroring the Telegram late-auth cover (D16). Also spot-check: a late Telegram auth on a
       WhatsApp-first bot still shows the Telegram cover (D16 unchanged).
       source: 08-DEVICE-UAT.md D17 / 08-REVIEW IN-02.

    6. **D15 disposition — no WhatsApp reaction-state key surfaces (platform-limit confirmation).**
       expected: with the Editor open (or on the build with logcat), capture the `[D15-probe] wa msgId=…
       reactionsKey=False reactionKey=False` line(s) when a WhatsApp quote/message resolves — AND/OR grep an
       Editor `response.txt` dump for a WhatsApp chat containing a reacted message for any `"reactions"` key on
       target rows. If BOTH come back empty (as predicted): D15 is closed as a Wappi WA platform limit (keep
       the CLAUDE.md note). If a reaction-state key DOES surface: revert the CLAUDE.md note and spin round 7
       (absence-based WA reconcile).
       source: 08-DEVICE-UAT.md D15 / 08-REVIEW IN-01. NOTE: `secrets.json` is deny-ruled — any live
       `messages/id/get` call is owner-run (the app authed in Play Mode, or a direct token call by the owner);
       the executor never handles the token.

    7. **D2-view residual `[D2-merge]` discriminator (NICE-TO-HAVE).**
       If item 1 passes, note whether any `[D2-merge]` line fired during the rapid-change repro (ideally none
       for genuine external changes). Absence is the expected healthy signal — record "not fired" or paste any
       lines seen.
       source: 08-30 D2-merge log.
  </how-to-verify>

  <on-completion>
    - Add a §Round 6 re-verify block to 08-DEVICE-UAT.md (mirroring §Round 5): transcribe every verdict
      VERBATIM, each mapped to its source anchor; update the status header, the Round-6 Overall line, and the
      §Overall-result bullet. Paste any captured [D2-merge]/[D15-probe] lines into the FAIL rows. Update the
      D2-view / D15 / D17 Defects rows with the round-6 outcome.
    - **All PASS:** flip Gate A → PASS; re-aggregate the re-deferred I.3 #10 (01-VERIFICATION sign-off,
      previously blocked by D5 — now unblocked); unblock Gates B (prod replication, 08-02) and C (milestone
      close, 08-03); close D15 as platform-limited (keep the CLAUDE.md note). Note prod bagkz stays dormant
      until 08-02.
    - **Any FAIL:** keep Gate A = ISSUES; file the specifics in §Defects with anchors + the captured logs, and
      spin round 7 via `/gsd-plan-phase 08 --gaps`. If the D15 probe surfaced a reaction-state key, note the
      round-7 absence-reconcile scope and revert the CLAUDE.md platform-limit note. Do NOT hand-patch fixes in
      the runbook.
    - No app code changes in this plan (planning docs only). G6 is resolved and echo-hex is closed — do NOT
      re-open either.
  </on-completion>

  <verify>Owner records exactly one PASS/FAIL/N-A verdict per item (D2-view rapid-change repaint, WR-02 removal-stays-removed, WA add/change invariant, TG add/change/remove invariant, D17 late-WA cover + D16 spot-check, D15 disposition, D2-merge nice-to-have), all transcribed verbatim into 08-DEVICE-UAT.md and mapped to their anchors; every FAIL has a §Defects row + the captured [D2-merge]/[D15-probe] log line; the Overall line + Gate A disposition + the D15 close/round-7 decision are set. Pre-verify EditMode suite green at the current baseline +3 (read fresh from the bridge).</verify>
  <done>All items have a recorded owner verdict; 08-DEVICE-UAT.md §Round 6 + §Defects + Overall updated; Gate A disposed — all-PASS → PASS (I.3 #10 re-aggregated, Gates B/C unblocked, D15 closed platform-limited) or any FAIL → ISSUES with round-7 scope named and the discriminating logs captured. G6 not carried; echo-hex not carried.</done>
  <resume-signal>Type "approved" (record all verdicts + capture any FAIL logs + set the D15 disposition), or describe the failures per item (with the [D2-merge]/[D15-probe] log lines) to spin round 7.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner Editor/device ↔ dev n8n / dev Telegram + WhatsApp profiles | Owner-run verification only; prod bagkz stays dormant. No code, no new surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-33-01 | (all STRIDE categories) | owner-run re-verify checkpoint (no code) | accept | This plan changes no app code and adds no surface. It records owner verdicts into planning docs; `secrets.json` is deny-ruled and referenced by name only (any live `messages/id/get` D15 probe is owner-run — the executor never handles the token). G6 (the only real-world clone-against-real-contacts risk) is already resolved. |
</threat_model>

<verification>
- Pre-verify: EditMode suite completed/Passed with 0 failures at the CURRENT baseline (+3 from round 6);
  baseline read FRESH from the bridge, never hardcoded.
- Every item has exactly one owner verdict, transcribed verbatim; every FAIL has a §Defects row + anchor + the
  captured [D2-merge]/[D15-probe] log line.
- Overall line updated; Gate A disposition recorded; the D15 close/round-7 decision recorded. G6 + echo-hex
  not re-opened.
</verification>

<success_criteria>
- Owner verified the round-6 fixes (Editor Play-Mode for D2-view + WR-02; ONE Android build for the Gate A
  sweep incl. D17 + invariants) and recorded verbatim verdicts; the D15 disposition is recorded (probe empty →
  platform-limited close, or a reaction-state key → round-7 reconcile).
- Gate A disposition set: all-PASS → PASS (I.3 #10 re-aggregated, Gates B/C unblocked, D15 closed) or any FAIL
  → ISSUES with round-7 gaps filed and the discriminating logs captured.
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-33-SUMMARY.md`.
</output>
