---
phase: 08-device-uat-milestone-closeout
plan: 29
type: execute
wave: 14
depends_on: [26, 27, 28]
gap_closure: true
autonomous: false
requirements: []          # closeout phase — no new v1.1 REQ ids (per ROADMAP Phase 8 + gap_closure)
files_modified:
  - .planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
  - .planning/phases/08-device-uat-milestone-closeout/08-29-SUMMARY.md

must_haves:
  truths:
    - "The owner runs ONE Android build covering D2-view, D15, and D16 and records a verbatim verdict for each (plus the WhatsApp-unchanged invariant checks)."
    - "On all-PASS, Gate A flips to PASS, I.3 #10 re-aggregates, and Gates B/C unblock; any FAIL spins round 6 with the captured [D2-view]/[D15] logs as the discriminator."
    - "G6 is NOT carried (resolved post-08-25); the echo-hex capture stays nice-to-have, non-blocking."
  artifacts:
    - path: ".planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md"
      provides: "round-5 verdicts appended (D2-view / D15 / D16 + WhatsApp invariants), Overall + Gate A updated"
      contains: "Round 5"
  key_links:
    - from: "owner device pass"
      to: "08-DEVICE-UAT.md Defects table + Overall"
      via: "verbatim verdict transcription per item, mapped to its source anchor"
      pattern: "D2-view|D15|D16"
---

<objective>
Round-5 consolidated owner device re-verify — the Gate A gate for milestone v1.1. ONE Android build off
the post-08-28 tree (fixes 08-26 D2-view + 08-27 D15 + 08-28 D16 all included) that the owner runs to
confirm the three round-5 residuals are closed, mirroring the 08-10 / 08-16 / 08-21 / 08-25 passes.

This plan writes the runbook autonomously; RUNNING it is the owner gate. The phase stays open until the
owner records verdicts. **Do NOT tick these on the owner's behalf.** No app code changes here.

Carried notes for this pass:
- **G6 (deactivate the dev test clone) is RESOLVED** — the owner confirmed "G6 done" post-08-25. Do NOT
  carry it as a checkpoint line item; it is done.
- **D2-ext echo-hex capture** — still nice-to-have, non-blocking (the data layer is proven). Capture if
  convenient; absence is fine.
- **New this round:** two device-log capture asks that make a FAIL discriminable rather than another guess
  — the `[D2-view] post-render` state log (08-26) tells exception vs cull-state vs submesh-churn; the
  `[D15] wa-reaction` log (08-27) tells which of the three IN-02 removal shapes WhatsApp uses.

Purpose: prove Gate A can close (or spin round 6 with discriminating logs) so the milestone-closeout
mechanics (Gates B/C) proceed. Output: round-5 verdicts in 08-DEVICE-UAT.md + a Gate A disposition.

**Standing rule (shared live tree):** parallel sessions (phases 9/10/11) are committing to this working
tree. This plan touches ONLY the two planning docs above — stage ONLY those; never `git add -A`.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.planning/phases/08-device-uat-milestone-closeout/08-25-SUMMARY.md
@.planning/phases/08-device-uat-milestone-closeout/08-26-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-27-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-28-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Owner device re-verify — D2-view / D15 / D16 (round 5)</name>
  <files>.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md, .planning/phases/08-device-uat-milestone-closeout/08-29-SUMMARY.md</files>
  <read_first>
    - 08-DEVICE-UAT.md §Defects table (D2-view, D15, D16 rows) + §Round 4 re-verify block + §Overall (mirror its structure for a new §Round 5 re-verify block)
    - 08-26-PLAN.md / 08-27-PLAN.md / 08-28-PLAN.md (what each fix does + its device confirmation line + the log tokens to capture)
    - 08-25-SUMMARY.md (round-4 owner-verbatim two-message-authoritative format to mirror; G6 resolved — no carry)
  </read_first>
  <action>Owner-run gate (not executor work): after the pre-build test gate passes, build ONE Android device build with 08-26/08-27/08-28 merged, then re-verify each item below against its 08-DEVICE-UAT.md anchor and record exactly one verdict (PASS/FAIL/N/A) VERBATIM. Do NOT tick on the owner's behalf. G6 is resolved (no carry). On all-PASS: flip Gate A -> PASS, re-aggregate I.3 #10, unblock Gates B/C; on any FAIL: keep Gate A = ISSUES, capture the [D2-view]/[D15] device logs into the Defects row, and spin round 6 via /gsd-plan-phase 08 --gaps. No app code changes in this plan.</action>

  <pre-build-gate>
    Before the build, confirm the EditMode suite is GREEN on the tree the owner will build from — read
    the total FRESH from the bridge (`Temp/claude/test-summary.json`, or `Tools/run-tests-headless.sh` if
    the Editor is closed). Do NOT assert an absolute total: the suite grows concurrently from parallel
    phases (1176+ as of round 4) and prior rounds carried stale hardcoded numbers that had to be corrected
    mid-run. The gate is "completed/Passed, 0 failures at the current baseline", where the baseline is
    whatever the bridge reports at run time. Fixes 08-26/08-27/08-28 add: 4 (D2-view converter tests) +
    1 (D15 removal-redelivery pin) + 0 (D16) = **+5** over the pre-round-5 baseline.
  </pre-build-gate>

  <what-built>
    Round-5 fixes on ONE Android build:
    - **D2-view (08-26):** the poll-driven HandleReactionsChanged now routes through the same hardened
      one-frame-deferred re-render (RefreshReactionsVisual -> ForceReRender -> ForceMeshUpdate) the
      reaction-bar dismiss used, so a reaction changed IN the Telegram app repaints the bubble; the emoji
      converter + pill render are throw-safe (WR-01); a new `[D2-view] post-render` log reports pill
      active / label length / canvasRenderer.cull to discriminate a FAIL.
    - **D15 (08-27):** a WhatsApp reaction removed in the WhatsApp app now clears in-app when re-emitted
      under the same reaction id (candidate a, idempotent); a `[D15] wa-reaction` log records the removal
      shape (rawId/stanzaId/bodyEmpty/seen) for round-5 discrimination of candidates (a)/(b)/(c).
    - **D16 (08-28):** a bot that already has WhatsApp and authorizes Telegram later stamps the 300s
      Telegram sync window at auth success, so the Telegram post-creation cover fires; WhatsApp late-auth
      stamps nothing new (byte-identical, documented parity decision).
  </what-built>

  <how-to-verify>
    Record EXACTLY ONE verdict per item (PASS / FAIL / N/A), transcribed VERBATIM (do not paraphrase, do
    not tick on the owner's behalf). Any FAIL -> add/append a row in 08-DEVICE-UAT.md §Defects with its
    source anchor AND paste the relevant captured device-log line(s).

    1. **D2-view — reaction changed IN the Telegram app repaints the bubble (the round-4 repro).**
       expected: on a Telegram bot, change a reaction on a message IN the Telegram app (remote change via
       the live poll). The bubble pill in-app updates to the new emoji — it does NOT stay stale. Repeat a
       few times, incl. changing on one bubble then another (the old bug was intermittent).
       source: 08-DEVICE-UAT.md D2-view / B9–B13.
       IF FAIL: capture the `[D2-view] post-render id=… active=… len=… culled=…` line (active/len/culled
       tells submesh vs cull vs exception) — paste it into the Defects row.

    2. **D2-view — WhatsApp add/change unaffected.**
       expected: on a WhatsApp bot, add then change a reaction — the pill repaints exactly as before.
       source: WhatsApp byte-identical invariant (08-26).

    3. **D15 — a reaction REMOVED in the WhatsApp app clears in-app.**
       expected: on a WhatsApp bot, add a reaction in the WhatsApp app, then REMOVE it in the WhatsApp app
       — the pill disappears in-app within a poll cycle (no longer sticks).
       source: 08-DEVICE-UAT.md D15 / round-4 item 2.
       IF FAIL: capture the `[D15] wa-reaction rawId=… stanza=… bodyEmpty=… seen=…` line(s) for the
       removal — they identify which shape (a re-emit / no raw / missing stanza) WhatsApp uses — paste into
       the Defects row so round 6 targets exactly one site.

    4. **D15 — WhatsApp add/change reaction still repaints (invariant).**
       expected: adding and changing a reaction in the WhatsApp app still updates the pill as before (the
       unseen-id path is untouched).
       source: WhatsApp byte-identical invariant (08-27).

    5. **D16 — late Telegram auth on a WhatsApp bot shows the Telegram sync cover.**
       expected: on a bot that already has WhatsApp, authorize Telegram from settings; when you open the
       bot's Telegram channel, the ~5-min post-creation sync cover (spinner + progress + countdown, brand
       blue per D14) shows over the chat list.
       source: 08-DEVICE-UAT.md D16 / 08-19 late-channel follow-up.

    6. **D16 — WhatsApp byte-identical: late WhatsApp auth shows NO new cover.**
       expected: on a bot that already has Telegram, authorizing WhatsApp later does NOT newly show a
       WhatsApp cover (the documented parity decision — WhatsApp behaviour is unchanged).
       source: WhatsApp byte-identical invariant (08-28) / parity decision.

    7. **D2-ext echo-hex (NICE-TO-HAVE, non-blocking).**
       If convenient during D2-view testing, capture the tapi reaction-echo hex from the `[TG reaction
       echo]` Editor log (ChatManager.cs) / `Tools/tapi/probe-message.sh`. Absence is fine — record "not
       captured".
       source: 08-DEVICE-UAT.md D2-ext.
  </how-to-verify>

  <on-completion>
    - Add a §Round 5 re-verify block to 08-DEVICE-UAT.md (mirroring §Round 4): transcribe every verdict
      VERBATIM, each mapped to its source anchor; update the status header, the Round-5 Overall line, and
      the §Overall-result bullet. Paste any captured [D2-view]/[D15] device-log lines into the FAIL rows.
    - **All PASS:** flip Gate A -> PASS; re-aggregate the re-deferred I.3 #10 (01-VERIFICATION sign-off,
      previously blocked by D5 — now unblocked); unblock Gates B (prod replication, 08-02) and C
      (milestone close, 08-03). Note prod bagkz stays dormant until 08-02.
    - **Any FAIL:** keep Gate A = ISSUES; file the specifics in §Defects with anchors + the captured logs,
      and spin round 6 via `/gsd-plan-phase 08 --gaps`. Do NOT hand-patch fixes in the runbook.
    - No app code changes in this plan (planning docs only). G6 is resolved — do NOT re-open it.
  </on-completion>

  <verify>Owner records exactly one PASS/FAIL/N-A verdict per item (D2-view repaint + WA-unchanged, D15 removal + WA add/change, D16 late-TG cover + WA-unchanged) + the D2-ext echo-hex note, all transcribed verbatim into 08-DEVICE-UAT.md and mapped to their anchors; every FAIL has a §Defects row + the captured [D2-view]/[D15] log line; the Overall line + Gate A disposition are set. Pre-build EditMode suite green at the current baseline +5 (read fresh from the bridge).</verify>
  <done>All items have a recorded owner verdict; 08-DEVICE-UAT.md §Round 5 + §Defects + Overall updated; Gate A disposed — all-PASS -> PASS (I.3 #10 re-aggregated, Gates B/C unblocked) or any FAIL -> ISSUES with round-6 scope named and the discriminating logs captured. G6 not carried (resolved).</done>
  <resume-signal>Type "approved" (record all verdicts + capture any FAIL logs), or describe the failures per item (with the [D2-view]/[D15] log lines) to spin round 6.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner device ↔ dev n8n / dev Telegram profile | Owner-run verification only; prod bagkz stays dormant. No code, no new surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-29-01 | (all STRIDE categories) | owner-run device checkpoint (no code) | accept | This plan changes no app code and adds no surface. It records owner verdicts into planning docs; secrets.json is deny-ruled and referenced by name only. G6 (the only real-world clone-against-real-contacts risk) is already resolved. |
</threat_model>

<verification>
- Pre-build: EditMode suite completed/Passed with 0 failures at the CURRENT baseline (+5 from round 5);
  baseline read FRESH from the bridge, never hardcoded.
- Every item has exactly one owner verdict, transcribed verbatim; every FAIL has a §Defects row + anchor +
  the captured [D2-view]/[D15] log line.
- Overall line updated; Gate A disposition recorded. G6 not re-opened.
</verification>

<success_criteria>
- Owner ran ONE Android build covering D2-view, D15, D16 (+ WhatsApp-parity checks) and recorded verbatim
  verdicts; the D2-ext echo-hex is recorded (captured or "not captured").
- Gate A disposition set: all-PASS -> PASS (I.3 #10 re-aggregated, Gates B/C unblocked) or any FAIL ->
  ISSUES with round-6 gaps filed and the discriminating device logs captured.
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-29-SUMMARY.md`.
</output>
