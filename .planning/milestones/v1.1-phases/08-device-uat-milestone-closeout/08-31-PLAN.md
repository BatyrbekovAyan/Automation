---
phase: 08-device-uat-milestone-closeout
plan: 31
type: execute
wave: 15
depends_on: []
gap_closure: true
autonomous: true
requirements: []          # closeout phase — no new v1.1 REQ ids (per ROADMAP Phase 8 + gap_closure)
files_modified:
  - Assets/Scripts/Main/ChatManager.cs
  - Assets/Scripts/Main/ChatManager.QuoteResolve.cs
  - CLAUDE.md

must_haves:
  truths:
    - "The 08-27 candidate-(a) already-seen re-process block (ChatManager.cs:734-745) is removed — an own WhatsApp reaction removed or changed IN our app is no longer resurrected/reverted by the still-re-delivering add raw on the next poll (WR-02)."
    - "The [D15] reaction-raw shape log (both branches) is preserved until D15 formally closes."
    - "A WhatsApp messages/id/get response is probed (Editor-only, reusing the existing authed QuoteResolve call — no agent-side live API call, no token handling) for any reaction-state key, so the round-6 checkpoint can confirm the platform-limit disposition."
    - "CLAUDE.md documents the confirmed WhatsApp reaction-removal platform limit next to the message/reaction endpoint (the chat/delete isDeleted style)."
  artifacts:
    - path: "Assets/Scripts/Main/ChatManager.cs"
      provides: "WR-02 revert — the candidate-(a) WhatsApp reaction re-process removed; the already-seen fall-through resumes at the media/quote/reactions refresh; the [D15] wa-reaction log retained"
      contains: "[D15] wa-reaction"
    - path: "Assets/Scripts/Main/ChatManager.QuoteResolve.cs"
      provides: "Editor-only [D15-probe] WhatsApp reaction-state key-presence log on the existing messages/id/get response"
      contains: "[D15-probe]"
    - path: "CLAUDE.md"
      provides: "confirmed WhatsApp reaction-removal platform-limit note under the message/reaction endpoint"
      contains: "message/reaction"
  key_links:
    - from: "an own WhatsApp reaction removed/changed in-app"
      to: "no stale resurrection on the next poll"
      via: "the already-seen fall-through no longer re-runs HandleReactionEvent (WR-02 revert)"
      pattern: "candidate"
---

<objective>
Two coupled WhatsApp-reaction dispositions from round 5:

**WR-02 (the harmful 08-27 fix — REVERT).** The round-5 `[D15]` evidence proved a WhatsApp add raw keeps
re-delivering every poll with `seen=true` even after the reaction was removed (Wappi retains stale reaction
stanzas). 08-27's candidate-(a) block (ChatManager.cs:734-745) makes every seen WhatsApp `type=="reaction"`
raw re-run `HandleReactionEvent` each poll — which RESURRECTS an own reaction the owner removed in-app (the
persisted add raw re-adds it within one poll) and can REVERT a changed reaction to the stale emoji. It is
inert for its intended purpose (candidate a is refuted) and harmful outside it. REVERT it.

**D15 (diagnosis-first, likely a documented platform limit).** Round-5 answered the removal SHAPE =
candidate (b): NO empty-body removal raw ever arrives; the add raw just re-delivers. The model evidence
(`RawMessage.reactions` is captured channel-agnostically yet `Normalize` maps `reactions[]` ONLY on the
Telegram branch) predicts WhatsApp target rows carry no reaction state. Add an Editor-only, secret-free probe
that checks whether a WhatsApp `messages/id/get` payload EVER carries a reaction-state key (reusing the
existing authed QuoteResolve call), and DOCUMENT the WhatsApp reaction-removal platform limit in CLAUDE.md.
The live probe result + the final D15 close/round-7 decision ride the 08-33 checkpoint.

Keep the `[D15]` instrumentation logs (both branches) — still needed until D15 closes. The `96da6f4` pin
(`Apply_RemovalThenRedelivery_SecondIsIdempotentNoOp`) STAYS unchanged: it pins `ReactionStore.Apply`
idempotency (a pure reducer property), not the reverted re-process block — a valid property regardless.

Purpose: stop the WhatsApp reaction resurrection and put D15 on a documented disposition. Output: the WR-02
revert + the D15 probe + the platform-limit doc. Telegram byte-identical (all changes are WhatsApp-gated or
Editor-only diagnostics).

**Standing rule (shared live tree):** parallel sessions (phases 9/10/11) are committing to this working
tree. Stage ONLY this plan's three files when committing — never `git add -A`.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-REVIEW.md
@.planning/phases/08-device-uat-milestone-closeout/08-27-SUMMARY.md
@.planning/phases/08-device-uat-milestone-closeout/08-29-SUMMARY.md

<interfaces>
<!-- Current code, line numbers verified 2026-07-21 against the live tree. -->

ChatManager.cs SyncLatestMessages — the [D15] LOG (KEEP) at 655-662 and the candidate-(a) RE-PROCESS
(REVERT) at 734-745:
```csharp
// 655-660  D15 diagnostic comment (KEEP)
if (ActiveChannel == ChatChannel.WhatsApp && raw.type == "reaction")            // 661  (KEEP — the log gate)
    Debug.Log($"[D15] wa-reaction rawId={raw.id} stanza=... bodyEmpty=... seen=...");  // 662  (KEEP)

// ... the `if (seenMessageIds.Add(raw.id)) { ... }` UNSEEN block (664-732, UNCHANGED — the reaction
// case inside it at 676-680 stays: brand-new reaction raws still reach HandleReactionEvent) ...

// 734-740  // [D15] Candidate-(a) fix (08-REVIEW IN-02): ... comment (REVERT)
if (ActiveChannel == ChatChannel.WhatsApp && raw.type == "reaction")            // 741  (REVERT)
{
    if (HandleReactionEvent(raw, cachedList, chatId)) hasStatusUpdates = true;  // 743  (REVERT)
    continue;                                                                    // 744  (REVERT)
}
                                                                                 // 746 blank
// Already-cached message: refresh stale media URLs ...                          // 747  (the fall-through resumes here)
NormalizedMessage reconcileNorm = Normalize(raw);                                // 754
```
Deleting 734-745 restores the pre-08-27 already-seen fall-through: after the unseen block, a seen raw goes
straight to the media/quote/reactions refresh at 754. WhatsApp seen reaction raws are then dropped by the
seen gate exactly as before 08-27 (that is the correct pre-08-27 behavior — no resurrection).

ChatManager.QuoteResolve.cs DrainQuoteResolveQueue — the existing authed messages/id/get parse (127-154):
```csharp
if (obj["status"]?.ToString() == "done" && obj["message"] is JObject msg)      // 138
{
    QuotedPreview q = ReplyParser.FromFetchedMessage(msg, ParseMessageType);    // 140
    text = q.text ?? ""; senderName = q.senderName ?? ""; type = q.type;        // 141
    // <-- insert the D15 probe here (msg is in scope)
}
```
`Debug` resolves to UnityEngine.Debug here (used unqualified at 147/152). `ActiveChannel` and
`ChatChannel.WhatsApp` are in scope (ChatManager partial). This call already runs authed at owner runtime;
the probe adds ZERO new request and never handles the token.

CLAUDE.md — the WhatsApp reaction endpoint bullet (Wappi.pro (WhatsApp) → Endpoints), current line ~104:
`- \`message/reaction\` — send/remove an emoji reaction (body \`{ body, message_id }\`; empty \`body\` removes; targets by \`message_id\`, no recipient)`
Its siblings (`chat/delete` line 105, `auth/code` line 108) carry **Confirmed platform limit** notes in the
exact style to mirror.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert the 08-27 candidate-(a) WhatsApp reaction re-process (WR-02); keep the [D15] log</name>
  <files>Assets/Scripts/Main/ChatManager.cs</files>
  <read_first>
    - Assets/Scripts/Main/ChatManager.cs lines 650-770 (the SyncLatestMessages loop: the [D15] log at 655-662, the unseen `seenMessageIds.Add` block 664-732, the candidate-(a) re-process 734-745, the fall-through media/quote/reactions refresh 747-769)
    - 08-REVIEW.md WR-02 (the resurrect/revert mechanism + the exact fix: revert 734-745, keep the [D15] shape log at 661-662)
    - Assets/Tests/Editor/Chat/ReactionStoreTests.cs lines 51-65 (the `Apply_RemovalThenRedelivery_SecondIsIdempotentNoOp` pin — read it, confirm it pins `ReactionStore.Apply` idempotency and NOT the reverted block, then LEAVE IT UNCHANGED)
  </read_first>
  <action>
    DELETE the candidate-(a) re-process block at ChatManager.cs:734-745 — the `// [D15] Candidate-(a) fix ...`
    comment (734-740) AND the `if (ActiveChannel == ChatChannel.WhatsApp && raw.type == "reaction") { if
    (HandleReactionEvent(raw, cachedList, chatId)) hasStatusUpdates = true; continue; }` (741-745). After the
    deletion the already-seen fall-through resumes directly at `// Already-cached message: refresh stale media
    URLs ...` → `NormalizedMessage reconcileNorm = Normalize(raw);` (the pre-08-27 behavior).

    DO NOT touch:
    - The `[D15]` LOG block at 655-662 (the shape log, both branches — KEEP until D15 closes).
    - The unseen-branch reaction case at 676-680 (`if (norm.messageType == MessageType.Reaction) { if
      (HandleReactionEvent(...)) ...; continue; }`) — brand-new WhatsApp reaction raws still ingest normally.
    - The Telegram-only `RefreshCachedMessageReactions` refresh at the fall-through (765-769).
    - `ReactionStoreTests.cs` — the `96da6f4` pin stays (it pins the reducer property, still valid).

    Add nothing new: this is a pure deletion. Do NOT add any comment containing the token `[D15]` or the
    WhatsApp reaction gate string to ChatManager.cs (both are acceptance-grepped for an exact post-revert
    count — the deletion must LOWER those counts, never re-introduce the tokens).
  </action>
  <acceptance_criteria>
    - `grep -c "\[D15\]" Assets/Scripts/Main/ChatManager.cs` → 1 (only the surviving `[D15] wa-reaction` log string at ~662; the candidate-(a) `[D15]` comment is gone).
    - `grep -c 'ActiveChannel == ChatChannel.WhatsApp && raw.type == "reaction"' Assets/Scripts/Main/ChatManager.cs` → 1 (only the log gate survives; the re-process gate is removed — the WR-02 revert proof).
    - `grep -c "Candidate-(a)" Assets/Scripts/Main/ChatManager.cs` → 0 (the reverted block's comment is gone).
    - `grep -c "\[D15\] wa-reaction" Assets/Scripts/Main/ChatManager.cs` → 1 (the shape log is retained).
    - `Apply_RemovalThenRedelivery_SecondIsIdempotentNoOp` still present + unchanged in ReactionStoreTests.cs (`grep -c "Apply_RemovalThenRedelivery_SecondIsIdempotentNoOp" Assets/Tests/Editor/Chat/ReactionStoreTests.cs` → 1).
    - Compiles clean.
  </acceptance_criteria>
  <verify>
    <automated>Drop Temp/claude/run-tests.trigger; read Temp/claude/test-summary.json — full EditMode suite green at the CURRENT baseline (delta 0; a deletion of glue, no test change — the 96da6f4 pin still passes). A completed/Passed with 0 failures confirms compile + no regression. Read the baseline FRESH; do NOT hardcode a total.</automated>
  </verify>
  <done>The candidate-(a) re-process is removed; the [D15] shape log is retained; the already-seen fall-through resumes at the media/quote/reactions refresh; the 96da6f4 pin is unchanged and green; suite green (delta 0).</done>
</task>

<task type="auto">
  <name>Task 2: D15 probe — Editor-only WhatsApp reaction-state key check on the existing messages/id/get call</name>
  <files>Assets/Scripts/Main/ChatManager.QuoteResolve.cs</files>
  <read_first>
    - Assets/Scripts/Main/ChatManager.QuoteResolve.cs lines 103-174 (DrainQuoteResolveQueue: the authed `messages/id/get` request at 127, the success parse at 133-149, the `obj["message"] is JObject msg` block at 138-142 where `msg` is in scope)
    - 08-REVIEW.md IN-01 (the D15 assessment: `reactions[]` on WA target rows has no code/model evidence; the recommendation is a diagnosis-first probe that logs ONLY whether reaction-state keys exist — mirror the [D15] log discipline)
    - Assets/Scripts/Chat/RawMessage.cs lines 47-51 (the channel-agnostic `reactions` JToken + the comment that WhatsApp reactions arrive as separate type:"reaction" rows, not on the message — the model evidence the probe confirms)
  </read_first>
  <action>
    Inside DrainQuoteResolveQueue, in the `if (obj["status"]?.ToString() == "done" && obj["message"] is
    JObject msg)` block, AFTER the line `text = q.text ?? ""; senderName = q.senderName ?? ""; type = q.type;`,
    add the Editor-only probe:
    ```csharp
    #if UNITY_EDITOR
    // D15 probe (round-6, 08-REVIEW IN-01): does a WhatsApp messages/id/get message payload EVER carry
    // current reaction state? If a WA target row exposed a reactions[] key we could clear a reaction
    // REMOVED in the WhatsApp app (round-5 answered candidate (b): no removal raw ever arrives). Log
    // key-presence booleans only (never content); reuses THIS existing authed call — no new API request,
    // no token handling. Empty on every WA probe ⇒ the documented Wappi WA platform limit (Task 3).
    if (ActiveChannel == ChatChannel.WhatsApp)
        Debug.Log($"[D15-probe] wa msgId={qid} reactionsKey={msg["reactions"] != null} reactionKey={msg["reaction"] != null}");
    #endif
    ```
    This piggybacks on the already-authed QuoteResolve call (fires whenever a WhatsApp quote is resolved), so
    it answers "does a WhatsApp per-message payload carry any reaction-state key" with zero new network code
    and no secret handling. Do NOT add a new API caller; do NOT trigger from ChatManager.cs (that would
    overlap Task 1's file). The targeted-per-reaction-target confirmation rides the 08-33 checkpoint owner
    step (response.txt grep / live probe). CRITICAL: keep the bracketed `[D15-probe]` tag ONLY inside the
    `Debug.Log` string — the comment opener is deliberately UNBRACKETED (`// D15 probe`) so the acceptance
    grep counts exactly one occurrence (the 08-27 `[D15]` comment-count lesson).
  </action>
  <acceptance_criteria>
    - `grep -c "\[D15-probe\]" Assets/Scripts/Main/ChatManager.QuoteResolve.cs` → 1 (the log string only; the comment is unbracketed `// D15 probe`).
    - `grep -c "UNITY_EDITOR" Assets/Scripts/Main/ChatManager.QuoteResolve.cs` → ≥1 (the probe is Editor-guarded).
    - `grep -c "SendWebRequest\|UnityWebRequest.Get" Assets/Scripts/Main/ChatManager.QuoteResolve.cs` unchanged from before (no NEW request added — the probe reuses the existing call).
    - The probe sits inside the `obj["message"] is JObject msg` block (msg in scope); compiles clean.
  </acceptance_criteria>
  <verify>
    <automated>Drop Temp/claude/run-tests.trigger; read Temp/claude/test-summary.json — full EditMode suite green at the CURRENT baseline (delta 0; an Editor-only diagnostic log, no test). Read the baseline FRESH; do NOT hardcode a total.</automated>
  </verify>
  <done>An Editor-only [D15-probe] logs whether a WhatsApp messages/id/get payload carries a reaction-state key, reusing the existing authed call (no new request, no secret); suite green (delta 0).</done>
</task>

<task type="auto">
  <name>Task 3: Document the WhatsApp reaction-removal platform limit in CLAUDE.md (D15 disposition)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md the "Wappi.pro (WhatsApp)" → Endpoints list, specifically the `message/reaction` bullet (~line 104) and the `chat/delete` (105) + `auth/code` (108) "Confirmed platform limit" notes whose style to mirror
    - 08-29-SUMMARY.md §"Captured log evidence" Screenshot 2 (the [D15] timeline: the ADD raw re-delivering seen=true every poll, NO empty-body raw ever after the in-WhatsApp removal — candidate (b))
  </read_first>
  <action>
    Append a confirmed-platform-limit note to the `message/reaction` endpoint bullet in the Wappi.pro
    (WhatsApp) Endpoints list, in the chat/delete style. Add (as a sentence continuing that bullet):

    "**Confirmed platform limit** (D15): a reaction ADD/CHANGE arrives as a `type:\"reaction\"` row on the
    message poll, but a reaction REMOVED in the WhatsApp app itself emits NO row — round-5 device logs show
    the original add raw just re-delivers (`seen=true`) every poll with no empty-body removal ever, and
    WhatsApp message payloads (`messages/get` / `messages/id/get`) carry no current `reactions[]` state
    (unlike tapi/Telegram, where reactions ride on the target message and reconcile via
    `TelegramReactionMerge`). So there is no signal to clear an in-app-removed WhatsApp reaction; it stays
    shown in our app. This is a Wappi/WhatsApp constraint (like `chat/delete`'s `isDeleted` quirks), not an
    app bug — don't 'fix' it client-side. A one-shot Editor `[D15-probe]` (ChatManager.QuoteResolve.cs)
    re-confirms no reaction-state key surfaces on a WhatsApp payload; if one ever does, an absence-based
    reconcile becomes possible (round 7)."

    Do NOT edit any other CLAUDE.md section. Keep the existing bullet text intact and append after it.
    The final close of D15 (keep this note vs. revert + spin round 7 if the probe surprises) is recorded at
    the 08-33 checkpoint.
  </action>
  <acceptance_criteria>
    - `grep -c "Confirmed platform limit\*\* (D15)" CLAUDE.md` → 1 (the new D15 note present).
    - `grep -c "message/reaction" CLAUDE.md` → ≥1 (the endpoint bullet still present; the note appended to it).
    - `grep -c "D15-probe" CLAUDE.md` → 1 (the note references the probe seam).
    - No other CLAUDE.md section changed (`git diff --stat CLAUDE.md` shows a single localized hunk in the Wappi WhatsApp endpoints list).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "Confirmed platform limit\*\* (D15)" CLAUDE.md returns exactly one line inside the Wappi.pro (WhatsApp) Endpoints section (adjacent to the message/reaction bullet). No code compiled by this task — CLAUDE.md is documentation.</automated>
  </verify>
  <done>CLAUDE.md documents the WhatsApp reaction-removal platform limit under the message/reaction endpoint, in the confirmed-platform-limit style, referencing the [D15-probe]; the D15 close/round-7 decision is deferred to the 08-33 checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wappi WhatsApp reaction raws → ReactionStore | Untrusted re-delivered reaction stanzas on the message poll. |
| Wappi messages/id/get response → Editor probe log | Untrusted per-message payload inspected for reaction-state key presence only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-31-01 | Tampering | a stale re-delivered add raw resurrecting/reverting an in-app-removed reaction | mitigate | Revert the candidate-(a) re-process — seen WhatsApp reaction raws are dropped at the seen gate (pre-08-27 behavior), so a stale add raw can no longer re-add a removed reaction on the next poll. Brand-new reaction raws still ingest via the unseen branch (unchanged). |
| T-08-31-02 | Information Disclosure | Editor `[D15-probe]` / `[D15]` diagnostic logs | accept | Both are Editor/compiled UAT logs capped to ids + booleans (never emoji/body/reactor content); the probe reuses the existing authed call and never handles the token (`secrets.json` deny-ruled). Grep-removable at phase close (IN-03). |
| T-08-31-03 | (all STRIDE) | CLAUDE.md platform-limit documentation | accept | Documentation only; no code, no surface. Records a confirmed Wappi/WhatsApp constraint. |

No new network endpoint, secret, auth path, or scene mutation. Telegram byte-identical (the revert is
WhatsApp-gated; the probe is WhatsApp-gated + Editor-only; the doc is prose).
</threat_model>

<verification>
- WR-02: the candidate-(a) block is gone; `[D15]` count in ChatManager.cs == 1 (log only); the WhatsApp
  reaction gate count == 1 (log gate only). The 96da6f4 pin unchanged + green.
- D15 probe: one Editor-only `[D15-probe]` in ChatManager.QuoteResolve.cs, no new request; suite delta 0.
- CLAUDE.md: one confirmed-platform-limit (D15) note appended to the message/reaction bullet.
- Full EditMode suite green at the CURRENT baseline (read FRESH; delta 0 across all three tasks). Telegram
  byte-identical.
</verification>

<success_criteria>
- The harmful 08-27 candidate-(a) re-process is reverted — an own WhatsApp reaction removed/changed in-app is
  no longer resurrected/reverted on the next poll; the [D15] shape log is retained until D15 closes.
- A secret-free Editor `[D15-probe]` checks whether a WhatsApp payload carries reaction state (for the 08-33
  checkpoint), and CLAUDE.md documents the platform limit.
- Full suite green (delta 0); the D15 close/round-7 decision rides the checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-31-SUMMARY.md`.
</output>
