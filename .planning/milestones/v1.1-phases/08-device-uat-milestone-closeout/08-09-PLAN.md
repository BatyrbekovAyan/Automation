---
phase: 08-device-uat-milestone-closeout
plan: 09
type: execute
wave: 3
depends_on: [05]
files_modified:
  - Assets/Scripts/UI/EmptyStateView.cs
  - Assets/Scripts/Main/ChatManager.cs
  - Assets/Scripts/UI/ChatListSyncIndicator.cs
  - Assets/Editor/ChatListSyncIndicatorBuilder.cs
  - Assets/Scenes/Main.unity
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "The empty-state copy (create-bot + WhatsApp/Telegram connect states) reads in Russian, matching the app's RU voice"
    - "On Telegram, the chat list shows a sync/loading indicator while its initial load/refresh is in flight (WhatsApp-parity affordance)"
    - "WhatsApp chat-list behavior and its existing 'Setting things up' sync window are unchanged"
  artifacts:
    - path: "Assets/Scripts/UI/EmptyStateView.cs"
      provides: "Russian empty-state strings"
      contains: "не подключён"
    - path: "Assets/Scripts/UI/ChatListSyncIndicator.cs"
      provides: "Telegram chat-list sync indicator driven by ChatManager sync events"
      contains: "OnChatListSyncStart"
  key_links:
    - from: "ChatManager.SyncAllChats try/finally"
      to: "OnChatListSyncStart / OnChatListSyncEnd events"
      via: "sync lifecycle signal"
      pattern: "OnChatListSync"
    - from: "ChatListSyncIndicator"
      to: "shows only when ActiveChannel == Telegram"
      via: "channel-gated display"
      pattern: "Telegram"
---

<objective>
Close **D8** and **D9** (low, owner-approved polish) — the RU-localization sweep and the Telegram chat-list sync indicator.

- **D8 (RU copy):** residual English empty-state copy ("Telegram not connected" / "Connect Telegram", plus the create-bot + WhatsApp variants) must be Russianised (06-HUMAN-UAT IN-09). They live as literals in `EmptyStateView.cs` (lines ≈192–218).
- **D9 (TG sync indicator):** the Telegram chat list appears instantly from cache with NO sync/loading indicator (owner: "Telegram shows chats instantly with no sync indicator"). WhatsApp has its post-creation "Setting things up" window (`SyncingView`), but there's no lightweight "refreshing" cue for the Telegram list's initial load/refresh. Add a minimal, native-feeling sync affordance for the Telegram list, driven by the existing `SyncAllChats` lifecycle.

Purpose: the app reads fully in Russian at the empty states, and Telegram gets a WhatsApp-parity sync cue.

Constraint: WhatsApp chat-list behavior byte-identical; its existing `SyncingView` window is untouched. No server/n8n changes. This plan touches `Assets/Scenes/Main.unity` (D9 indicator) via a headless builder — commit the scene mutation IMMEDIATELY after the builder runs (see memory: parallel scene clobber). depends_on 08-05 because both edit `ChatManager.cs`.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.claude/skills/unity-ui-builder/SKILL.md

<interfaces>
<!-- Extracted from the codebase. -->

// EmptyStateView.cs — the English literals to Russianise (≈192–218):
titleLabel.text = "Create your first bot";
bodyLabel.text  = "An AI assistant that answers your customers on WhatsApp, day and night.";
primaryButtonLabel.text = "Create a bot";
titleLabel.text = "WhatsApp not connected";   bodyLabel.text = "Connect WhatsApp to this bot to see its chats.";   primaryButtonLabel.text = "Connect WhatsApp";
titleLabel.text = "Telegram not connected";   bodyLabel.text = "Connect Telegram to this bot to see its chats.";   primaryButtonLabel.text = "Connect Telegram";

// ChatManager.cs — the chat-list sync coroutine (add lifecycle events here):
IEnumerator SyncAllChats(string cachePath, string cachedJson){
  _chatListSyncing = true;               // ← fire OnChatListSyncStart here
  try { ... }
  finally { _chatListSyncing = false; }  // ← fire OnChatListSyncEnd here
}
// ChatManager.ActiveChannel (ChatManager.Channel.cs): public ChatChannel ActiveChannel { get; }
// ChatManager.ChatListPanel (public GameObject) — parent to attach the indicator under

// SyncingView.cs — the WhatsApp "Setting things up" window (DO NOT touch; different concept, window-based)
// Builder idiom to mirror: Assets/Editor/EmptyStateTelegramIconBuilder.cs / ChannelSwitcherBuilder.cs
//   ([MenuItem] + StampHeadless, run via Tools/run-editor-builder.sh, scene committed immediately)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (D8): Russianise the empty-state copy</name>
  <read_first>
    - Assets/Scripts/UI/EmptyStateView.cs — the `ConfigureForReason` block with the 9 English literals (≈188–220).
    - Any nearby RU strings in the app (e.g. EmptyStateView Telegram copy set in 05-02, or ProfileSubPages) to match tone/voice.
    - 08-DEVICE-UAT.md — F9 / D8 (owner decision KEEP the RU sweep).
  </read_first>
  <action>
    Replace the English literals with these Russian strings (match the app's existing RU voice; keep «WhatsApp»/«Telegram» brand names Latin):
      - "Create your first bot" → "Создайте первого бота"
      - "An AI assistant that answers your customers on WhatsApp, day and night." → "ИИ-ассистент, который отвечает вашим клиентам в WhatsApp круглосуточно."
      - "Create a bot" → "Создать бота"
      - "WhatsApp not connected" → "WhatsApp не подключён"
      - "Connect WhatsApp to this bot to see its chats." → "Подключите WhatsApp к этому боту, чтобы видеть его чаты."
      - "Connect WhatsApp" → "Подключить WhatsApp"
      - "Telegram not connected" → "Telegram не подключён"
      - "Connect Telegram to this bot to see its chats." → "Подключите Telegram к этому боту, чтобы видеть его чаты."
      - "Connect Telegram" → "Подключить Telegram"
    Scan the rest of `EmptyStateView.cs` for any other user-facing English literal and Russianise it too. Do NOT translate identifiers, log strings, or brand names.
  </action>
  <acceptance_criteria>
    - `grep -q "не подключён" Assets/Scripts/UI/EmptyStateView.cs` (both WhatsApp + Telegram states)
    - `grep -q "Создать бота" Assets/Scripts/UI/EmptyStateView.cs`
    - `grep -qE "\"(WhatsApp not connected|Telegram not connected|Connect Telegram|Create a bot)\"" Assets/Scripts/UI/EmptyStateView.cs` returns NOTHING (English removed)
    - Full EditMode suite green (≥1036).
  </acceptance_criteria>
  <verify>
    <automated>Tools/run-tests-headless.sh (Editor closed) OR in-Editor bridge — full suite green, 0 failed</automated>
  </verify>
  <done>Empty-state copy reads in Russian; brand names preserved.</done>
</task>

<task type="auto">
  <name>Task 2 (D9): ChatManager sync-lifecycle events + the Telegram sync-indicator MonoBehaviour</name>
  <read_first>
    - Assets/Scripts/Main/ChatManager.cs — `SyncAllChats` (≈396–435): the `_chatListSyncing = true` / `finally { _chatListSyncing = false; }` bracket; the events block (≈59–116) for where to declare new events; `ChatListPanel` field.
    - Assets/Scripts/UI/SyncingView.cs — the CanvasGroup/spinner/DOTween pattern to mirror for a lightweight indicator (but DO NOT reuse it — it's the WhatsApp window).
    - Assets/Scripts/Main/ChatManager.Channel.cs — `ActiveChannel` getter + `OnActiveChannelChanged` (hide the indicator on a channel switch away from Telegram).
    - .claude/skills/unity-ui-builder/SKILL.md — reference units (1080×1920), spinner via DOTween, TMP for text, RoundedCorners for the pill.
  </read_first>
  <action>
    In `ChatManager.cs`, add two events and fire them around the existing sync bracket in `SyncAllChats`:
      `public event Action OnChatListSyncStart;` and `public event Action OnChatListSyncEnd;`
      Fire `OnChatListSyncStart?.Invoke();` right after `_chatListSyncing = true;` and `OnChatListSyncEnd?.Invoke();` in the `finally` (alongside `_chatListSyncing = false;`). Fire for BOTH channels (unconditional) — display gating lives in the indicator, so WhatsApp is unaffected (its `SyncingView` uses different events).

    Create `Assets/Scripts/UI/ChatListSyncIndicator.cs` — a lightweight MonoBehaviour that:
      - Subscribes in `OnEnable` to `OnChatListSyncStart`/`OnChatListSyncEnd` and `OnActiveChannelChanged`; unsubscribes in `OnDisable`.
      - Shows a small top-of-list pill/spinner (e.g. a rotating spinner + «Синхронизация…» in a rounded pill) ONLY when `ChatManager.Instance.ActiveChannel == ChatChannel.Telegram`. On WhatsApp, `OnChatListSyncStart` is ignored (no-op) so WA is byte-identical.
      - Hides on `OnChatListSyncEnd`, on a channel switch away from Telegram, and when disabled.
      - Builds its visual either from `[SerializeField]` refs stamped by the Task-3 builder, or self-constructs a minimal spinner+label at runtime under `ChatListPanel` (choose the lighter path; if self-constructing, still expose the parent via `[SerializeField]` or resolve `ChatManager.Instance.ChatListPanel`). Use reference units (dp×3): pill height ~72, label ~32, spinner ~48; RoundedCorners on the pill; TMP for the label; DOTween linear spin (mirror `SyncingView.StartSpinner`).
      - Null-guard everything (missing refs / null ChatManager ⇒ no-op).
    Keep it TG-gated so no WhatsApp regression.
  </action>
  <acceptance_criteria>
    - `grep -qE "OnChatListSyncStart|OnChatListSyncEnd" Assets/Scripts/Main/ChatManager.cs` (both declared) AND both fired in `SyncAllChats` (start after `_chatListSyncing = true`, end in `finally`) — verify in diff.
    - `test -f Assets/Scripts/UI/ChatListSyncIndicator.cs` and `grep -q "OnChatListSyncStart" Assets/Scripts/UI/ChatListSyncIndicator.cs`
    - `grep -qi "ActiveChannel == ChatChannel.Telegram" Assets/Scripts/UI/ChatListSyncIndicator.cs` (display TG-gated)
    - `grep -q "Синхронизация" Assets/Scripts/UI/ChatListSyncIndicator.cs` (RU label)
    - Full EditMode suite green (≥1036).
  </acceptance_criteria>
  <verify>
    <automated>Tools/run-tests-headless.sh (Editor closed) OR in-Editor bridge — full suite green, 0 failed</automated>
  </verify>
  <done>ChatManager signals sync start/end; the indicator is ready to show on Telegram only.</done>
</task>

<task type="auto">
  <name>Task 3 (D9): Headless builder to stamp the indicator into Main.unity + commit the scene</name>
  <read_first>
    - Assets/Editor/EmptyStateTelegramIconBuilder.cs AND Assets/Editor/ChannelSwitcherBuilder.cs — the `[MenuItem]` + `StampHeadless` idiom, `SerializedObject` ref stamping, and idempotent delete-and-rebuild (Edit-Mode only, save the scene, no Undo grouping).
    - Tools/run-editor-builder.sh — the Editor-closed headless runner + sentinel verdict; and the in-Editor bridge fallback if the owner's Editor is open (headless refuses on a held lock).
    - .claude/rules/editor-scripts.md — builder conventions.
    - Memory: "Builders must rewire consumers" + "Parallel scene clobber" (commit scene mutation IMMEDIATELY after apply; verify payload by GUID/name grep).
  </read_first>
  <action>
    Create `Assets/Editor/ChatListSyncIndicatorBuilder.cs` — a `[MenuItem("Tools/Chat Sync Indicator/Build")]` (+ a `StampHeadless` entrypoint for `run-editor-builder.sh`) that:
      - Finds the chat list panel object in `Main.unity` (the same parent `ChatListSyncIndicator` attaches under; e.g. under `Screen_Whatsapp/ChatsPanel` or wherever `ChatManager.ChatListPanel` points).
      - Adds the `ChatListSyncIndicator` component (idempotent: delete any prior instance first, then rebuild) and builds/wires its minimal pill+spinner+label children if the component uses `[SerializeField]` refs; stamps them via `SerializedObject`.
      - Places the pill at the top of the chat list content, using reference units, hidden by default (CanvasGroup alpha 0), matching the app's visual language.
      - Saves the scene (`EditorSceneManager.MarkSceneDirty` + `SaveScene`), Edit-Mode only.
    Run it headless via `Tools/run-editor-builder.sh` (or the in-Editor bridge if the owner's Editor is open — never force a headless run against a held lock). Then COMMIT the `Main.unity` mutation immediately (stage `Assets/Scenes/Main.unity` + the new `.cs`/`.meta`), and verify the payload by grepping the scene for the indicator object name / component GUID.
  </action>
  <acceptance_criteria>
    - `test -f Assets/Editor/ChatListSyncIndicatorBuilder.cs` and `grep -q "MenuItem" Assets/Editor/ChatListSyncIndicatorBuilder.cs` and `grep -qi "StampHeadless" Assets/Editor/ChatListSyncIndicatorBuilder.cs`
    - After the builder runs: `grep -qi "ChatListSyncIndicator" Assets/Scenes/Main.unity` (component stamped) — OR the SUMMARY documents that the owner must run the menu item (Editor was open) with the exact menu path.
    - The builder is idempotent (delete-and-rebuild) — verify in the diff/read.
    - Full EditMode suite green (≥1036); scene object count/other refs not clobbered (spot-check per memory).
  </acceptance_criteria>
  <verify>
    <automated>Tools/run-editor-builder.sh (Editor closed) sentinel = success; then Tools/run-tests-headless.sh — full suite green</automated>
  </verify>
  <done>The Telegram sync indicator exists in the scene, hidden by default, and shows during a TG chat-list sync. Device confirmation rides 08-10. Scene committed immediately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sync coroutine lifecycle → indicator | A stuck indicator would misrepresent app state |
| Localized strings → UI | Copy is static (no user data), no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-01 | Denial of Service | indicator never hides (stuck "syncing") | mitigate | `OnChatListSyncEnd` fires in `SyncAllChats`' `finally` (runs even on early `yield break`/error), plus hide on channel-switch-away. |
| T-08-09-02 | Tampering | scene clobber losing other refs during the Main.unity stamp | mitigate | Idempotent delete-and-rebuild builder + immediate commit + GUID/name grep verification (per project memory). |
| T-08-09-03 | Information Disclosure | localized copy leaking data | accept | Strings are static UI copy with no user/secret data. No risk. |
</threat_model>

<verification>
- EditMode suite green (≥1036).
- `grep` acceptance checks pass; English empty-state literals gone; RU present.
- WhatsApp `SyncingView` window untouched; the new events fire for both channels but only the TG indicator consumes them.
- Device re-verify (08-10): D8 RU copy visible; D9 sync indicator on the TG list load/refresh.
</verification>

<success_criteria>
- Empty states read in Russian.
- Telegram list shows a sync indicator on initial load/refresh; WhatsApp unchanged.
- Full suite green; scene committed cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-09-SUMMARY.md` with the RU strings applied and the indicator wiring + scene-commit note.
</output>
