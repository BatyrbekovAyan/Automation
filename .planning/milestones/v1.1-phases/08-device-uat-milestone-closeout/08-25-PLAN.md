---
phase: 08-device-uat-milestone-closeout
plan: 25
type: execute
wave: 12
depends_on: [22, 23, 24]
gap_closure: true
autonomous: false
requirements: []          # closeout phase — no new v1.1 REQ ids (per ROADMAP Phase 8 + gap_closure)
files_modified:
  - .planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
  - .planning/phases/08-device-uat-milestone-closeout/08-25-SUMMARY.md

must_haves:
  truths:
    - "The owner runs ONE Android build covering D2-view, D12-ext, and D14 and records a verbatim verdict for each."
    - "G6 (deactivate the dev test clone) gets an explicit disposition — the checkpoint does NOT accept an all-PASS without it."
    - "On all-PASS dispositions, Gate A flips to PASS, I.3 #10 re-aggregates, and Gates B/C unblock; any FAIL spins round 5."
  artifacts:
    - path: ".planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md"
      provides: "round-4 verdicts appended (D2-view / D12-ext / D14 + G6), Overall updated"
      contains: "round-4"
  key_links:
    - from: "owner device pass"
      to: "08-DEVICE-UAT.md Defects table + Overall"
      via: "verbatim verdict transcription per item, mapped to its source anchor"
      pattern: "D2-view|D12-ext|D14"
---

<objective>
Round-4 consolidated owner device re-verify — the Gate A gate for milestone v1.1. ONE Android build
off the post-08-24 tree (fixes 08-22 D2-view + 08-23 D12-ext + 08-24 D14 all included) that the owner
runs to confirm the three round-4 residuals are closed, mirroring the 08-10 / 08-16 / 08-21 passes.

This plan writes the runbook autonomously; RUNNING it is the owner gate. The phase stays open until
the owner records verdicts. **Do NOT tick these on the owner's behalf.** No app code changes here.

Two carried asks ride this pass:
1. **D2-ext echo-hex capture** — still-uncaptured after three rounds; now NICE-TO-HAVE only (the D2-ext
   data layer is proven correct by the owner's log observation). Capture if convenient; not a blocker.
2. **G6 dev-test-clone deactivation** — **BLOCKING this time.** Outstanding across THREE consecutive
   checkpoints (08-10 → 08-16 → 08-21); clones run against REAL contacts (bot-activation policy). The
   checkpoint MUST NOT accept an all-PASS without an explicit G6 disposition.

Purpose: prove Gate A can close (or spin round 5) so the milestone-closeout mechanics (Gates B/C) proceed.
Output: round-4 verdicts in 08-DEVICE-UAT.md + a Gate A disposition.

**Standing rule (shared live tree):** parallel sessions (phases 9/10/11) are committing to this working
tree. This plan touches ONLY the two planning docs above — stage ONLY those; never `git add -A`.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.planning/phases/08-device-uat-milestone-closeout/08-21-SUMMARY.md
@.planning/phases/08-device-uat-milestone-closeout/08-22-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-23-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-24-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Owner device re-verify — D2-view / D12-ext / D14 + G6 (BLOCKING)</name>
  <files>.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md, .planning/phases/08-device-uat-milestone-closeout/08-25-SUMMARY.md</files>
  <action>Owner-run gate (not executor work): after the pre-build test gate passes, build ONE Android device build with 08-22/08-23/08-24 merged, then re-verify each item below against its 08-DEVICE-UAT.md anchor and record exactly one verdict (PASS/FAIL/N/A) VERBATIM. Do NOT tick on the owner's behalf. G6 (deactivate the dev clone) is a BLOCKING disposition item — the gate refuses an all-PASS without it. On all-PASS + G6 dispositioned: flip Gate A → PASS, re-aggregate I.3 #10, unblock Gates B/C; on any FAIL: keep Gate A = ISSUES and spin round 5 via /gsd-plan-phase 08 --gaps. No app code changes in this plan.</action>
  <read_first>
    - 08-DEVICE-UAT.md §Defects table (D2-view, D12-ext, D14 rows) + §Overall
    - 08-22-PLAN.md / 08-23-PLAN.md / 08-24-PLAN.md (what each fix does + its device confirmation line)
    - 08-21-SUMMARY.md (round-3 owner-verbatim format to mirror; G6 third-carry note)
  </read_first>

  <pre-build-gate>
    Before the build, confirm the EditMode suite is GREEN on the tree the owner will build from — read
    the total FRESH from the bridge (`Temp/claude/test-summary.json`, or `Tools/run-tests-headless.sh`
    if the Editor is closed). Do NOT assert an absolute total: the suite grows concurrently from parallel
    phases (1170+ as of 09-03) and round-3 carried a stale hardcoded number that had to be corrected
    mid-run. The gate is "completed/Passed, 0 failures at the current baseline", where the baseline is
    whatever the bridge reports at run time. Fixes 08-22/08-23/08-24 add: 0 (D2-view) + 6 (D12-ext
    EmptyStateReasonPolicy) + 0 (D14) = **+6** over the pre-round-4 baseline.
  </pre-build-gate>

  <what-built>
    Round-4 fixes on ONE Android build:
    - **D2-view (08-22):** the reaction bar re-renders the pressed bubble's pill one frame after
      dismiss (after the lifted overrideSorting Canvas is destroyed), so a locally-changed reaction
      no longer strands a stale pill; a compiled `[D2-view]` log fires in HandleReactionsChanged.
    - **D12-ext (08-23):** the create-first-bot CTA coerces its reason to NoBots via the authoritative
      resolver and re-derives on a channel switch, so it survives a WhatsApp↔Telegram chip switch on
      both channels; OpenCurrentBotAuth now logs instead of failing silently.
    - **D14 (08-24):** the Telegram post-creation cover's spinner + progress fill + countdown render
      in Telegram brand blue (#2AABEE); the WhatsApp cover is unchanged.
  </what-built>

  <how-to-verify>
    Record EXACTLY ONE verdict per item (PASS / FAIL / N/A), transcribed VERBATIM (do not paraphrase,
    do not tick on the owner's behalf). Any FAIL → add/append a row in 08-DEVICE-UAT.md §Defects with
    its source anchor.

    1. **D2-view — reaction bubble repaint (the round-3 repro).**
       expected: change a reaction on message bubble A (long-press → tap an emoji), then open the
       reaction bar on ANOTHER bubble B and change its reaction. Bubble A's pill shows the emoji you
       set on A — it does NOT stay stale. Repeat a few times (the old bug was intermittent).
       source: 08-DEVICE-UAT.md D2-view / B9–B13.

    2. **D2-view — WhatsApp unaffected.**
       expected: on a WhatsApp bot, add/change a reaction — the pill repaints exactly as before.
       source: WhatsApp byte-identical invariant (08-22).

    3. **D12-ext — create-bot CTA survives a channel switch (BOTH channels, zero bots).**
       expected: with NO bots, open the Chats screen; tap «Создать бота» → the Add-Bot form opens (this
       already worked). Then switch the WhatsApp↔Telegram chip and tap «Создать бота» again — it STILL
       opens the form, on BOTH channels, with the channel you're viewing preselected. Switch back and
       forth a couple of times to confirm it never goes dead.
       source: 08-DEVICE-UAT.md D12-ext / F-group.

    4. **D12-ext — no stale wrong-channel card over the cover (WR-02).**
       expected: on a Telegram-only bot inside its ~5-min sync cover, flip to WhatsApp (its «WhatsApp
       не подключён» card shows), then flip back to Telegram — you see the Telegram syncing cover, NOT
       a leftover «WhatsApp не подключён» card sitting on top / blocking taps.
       source: 08-DEVICE-UAT.md D12-ext / 08-REVIEW WR-02.

    5. **D14 — Telegram cover reads Telegram-blue.**
       expected: create a fresh Telegram bot → the post-creation cover's spinner, progress fill, and
       countdown are Telegram brand blue (#2AABEE), matching the blue empty-state accent + switcher chip.
       source: 08-DEVICE-UAT.md D14 / D13 cover.

    6. **D14 — WhatsApp cover byte-identical.**
       expected: a fresh WhatsApp bot's cover is unchanged — green spinner/fill, green countdown.
       source: WhatsApp byte-identical invariant (08-24).

    7. **G6 — deactivate the dev test clone (BLOCKING line item).**
       expected: once any dev-n8n test window closes, the per-bot Telegram/WhatsApp clone is DEACTIVATED
       (clones run against real contacts); prod bagkz stayed dormant. **This checkpoint does NOT accept
       an all-PASS without an explicit G6 disposition** (done / not-needed-this-pass-because-no-clone-active
       / still-outstanding). Record which.
       source: 08-DEVICE-UAT.md G6 / 04-HUMAN-UAT.md #6.

    8. **D2-ext echo-hex (NICE-TO-HAVE, non-blocking).**
       If convenient during D2-view testing, capture the tapi reaction-echo hex from the `[TG reaction echo]`
       Editor log (ChatManager.cs) / `Tools/tapi/probe-message.sh`. Absence is fine — record "not captured".
       source: 08-DEVICE-UAT.md D2-ext.
  </how-to-verify>

  <on-completion>
    - Transcribe every verdict VERBATIM into 08-DEVICE-UAT.md (a round-4 block in the Defects table +
      the Overall line), each mapped to its source anchor. Record the G6 disposition explicitly.
    - **All PASS (and G6 dispositioned):** flip Gate A → PASS; re-aggregate the re-deferred I.3 #10
      (01-VERIFICATION sign-off, previously blocked by D5 — now unblocked); unblock Gates B (prod
      replication, 08-02) and C (milestone close, 08-03). Note prod bagkz stays dormant until 08-02.
    - **Any FAIL:** keep Gate A = ISSUES; file the specifics in §Defects with anchors and spin round 5
      via `/gsd-plan-phase 08 --gaps`. Do NOT hand-patch fixes in the runbook.
    - No app code changes in this plan (planning docs only).
  </on-completion>

  <verify>Owner records exactly one PASS/FAIL/N-A verdict per item (D2-view, D12-ext incl. WR-02, D14, WhatsApp-parity checks) + an explicit G6 disposition + the D2-ext echo-hex note, all transcribed verbatim into 08-DEVICE-UAT.md and mapped to their anchors; the Overall line + Gate A disposition are set. Pre-build EditMode suite green at the current baseline +6 (read fresh from the bridge).</verify>
  <done>All items have a recorded owner verdict; G6 dispositioned (gate refuses all-PASS otherwise); 08-DEVICE-UAT.md §Defects + Overall updated; Gate A disposed — all-PASS → PASS (I.3 #10 re-aggregated, Gates B/C unblocked) or any FAIL → ISSUES with round-5 scope named.</done>
  <resume-signal>Type "approved" (record all verdicts + the G6 disposition), or describe the failures per item to spin round 5.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner device ↔ dev n8n / dev Telegram profile | Owner-run verification only; prod bagkz stays dormant. No code, no new surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-25-01 | (all STRIDE categories) | owner-run device checkpoint (no code) | accept | This plan changes no app code and adds no surface. It records owner verdicts into planning docs; secrets.json is deny-ruled and referenced by name only. The only real-world risk is the G6 clone running against real contacts — mitigated by making G6 a BLOCKING disposition item on this checkpoint. |
</threat_model>

<verification>
- Pre-build: EditMode suite completed/Passed with 0 failures at the CURRENT baseline (+6 from round 4);
  baseline read FRESH from the bridge, never hardcoded.
- Every item has exactly one owner verdict, transcribed verbatim; every FAIL has a §Defects row + anchor.
- G6 has an explicit disposition (the gate refuses all-PASS otherwise).
- Overall line updated; Gate A disposition recorded.
</verification>

<success_criteria>
- Owner ran ONE Android build covering D2-view, D12-ext, D14 (+ WhatsApp-parity checks) and recorded
  verbatim verdicts.
- G6 dispositioned explicitly; D2-ext echo-hex recorded (captured or "not captured").
- Gate A disposition set: all-PASS → PASS (I.3 #10 re-aggregated, Gates B/C unblocked) or any FAIL →
  ISSUES with round-5 gaps filed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-25-SUMMARY.md`.
</output>
