---
phase: 08-device-uat-milestone-closeout
plan: 21
type: execute
wave: 10
depends_on: [17, 18, 19, 20]
files_modified: []
autonomous: false
gap_closure: true
requirements: []

must_haves:
  truths:
    - "The owner re-verifies every round-3 defect (D2-ext, D12, D13) on ONE device build in a single ordered pass"
    - "Each re-verify item maps back to its exact 08-DEVICE-UAT.md anchor + defect id, and the pass captures the outstanding D2-ext echo-hex evidence"
    - "On all-PASS this dispositions Gate A → PASS and unblocks Gates B (prod replication) + C (milestone close); any FAIL spins a further gap round"
  artifacts:
    - path: ".planning/phases/08-device-uat-milestone-closeout/08-21-SUMMARY.md"
      provides: "Recorded round-3 device re-verify verdicts + Gate A disposition + captured D2-ext evidence"
      contains: "Gate A"
  key_links:
    - from: "08-21 re-verify verdicts"
      to: "08-DEVICE-UAT.md Defects D2-ext/D12/D13 + Overall line"
      via: "owner records PASS/FAIL per item"
      pattern: "D13"
---

<objective>
The consolidated owner-run device RE-VERIFY of the round-3 gap-closure plans (08-17..08-20) on ONE build, one ordered pass — do NOT scatter per-plan device gates (mirror 08-10 / 08-16). This closes the loop reopened by the 08-16 round-2 re-verify (Gate A still ISSUES): the three open defects **D2-ext, D12, D13** get re-tested on a real device build, and the result dispositions Gate A.

Purpose: a single, unambiguous device pass that confirms the three round-3 defects are fixed (or files any residual as a further gap) AND captures the device-only evidence still owed — the D2-ext reaction echo-hex. The autonomous fixes cannot be device-verified by Claude (no device build, no live Telegram infra here) — running this pass IS the owner gate.

Output: `08-21-SUMMARY.md` recording each verdict + the Gate A disposition. **On all-PASS:** `08-DEVICE-UAT.md` Overall flips PASS, the re-deferred I.3 #10 (01-VERIFICATION sign-off) re-aggregates via the Gate A closure, and the milestone-close mechanics proceed (Gate B prod replication → Gate C close). **On any FAIL:** that item spins its own `/gsd-plan-phase 08 --gaps` round-4.

Constraint: `checkpoint:human-verify` — NO app code changes here. Prod bagkz stays dormant; bot clones stay INACTIVE except during the test window. The still-outstanding G6 reminder rides this pass.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.planning/phases/08-device-uat-milestone-closeout/08-16-SUMMARY.md
@.planning/phases/08-device-uat-milestone-closeout/08-17-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-18-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-19-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-20-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Owner runs the round-3 device re-verify (D2-ext / D12 / D13) on ONE build</name>
  <read_first>
    - .planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md — the Defects table rows D2-ext, D12, D13 (symptoms + source anchors) and the G6 deactivate-clone reminder
    - .planning/phases/08-device-uat-milestone-closeout/08-17-SUMMARY.md / 08-18-SUMMARY.md / 08-19-SUMMARY.md / 08-20-SUMMARY.md — what each round-3 fix changed + its device-verify expectation and any capture ask (produced by the executors before this checkpoint)
  </read_first>
  <action>Owner-run gate (not executor work): build ONE Android device build with 08-17..08-20 merged, then re-verify each item below against its 08-DEVICE-UAT.md anchor and record PASS/FAIL/N/A verbatim. Do NOT tick on the owner's behalf. On all-PASS, flip Gate A → PASS, re-aggregate I.3 #10, and proceed to Gate B/C; on any FAIL, spin a round-4 gap plan for that item.</action>
  <files>(no app code — the owner builds off the post-08-20 tree; this plan only writes planning docs)</files>
  <what-built>
Round-3 gap fixes landed on the working tree (built into ONE Android build @ the post-08-20 tree):
  - **08-17 (D2-ext):** a TG-app-side reaction change/removal now reflects in-app within a poll cycle for loaded messages (routed fix — poll-window coverage and/or Merge absence semantics), Telegram-gated, WhatsApp byte-identical.
  - **08-18 (D12):** the Telegram empty-state «Создать бота» CTA now opens the Add-Bot overlay (root cause fixed per runtime/scene evidence), Telegram preselected; WhatsApp CTA byte-identical.
  - **08-19 (D13a):** a freshly-created Telegram bot now shows the WhatsApp-parity post-creation cover (~5-min progress slider over the chats list), channel-appropriate copy; WhatsApp cover byte-identical.
  - **08-20 (D13b):** the D9 «Синхронизация…» pill is removed entirely (scene + scripts + gate + builder + tests + events).
Suite green FRESH before the build (08-17/08-19 add tests; 08-20 removes 6 → new baseline stated in each SUMMARY; the tree's final EditMode total is recorded).
  </what-built>
  <how-to-verify>
Build ONE Android device build off the post-08-20 tree, then run these in order (tick exactly one box per item; on any FAIL add a Defects row with the source anchor). Auth the dev Telegram profile; prod bagkz stays dormant.

1. **D2-ext — TG-app-side reaction change/removal reflects in-app.** On a Telegram chat, with the app OPEN on that chat: from the Telegram app itself (another device, or the contact) ADD a reaction, then CHANGE it, then REMOVE it — on a message currently visible in the open chat. **Expected:** each delta reflects in-app within a poll cycle (~3s) without re-entering the chat; a removal clears the pill and does NOT resurrect. Also try it on a message the owner scrolled UP to (older). **Capture (still owed):** with the Editor console reachable OR via logcat, capture the `[TG reaction echo] '…' [hex] user_id=… ownId=…` line(s) for the change/removal (the 08-11 `#if UNITY_EDITOR` log) and paste them into the SUMMARY. **verdict:** ☐ PASS ☐ FAIL ☐ N/A | **source:** 08-DEVICE-UAT.md D2-ext / B9-B13
2. **D12 — Telegram create-bot CTA opens the form.** On the Telegram channel with no bots (Telegram-themed empty state), tap «Создать бота». **Expected:** the Add-Bot overlay opens with **Telegram** preselected. Then repeat on the WhatsApp channel: «Создать бота» opens with WhatsApp preselected (byte-identical). **verdict:** ☐ PASS ☐ FAIL ☐ N/A | **source:** 08-DEVICE-UAT.md D12 / F-group
3. **D13 cover — fresh TG bot shows the ~5-min cover.** Create a NEW Telegram bot in-app (auth the dev TG profile). **Expected:** immediately after creation the chats list shows the full cover page — spinner + headline + a time-based progress slider + countdown (~5 min) — channel-appropriate copy (not "from WhatsApp"); it elapses and reveals the Telegram chat list. Confirm a WhatsApp bot creation still shows its cover unchanged. **verdict:** ☐ PASS ☐ FAIL ☐ N/A | **source:** 08-DEVICE-UAT.md D13 (cover) / E/B-group
4. **D13 pill — the «Синхронизация…» pill is gone everywhere.** On the Telegram chat list (fresh load + channel switches + bot switches), confirm NO «Синхронизация…» pill appears anywhere; WhatsApp shows no pill either (unchanged). **verdict:** ☐ PASS ☐ FAIL ☐ N/A | **source:** 08-DEVICE-UAT.md D13 (pill removal) / D9-superseded
5. **G6 reminder (carried, still OUTSTANDING) — deactivate the dev test clone** once the window closes (bot-activation policy: clones run against real contacts). Confirm prod bagkz stayed dormant. **verdict:** ☐ DONE ☐ N/A (reason: ______)

Record verdicts VERBATIM from the owner's stated results — do NOT tick on the owner's behalf. Then set the Overall line and update 08-DEVICE-UAT.md's Defects/Overall accordingly.
  </how-to-verify>
  <verify>Owner records a PASS/FAIL/N-A verdict per item (D2-ext, D12, D13-cover, D13-pill) + the G6 disposition, transcribed verbatim into 08-21-SUMMARY.md, each mapped to its 08-DEVICE-UAT.md anchor; Gate A disposition set.</verify>
  <done>All five items have a recorded owner verdict; 08-DEVICE-UAT.md Defects + Overall updated; Gate A disposed (PASS unblocks Gates B/C + re-aggregates I.3 #10; any FAIL names round-4 scope).</done>
  <acceptance_criteria>
    - `08-21-SUMMARY.md` records a per-item verdict (D2-ext, D12, D13-cover, D13-pill, G6) transcribed verbatim from the owner, each mapped to its 08-DEVICE-UAT.md anchor.
    - The D2-ext echo-hex capture is pasted into the SUMMARY OR its absence is explicitly noted (as in 08-16, if the owner did not capture it).
    - `08-DEVICE-UAT.md` Defects table + Overall line updated: D2-ext/D12/D13 marked RESOLVED (on PASS) or a residual filed (on FAIL); the G6 reminder disposition recorded.
    - Gate A disposition stated: on all-PASS → Gate A PASS + I.3 #10 re-aggregation noted + Gates B/C unblocked; on any FAIL → round-4 scope named.
    - No app code changed by this plan (`git status --porcelain Assets` clean except intended doc/planning files).
  </acceptance_criteria>
  <resume-signal>Owner records PASS/FAIL per item (verbatim) + the G6 disposition; type "approved" with the verdicts, or describe the failures to spin round-4.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Owner-run verification only; no code, no network surface, no secrets introduced. Live TG auth + any echo-hex/logcat capture is owner-run and read-only; the token stays in Secrets.Data, deny-ruled, never printed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-21-01 | Info disclosure | Device/Editor capture (echo-hex, logcat) | accept | Owner-run, read-only; no secret is logged; the tapi/Wappi token is never printed. |
| T-08-21-02 | Repudiation | Bot-activation policy (dev clone) | mitigate | The pass carries the explicit G6 reminder to DEACTIVATE the dev test clone after the window (real contacts); prod bagkz stays dormant. |
</threat_model>

<verification>
- Owner records verbatim verdicts for D2-ext / D12 / D13-cover / D13-pill / G6 in 08-21-SUMMARY.md, each anchored to 08-DEVICE-UAT.md.
- Gate A disposition set; on all-PASS, Gates B/C unblocked and I.3 #10 re-aggregated.
</verification>

<success_criteria>
On all-PASS: D2-ext (TG-app reaction deltas reflect), D12 (CTA opens the form, TG preselected), D13 (cover appears on fresh TG bot; pill gone everywhere) are confirmed on device; Gate A → PASS; milestone-close mechanics (Gate B → C) proceed; the dev test clone is deactivated and prod bagkz stayed dormant. On any FAIL: the residual spins a round-4 gap plan.
</success_criteria>

<output>
After the owner records results, create `.planning/phases/08-device-uat-milestone-closeout/08-21-SUMMARY.md` (verdict table + Gate A disposition + captured D2-ext evidence or its explicit absence), and update `.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md` (Defects + Overall) and STATE.md accordingly.
</output>
