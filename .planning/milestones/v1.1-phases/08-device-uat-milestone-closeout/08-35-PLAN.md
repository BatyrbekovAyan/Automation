---
phase: 08-device-uat-milestone-closeout
plan: 35
type: execute
wave: 18
depends_on: [34]
gap_closure: true
autonomous: false
requirements: []          # closeout phase — no new v1.1 REQ ids (per ROADMAP Phase 8 + gap_closure)
files_modified:
  - .planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
  - .planning/phases/08-device-uat-milestone-closeout/08-35-SUMMARY.md

must_haves:
  truths:
    - "The owner runs the D2-view rapid-change repro in the Unity Editor Play Mode: tap a reaction IN OUR app, then change the OWN reaction in the Telegram app 2-3 times within 90s — EVERY change repaints the in-app pill (the [D2-merge] suppressed line must NOT fire for genuine external changes)."
    - "The original stale-echo defense is re-checked (Editor): tapping in-app does not flicker back to the old emoji while the server echo lags — the displaced-match suppress still defends the round-2 D2 case."
    - "In-app WhatsApp AND Telegram add/change/remove invariants hold (count 1, change leaves one pill, removal stays cleared)."
    - "The [D15-probe] fires deterministically (no owner choreography) with reactionsKey/reactionKey booleans → both False confirms the platform limit (finalize the CLAUDE.md note, remove 'probe confirmation pending'); a surfaced key spins an absence-reconcile round."
    - "ONE Android build is run as the final Gate A sweep ONLY if all Editor items pass; all-PASS flips Gate A → PASS, unblocks Gates B/C and re-aggregates I.3 #10."
  artifacts:
    - path: ".planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md"
      provides: "round-7 verdicts appended (D2-view rapid-change / stale-echo sanity / WA+TG invariants / D15 probe disposition), Overall + Gate A updated, Defects rows resolved-or-refiled"
      contains: "Round 7"
  key_links:
    - from: "owner Editor Play-Mode + one device build"
      to: "08-DEVICE-UAT.md Defects table + Overall + Gate A"
      via: "verbatim verdict transcription per item, mapped to its source anchor, any FAIL log captured"
      pattern: "D2-view|D15|Gate A"
---

<objective>
Round-7 consolidated owner re-verify — the Gate A gate for milestone v1.1. The owner verifies the 08-34
fixes (CR-01a displaced-emoji discrimination + CR-02 Reconcile seam + WR-01 pin + the D15 deterministic
probe trigger) and records a verbatim verdict per item.

**KEY CHANGE from round 6:** D2-view is now discriminated by the DISPLACED pre-tap emoji, and the fix
lands through the live-poll call sites (CR-02, which the round-6 fix silently missed). D2-view is
Editor-reproducible (round-5/6 proved it repros in the Unity Editor Console), so **Editor Play-Mode
verification is sufficient for the rapid-change repro + the stale-echo sanity**, with **ONE Android build
only for the final Gate A confirmation sweep** (WA/TG invariants). This plan writes the runbook block
autonomously; RUNNING it is the owner gate. The phase stays open until the owner records verdicts. **Do NOT
tick these on the owner's behalf. No app code changes here.**

Discriminating signals for this pass:
- **`[D2-merge]` now fires ONLY when a DISPLACED-matching stale echo is suppressed** — for a genuine
  external own-change it should NOT fire (the change should just apply and repaint). Its presence during
  the rapid-change repro means a displaced-back-to edge (the documented, accepted residual) or a defect.
- **`[D15-probe] wa msgId=… reactionsKey=… reactionKey=…`** now fires deterministically on the first
  WhatsApp reaction raw of the Editor session — no WhatsApp quoted-reply resolve needed (round 6's probe
  never triggered because it waited on a quote resolve that did not happen).

Purpose: prove Gate A can close (or spin the next round with discriminating logs) so the milestone-closeout
mechanics (Gates B/C) proceed. Output: round-7 verdicts in 08-DEVICE-UAT.md + a Gate A disposition + the
D15 close decision (finalize the CLAUDE.md platform-limit note on both-false).

**Standing rule (shared live tree):** parallel sessions (phases 9/10/11) are committing to this working
tree. This plan touches ONLY the two planning docs above — stage ONLY those; never `git add -A`.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.planning/phases/08-device-uat-milestone-closeout/08-33-SUMMARY.md
@.planning/phases/08-device-uat-milestone-closeout/08-34-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-REVIEW.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Owner re-verify — D2-view rapid-change / stale-echo sanity / WA+TG invariants / D15 disposition (round 7)</name>
  <files>.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md, .planning/phases/08-device-uat-milestone-closeout/08-35-SUMMARY.md</files>
  <read_first>
    - 08-DEVICE-UAT.md §Defects table (D2-view, D15 rows) + §Round 6 re-verify block + §Overall (mirror its structure for a new §Round 7 re-verify block)
    - 08-34-PLAN.md (what CR-01a / CR-02 / WR-01 / the D15 probe do + the log tokens to capture: [D2-merge], [D15-probe], [D15])
    - 08-33-SUMMARY.md (round-6 owner-verbatim transcription format to mirror; the exact round-6 capture that D2-view must now beat)
  </read_first>
  <action>Owner-run gate (not executor work). After the pre-build test gate passes, verify the fix items in the
  Unity Editor Play Mode (D2-view rapid-change repro + stale-echo sanity are Editor-reproducible), capture the
  deterministic D15 probe signal, then — only if the Editor items pass — build ONE Android device build (08-34
  merged) for the final Gate A confirmation sweep (WA/TG invariants). Record exactly one verdict (PASS/FAIL/N/A)
  per item VERBATIM. Do NOT tick on the owner's behalf. On all-PASS: flip Gate A → PASS, re-aggregate I.3 #10,
  unblock Gates B/C, and FINALIZE the CLAUDE.md D15 platform-limit note (remove "probe confirmation pending").
  On any FAIL: keep Gate A = ISSUES, capture the [D2-merge]/[D15-probe] logs into the Defects row, and spin the
  next round via /gsd-plan-phase 08 --gaps. No app code changes in this plan.</action>

  <pre-build-gate>
    Before verifying, confirm the EditMode suite is GREEN on the tree the owner will verify/build from — read
    the total FRESH from the bridge (`Temp/claude/test-summary.json`, or `Tools/run-tests-headless.sh` if the
    Editor is closed). Do NOT assert an absolute total: the suite grows concurrently from parallel phases
    (~1184 as of round 6) and prior rounds carried stale hardcoded numbers that had to be corrected mid-run.
    The gate is "completed/Passed, 0 failures at the current baseline". Round-7 fixes add **+7** (08-34: Task 1
    +6 = displaced third-value adopt ×2, tombstone-different-emoji re-add, WR-01 revert pin, VS16 displaced
    seam, JsonUtility default-null pin; Task 2 +1 = the Reconcile through-the-seam test; Task 3 +0 = the
    Editor-only probe glue). Several existing tests across BOTH TelegramReactionMergeTests.cs and
    TelegramReactionReceiveTests.cs were updated in place (count flat — the sibling
    Merge_FreshEmojiChange_BeatsStaleServerEcho fixture now carries a displaced pre-tap value). Read it fresh, never hardcode.
  </pre-build-gate>

  <what-built>
    Round-7 fixes (08-34):
    - **D2-view (CR-01a):** `TelegramReactionMerge` now discriminates a stale echo from a genuine external
      own-change via the DISPLACED pre-tap emoji carried ON the optimistic `MessageReaction` entry
      (`displacedEmoji`, stamped by ReactionSend). A differing server-me is suppressed ONLY when it equals the
      displaced value (the round-2 stale-echo defense); any THIRD value adopts and repaints. The removal
      tombstone is gated the same way (displaced = the removed emoji).
    - **CR-02 (the seam that makes it land):** a pure `Reconcile(cached, server, now, out renderChanged)` seam;
      `RefreshCachedMessageReactions` now ALWAYS adopts the reconciled list (all three live-poll call sites
      funnel through it), so the confirm/fold freshness-consumption is no longer discarded by the old
      multiset-only guard — the reason the round-6 "confirmation-clears-grace" fix never actually landed.
    - **WR-01 (pin):** a failed-POST revert entry carries null displaced ⇒ any differing echo adopts (a
      ghost-landed or mid-flight external change self-corrects) — pinned by test.
    - **D15 (IN-01):** an Editor-only one-shot arms on the FIRST WhatsApp `type:"reaction"` raw of the session,
      enqueuing the reaction's target stanza id into the existing authed messages/id/get drain so
      `[D15-probe] wa msgId=… reactionsKey=… reactionKey=…` fires deterministically. Zero new secrets/endpoint.
    - **`[D2-merge]` message updated** — it now reads "suppressed stale displaced echo '…' under fresh local
      '…' age=…s" and fires ONLY on a displaced-matching suppress.
  </what-built>

  <how-to-verify>
    Record EXACTLY ONE verdict per item (PASS / FAIL / N/A), transcribed VERBATIM (do not paraphrase, do not
    tick on the owner's behalf). Any FAIL → add/append a row in 08-DEVICE-UAT.md §Defects with its source
    anchor AND paste the relevant captured log line(s).

    **Editor Play-Mode is sufficient for items 1-2 (Editor-reproducible). Items 3-4 (invariants) ride ONE
    Android build — the final Gate A sweep, run only if items 1-2 pass.**

    1. **D2-view — rapid own-reaction change in the Telegram app repaints EVERY time.**
       expected: on a Telegram bot, tap a reaction on a message IN OUR app (arms the optimistic + displaced
       state), then within 90 s change your OWN reaction on that message IN the Telegram app 2-3 times (e.g.
       🥺 → 🔥 → 👎). Each change repaints the in-app bubble pill to the new emoji — NO change is dropped after
       the first (this is the exact round-6 capture, now fixed).
       source: 08-DEVICE-UAT.md D2-view / B9–B13.
       IF FAIL: capture any `[D2-merge] suppressed stale displaced echo '…' under fresh local '…' age=…s` lines
       (a genuine external change should NOT log this — its presence means the suppression path is still eating
       the change) plus the `[TG reaction echo]` timeline — paste into the Defects row.

    2. **Stale-echo defense sanity — no flicker back to the old emoji.**
       expected: on a Telegram bot, tap a reaction (or change one) IN OUR app and leave it — the pill stays on
       the new emoji and does NOT flicker back to the previous one while the server echo lags for a poll or two
       (the displaced-match suppress still defends the original round-2 D2 case). It settles cleanly once the
       server confirms.
       source: 08-DEVICE-UAT.md D2 / 08-REVIEW CR-01 (never-clear-on-displaced-match).

    3. **In-app WhatsApp AND Telegram add/change/remove invariants (ONE build).**
       expected: on a WhatsApp bot AND on a Telegram bot, add / change / remove your own reaction IN OUR app —
       count is 1 (not «2»), a change leaves ONE pill, a removal clears and stays cleared. WhatsApp repaints
       exactly as before (byte-identical); the round-2/round-4/round-6 Telegram fixes still hold.
       source: WhatsApp/Telegram reaction invariants (08-06/08-11/08-17/08-30/08-34).

    4. **Final Gate A device sweep — no regression on one build.**
       expected: the merged 08-34 build behaves correctly for items 1-3 on a real device (spot-check the
       rapid-change repro once on-device if a live TG-app path is available; otherwise device-verify the
       invariants and rely on the Editor pass for the rapid-change repro). Nothing regressed vs round 6's
       passing items (WA add/change, TG add/change/remove, the D17 cover).
       source: 08-DEVICE-UAT.md §Overall Gate A sweep.

    5. **D15 disposition — deterministic probe fires; no WhatsApp reaction-state key surfaces.**
       expected: with the Editor open on a WhatsApp bot, when a reaction raw arrives (someone reacts to a WA
       message, or you react from the WhatsApp phone app), the log shows `[D15-probe] arming target-payload
       probe for stanza=…` followed by `[D15-probe] wa msgId=… reactionsKey=False reactionKey=False`. Both
       False (as predicted) ⇒ D15 is closed as a Wappi WhatsApp platform limit: FINALIZE the CLAUDE.md note
       (remove "probe confirmation pending"). If EITHER key comes back True ⇒ a reaction-state key surfaced:
       revert nothing yet, keep the note, and spin the next round for an absence-based WA reconcile.
       source: 08-DEVICE-UAT.md D15 / 08-REVIEW IN-01. NOTE: `secrets.json` is deny-ruled — the app is authed
       in Play Mode; the probe reuses the existing authed messages/id/get seam, the executor never handles a token.
  </how-to-verify>

  <on-completion>
    - Add a §Round 7 re-verify block to 08-DEVICE-UAT.md (mirroring §Round 6): transcribe every verdict
      VERBATIM, each mapped to its source anchor; update the status header, the Round-7 Overall line, and the
      §Overall-result bullet. Paste any captured [D2-merge]/[D15-probe] lines into the FAIL rows. Update the
      D2-view / D15 Defects rows with the round-7 outcome.
    - **All PASS:** flip Gate A → PASS; re-aggregate the re-deferred I.3 #10 (01-VERIFICATION sign-off);
      unblock Gates B (prod replication, 08-02) and C (milestone close, 08-03); finalize the CLAUDE.md D15
      platform-limit note (both-false probe → drop "probe confirmation pending"). Note the `[D2-merge]` /
      `[D15-probe]` / `[D15]` Editor diagnostics are tagged for removal at phase close (08-REVIEW IN-02/IN-03).
      Prod bagkz stays dormant until 08-02.
    - **Any FAIL:** keep Gate A = ISSUES; file the specifics in §Defects with anchors + the captured logs, and
      spin the next round via `/gsd-plan-phase 08 --gaps`. If the D15 probe surfaced a reaction-state key, note
      the absence-reconcile scope. Do NOT hand-patch fixes in the runbook.
    - No app code changes in this plan (planning docs only).
  </on-completion>

  <verify>Owner records exactly one PASS/FAIL/N-A verdict per item (D2-view rapid-change repaint, stale-echo sanity, WA+TG add/change/remove invariants, final device sweep, D15 deterministic-probe disposition), all transcribed verbatim into 08-DEVICE-UAT.md and mapped to their anchors; every FAIL has a §Defects row + the captured [D2-merge]/[D15-probe] log line; the Overall line + Gate A disposition + the D15 close decision are set. Pre-verify EditMode suite green at the current baseline +7 (read fresh from the bridge).</verify>
  <done>All items have a recorded owner verdict; 08-DEVICE-UAT.md §Round 7 + §Defects + Overall updated; Gate A disposed — all-PASS → PASS (I.3 #10 re-aggregated, Gates B/C unblocked, D15 CLAUDE.md note finalized) or any FAIL → ISSUES with the next round's scope named and the discriminating logs captured.</done>
  <resume-signal>Type "approved" (record all verdicts + capture any FAIL logs + set the D15 disposition), or describe the failures per item (with the [D2-merge]/[D15-probe] log lines) to spin the next round.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner Editor/device ↔ dev n8n / dev Telegram + WhatsApp profiles | Owner-run verification only; prod bagkz stays dormant. No code, no new surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-35-01 | (all STRIDE categories) | owner-run re-verify checkpoint (no code) | accept | This plan changes no app code and adds no surface. It records owner verdicts into planning docs; `secrets.json` is deny-ruled and referenced by name only — the deterministic D15 probe reuses the app's already-authed messages/id/get seam in Play Mode, so the executor never handles a token. The only real-world clone-against-real-contacts risk (G6) is already resolved. |
</threat_model>

<verification>
- Pre-verify: EditMode suite completed/Passed with 0 failures at the CURRENT baseline (+7 from round 7);
  baseline read FRESH from the bridge, never hardcoded.
- Every item has exactly one owner verdict, transcribed verbatim; every FAIL has a §Defects row + anchor + the
  captured [D2-merge]/[D15-probe] log line.
- Overall line updated; Gate A disposition recorded; the D15 close decision recorded (both-false probe →
  CLAUDE.md note finalized).
</verification>

<success_criteria>
- Owner verified the round-7 fixes (Editor Play-Mode for the D2-view rapid-change repro + stale-echo sanity;
  ONE Android build for the Gate A invariants sweep) and recorded verbatim verdicts; the D15 disposition is
  recorded (deterministic probe empty → platform-limited close, or a reaction-state key → next-round reconcile).
- Gate A disposition set: all-PASS → PASS (I.3 #10 re-aggregated, Gates B/C unblocked, D15 CLAUDE.md note
  finalized) or any FAIL → ISSUES with the next round's gaps filed and the discriminating logs captured.
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-35-SUMMARY.md`.
</output>
