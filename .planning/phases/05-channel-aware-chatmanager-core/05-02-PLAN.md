---
phase: 05-channel-aware-chatmanager-core
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - Assets/Scripts/Main/ChatManager.Channel.cs
  - Assets/Scripts/Main/ChatManager.BotState.cs
  - Assets/Scripts/Main/ChatManager.cs
  - Assets/Scripts/UI/EmptyStateView.cs
  - Assets/Tests/Editor/Chat/ChannelResolutionTests.cs
  - Assets/Tests/Editor/Chat/ChannelCacheRootTests.cs
autonomous: true
requirements: [CHAT-01, CHAT-11]

must_haves:
  truths:
    - "ChatManager.ActiveChannel is a public-read property persisted per bot in PlayerPrefs key {botId}ActiveChatChannel (int), defaulting to WhatsApp when the key is missing"
    - "SetActiveChannel performs the full SetActiveBot reset choreography (stop sync-wait, drop outbox, clear Chats/chatLookup, OnChatListCleared, StopAllCoroutines, zero _chatFetchesInFlight, _chatListSyncing=false, clear vthumb+media queues, BeginLoadForActiveBot) and fires OnActiveChannelChanged"
    - "GetActiveProfileId returns the active channel's profile id (whatsappProfileId or telegramProfileId) via the same IsValidProfileId guard"
    - "GetCacheRoot keeps BotCache/{botId}/ for WhatsApp and BotCache/{botId}/telegram/ for Telegram — a dual-channel bot no longer collides chats.json"
    - "On bot switch/startup the persisted channel restores; if it is unconnected but the other channel IS connected, the connected one is auto-selected and the correction persisted"
    - "A Telegram-authed bot with no WhatsApp shows the BotHasNoTelegram empty state ONLY when Telegram is the active-and-unconnected channel; the sync-gate applies only when ActiveChannel==WhatsApp"
    - "Every existing WhatsApp behavior is byte-identical (WhatsApp cache stays at the legacy root, same empty-state, same sync window) and the full EditMode suite is green"
  artifacts:
    - path: "Assets/Scripts/Main/ChatManager.Channel.cs"
      provides: "ActiveChannel property, SetActiveChannel choreography, OnActiveChannelChanged event, channel resolution helpers"
      contains: "ActiveChannel"
      min_lines: 40
    - path: "Assets/Scripts/Main/ChatManager.cs"
      provides: "EmptyStateReason.BotHasNoTelegram"
      contains: "BotHasNoTelegram"
    - path: "Assets/Scripts/UI/EmptyStateView.cs"
      provides: "Telegram empty-state copy + CTA reuse"
      contains: "Telegram"
  key_links:
    - from: "Assets/Scripts/Main/ChatManager.Channel.cs"
      to: "PlayerPrefs {botId}ActiveChatChannel"
      via: "SetActiveChannel persists; resolution restores on bot switch/startup"
      pattern: "ActiveChatChannel"
    - from: "Assets/Scripts/Main/ChatManager.BotState.cs"
      to: "ActiveChannel"
      via: "GetActiveProfileId switch + GetCacheRoot channel subdir"
      pattern: "ActiveChannel"
---

<objective>
Give ChatManager a channel dimension. Add `ActiveChannel` (persisted per bot), a `SetActiveChannel` that reuses `SetActiveBot`'s full reset/guard-clear choreography, the `OnActiveChannelChanged` event (Phase 6 consumes it), channel-aware `GetActiveProfileId` and `GetCacheRoot`, channel resolution on bot switch/startup (auto-select the connected channel), and generalize the empty-state/sync-window so a Telegram-only bot no longer dead-ends on "WhatsApp not connected".

This is the identity/state seam. It does NOT yet swap the 11 URL literals or add parser divergences (that is 05-03) — but because `ActiveChannel` defaults to WhatsApp and WhatsApp keeps its legacy cache root, the build stays green and WhatsApp behavior is byte-identical.

Purpose: unblock the switcher UI (Phase 6) and give 05-03/05-04 a channel to read.
Output: a new `ChatManager.Channel.cs` partial, edits to BotState.cs + ChatManager.cs + EmptyStateView.cs, and channel-resolution / cache-root tests.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-channel-aware-chatmanager-core/05-CONTEXT.md
@.planning/phases/05-channel-aware-chatmanager-core/05-01-SUMMARY.md

# MANDATORY skill reads before matching work:
# - .claude/skills/chat-data-flow/SKILL.md (ChatManager events + pipeline — REQUIRED)
# - .claude/skills/bot-persistence/SKILL.md ({botId}ActiveChatChannel must follow key conventions)

@.planning/research/telegram-parity/auth-bot-entity.md
@.planning/research/telegram-parity/chatmanager-pipeline.md

<interfaces>
<!-- From 05-01 (already committed): -->
public enum ChatChannel { WhatsApp = 0, Telegram = 1 }
// WappiEndpoints.Sync(ChatChannel, path); ChatIdFormat.{Recipient,DisplayFallback,IsGroup}

<!-- Existing ChatManager.BotState.cs surface you are extending (exact current bodies): -->
public string CurrentBotId { get; private set; } = "_default";
public string GetCacheRoot()            // -> {persistentDataPath}/BotCache/{SanitizeBotId(CurrentBotId)}/
public void SetActiveBot(string botId)  // stops _syncWaitRoutine, _outbox=null, persists LastSelectedBotForChats,
                                        // Chats.Clear + chatLookup.Clear + OnChatListCleared + OnActiveBotChanged,
                                        // StopAllCoroutines, _chatFetchesInFlight=0, _chatListSyncing=false,
                                        // ClearVideoThumbQueue, ClearMediaDownloadQueue, BeginLoadForActiveBot
private static bool IsValidProfileId(string profileId)  // !null/empty && != Bot.UnauthedProfileSentinel ("-1")
private string GetActiveProfileId()     // CURRENTLY returns only bot.whatsappProfileId
public bool IsWhatsAppSyncing(string botId, out long syncUntilUnixMs)   // key suffix "WhatsappSyncUntil"
public EmptyStateReason? ComputeCurrentEmptyState()
private void BeginLoadForActiveBot()    // aborts with OnEmptyState(BotHasNoWhatsApp) when whatsappProfileId invalid
public void RefreshActiveBotChats()
private const string LastSelectedBotPrefKey = "LastSelectedBotForChats";

<!-- Bot entity fields (Assets/Scripts/Main/Bot.cs — already present, plain public): -->
public string whatsappProfileId;   public string telegramProfileId;   // "-1" == unauthed sentinel

<!-- ChatManager.cs (existing): -->
public event Action<string> OnActiveBotChanged;
public event Action<EmptyStateReason> OnEmptyState;
public enum EmptyStateReason { NoBotsExist, BotHasNoWhatsApp }   // ADD BotHasNoTelegram
IEnumerator SyncAllChats(...)   // line 388 fires OnEmptyState(BotHasNoWhatsApp) on null profile

<!-- EmptyStateView.cs (existing) — English WhatsApp copy to MIRROR for Telegram: -->
//   BotHasNoWhatsApp: title "WhatsApp not connected"
//     body "Connect WhatsApp to this bot to see its chats."  button "Connect WhatsApp"
//     button listener -> OpenCurrentBotAuth (switches to Bots tab, invokes bot.EditButton -> BotSettings)
</interfaces>

DO-NOT-TOUCH NOTE (leave for the seam summary so nobody "fixes" it early):
  N8nSuggestionsProvider.cs reads bot.whatsappProfileId directly and BYPASSES GetActiveProfileId.
  It is Phase 7 scope — do NOT route it through the channel seam in this phase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatManager.Channel.cs — ActiveChannel, SetActiveChannel, OnActiveChannelChanged, resolution</name>
  <files>Assets/Scripts/Main/ChatManager.Channel.cs</files>
  <read_first>.claude/skills/chat-data-flow/SKILL.md</read_first>
  <action>
    Create a new partial `Assets/Scripts/Main/ChatManager.Channel.cs` (`public partial class ChatManager`) housing the channel identity (prefer a new partial over bloating BotState.cs — Claude's discretion per D4):

    1. `public ChatChannel ActiveChannel { get; private set; } = ChatChannel.WhatsApp;`
    2. Persistence: `private const string ActiveChannelKeySuffix = "ActiveChatChannel";` and helpers to read/write `PlayerPrefs.GetInt(CurrentBotId + ActiveChannelKeySuffix, 0)` — mirror SetActiveBot's `PlayerPrefs.SetString(...); PlayerPrefs.Save();` style. Defensive read: clamp any value that is not a defined ChatChannel (not 0 or 1) to WhatsApp (0) — PlayerPrefs are user-editable on disk (SanitizeBotId precedent).
    3. `public event Action<ChatChannel> OnActiveChannelChanged;`
    4. `public void SetActiveChannel(ChatChannel channel)`: early-return if `channel == ActiveChannel`. If a chat is currently open (currentChatId non-empty), route through `ShowChatList()` first (so the message view doesn't strand). Then mirror SetActiveBot's body EXACTLY (D4): stop `_syncWaitRoutine`; set ActiveChannel + persist `{botId}ActiveChatChannel`; `_outbox = null`; `Chats.Clear(); chatLookup.Clear(); OnChatListCleared?.Invoke();` fire `OnActiveChannelChanged?.Invoke(channel);` `StopAllCoroutines();` `_chatFetchesInFlight = 0; _chatListSyncing = false; ClearVideoThumbQueue(); ClearMediaDownloadQueue();` then `BeginLoadForActiveBot();`
    5. `public ChatChannel ResolveChannelForBot(string botId)` (pure-ish helper used by bot-switch/startup, Task 2 calls it): read the persisted channel for botId; find the bot; if the persisted channel is unconnected (`!IsValidProfileId(profile for that channel)`) but the OTHER channel IS connected, return the connected one AND persist the correction (`PlayerPrefs.SetInt(botId + ActiveChannelKeySuffix, (int)corrected); PlayerPrefs.Save();`); default WhatsApp when the key is missing. Add a small helper `ProfileIdForChannel(Bot bot, ChatChannel ch)` switching over whatsappProfileId/telegramProfileId.

    Do NOT change any URL literal or parser here.
  </action>
  <verify>
    <automated>Tools/run-tests-headless.sh</automated>
  </verify>
  <done>ChatManager.Channel.cs compiles into the suite; ActiveChannel defaults WhatsApp; SetActiveChannel reproduces the SetActiveBot reset set; OnActiveChannelChanged fires. Suite green.</done>
  <acceptance_criteria>
    - `grep -q "ChatChannel ActiveChannel" Assets/Scripts/Main/ChatManager.Channel.cs`
    - `grep -q "ActiveChatChannel" Assets/Scripts/Main/ChatManager.Channel.cs`
    - `grep -q "OnActiveChannelChanged" Assets/Scripts/Main/ChatManager.Channel.cs`
    - `grep -q "public void SetActiveChannel" Assets/Scripts/Main/ChatManager.Channel.cs`
    - `grep -q "ClearMediaDownloadQueue" Assets/Scripts/Main/ChatManager.Channel.cs`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Channel-aware GetActiveProfileId / GetCacheRoot / empty-state / sync-gate + channel restore on switch</name>
  <files>Assets/Scripts/Main/ChatManager.BotState.cs, Assets/Scripts/Main/ChatManager.cs</files>
  <read_first>.claude/skills/bot-persistence/SKILL.md</read_first>
  <action>
    In `ChatManager.cs`: add `BotHasNoTelegram` to `enum EmptyStateReason` (append after BotHasNoWhatsApp). In `SyncAllChats` (line ~388) change the null-profile empty-state fire to be channel-aware: fire `BotHasNoWhatsApp` when `ActiveChannel==WhatsApp` else `BotHasNoTelegram`.

    In `ChatManager.BotState.cs`:
    - `GetActiveProfileId()`: switch on `ActiveChannel` over `bot.whatsappProfileId` / `bot.telegramProfileId`, keeping the same `IsValidProfileId` guard (zero call-site churn per D4).
    - `GetCacheRoot()`: WhatsApp keeps `BotCache/{botId}/` (legacy, byte-identical — no migration). Telegram appends a constant `telegram` subdir: `BotCache/{botId}/telegram/`. The channel segment is a hardcoded constant (not user input); botId stays sanitized by SanitizeBotId. `PurgeCacheForBot` already deletes `BotCache/{botId}` recursively so both channels are covered — do NOT restructure it (a test in Task 3 asserts this).
    - `BeginLoadForActiveBot()`: the no-profile abort fires `BotHasNoWhatsApp` when `ActiveChannel==WhatsApp` else `BotHasNoTelegram`. Gate the sync window on channel: `IsWhatsAppSyncing` applies ONLY when `ActiveChannel==WhatsApp` (Telegram skips the sync-gate entirely per D4 — no Telegram window is written at auth). Same channel-aware profile check in `RefreshActiveBotChats`.
    - `ComputeCurrentEmptyState()`: compute `hasChannel = IsValidProfileId(GetActiveProfileId())`, `syncing = ActiveChannel==WhatsApp && hasChannel && IsWhatsAppSyncing(...)`; map the resolver's no-connection state to `BotHasNoWhatsApp` (WhatsApp) or `BotHasNoTelegram` (Telegram).
    - Channel restore: when `SetActiveBot(botId)` runs and at startup resolution (`ResolveInitialActiveBot` / `InitializeActiveBotNextFrame`), set `ActiveChannel` from `ResolveChannelForBot(botId)` (Task 1) BEFORE `BeginLoadForActiveBot` so the loaded channel matches the persisted/auto-corrected choice. Keep the read-path sync-window key `WhatsappSyncUntil` unchanged for WhatsApp.
  </action>
  <verify>
    <automated>Tools/run-tests-headless.sh</automated>
  </verify>
  <done>GetActiveProfileId + GetCacheRoot + empty-state + sync-gate are channel-aware; WhatsApp path byte-identical (legacy root, BotHasNoWhatsApp, sync window intact); bot switch/startup restores the channel. Suite green.</done>
  <acceptance_criteria>
    - `grep -q "BotHasNoTelegram" Assets/Scripts/Main/ChatManager.cs`
    - `grep -q "telegramProfileId" Assets/Scripts/Main/ChatManager.BotState.cs`
    - `grep -q "telegram" Assets/Scripts/Main/ChatManager.BotState.cs`
    - `grep -q "ActiveChannel" Assets/Scripts/Main/ChatManager.BotState.cs`
    - `grep -q "ResolveChannelForBot" Assets/Scripts/Main/ChatManager.BotState.cs`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: EmptyStateView Telegram copy + channel-resolution/cache-root tests; full suite green</name>
  <files>Assets/Scripts/UI/EmptyStateView.cs, Assets/Tests/Editor/Chat/ChannelResolutionTests.cs, Assets/Tests/Editor/Chat/ChannelCacheRootTests.cs</files>
  <read_first>.claude/skills/unity-ui-builder/SKILL.md</read_first>
  <action>
    In `EmptyStateView.ConfigureForReason`, add a `case EmptyStateReason.BotHasNoTelegram:` that MIRRORS the existing WhatsApp copy in English (D4): title `"Telegram not connected"`, body `"Connect Telegram to this bot to see its chats."`, button `"Connect Telegram"`, wired to the SAME `OpenCurrentBotAuth` listener (it opens BotSettings — no channel-specific routing needed this phase). No new scene objects; reuse the existing serialized labels/button. This is code-only copy.

    Add EditMode tests:
    - `ChannelCacheRootTests.cs`: assert the cache-root path convention (WhatsApp segment vs the `telegram` subdir) using a pure/derivable seam. If GetCacheRoot is not directly unit-testable (it depends on persistentDataPath + CurrentBotId), extract the channel→subdir mapping into a tiny pure helper (e.g. `ChannelCachePath.SubDir(ChatChannel)` returning "" for WhatsApp and "telegram" for Telegram) and test THAT, then have GetCacheRoot call it — follow the WhatsAppSyncGate pure-seam precedent.
    - `ChannelResolutionTests.cs`: cover ResolveChannelForBot decision logic via a pure core if PlayerPrefs/Bot lookups make it awkward — e.g. extract `ChannelResolver.Resolve(int persistedChannel, bool waConnected, bool tgConnected)` returning the effective ChatChannel (persisted if connected; else the other connected; else WhatsApp) and test the matrix (persisted-TG-but-only-WA-connected → WhatsApp; persisted-WA-but-only-TG-connected → Telegram; both connected → persisted; neither → persisted/default). Wire ResolveChannelForBot to call it.

    Run the FULL suite headless; require green. Commit new .cs + .meta together.
  </action>
  <verify>
    <automated>Tools/run-tests-headless.sh</automated>
  </verify>
  <done>Telegram empty-state copy present; cache-root + channel-resolution matrices unit-tested; full suite green.</done>
  <acceptance_criteria>
    - `grep -q "Telegram not connected" Assets/Scripts/UI/EmptyStateView.cs`
    - `grep -q "Connect Telegram" Assets/Scripts/UI/EmptyStateView.cs`
    - `test -f Assets/Tests/Editor/Chat/ChannelResolutionTests.cs && test -f Assets/Tests/Editor/Chat/ChannelCacheRootTests.cs`
    - `Tools/run-tests-headless.sh` reports 0 failures
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PlayerPrefs (on-disk, user-editable) → ChatManager | `{botId}ActiveChatChannel` int and bot ids drive channel selection + cache path |
| ChatManager → filesystem | GetCacheRoot derives the per-channel cache directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0502-01 | Tampering | {botId}ActiveChatChannel PlayerPrefs value | mitigate | Read defensively; clamp any value that is not a defined ChatChannel (0/1) to WhatsApp. No out-of-range enum cast reaches the switch. |
| T-0502-02 | Information Disclosure | GetCacheRoot cross-channel leakage | mitigate | Per-channel root isolates chats.json/messages/media; SetActiveChannel full reset clears in-memory Chats/chatLookup + guards so a stale channel's data never renders after a switch. |
| T-0502-03 | Tampering (path traversal) | GetCacheRoot telegram subdir | accept | Channel segment is a hardcoded constant ("telegram"); botId already passes SanitizeBotId. No user-controlled path component in the channel segment. |
</threat_model>

<verification>
- Full EditMode suite green (Editor closed) via `Tools/run-tests-headless.sh`.
- WhatsApp regression: `grep "BotCache" Assets/Scripts/Main/ChatManager.BotState.cs` shows the WhatsApp root unchanged (no `telegram` for the WhatsApp branch); WhatsApp empty-state + sync window untouched.
- `ActiveChannel` defaults WhatsApp; existing WhatsApp-only bots behave exactly as before (no channel key written until a switch occurs).
</verification>

<success_criteria>
- ChatManager has ActiveChannel (persisted), SetActiveChannel (full reset choreography), OnActiveChannelChanged.
- GetActiveProfileId / GetCacheRoot / empty-state / sync-gate are channel-aware; Telegram-only bots surface BotHasNoTelegram instead of dead-ending on WhatsApp copy.
- Bot switch/startup restores the persisted channel and auto-selects the connected one.
- Telegram + WhatsApp caches isolated (CHAT-11); WhatsApp byte-identical; suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-channel-aware-chatmanager-core/05-02-SUMMARY.md` (note the N8nSuggestionsProvider do-not-touch carry-forward for Phase 7).
</output>
