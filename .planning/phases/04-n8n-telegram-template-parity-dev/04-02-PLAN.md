---
phase: 04-n8n-telegram-template-parity-dev
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Main/Manager.cs
  - .planning/phases/04-n8n-telegram-template-parity-dev/04-HUMAN-UAT.md
autonomous: true
requirements: [TPL-05, TPL-06]

must_haves:
  truths:
    - "CreateTelegramWorkflow create calls (FromStart + FromEdit) send a WhatsappWorkflowId form field carrying the bot's WhatsApp workflow id (or '-1' when absent)"
    - "CreateWhatsappWorkflow create calls (FromStart + FromEdit) send a TelegramWorkflowId form field carrying the bot's Telegram workflow id (or '-1' when absent)"
    - "The new form fields follow the existing WWWForm.AddField pattern (read server-side from $json.body like the other Create fields)"
    - "04-HUMAN-UAT.md documents the locked owner deploy + live e2e steps for TPL-06"
  artifacts:
    - path: "Assets/Scripts/Main/Manager.cs"
      provides: "opposite-channel workflow id in all 4 create-workflow form builders"
      contains: "WhatsappWorkflowId"
    - path: ".planning/phases/04-n8n-telegram-template-parity-dev/04-HUMAN-UAT.md"
      provides: "owner-driven dev deploy + e2e gate (TPL-06)"
      min_lines: 40
  key_links:
    - from: "CreateTelegramWorkflowFromStart / FromEdit"
      to: "CreateTelegramWorkflow webhook"
      via: "form.AddField(\"WhatsappWorkflowId\", ...)"
      pattern: "AddField\\(\"WhatsappWorkflowId\""
    - from: "CreateWhatsappWorkflowFromStart / FromEdit"
      to: "CreateWhatsappWorkflow webhook"
      via: "form.AddField(\"TelegramWorkflowId\", ...)"
      pattern: "AddField\\(\"TelegramWorkflowId\""
---

<objective>
Client half of TPL-05: when Unity creates a channel's n8n workflow, pass the OPPOSITE channel's workflow id in the create form so the server-side re-stamp (04-01) can convert that bot's pre-auth RAG chunks (metadata sentinel "-1") to the newly created workflow id. Plus write the owner-driven dev deploy + live e2e gate (TPL-06) — the phase's human checkpoint, mirroring Phase 3's 03-HUMAN-UAT pattern.

Purpose: A file uploaded before a channel was authed becomes RAG-retrievable once that channel's workflow is created — end to end, once the owner runs the dev proof.
Output: Manager.cs form-field additions + 04-HUMAN-UAT.md.

NOT in this plan: any n8n workflow JSON edit (04-01 owns those), any ChatManager/chat-pipeline change (Phase 5), the client suggestions payload channel field (Phase 7).
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-n8n-telegram-template-parity-dev/04-CONTEXT.md

# Read before editing:
@.claude/skills/unity-api-integration/SKILL.md
@.claude/rules/networking.md

<interfaces>
The four create-workflow coroutines in Assets/Scripts/Main/Manager.cs, with the exact insertion points (add the new field alongside the existing `*ProfileId` AddField, following the same WWWForm pattern):

CreateWhatsappWorkflowFromStart (~line 2725) builds `form` and does:
```csharp
form.AddField("WhatsappProfileId", whatsappProfileId);   // ~2734
```
CreateWhatsappWorkflowFromEdit (~line 2777):
```csharp
form.AddField("WhatsappProfileId", openBot.GetComponent<Bot>().whatsappProfileId);   // ~2824
```
CreateTelegramWorkflowFromStart (~line 2864):
```csharp
form.AddField("TelegramProfileId", telegramProfileId);   // ~2873
```
CreateTelegramWorkflowFromEdit (~line 2919):
```csharp
form.AddField("TelegramProfileId", openBot.GetComponent<Bot>().telegramProfileId);   // ~2966
```

Sentinel constant: `Bot.UnauthedProfileSentinel` == "-1". A fresh Bot's `whatsappWorkflowId`/`telegramWorkflowId` may be empty at FromStart time (in the wizard both create coroutines are started in the same frame at lines ~1391/1402, so the opposite channel's id is often not yet resolved) — this is expected and safe: the server WHERE clause matches nothing on ""/"-1". Coerce empty/null → "-1" so the field is always a clean scalar.

Server side (04-01) reads these fields as `$('Unity Webhook').first().json.body.WhatsappWorkflowId` / `.TelegramWorkflowId` — the SAME body-field pattern the existing Name/BusinessTypeId fields use, so no transport change is needed (do NOT "fix" WWWForm transport).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add opposite-channel workflow id to all 4 create-workflow forms (TPL-05 client)</name>
  <read_first>
    - Assets/Scripts/Main/Manager.cs lines 2725-3003 (the four create-workflow coroutines)
    - Assets/Scripts/Main/Manager.cs line ~139-140 (whatsappProfileId/telegramProfileId fields) and Bot.UnauthedProfileSentinel usage
  </read_first>
  <files>Assets/Scripts/Main/Manager.cs</files>
  <action>
    Add ONE `form.AddField(...)` line in each of the four create-workflow coroutines, placed immediately after the existing `*ProfileId` AddField (so field order stays grouped). Coerce empty/null to the "-1" sentinel with a helper-free inline guard (keep it consistent across all four).

    (a) CreateTelegramWorkflowFromStart (bot param `bot`): after `form.AddField("TelegramProfileId", telegramProfileId);` add:
        ```csharp
        string waId = bot.GetComponent<Bot>().whatsappWorkflowId;
        form.AddField("WhatsappWorkflowId", string.IsNullOrEmpty(waId) ? Bot.UnauthedProfileSentinel : waId);
        ```

    (b) CreateTelegramWorkflowFromEdit (uses `openBot`): after `form.AddField("TelegramProfileId", openBot.GetComponent<Bot>().telegramProfileId);` add:
        ```csharp
        string waId = openBot.GetComponent<Bot>().whatsappWorkflowId;
        form.AddField("WhatsappWorkflowId", string.IsNullOrEmpty(waId) ? Bot.UnauthedProfileSentinel : waId);
        ```

    (c) CreateWhatsappWorkflowFromStart (bot param `bot`): after `form.AddField("WhatsappProfileId", whatsappProfileId);` add:
        ```csharp
        string tgId = bot.GetComponent<Bot>().telegramWorkflowId;
        form.AddField("TelegramWorkflowId", string.IsNullOrEmpty(tgId) ? Bot.UnauthedProfileSentinel : tgId);
        ```

    (d) CreateWhatsappWorkflowFromEdit (uses `openBot`): after `form.AddField("WhatsappProfileId", openBot.GetComponent<Bot>().whatsappProfileId);` add:
        ```csharp
        string tgId = openBot.GetComponent<Bot>().telegramWorkflowId;
        form.AddField("TelegramWorkflowId", string.IsNullOrEmpty(tgId) ? Bot.UnauthedProfileSentinel : tgId);
        ```

    Use distinct local variable names per method if the compiler flags scope collisions; keep the AddField key strings EXACTLY `"WhatsappWorkflowId"` and `"TelegramWorkflowId"` (they must match the server's `body.<FieldName>` reads in 04-01). Do not alter any other form fields, the request URL, or the response handling.
  </action>
  <verify>
    <automated>cd /Users/ayan/Projects/Automation && grep -c 'AddField("WhatsappWorkflowId"' Assets/Scripts/Main/Manager.cs | grep -qx 2 && grep -c 'AddField("TelegramWorkflowId"' Assets/Scripts/Main/Manager.cs | grep -qx 2 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AddField("WhatsappWorkflowId"' Manager.cs` == 2 (both Telegram create methods)
    - `grep -c 'AddField("TelegramWorkflowId"' Manager.cs` == 2 (both WhatsApp create methods)
    - Each new field value uses `Bot.UnauthedProfileSentinel` as the empty/null fallback
    - The validate-cs.sh Edit/Write hook passes (no compile-shape errors introduced); balanced braces, no other form field or URL changed
    - Do NOT run the Unity test suite (Editor state unknown) — the phase verifier records that a device/Editor compile pass is owner-confirmed
  </acceptance_criteria>
  <done>All four create-workflow forms carry the opposite channel's workflow id (sentinel-guarded), so the server re-stamp can key on it.</done>
</task>

<task type="auto">
  <name>Task 2: Write 04-HUMAN-UAT.md (TPL-06 owner deploy + e2e gate)</name>
  <read_first>
    - 04-CONTEXT.md > Deploy + e2e (TPL-06) — OWNER gate (the locked step list)
    - .planning/phases/03-*/03-HUMAN-UAT.md if present (mirror its structure/tone)
    - Tools/n8n/README.md (import by literal id, keep templates inactive, shared webhook path) + Tools/n8n/rotate-tunnel.py role
  </read_first>
  <files>.planning/phases/04-n8n-telegram-template-parity-dev/04-HUMAN-UAT.md</files>
  <action>
    Write the owner-driven human-gate doc for TPL-06 (the whole flow proven e2e on dev; dev n8n is not running and its API key is in deny-ruled secrets.json, so this is owner-executed, not agent-executed). Structure it as a runnable checklist with these locked steps (from CONTEXT), each with an expected result:

    1. Pre-flight: run `python3 Tools/n8n/verify-telegram-parity.py` (from 04-01) — must exit 0 before deploying. ALSO confirm on dev n8n that the executeQuery Postgres credential (id `vvRrFiEXzLVqKjOx`, name "Postgres") resolves and its role can UPDATE `documents` (quick check: execute the Restamp RAG Chunks node once in the n8n editor with harmless parameters, e.g. $2 = "-1" — a no-match UPDATE returning 0 rows without a credential/permission error passes) BEFORE running the e2e.
    2. Start dev n8n (localhost:5678) and a cloudflared tunnel; run `python3 Tools/n8n/rotate-tunnel.py` to re-point secrets.json + Wappi callbacks (cite the 2026-07-03 outage lesson — a missed rotate step stops replies).
    3. Import/update the 4 edited workflows by LITERAL id (Telegram_Bot 4VN3gsFaC2HUYmcc, CreateTelegramWorkflow Uz6HBBUpAiUqVysB, CreateWhatsappWorkflow XuvOp7TxOImOAmlj, Suggest_Replies 9PTyYcelRQI7bGDb); keep the two bot templates INACTIVE (they share webhook path 0091024b-7b46; only per-bot clones go active). Recreate any pre-existing dev Telegram clone (old clones carry wrong api/sync URLs).
    4. Authorize a dev Telegram profile (the same one Phase 3 needs) and create a Telegram bot from the app.
    5. Run the conversation e2e and record PASS/FAIL for each:
       - text: send a text to the Telegram-authed bot → an AI reply arrives IN Telegram (proves tapi outbound + type:"text" routing).
       - voice: send a voice message → transcription + a humanized pause (length_seconds fallback), reply arrives.
       - memory: a multi-turn exchange stays coherent (sessionKey profile_id:chatId — no fragmentation).
       - pre-auth file re-stamp: upload a price-list file to a bot BEFORE authing Telegram, then auth Telegram / create the TG workflow, then ask a price question in Telegram → the answer is grounded in that file (chunks re-stamped from botTgId "-1" to the new TG workflow id). Include a Supabase spot-check hint: `select count(*) from documents where metadata->>'botTgId' = '<newTgWorkflowId>';` should be > 0.
    6. DEACTIVATE the clone after the test window (bot workflows stay inactive except during active testing — real contacts). Prod bagkz stays dormant.

    Add a "Result" section for the owner to mark overall PASS/FAIL + notes, and a "Blocks" line noting this gate closes TPL-06 and the phase. Note that agent-side work (04-01 + 04-02 Task 1) is code-complete before this gate runs.
  </action>
  <verify>
    <automated>cd /Users/ayan/Projects/Automation && test -f .planning/phases/04-n8n-telegram-template-parity-dev/04-HUMAN-UAT.md && grep -q "verify-telegram-parity.py" .planning/phases/04-n8n-telegram-template-parity-dev/04-HUMAN-UAT.md && grep -q "rotate-tunnel" .planning/phases/04-n8n-telegram-template-parity-dev/04-HUMAN-UAT.md && grep -q "botTgId" .planning/phases/04-n8n-telegram-template-parity-dev/04-HUMAN-UAT.md && grep -q "vvRrFiEXzLVqKjOx" .planning/phases/04-n8n-telegram-template-parity-dev/04-HUMAN-UAT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/04-n8n-telegram-template-parity-dev/04-HUMAN-UAT.md`
    - Contains the 6 locked steps (verifier pre-flight incl. the Postgres credential `vvRrFiEXzLVqKjOx` resolve/UPDATE-permission check, n8n+tunnel start, import-by-literal-id + keep templates inactive, authorize TG profile + create bot, the 4 e2e checks incl. pre-auth file re-stamp with the Supabase spot-check, deactivate clone)
    - References the 4 workflow ids, `rotate-tunnel.py`, `verify-telegram-parity.py`, and credential id `vvRrFiEXzLVqKjOx`
    - Has an owner PASS/FAIL result section and states it closes TPL-06 / the phase
  </acceptance_criteria>
  <done>The owner has a complete, locked runbook to prove the Telegram template parity + re-stamp end to end on dev and close TPL-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unity client → n8n Create webhook | The new WhatsappWorkflowId / TelegramWorkflowId form fields are client-emitted and flow into the server-side re-stamp UPDATE (04-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering (SQLi vector) | New form fields feeding the server UPDATE | mitigate | Client sends the bot's OWN opposite-channel workflow id (not free-text user input), sentinel-guarded to "-1" when absent. The actual injection defense is server-side parameterization (T-04-01 in 04-01: queryReplacement `$1`/`$2`). The client change carries inert scalar data only — it never builds SQL. |
| T-04-06 | Information Disclosure | Response handling (unchanged) | accept | The create-response parsing (`response.Contains("\"id\":")` + ExtractWorkflowId) is untouched; no new data is logged or surfaced. Server keeps Postgres errors out of the body (T-04-02). |
</threat_model>

<verification>
- `grep -c 'AddField("WhatsappWorkflowId"' Manager.cs` == 2 and `grep -c 'AddField("TelegramWorkflowId"' Manager.cs` == 2.
- Each new field value falls back to `Bot.UnauthedProfileSentinel`.
- validate-cs.sh Edit/Write hook passes; no other form field / URL / response logic changed.
- 04-HUMAN-UAT.md exists with the 6 locked owner steps (TPL-06), including the Postgres credential (`vvRrFiEXzLVqKjOx`) resolve/UPDATE-permission pre-flight.
- C# suite is NOT run here (Editor state unknown) — owner confirms an Editor/device compile pass as part of the gate.
</verification>

<success_criteria>
- All four create-workflow forms send the opposite channel's workflow id, sentinel-guarded (TPL-05 client half) — enabling the 04-01 server re-stamp.
- 04-HUMAN-UAT.md gives the owner a complete, locked dev deploy + live e2e runbook that closes TPL-06 and the phase.
</success_criteria>

<output>
After completion, create `.planning/phases/04-n8n-telegram-template-parity-dev/04-02-SUMMARY.md`.
</output>
