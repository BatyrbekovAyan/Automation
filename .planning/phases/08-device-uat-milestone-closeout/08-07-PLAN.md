---
phase: 08-device-uat-milestone-closeout
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/UI/MessageItemView.cs
  - Assets/Scripts/Chat/TelegramVideoNoteHeuristic.cs
  - Assets/Tests/Editor/Chat/TelegramVideoNoteHeuristicTests.cs
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "An INCOMING Telegram video note (кружок) floats bubble-free (no white/coloured bubble behind the circle), matching the already-verified outgoing behavior"
    - "The video-note duration badge renders with fully rounded corners on device (no sharp left/right edges)"
    - "WhatsApp media bubbles are byte-identical (isVideoNote is Telegram-only, default false)"
  artifacts:
    - path: "Assets/Scripts/UI/MessageItemView.cs"
      provides: "Incoming video-note transparency + duration-badge rounded-corner refresh"
      contains: "BubbleTransparencyPolicy"
  key_links:
    - from: "incoming кружок Normalize → vm.isVideoNote"
      to: "BubbleTransparencyPolicy.IsTransparent(...) at bubble apply"
      via: "transparent bubble on the incoming path"
      pattern: "isVideoNote"
    - from: "ToggleDurationBadgeOverlay pill"
      to: "ImageWithRoundedCorners refreshed after layout"
      via: "corner radius applied post-size"
      pattern: "ImageWithRoundedCorners"
---

<objective>
Close **D3** (medium) — Telegram video-note (кружок) presentation, two symptoms, diagnosis-first:

- **D3a (badge corners):** the duration badge's left/right corners render SHARP on device (owner: "left and right sides of badge look sharp, i think round corners needs refresh or something"). The circle itself floats correctly; only the badge pill's rounded corners fail. This is the known `Nobi.UiRoundedCorners` gotcha — the `ImageWithRoundedCorners` shader reads rect dimensions inside `Refresh()`, and the badge pill's width is content-sized (settles a frame after creation), so the radius is computed against a stale/zero rect.
- **D3b (incoming white bubble):** in the owner's NEW Telegram bot, a video note renders as a round video ON a white background bubble instead of floating bubble-free (owner: "round video with white background bubble"). 05-08's `BubbleTransparencyPolicy` was verified on OUTGOING notes; the INCOMING path either doesn't set `isVideoNote` (heuristic miss on that note's shape) or `isPlaceholderActive` is true so the bubble stays visible.

Purpose: an incoming кружок floats chrome-free (parity with outgoing), and its duration badge is fully rounded on device.

Constraint: WhatsApp media byte-identical (`isVideoNote` is Telegram-only, default false). Stays out of `ChatManager.cs` — the heuristic fix (if any) lands in the `TelegramVideoNoteHeuristic` pure seam that `Normalize` already calls; the transparency/badge fixes land in `MessageItemView.cs`. If diagnosis shows the fix genuinely requires a `ChatManager.cs` edit, flag it in the SUMMARY (D5/D7 own `ChatManager.cs` in adjacent waves).
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.claude/skills/chat-data-flow/SKILL.md
@.claude/skills/unity-ui-builder/SKILL.md

<interfaces>
<!-- Extracted from the codebase. -->

// BubbleTransparencyPolicy.cs (pure) — 05-08 seam
public static bool IsTransparent(bool isSticker, bool isVideoNote, bool isPlaceholderActive, bool hideBubble)
//   = ((isSticker || isVideoNote) && !isPlaceholderActive) || hideBubble

// MessageItemView.cs — where it's applied (≈3856–3874):
//   bool isPlaceholderActive = isDownloadActive || isExpiredActive;
//   bool isTransparent = BubbleTransparencyPolicy.IsTransparent(currentVm.isSticker, currentVm.isVideoNote, isPlaceholderActive, hideBubble);
// MessageItemView.cs — Telegram overlays (≈1941–2087):
//   ToggleDurationBadgeOverlay(container, isNote, isNote ? vm.duration : 0)   // "NoteDurationBadge"
//   GetOrCreateOverlayPill(...) → root.AddComponent<ImageWithRoundedCorners>()  // the badge pill
// MessageItemView.cs — an existing post-layout RoundedCorners refresh idiom lives at ≈2856 / ≈2892
//   (comment: "The ImageWithRoundedCorners shader reads rect dimensions inside Refresh(); if …")

// TelegramVideoNoteHeuristic.cs (pure) — sets vm.isVideoNote in Normalize (ChatManager.cs ≈1695)
public static bool IsVideoNote(string baseType, string fileName, JToken mediaInfo)
//   square + video.mp4 + <=60s; is_round DELIBERATELY ignored (unreliable)

// MessageMediaMerge.cs — carries isVideoNote through a media refresh (≈118–122)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (D3b): Diagnose + fix the incoming video-note white bubble</name>
  <read_first>
    - Assets/Scripts/Chat/TelegramVideoNoteHeuristic.cs — full (the square + video.mp4 + ≤60s rule; what inputs it needs from `mediaInfo`).
    - Assets/Scripts/Main/ChatManager.cs — the Normalize call at ≈1693–1695 (`msg.isVideoNote = TelegramVideoNoteHeuristic.IsVideoNote(raw.type, raw.fileName, raw.mediaInfo);`) and how incoming vs outgoing `raw` shapes differ (dimensions/mime/fileName presence on the incoming path).
    - Assets/Scripts/UI/MessageItemView.cs — the transparency apply site (≈3856–3874) + `isDownloadActive`/`isExpiredActive` (what makes `isPlaceholderActive` true for an incoming note).
    - Assets/Scripts/Chat/BubbleTransparencyPolicy.cs — the pure decision (+ BubbleTransparencyPolicyTests.cs for the pattern).
    - Assets/Scripts/Chat/MessageMediaMerge.cs — isVideoNote carry-through on refresh.
    - .planning/STATE.md 05-08 note (video note floats BUBBLE-FREE via BubbleTransparencyPolicy + isVideoNote — verified OUTGOING).
    - 08-DEVICE-UAT.md — Defects row D3 + Group E1 owner note.
  </read_first>
  <action>
    DIAGNOSE FIRST (record in SUMMARY, 2–4 sentences): determine which is true for the incoming кружок —
      (i) `vm.isVideoNote == false` because `TelegramVideoNoteHeuristic.IsVideoNote` receives incoming-shape inputs it doesn't match (e.g. dimensions absent/non-square in the incoming `media_info`, or `fileName`/mime differs from the outgoing "video.mp4"); OR
      (ii) `vm.isVideoNote == true` but `isPlaceholderActive` is true (a download/expired card is showing), so the bubble is intentionally visible; OR
      (iii) some other cause — document it.
    Use the captured tapi samples (Tools/tapi/samples/) or the incoming-note fields to confirm. If the hypothesis is wrong, follow the evidence.

    FIX per finding:
      - If (i): make `TelegramVideoNoteHeuristic.IsVideoNote` recognize the incoming note shape WITHOUT weakening the outgoing case and WITHOUT reintroducing `is_round` (still unreliable). Keep it a pure, null-tolerant change in the seam; extend `TelegramVideoNoteHeuristicTests.cs` with the incoming shape → true and a WhatsApp/plain-video shape → false (regression net).
      - If (ii): the incoming note should still float once its media is present. Confirm the transparency correctly flips to transparent when `isPlaceholderActive` becomes false (media downloaded) and that the incoming download path actually clears the placeholder — fix whichever step leaves the bubble opaque for a settled (non-placeholder) incoming note.
    Keep the change Telegram-only (guard on `isVideoNote`, default false) so WhatsApp bubbles are byte-identical.
  </action>
  <acceptance_criteria>
    - SUMMARY has a "Root cause (D3b)" line naming (i)/(ii)/(iii).
    - If the heuristic changed: `grep -c "\[Test\]" Assets/Tests/Editor/Chat/TelegramVideoNoteHeuristicTests.cs` INCREASES vs before, and a new test covers the incoming-note shape → true plus a plain-video/WhatsApp shape → false.
    - `grep -q "is_round" Assets/Scripts/Chat/TelegramVideoNoteHeuristic.cs` still shows it is NOT read as a positive signal (comment/ignore preserved).
    - No `ChatManager.cs` in this plan's git diff UNLESS the SUMMARY explicitly flags a required exception: `git diff --name-only | grep -q "ChatManager.cs"` returns nothing (or SUMMARY documents the flagged exception).
    - Full EditMode suite green (≥1036).
  </acceptance_criteria>
  <verify>
    <automated>Tools/run-tests-headless.sh "TelegramVideoNoteHeuristic|BubbleTransparency" then full suite (Editor closed) OR in-Editor bridge — all pass</automated>
  </verify>
  <done>An incoming кружок's transparency is decided the same as an outgoing one; the white bubble is gone for a settled incoming note. Device confirmation rides 08-10.</done>
</task>

<task type="auto">
  <name>Task 2 (D3a): Refresh the duration-badge rounded corners after layout</name>
  <read_first>
    - Assets/Scripts/UI/MessageItemView.cs — `ToggleDurationBadgeOverlay` (≈2039), `GetOrCreateOverlayPill` (≈2081–2090, the `AddComponent<ImageWithRoundedCorners>()` on the pill), AND the existing post-layout refresh idiom at ≈2856 / ≈2892 (the pattern for forcing `ImageWithRoundedCorners.Refresh()` after the rect is sized).
    - Memory gotcha (project): `Nobi.UiRoundedCorners` lives in its own UPM assembly; `Type.GetType(..., Assembly-CSharp)` silently fails → square corners; the shader reads rect dims inside `Refresh()`.
    - .claude/skills/unity-ui-builder/SKILL.md — the "Rounded corners need a script component + refresh" gotcha.
  </read_first>
  <action>
    Make the duration-badge pill's rounded corners apply on device by forcing an `ImageWithRoundedCorners` refresh AFTER the pill's rect has been sized (its width is content-driven, so the first `Refresh()` runs against a stale/zero rect → sharp left/right edges).
    Reuse the existing post-layout refresh idiom already used elsewhere in `MessageItemView` (≈2856/≈2892): after the badge label text + `ContentSizeFitter`/layout settle, call `LayoutRebuilder.ForceRebuildLayoutImmediate(...)` on the pill's RectTransform (or wait one frame via the existing deferred-refresh mechanism this file uses) and then invoke the pill's `ImageWithRoundedCorners.Refresh()` (or toggle `enabled` off/on) so the shader recomputes against the final rect. Apply it wherever the badge is (re)shown in `ToggleDurationBadgeOverlay`. Do NOT change the badge's radius value or its layout — only ensure the refresh runs post-size. Keep it scoped to the note badge overlay (Telegram-only path) so nothing else re-renders.

    If a one-frame deferral is needed, use the same coroutine/next-frame pattern the surrounding media-refresh code already uses (this bubble is pooled — guard against a recycled pill refreshing for a prior message).
  </action>
  <acceptance_criteria>
    - `grep -n "NoteDurationBadge" Assets/Scripts/UI/MessageItemView.cs` shows the badge path now triggers a rounded-corners refresh (a `Refresh()` / `ForceRebuildLayoutImmediate` / `enabled` toggle on the pill after sizing) — verify by reading the diff.
    - `grep -q "ImageWithRoundedCorners" Assets/Scripts/UI/MessageItemView.cs` (badge still uses the corner component).
    - The change is confined to the note-badge overlay (no edit to WhatsApp media bubble sizing) — confirm in the diff.
    - Full EditMode suite green (≥1036) — no test needed (MonoBehaviour rendering glue), but the suite must not regress.
  </acceptance_criteria>
  <verify>
    <automated>Tools/run-tests-headless.sh (Editor closed) OR in-Editor bridge — full suite green, 0 failed</automated>
  </verify>
  <done>The duration badge's corners are refreshed after its rect is sized; sharp edges are gone on device (owner-confirmed at 08-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tapi media `media_info` → heuristic | Untrusted/absent dimensions decide the video-note treatment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Tampering | `TelegramVideoNoteHeuristic.IsVideoNote` on malformed media_info | mitigate | Keep the seam null-tolerant; a non-matching shape returns false (plain video), never throws — unit-tested. |
| T-08-07-02 | Denial of Service | badge layout refresh on a pooled bubble | mitigate | Guard the deferred refresh against a recycled pill (the file's existing pooled-bubble guard pattern) so it never repaints for a prior message. |
| T-08-07-03 | Information Disclosure | a non-note video mis-flagged as a кружок | accept | Purely cosmetic (circular crop + badge); no data exposure. Low risk. |
</threat_model>

<verification>
- EditMode suite green (≥1036) incl. any new `TelegramVideoNoteHeuristicTests`.
- No `ChatManager.cs` edit (or a flagged, documented exception).
- Device re-verify (08-10): E1 incoming note float + B5 badge corners.
</verification>

<success_criteria>
- Incoming кружок floats bubble-free like the outgoing one.
- Duration badge fully rounded on device.
- WhatsApp media byte-identical; full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-07-SUMMARY.md` with the confirmed D3b cause and the badge-refresh mechanism used.
</output>
