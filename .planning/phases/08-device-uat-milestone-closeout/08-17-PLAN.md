---
phase: 08-device-uat-milestone-closeout
plan: 17
type: execute
wave: 8
depends_on: []
files_modified:
  - Assets/Scripts/Main/ChatManager.cs
  - Assets/Scripts/Chat/TelegramReactionMerge.cs
  - Assets/Tests/Editor/Chat/TelegramReactionMergeTests.cs
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "A reaction ADDED or CHANGED in the Telegram app itself, on a message currently loaded in the open chat, reflects in-app within one live-poll cycle (~3s) without re-entering the chat"
    - "A reaction REMOVED in the Telegram app itself clears the in-app pill (no stale pill that never self-heals) for a loaded message"
    - "WhatsApp reaction rendering is byte-identical — the reconcile stays Telegram-gated (ActiveChannel == ChatChannel.Telegram at every call site)"
  artifacts:
    - path: "Assets/Scripts/Main/ChatManager.cs"
      provides: "Server-originated reaction-delta reconcile that covers the loaded window (D2-ext fix, Telegram-gated)"
      contains: "RefreshCachedMessageReactions"
    - path: "Assets/Scripts/Chat/TelegramReactionMerge.cs"
      provides: "Absence-authoritative merge — an empty/absent server reactions[] clears a non-fresh cached entry"
      contains: "Merge"
    - path: "Assets/Tests/Editor/Chat/TelegramReactionMergeTests.cs"
      provides: "Regression tests for the routed D2-ext fix (absence-clears + / or loaded-window coverage seam)"
      contains: "[Test]"
  key_links:
    - from: "OpenChatLivePollRoutine (D5 live poll, ~3s)"
      to: "RefreshCachedMessageReactions → TelegramReactionMerge.Merge → OnMessageReactionsChanged"
      via: "SyncLatestMessages reconcile of already-cached messages in the fetched window"
      pattern: "RefreshCachedMessageReactions"
    - from: "empty/absent server reactions[]"
      to: "cached pill cleared"
      via: "Merge returns a list that drops a non-fresh cached entry"
      pattern: "TelegramReactionMerge.Merge"
---

<objective>
Close **D2-ext (medium, round 3)**: reaction changes/removals performed IN the Telegram app itself (owner on another device, or the contact) may not reflect in our app — intermittent (owner: *"if i change or remove reaction in telegram itself it may not change in our app"*).

**DIAGNOSIS-FIRST — do NOT assume the hypothesis.** The round-2 fix (08-11) resolved the owner's own-reaction identity/VS16 symptoms; this residual is a distinct, server/TG-client-originated *reflection* gap. Static reading has already been wrong twice this phase (D12/D14), so Task 1 gathers concrete evidence and records a verdict BEFORE Task 2 touches the fix path.

Purpose: a reaction the owner (or contact) changes in the Telegram app appears/updates/clears in-app within a poll cycle for the messages the owner is realistically looking at.

Constraint: **WhatsApp reaction behavior stays byte-identical.** The reconcile is already `ActiveChannel == ChatChannel.Telegram`-gated at both call sites (ChatManager.cs:760 and :1328) and `TelegramReactionMerge` is Telegram-only — keep it that way. Preserve the strictly-serial messages/get queue (`_chatFetchesInFlight` gate + `CrossChatResponseGuard`) per the Wappi/tapi response-crossing constraint. Device confirmation + the echo-hex capture ride the 08-21 re-verify checkpoint.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.planning/phases/08-device-uat-milestone-closeout/08-11-SUMMARY.md
@.claude/skills/chat-data-flow/SKILL.md

<interfaces>
<!-- Extracted from the codebase — the executor works from these, no scavenger hunt needed. -->

// RECONCILE (Telegram-gated) — ChatManager.cs
//   Site 1 (live poll / latest window) ~:760
//     if (ActiveChannel == ChatChannel.Telegram &&
//         RefreshCachedMessageReactions(reconcileNorm, cachedList)) hasStatusUpdates = true;
//   Site 2 (pagination / already-seen) ~:1328
//     if (ActiveChannel == ChatChannel.Telegram &&
//         RefreshCachedMessageReactions(reconcileNorm, _activeChatCache)) cacheDirty = true;
//
//   private bool RefreshCachedMessageReactions(NormalizedMessage refreshed, List<MessageViewModel> cachedList) // ~:1851
//     for each cached where cached.messageId == refreshed.id:
//       merged = TelegramReactionMerge.Merge(cached.reactions, refreshed.reactions, nowUnixSeconds);
//       if (SameReactions(cached.reactions, merged)) return false;
//       cached.reactions = merged; OnMessageReactionsChanged?.Invoke(cached); return true;

// THE POLL WINDOW — ChatManager.cs
//   public static int MessagesPerPage = 50;                                     // :14
//   IEnumerator SyncLatestMessages(chatId, cachedList)                          // :592
//     url = ...messages/get?...&limit={MessagesPerPage}&offset=0                // :602  ← LATEST 50 ONLY
//   D5 live poll (ChatManager.LivePoll.cs) REUSES SyncLatestMessages every OpenChatLivePollGate.IntervalSeconds (~3s)
//     — so a message OUTSIDE the latest 50 is never re-fetched → its reconcile never runs.

// MERGE (Telegram-only) — TelegramReactionMerge.cs
//   public static List<MessageReaction> Merge(cached, server, nowUnix)
//     result = server != null ? new List(server) : new List();
//     mine = FindMine(cached);  // reactorKey == OutgoingReaction.MeReactorKey
//     if (mine != null && IsFreshOptimistic(mine, nowUnix)) { ...tombstone / replace / fold... }
//     return result.Count > 0 ? result : null;   // server wins for everyone except a FRESH optimistic "me"
//   IsFreshOptimistic: mine.time > 0 && now - mine.time <= OptimisticGraceSeconds(90)
//     — a server-mapped "me" echo carries time=0 → never fresh → server is authoritative.

// EVIDENCE INSTRUMENTATION (already in the tree, Editor-only, Telegram-gated) — ChatManager.cs:1688
//   #if UNITY_EDITOR LogTelegramReactionEcho(raw.reactions, _tgOwnUserId)
//     → Debug.Log($"[TG reaction echo] '{emoji}' [{CodePointHex(emoji)}] user_id={userId} ownId={ownUserId}")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose the D2-ext reflection gap — enumerate, gather evidence, record a verdict</name>
  <files>(diagnosis — no production edit; instrumentation already present. Record findings in the SUMMARY.)</files>
  <read_first>
    - Assets/Scripts/Main/ChatManager.cs — the two reconcile call sites (~760, ~1328), RefreshCachedMessageReactions (~1851), SyncLatestMessages (~592, note the `limit={MessagesPerPage}&offset=0`), and LogTelegramReactionEcho (~1688)
    - Assets/Scripts/Main/ChatManager.LivePoll.cs — the D5 open-chat live poll (reuses SyncLatestMessages every ~3s; brandNew diff + already-cached reconcile)
    - Assets/Scripts/Chat/TelegramReactionMerge.cs — Merge (absence path: server empty/null → result empty → non-fresh cached entry dropped), StampRemovalTombstone, IsFreshOptimistic
    - Assets/Scripts/Chat/TelegramReactionMapper.cs — how a server message's reactions[] maps to NormalizedMessage.reactions (does an ABSENT reactions[] yield null vs an empty list?)
    - Assets/Scripts/Chat/NormalizedMessage.cs — the reactions field
    - Tools/tapi/README.md + Tools/tapi/probe-message.sh — the read-only capture path (owner-gated; reference-only unless the owner runs it)
  </read_first>
  <action>
Enumerate and test each hypothesis with concrete evidence. Record the VERDICT (confirmed/refuted + the evidence) for each in the SUMMARY — this routes Task 2.

**H1 — poll-window coverage (leading, static-confirmable).** The live poll reuses `SyncLatestMessages`, which fetches `messages/get?...&limit={MessagesPerPage}&offset=0` = the latest 50 only (MessagesPerPage=50, ChatManager.cs:14). `RefreshCachedMessageReactions` runs only for messages IN the fetched batch. So a reaction changed/removed in the TG app on a loaded message OLDER than the latest 50 (owner scrolled up) is never re-fetched → its pill never reconciles. This matches "intermittent / may not" (depends on the reacted message's recency).
  - Evidence: confirm `MessagesPerPage=50` and that both reconcile sites only run inside the fetched-batch loop (not over the whole `_activeChatCache`). Confirm the pagination site (~1328) reconciles only when the owner paginates PAST the message (so an old-message change reflects only on re-scroll, not live).
  - VERDICT routes to: Task 2 candidate A (extend the reconcile to cover the loaded window, Telegram-gated, serial-safe).

**H2 — within-window render.** For a message INSIDE the latest 50, confirm the reconcile actually fires AND re-renders: `RefreshCachedMessageReactions` invokes `OnMessageReactionsChanged`, and the bound `MessageItemView` re-renders its pill in place. (Own-app + incoming reactions already render, so this path is likely sound — but verify there is no gate that skips a reaction-ONLY delta, e.g. `Normalize(raw)` not populating `refreshed.reactions`, or `SameReactions` masking a real change.)
  - Evidence: trace `Normalize(raw).reactions` for a reaction-only changed message (non-null, mapped); confirm `SameReactions` returns false when the emoji/identity actually changed (CompareKey handles base vs qualified per 08-11).
  - VERDICT routes to: Task 2 candidate B (fix the render/subscription/Normalize gate) — only if a real within-window gap is found.

**H3 — absence-vs-removal in Merge.** When a reaction is REMOVED in the TG app, the server returns the message with an empty or ABSENT `reactions[]`. Trace: does `TelegramReactionMapper` yield `refreshed.reactions == null` (absent) or `== empty list`? Then `Merge(cached=[non-fresh entry], server=null/empty, now)` → `result` empty → `mine` non-fresh (time=0 or aged) → block skipped → returns null → `SameReactions([entry], null)` false → cached cleared. Confirm this holds for BOTH an own non-fresh "me" entry AND a contact's entry, and that no earlier guard SKIPS calling Merge when the server reactions are absent.
  - Evidence: a focused reasoning/test over `Merge` + the mapper's absent-vs-empty behavior.
  - VERDICT routes to: Task 2 candidate C (make an empty/absent server reactions[] authoritatively clear a non-fresh cached entry) — only if a real absence gap is found.

**Optional live evidence (owner-gated, non-blocking):** the 08-11 `[TG reaction echo]` Editor log (ChatManager.cs:1688) and a read-only `Tools/tapi/probe-message.sh` capture on the dev TG profile record the exact echoed reactions[] on a change/removal. Reference these as the confirmatory evidence for the 08-21 capture ask; do NOT block Task 2 on a live run (secrets stay deny-ruled; never print the token).

Do NOT modify production code in this task. If a temporary extra Debug.Log helps the local trace, guard it `#if UNITY_EDITOR` and REMOVE it before committing Task 2.
  </action>
  <acceptance_criteria>
    - The SUMMARY records a VERDICT (confirmed / refuted + one-line evidence) for EACH of H1, H2, H3, and names which Task 2 candidate (A / B / C) the verdict routes to.
    - `grep -n "MessagesPerPage = 50" Assets/Scripts/Main/ChatManager.cs` returns a hit (H1 window value cited).
    - `grep -c "ActiveChannel == ChatChannel.Telegram" Assets/Scripts/Main/ChatManager.cs` ≥ 2 (both reconcile sites remain Telegram-gated — the byte-identical-WhatsApp invariant is stated).
    - No production `.cs` file changed by Task 1 (diagnosis only): `git status --porcelain Assets/Scripts` shows no modified tracked source from this task.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "MessagesPerPage = 50" Assets/Scripts/Main/ChatManager.cs && grep -c "ActiveChannel == ChatChannel.Telegram" Assets/Scripts/Main/ChatManager.cs</automated>
  </verify>
  <done>Each hypothesis has a recorded verdict + evidence; the routed Task 2 candidate is named; no production code changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the routed D2-ext fix (Telegram-gated, WhatsApp byte-identical)</name>
  <files>Assets/Scripts/Main/ChatManager.cs, Assets/Scripts/Chat/TelegramReactionMerge.cs, Assets/Tests/Editor/Chat/TelegramReactionMergeTests.cs</files>
  <read_first>
    - The Task 1 VERDICT recorded in the SUMMARY (this routes which candidate you implement)
    - Assets/Scripts/Main/ChatManager.cs — RefreshCachedMessageReactions (~1851), the reconcile sites (~760/1328), SyncLatestMessages (~592)
    - Assets/Scripts/Chat/TelegramReactionMerge.cs — Merge + SameReactions
    - Assets/Tests/Editor/Chat/TelegramReactionMergeTests.cs — the existing 15 merge tests (match their style/naming; do not disturb the WR-01/WR-03/tombstone/fold cases)
    - Assets/Scripts/Chat/ChatManager.LivePoll.cs — the serial-gate + crossing invariants any new fetch MUST inherit
  </read_first>
  <behavior>
    Write the failing test(s) FIRST (RED), then implement (GREEN). Behaviors to encode (implement the ones the verdict routes to):
    - Candidate C (absence-clears): `Merge(cached=[{reactorKey:"me", emoji:"❤", time:0}], server=null, now)` returns null (cleared). Same for `server=[]`. Same for a contact-only entry: `Merge([{reactorKey:"u1", emoji:"👍"}], null, now)` returns null. A FRESH optimistic tombstone still suppresses within grace (existing WR-03 test stays green).
    - Candidate A (loaded-window coverage): a pure, UnityEngine-free helper that, given the loaded message count and the latest-page size, decides whether older loaded messages need a reaction-reconcile pass (so the decision is EditMode-testable without a live server). Telegram-only; the actual fetch reuses the existing serial messages/get path (no new concurrent caller).
    - Byte-identical WhatsApp: any new reconcile coverage stays behind `ActiveChannel == ChatChannel.Telegram`; `Merge`/new helper are never reached on the WhatsApp path.
  </behavior>
  <action>
Implement the fix the Task 1 verdict routes to. Pick the candidate(s) the evidence confirmed — do NOT implement all three speculatively.

**If H1 confirmed (candidate A — loaded-window coverage):** ensure a server-originated reaction delta on a currently-LOADED-but-older message reconciles. Keep it bounded and serial-safe:
  - Extract a pure decision helper (e.g., `ReactionReconcileWindow.NeedsWiderPass(loadedCount, latestPageSize)`) so the "does the loaded window exceed the latest page?" decision is EditMode-testable.
  - On the open-chat poll, when `ActiveChannel == ChatChannel.Telegram` AND the loaded cache exceeds the latest page, run a bounded reaction-reconcile pass over the loaded pages that REUSES the existing serial messages/get (inherit the `_chatFetchesInFlight` gate + `CrossChatResponseGuard` + the post-await `currentChatId` re-check — add NO new concurrent caller). Throttle it slower than the 3s live poll (it is a background correction, not the hot path) and only while the chat is settled. Kill it on channel/bot switch (StopAllCoroutines already covers this).
  - Do NOT widen the WhatsApp path — WhatsApp reactions flow through `ReactionStore`, untouched.

**If H3 confirmed (candidate C — absence authoritative):** make an empty/absent server `reactions[]` clear a non-fresh cached entry. Verify `TelegramReactionMapper` yields the intended null-vs-empty for an absent reactions[], and that `Merge` drops a non-fresh cached entry when the server list is empty/null (add the RED tests above first). If the mapper skips populating `refreshed.reactions` for a reaction-only delta, fix the mapper so a removal is representable.

**If H2 confirmed (candidate B — within-window render):** fix the specific gate the evidence found (e.g., a `SameReactions` false-positive, a missing `OnMessageReactionsChanged` re-render, or a Normalize gap) — minimal, Telegram-gated.

RED → GREEN with atomic commits (`test(08-17): …` then `feat(08-17): …`). Keep every change Telegram-gated; state the byte-identical-WhatsApp reasoning in the commit body. New `.cs` → follow up with a `chore(08-17): add Unity .meta` commit after import. Stage ONLY the named files (never `git add -A`).
  </action>
  <acceptance_criteria>
    - The RED test(s) for the routed candidate exist and fail before the fix, pass after (atomic `test(08-17)` → `feat(08-17)` commits).
    - Every new/changed reconcile path is Telegram-gated: `grep -n "ActiveChannel == ChatChannel.Telegram" Assets/Scripts/Main/ChatManager.cs` still returns ≥ 2 hits and any new coverage sits behind that guard.
    - No new concurrent messages/get caller introduced: any added fetch reuses the existing serial path (grep shows no new `StartCoroutine(.*messages/get)` outside the established SyncLatestMessages/GetMessagesRoutine seams).
    - EditMode suite green FRESH on `Library/ScriptAssemblies/Assembly-CSharp.dll` recompiled AFTER the edit: new total = 1124 + (count of new tests). State the exact arithmetic in the SUMMARY.
    - WhatsApp path untouched: `git diff` shows no change to any non-Telegram-gated reaction rendering; `ReactionStore.cs` is not modified.
  </acceptance_criteria>
  <verify>
    <automated>Editor OPEN → drop Temp/claude/run-tests.trigger, read Temp/claude/test-summary.json; Editor CLOSED → Tools/run-tests-headless.sh -testFilter "Reaction". Suite green; freshness gate = Assembly-CSharp.dll mtime AFTER the edit.</automated>
  </verify>
  <done>The routed D2-ext fix is implemented + tested; a TG-app-side reaction change/removal reflects in-app within a poll cycle for loaded messages; WhatsApp byte-identical; suite green FRESH with the stated new baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tapi `messages/get` → client | Untrusted server reaction data (reactions[] per message) crosses into the cache/merge. Already-authed read surface; no new endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-17-01 | Tampering | Reaction reconcile (RefreshCachedMessageReactions / Merge) | mitigate | Fix stays `ActiveChannel == ChatChannel.Telegram`-gated so a malformed reactions[] can never alter WhatsApp rendering; Merge already null-guards `server`/`cached`. |
| T-08-17-02 | DoS | Any widened reaction-reconcile fetch (candidate A) | mitigate | Reuse the existing strictly-serial messages/get queue (`_chatFetchesInFlight` gate) + throttle slower than the 3s live poll; add NO concurrent caller (Wappi/tapi response-crossing constraint). |
| T-08-17-03 | Info disclosure | Editor echo-hex / read-only tapi capture (evidence) | accept | Editor-only `#if UNITY_EDITOR` log; the tapi token stays in `Secrets.Data`, deny-ruled, never printed; capture is owner-run and read-only. |
</threat_model>

<verification>
- Task 1 records a verdict for H1/H2/H3 and routes to a Task 2 candidate.
- Task 2's routed fix has RED→GREEN tests; suite green FRESH at 1124 + new tests.
- `grep -c "ActiveChannel == ChatChannel.Telegram" Assets/Scripts/Main/ChatManager.cs` ≥ 2 (WhatsApp byte-identical).
- No new concurrent messages/get caller; serial queue preserved.
</verification>

<success_criteria>
A reaction added/changed/removed IN the Telegram app reflects in-app within a poll cycle for loaded messages (verified on device at 08-21); WhatsApp reaction UX byte-identical; EditMode suite green FRESH; the diagnosis verdict + echo-hex capture ask recorded for the re-verify.
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-17-SUMMARY.md` including the Task 1 verdict table (H1/H2/H3 + routed candidate) and the exact test-baseline arithmetic.
</output>
