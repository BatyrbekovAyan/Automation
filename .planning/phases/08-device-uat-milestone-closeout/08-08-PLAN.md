---
phase: 08-device-uat-milestone-closeout
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Chat/SwipeToDelete.cs
  - Assets/Scripts/UI/ChatItemView.cs
  - Assets/Scripts/Chat/ChatRowSwipePolicy.cs
  - Assets/Tests/Editor/Chat/ChatRowSwipePolicyTests.cs
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Swiping a Telegram chat row reveals NO delete affordance (the visual slide is gone), not just a no-op network call"
    - "Creating a bot and auto-returning to the Bots page no longer throws a NullReferenceException during chat-row Bind"
    - "WhatsApp swipe-to-delete still works exactly as before (byte-identical)"
  artifacts:
    - path: "Assets/Scripts/Chat/SwipeToDelete.cs"
      provides: "Lifecycle-null-safe row swipe (no NRE when ResetClosed runs before Awake)"
      contains: "_rt"
    - path: "Assets/Scripts/Chat/ChatRowSwipePolicy.cs"
      provides: "Pure channel gate deciding whether the swipe affordance is enabled"
      contains: "Enabled"
  key_links:
    - from: "ChatItemView.Bind"
      to: "ChatRowSwipePolicy.Enabled(ActiveChannel) → disable SwipeToDelete on Telegram"
      via: "channel-gated affordance"
      pattern: "ChatRowSwipePolicy"
    - from: "SwipeToDelete.SetContentX / ResetClosed / PlaceButton"
      to: "lazy _rt init (never null)"
      via: "null-safe pre-Awake Bind"
      pattern: "_rt"
---

<objective>
Close **D4** and **D6** (medium/approved) — both land in the chat-row swipe stack (`SwipeToDelete` + `ChatItemView`), so they share one plan.

- **D6 (NullReferenceException):** creating a bot and auto-returning to the Bots page throws an NRE. Owner-provided stack: `SwipeToDelete.SetContentX` (SwipeToDelete.cs:157) ← `ResetClosed` (:80) ← `ChatItemView.Bind` (ChatItemView.cs:122) ← `ChatListView.AddChat` (:61) ← `ChatManager.ParseChatsJson` (:351) ← `SyncAllChats` (:428). Root cause: `SetContentX` dereferences `_rt`, which is assigned only in `SwipeToDelete.Awake()`. On the post-creation fresh sync, `ChatItemView.Bind` calls `swipeToDelete.ResetClosed()` on a just-instantiated row whose `Awake()` hasn't run yet → `_rt` is null → NRE. Fires during row Bind on a fresh sync regardless of channel, so the guard must protect the WhatsApp path too.
- **D4 (remove TG swipe affordance):** owner decision — the per-row swipe-delete on Telegram must be removed VISUALLY, not just no-op'd. The network delete is already a guarded no-op on Telegram (05-03 `ActiveChannelSupportsChatDelete`); now the swipe slide itself must not engage on Telegram rows.

Purpose: no NRE after bot creation, and no swipe affordance on Telegram rows — with WhatsApp swipe-delete untouched.

Constraint: WhatsApp swipe-to-delete byte-identical. Stays out of `ChatManager.cs`.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.claude/skills/unity-ui-builder/SKILL.md
@.claude/skills/bot-persistence/SKILL.md

<interfaces>
<!-- Extracted from the codebase. -->

// SwipeToDelete.cs — _rt assigned only in Awake; the NRE sites deref it:
private RectTransform _rt;                       // = (RectTransform)transform in Awake()
private void Awake(){ _rt = (RectTransform)transform; _scroll = GetComponentInParent<ScrollRect>(); PlaceButton(0f); }
public void ResetClosed(){ _snap?.Kill(); _btnSnap?.Kill(); SetContentX(0f); PlaceButton(0f); IsOpen=false; ... }  // :76
private void SetContentX(float x) => _rt.anchoredPosition = new Vector2(x, _rt.anchoredPosition.y);  // :156 — NRE when _rt null
private void PlaceButton(float contentX){ if (deleteButton==null) return; deleteButton.anchoredPosition = ...; }

// ChatItemView.cs — Bind calls ResetClosed on the freshly-instantiated row (:122)
public void Bind(ChatViewModel model){ ...; if (swipeToDelete != null) swipeToDelete.ResetClosed(); ... }
// fields: public SwipeToDelete swipeToDelete;  public Button deleteButton;

// ChatManager.ActiveChannel (ChatManager.Channel.cs) — public getter for channel gating
public ChatChannel ActiveChannel { get; }

// 05-03: ActiveChannelSupportsChatDelete already no-ops the network delete on Telegram (network side done)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (D6): Make SwipeToDelete lifecycle-null-safe so a pre-Awake Bind never NREs</name>
  <read_first>
    - Assets/Scripts/Chat/SwipeToDelete.cs — full (`Awake`, `ResetClosed` :76, `SetContentX` :156, `PlaceButton` :162, `OnDisable`). Note `_rt` and `_scroll` are Awake-assigned.
    - Assets/Scripts/UI/ChatItemView.cs — `Bind` :122 (`swipeToDelete.ResetClosed()`), and `ChatListView.AddChat` (:51–66) which `Instantiate`s then `Bind`s synchronously (Awake may not have run yet on the new prefab).
    - .claude/rules/unity-general.md — lifecycle rules (Awake for self-init; null-check destroyed/uninitialized objects).
    - 08-DEVICE-UAT.md — Defects row D6 (the exact stack).
  </read_first>
  <action>
    Lazily initialize `_rt` (and `_scroll`) so any public entry point that runs before `Awake()` is safe. Add a private `RectTransform Rt` accessor (or an `EnsureInit()` guard) that does `if (_rt == null) _rt = (RectTransform)transform;` and route `SetContentX`, `PlaceButton`, `ResetClosed`, `OnDisable`, `Open`, `Close`, and the drag handlers through it. Keep `Awake` as-is (still assigns on the normal path). This is channel-agnostic — the NRE fires on the WhatsApp path too, so the guard must protect both.

    Concretely: change `SetContentX` to `=> Rt.anchoredPosition = new Vector2(x, Rt.anchoredPosition.y);` where `Rt` lazily initializes; ensure `_scroll` is likewise resolved lazily in the drag handlers (`_scroll ??= GetComponentInParent<ScrollRect>();`) so a pre-Awake drag can't NRE either. Do NOT remove `Awake`'s assignment.

    Extract the "is the row initialized" decision or keep it inline — but ADD a pure guard test target: create `Assets/Scripts/Chat/ChatRowSwipePolicy.cs` in Task 2 and keep D6 as the lifecycle guard (no pure seam needed for the null-init itself; it's MonoBehaviour lifecycle). Instead, verify D6 by reading: confirm every `_rt.` / `_scroll.` dereference is behind the lazy accessor.
  </action>
  <acceptance_criteria>
    - `grep -q "_rt = (RectTransform)transform" Assets/Scripts/Chat/SwipeToDelete.cs` (Awake path preserved) AND a lazy init exists: `grep -qE "_rt == null|_rt \?\?=|RectTransform Rt" Assets/Scripts/Chat/SwipeToDelete.cs`
    - `grep -q "_rt.anchoredPosition = new Vector2(x" Assets/Scripts/Chat/SwipeToDelete.cs` returns NOTHING (raw un-guarded deref removed) OR the deref is provably behind the lazy accessor (verify in diff).
    - `grep -qE "_scroll \?\?=|_scroll == null" Assets/Scripts/Chat/SwipeToDelete.cs` (scroll also lazy-safe)
    - Full EditMode suite green (≥1036).
  </acceptance_criteria>
  <verify>
    <automated>Tools/run-tests-headless.sh (Editor closed) OR in-Editor bridge — full suite green, 0 failed</automated>
  </verify>
  <done>`ResetClosed`/`SetContentX` no longer NRE when `Bind` runs before `Awake` on a freshly-synced row (WhatsApp + Telegram). Device confirmation: create a bot, no NRE on return (08-10).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (D4): Channel-gate the swipe affordance off on Telegram rows</name>
  <read_first>
    - Assets/Scripts/UI/ChatItemView.cs — `Bind` (36–129): where `swipeToDelete.ResetClosed()` and the `deleteButton` listener are wired; this is where to enable/disable the affordance per channel.
    - Assets/Scripts/Chat/SwipeToDelete.cs — how disabling the component (or a guard flag) stops the drag from engaging (`OnBeginDrag`/`OnDrag` route to parent when not dragging).
    - Assets/Scripts/Main/ChatManager.DeleteChat.cs (+ 05-03 `ActiveChannelSupportsChatDelete`) — confirm the network delete is already a Telegram no-op (so this is visual-only).
    - Assets/Tests/Editor/Chat/ChannelSwitcherModelTests.cs — a pure-seam channel test pattern.
  </read_first>
  <action>
    Create `Assets/Scripts/Chat/ChatRowSwipePolicy.cs` (pure, UnityEngine-free): `public static bool Enabled(ChatChannel channel) => channel == ChatChannel.WhatsApp;` with an XML-doc note that swipe-to-delete is a WhatsApp-only affordance (Telegram has no chat/delete endpoint; D4 owner decision).

    In `ChatItemView.Bind`, gate the affordance on `ChatRowSwipePolicy.Enabled(ChatManager.Instance.ActiveChannel)`:
      - When enabled (WhatsApp): keep the current behavior exactly — call `swipeToDelete.ResetClosed()`, wire the `deleteButton` listener. Byte-identical.
      - When disabled (Telegram): disable the swipe so NO slide engages — set `swipeToDelete.enabled = false` (and hide/disable the `deleteButton` GameObject) so `OnBeginDrag` never runs the horizontal-drag branch, AND still call `ResetClosed()` first so a recycled row that had been swiped on WhatsApp snaps shut before being disabled. Re-enable it on the WhatsApp path (pooled rows rebind across channels — never leave a Telegram-disabled swipe stuck off on a subsequent WhatsApp bind).
    Since rows are pooled and reused across bot/channel binds, ALWAYS set the enabled state explicitly on every `Bind` (both branches), never assume the prior state.

    Add tests in `Assets/Tests/Editor/Chat/ChatRowSwipePolicyTests.cs` (NUnit, no scene): `Enabled(ChatChannel.WhatsApp)==true`; `Enabled(ChatChannel.Telegram)==false`.
  </action>
  <acceptance_criteria>
    - `grep -q "public static bool Enabled" Assets/Scripts/Chat/ChatRowSwipePolicy.cs` and `grep -q "using UnityEngine" Assets/Scripts/Chat/ChatRowSwipePolicy.cs` returns NOTHING (pure)
    - `grep -q "ChatRowSwipePolicy" Assets/Scripts/UI/ChatItemView.cs` and `grep -qi "swipeToDelete.enabled" Assets/Scripts/UI/ChatItemView.cs`
    - `grep -c "\[Test\]" Assets/Tests/Editor/Chat/ChatRowSwipePolicyTests.cs` returns ≥ 2
    - The WhatsApp branch still calls `ResetClosed()` and wires `deleteButton` (verify in diff — WA byte-identical).
    - Full EditMode suite green (≥1036).
  </acceptance_criteria>
  <verify>
    <automated>Tools/run-tests-headless.sh "ChatRowSwipePolicy" then full suite (Editor closed) OR in-Editor bridge — all pass</automated>
  </verify>
  <done>Telegram rows show no swipe affordance; WhatsApp rows swipe-delete unchanged. Device confirmation rides 08-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prefab lifecycle → Bind | A row can be data-bound before its MonoBehaviours' `Awake` has run |
| Channel state → row affordance | The active channel must gate a destructive (WhatsApp-only) affordance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Denial of Service | `SwipeToDelete` NRE crashing the post-creation sync render | mitigate | Lazy `_rt`/`_scroll` init so `Bind`-before-`Awake` never dereferences null (both channels). |
| T-08-08-02 | Elevation / Unintended Action | swipe-delete reachable on a channel with no delete endpoint | mitigate | `ChatRowSwipePolicy.Enabled` disables the affordance on Telegram; network delete already no-ops (05-03). |
| T-08-08-03 | Tampering | pooled row leaking a prior channel's enabled state | mitigate | Enabled state set explicitly on every `Bind` (both branches), never inherited. |
</threat_model>

<verification>
- EditMode suite green (≥1036) incl. `ChatRowSwipePolicyTests`.
- `grep` acceptance checks pass; WhatsApp Bind path unchanged.
- Device re-verify (08-10): create a bot → no NRE; swipe a TG row → no slide; swipe a WA row → still deletes.
</verification>

<success_criteria>
- No NRE after bot creation on the Bots-page return.
- Telegram rows have no swipe affordance; WhatsApp swipe-delete byte-identical.
- Full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-08-SUMMARY.md` with the D6 lazy-init sites and the D4 channel-gate wiring.
</output>
