---
phase: 08-device-uat-milestone-closeout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Tools/n8n/verify-telegram-parity.py
  - Tools/n8n/build-suggest-replies.py
  - .planning/phases/08-device-uat-milestone-closeout/08-PROD-REPLICATION.md
autonomous: false
requirements: [PROD-01]   # carried milestone item: prod bagkz bulk replication (REQUIREMENTS.md §v2/carried)
user_setup:
  - service: n8n (prod bagkz Cloud)
    why: "Owner-run one-shot bulk copy of all dev workflow changes to the dormant prod instance"
    env_vars:
      - name: N8N_BASE_URL
        source: "https://bagkz.app.n8n.cloud (prod) — owner sets at deploy time"
      - name: N8N_API_KEY
        source: "prod bagkz n8n → Settings → API (owner-held; deny-ruled for Claude)"
    dashboard_config:
      - task: "Recreate credentials BY NAME on prod Cloud (Postgres via Session pooler 5432, Supabase bare host + service_role JWT, OpenAi, WappiAuthToken, Cohere, n8nAPIKey); apply prod Supabase migrations"
        location: "n8n Cloud Credentials + Supabase SQL editor"

must_haves:
  truths:
    - "The runbook gives the exact, ordered, idempotent-safe steps to bulk-copy ALL dev workflow changes to dormant prod bagkz: import as {name,nodes,connections,settings}, recreate creds BY NAME, keep BOTH bot templates INACTIVE, deploy Suggest_Replies via build-suggest-replies.py, apply the prod Supabase migrations (+ the re-stamp cred UPDATE grant on documents), and never activate a bot clone."
    - "verify-telegram-parity.py can be pointed at a prod re-export directory (go/no-go gate) and build-suggest-replies.py can bind real prod credential ids on a no-SQLite Cloud target — both without editing tracked source; dev behavior is byte-identical when the new options are absent."
    - "The webhook header-auth follow-up (carried R-02-01 + dashboard pre-prod note) is flagged as a pre-real-traffic item, NOT a copy blocker; no secret values appear anywhere in the doc."
  artifacts:
    - path: ".planning/phases/08-device-uat-milestone-closeout/08-PROD-REPLICATION.md"
      provides: "Ordered prod bulk-replication runbook naming each workflow + cred-by-name + INACTIVE-template invariant + go/no-go verify + security follow-up"
      contains: "INACTIVE"
    - path: "Tools/n8n/verify-telegram-parity.py"
      provides: "--dir override so the parity asserts can run against a prod re-export (default = committed workflows/)"
      contains: "--dir"
    - path: "Tools/n8n/build-suggest-replies.py"
      provides: "prod credential-id override (env/flag) for a Cloud target with no local SQLite"
      contains: "CRED_ID"
  key_links:
    - from: "08-PROD-REPLICATION.md go/no-go step"
      to: "Tools/n8n/verify-telegram-parity.py --dir <prod-export>"
      via: "post-import structural re-verify"
      pattern: "verify-telegram-parity.py"
    - from: "08-PROD-REPLICATION.md Suggest_Replies step"
      to: "Tools/n8n/build-suggest-replies.py --stage full (prod BASE + prod cred ids)"
      via: "scripted deploy"
      pattern: "build-suggest-replies.py"
    - from: "08-PROD-REPLICATION.md Supabase step"
      to: "Tools/n8n/supabase/*.sql (documents scoping + conversation_outcomes + re-stamp UPDATE grant)"
      via: "prod migration application"
      pattern: "conversation_outcomes"
---

<objective>
Deliver `08-PROD-REPLICATION.md` — the exact, ordered, one-shot runbook to bulk-copy ALL dev n8n workflow changes to the dormant prod bagkz instance — plus the two small deployer/verifier tweaks that make that copy scriptable + verifiable against a prod Cloud target. Prod bagkz stays DORMANT: a single bulk copy after dev sign-off, zero live prod bot traffic this phase.

Purpose: everything the milestone shipped on dev (fixed Telegram_Bot template, both Create orchestrators' RAG re-stamp, channel-branched Suggest_Replies, and the Dashboard/Upload/Delete/OrphanSweep family) must land on prod identically before v1.1 can close. The copy must be idempotent-safe, must recreate credentials by NAME on Cloud, and must never activate a bot clone.

Output: the runbook doc + prod-targetability tweaks to `verify-telegram-parity.py` and `build-suggest-replies.py`. The tweaks + doc are autonomous; the actual prod deploy is the owner gate (Task 3).

Read first (authoritative spec of what must land + the locked invariants): `08-CONTEXT.md §Decisions → "08-02"`, and `Tools/n8n/README.md` (the 12 canonical workflows + the "Known follow-ups before production-ready" list).
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-CONTEXT.md
@Tools/n8n/README.md
@.planning/phases/04-n8n-telegram-template-parity-dev/04-HUMAN-UAT.md

<interfaces>
<!-- Contracts the executor needs — extracted from the two scripts. Use these directly. -->

Tools/n8n/verify-telegram-parity.py (reads the COMMITTED workflows/ dir; no argv today):
```python
HERE = os.path.dirname(os.path.abspath(__file__))
WF = os.path.join(HERE, "workflows")          # <-- hardcoded; add an optional --dir override
def load(fname): ...                           # opens os.path.join(WF, fname)
# checks: check_telegram_bot(), check_restamp_orchestrator(CREATE_TG/CREATE_WA), check_suggest_replies()
# exits 0 + "ALL PARITY ASSERTS PASSED" | exits 1 + "PARITY FAIL: <reason>"
```

Tools/n8n/build-suggest-replies.py (BASE + api key already env-driven; creds resolve from local SQLite):
```python
BASE = os.environ.get("N8N_BASE_URL", "http://localhost:5678").rstrip("/")   # prod-targetable ✔
DEV_DB = os.path.expanduser("~/.n8n/database.sqlite")                        # absent on Cloud
FALLBACK_CREDS = { "openAiApi": ("WNHwKWlO2E9OClkA","OpenAi account"),
                   "supabaseApi": ("vrywn6AxQMlvbbzC","Supabase") }          # DEV ids — wrong on prod
def resolve_cred(cred_type):   # no local DB -> returns the pinned DEV fallback (silent-wrong on Cloud)
def api_key():                 # N8N_API_KEY env, else secrets.json["n8nAPIKey"]
def export_canonical(wid, out): # GET /workflows/{wid} from BASE -> canonical JSON incl. top-level id
# argparse: --stage {front,full}  --update <id>  --id-file PATH  --export ID OUT
```
</interfaces>

<constraints>
- Do NOT run any deployer or verifier AGAINST PROD from this plan. `verify-telegram-parity.py` (reads committed JSON, no network) is safe to run locally; `build-suggest-replies.py --help` is safe. Everything that touches prod is an owner step in the runbook.
- The two script edits are additive + backward-compatible: absent the new options, dev behavior is byte-identical. Do NOT touch the Suggest_Replies workflow-graph builder — the committed canonical JSON stays unchanged.
- These are Python tooling scripts, not Unity C# — no Editor/test-bridge involvement; verify with `python3`.
- No secret values (tokens, keys, JWTs, connection strings, cred ids-as-secrets) in the runbook. Credentials are referenced BY NAME only.
- Locked invariants (bake into the runbook, do not soften): import as `{name,nodes,connections,settings}` (UI export strips the top-level id → re-inject or import via API/CLI that preserves it); creds recreated BY NAME on Cloud (Postgres via **Session pooler 5432**, NOT 6543/Direct; Supabase = bare `https://<ref>.supabase.co` + legacy service_role JWT); activate with an explicit `Content-Type: application/json`; **keep BOTH bot templates INACTIVE** (shared webhook path `0091024b-7b46`); prod stays DORMANT.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make verify + deployer prod-targetable (additive, dev-byte-identical)</name>
  <files>Tools/n8n/verify-telegram-parity.py, Tools/n8n/build-suggest-replies.py</files>
  <action>
Two small, additive edits so the prod copy is scriptable + verifiable on a Cloud target that has no local SQLite DB:

1. **verify-telegram-parity.py** — add an optional `--dir PATH` argument (via `argparse`) that overrides the workflow directory, defaulting to the current hardcoded `os.path.join(HERE, "workflows")`. `load(fname)` must read from the chosen dir. This lets the owner re-export the 4 parity workflows FROM prod into a scratch dir and re-run the asserts as a post-import go/no-go (catches a UI-round-trip strip: dropped `ai_embedding` connection, stripped top-level id, dropped `mark_all`-guard, etc.). Keep the exit-0/"ALL PARITY ASSERTS PASSED" and exit-1/"PARITY FAIL" contract unchanged. Default (no `--dir`) must still verify the committed `workflows/` and pass.

2. **build-suggest-replies.py** — add credential-id overrides so a Cloud deploy binds the REAL prod creds (recreated by name) instead of silently falling back to the DEV `FALLBACK_CREDS` ids when `~/.n8n/database.sqlite` is absent. Accept `N8N_OPENAI_CRED_ID` / `N8N_SUPABASE_CRED_ID` env vars AND matching `--openai-cred` / `--supabase-cred` CLI flags (flag wins over env; env wins over the pinned fallback). Wire the override into `resolve_cred()` (or its callers) so an explicit override short-circuits the SQLite lookup entirely and is used verbatim, keeping the "fail loudly, never guess" spirit. When NO override is given and a local DB IS present, behavior is byte-identical to today (dev path unchanged). Document the two env vars + flags in the module docstring/usage so the runbook can reference them.

Keep both diffs minimal and self-contained. Do not alter the workflow graph, the validation logic, the enum, or any committed workflow JSON.
  </action>
  <verify>
    <automated>python3 Tools/n8n/verify-telegram-parity.py >/tmp/vp.out 2>&1 && grep -q "ALL PARITY ASSERTS PASSED" /tmp/vp.out && python3 Tools/n8n/verify-telegram-parity.py --help 2>&1 | grep -q -- "--dir" && python3 Tools/n8n/build-suggest-replies.py --help 2>&1 | grep -Eiq -- "--openai-cred|OPENAI_CRED_ID" && python3 Tools/n8n/build-suggest-replies.py --help 2>&1 | grep -Eiq -- "--supabase-cred|SUPABASE_CRED_ID" && echo OK</automated>
  </verify>
  <done>`verify-telegram-parity.py` accepts `--dir` (default = committed `workflows/`) and still exits 0 on the default; `build-suggest-replies.py --help` documents the OpenAI + Supabase cred-id overrides; no committed workflow JSON changed; dev path byte-identical when overrides are absent.</done>
</task>

<task type="auto">
  <name>Task 2: Write the ordered 08-PROD-REPLICATION.md runbook</name>
  <files>.planning/phases/08-device-uat-milestone-closeout/08-PROD-REPLICATION.md</files>
  <action>
Create `08-PROD-REPLICATION.md`: a numbered, idempotent-safe, one-shot runbook the owner runs ONCE against dormant prod bagkz after dev sign-off. Header: status OPEN/owner-run; a bold "prod stays DORMANT — no bot clone is created or activated this phase" banner. Include a **workflows-in-scope table** (id · name · role · active-state) covering all 12 canonical workflows from `Tools/n8n/README.md`, marking **WhatsApp_Bot `4wYitz5ek30SVNlT` + Telegram_Bot `4VN3gsFaC2HUYmcc` as INACTIVE** (shared webhook path `0091024b-7b46`), Suggest_Replies + Dashboard/Upload/Delete/DeleteBotFiles as active webhooks, and Delete_Orphan_Profiles as the hourly scheduled sweep (import with fresh/empty staticData, then activate).

Ordered steps (each with an owner-verifiable check):
1. **Pre-flight go/no-go on the committed source:** `python3 Tools/n8n/verify-telegram-parity.py` must print "ALL PARITY ASSERTS PASSED" before anything touches prod.
2. **Recreate credentials BY NAME on prod Cloud:** Postgres (name "Postgres", **Session pooler 5432 NOT 6543/Direct**, role must be able to `UPDATE documents` — this is the re-stamp cred, dev id `vvRrFiEXzLVqKjOx`; on prod it gets a NEW id, referenced by name), Supabase (name "Supabase", bare `https://<ref>.supabase.co` + legacy service_role JWT), OpenAi (name "OpenAi account"), WappiAuthToken (for the bot templates + Orphan sweep; attach via `PUT /api/v1/workflows/{id}` — the MCP builder strips generic-auth), Cohere + n8nAPIKey as needed. Record the NEW prod ids for OpenAi + Supabase (fed to Task-1's `--openai-cred`/`--supabase-cred` in step 5). NO secret VALUES in this doc — names + sources only.
3. **Apply prod Supabase migrations:** `Tools/n8n/supabase/2026-07-02-harden-rag-store.sql`, `2026-07-02-price-list-originals-bucket.sql` (once per project — needed before Store Original File works), `2026-07-07-conversation-outcomes.sql`; confirm the `documents` RAG-scoping contract and that the Postgres role from step 2 holds the **UPDATE grant on `documents`** (the re-stamp pre-flight: execute the `Restamp RAG Chunks` node once with a `-1` sentinel → a 0-row UPDATE with NO credential/permission error passes; "permission denied for table documents" FAILS — fix the grant first).
4. **Import the 12 workflows as `{name,nodes,connections,settings}` preserving literal ids** (UI drag-drop strips the top-level id → re-inject or use an id-preserving import). Confirm the Create/Edit handlers carry the **prod `https://bagkz.app.n8n.cloud/api/v1/...`** self-API URLs (NOT the dev localhost from `apply-dev-config.py`) and the prod Wappi callback (NOT a trycloudflare host). Note the README prod-pass cleanups: the trailing-space `/activate ` URL in CreateWhatsappWorkflow, the Edit-handler `Set Bussiness Type` typo + leftover unused cred refs. Keep BOTH bot templates INACTIVE.
5. **Deploy Suggest_Replies via the deployer:** `N8N_BASE_URL=https://bagkz.app.n8n.cloud N8N_API_KEY=<prod-key> N8N_OPENAI_CRED_ID=<prod-openai-id> N8N_SUPABASE_CRED_ID=<prod-supabase-id> python3 Tools/n8n/build-suggest-replies.py --stage full` (Suggest_Replies is the SHARED always-active webhook — activating IT is correct; this is NOT a bot clone). Then seed RAG-with-data on a prod bot so grounding is testable (deferred-from-dev item).
6. **Delete_Orphan_Profiles prod wiring:** recreate WappiAuthToken, repoint the 4 HTTP nodes' credential id (dev `ZowntFGvApDJ7UzQ`), import with fresh (empty) staticData, activate — no webhook, no Supabase.
7. **Go/no-go post-import re-verify:** export the 4 parity workflows FROM prod by id into a scratch dir (`build-suggest-replies.py --export <id> <out>` for each, with prod `N8N_BASE_URL`; name files to match `verify-telegram-parity.py`'s expected filenames), then `python3 Tools/n8n/verify-telegram-parity.py --dir <prod-export>` → must print "ALL PARITY ASSERTS PASSED". This is the deploy go/no-go.
8. **Security follow-up — FLAGGED, not blocking the copy:** add header-auth on the Dashboard/Suggest webhook family (carried **R-02-01** + the dashboard pre-prod note) BEFORE real prod traffic. Record it as a pre-real-traffic checklist item with a ☐, explicitly noted as not a copy blocker.
9. **Confirm prod stays dormant:** no per-bot clone created/activated; both templates INACTIVE; a final ☐ "prod bagkz dormant — bulk copy only".

End with an **owner result** block (☐ PASS/☐ FAIL per major step + a Notes area) and a one-line pointer that milestone close (08-03) gates on this runbook being executed or explicitly deferred.
  </action>
  <verify>
    <automated>D=.planning/phases/08-device-uat-milestone-closeout/08-PROD-REPLICATION.md; test -f "$D" && grep -q "Telegram_Bot" "$D" && grep -q "Suggest_Replies" "$D" && grep -q "INACTIVE" "$D" && grep -qi "by name" "$D" && grep -q "5432" "$D" && grep -q "verify-telegram-parity.py" "$D" && grep -q "build-suggest-replies.py" "$D" && grep -q "conversation_outcomes" "$D" && grep -q "UPDATE" "$D" && grep -qi "header-auth\|header auth" "$D" && grep -qi "dormant" "$D" && echo OK</automated>
  </verify>
  <done>`08-PROD-REPLICATION.md` exists with the 12-workflow scope table (both bot templates marked INACTIVE), the ordered cred-by-name / Session-pooler-5432 / Supabase-migration / re-stamp-UPDATE-grant steps, the `build-suggest-replies.py` + `verify-telegram-parity.py --dir` go/no-go, and the flagged header-auth follow-up; no secret values present.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Owner runs the one-shot prod bulk copy to dormant bagkz</name>
  <action>Owner-run gate (not executor work): execute `08-PROD-REPLICATION.md` end-to-end against prod bagkz (creds by name, templates INACTIVE, no bot clone, post-import `verify-telegram-parity.py --dir` go/no-go). Claude cannot run it — prod key + secrets.json are deny-ruled.</action>
  <what-built>The prod-replication runbook `08-PROD-REPLICATION.md` + prod-targetable `verify-telegram-parity.py` (`--dir`) and `build-suggest-replies.py` (cred-id overrides).</what-built>
  <how-to-verify>
This is the one-shot owner deploy to dormant prod bagkz — Claude cannot run it (`secrets.json` + prod n8n API key are deny-ruled, and prod is live infra).
1. Run `08-PROD-REPLICATION.md` end-to-end against prod bagkz: pre-flight verify → recreate creds by name → prod Supabase migrations → import the 12 workflows (templates INACTIVE) → deploy Suggest_Replies with the prod cred-id overrides → wire the orphan sweep → post-import `verify-telegram-parity.py --dir <prod-export>` go/no-go.
2. Keep BOTH bot templates inactive; create/activate NO bot clone; leave prod dormant.
3. Record PASS/FAIL per step in the runbook's result block. Note the header-auth follow-up as pending-before-real-traffic (not a blocker).
  </how-to-verify>
  <resume-signal>Type "approved" once the prod copy is done (go/no-go GREEN) OR describe the deferral (prod copy postponed) so 08-03 rolls it forward.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator machine → prod n8n Cloud | The owner drives `PUT/POST /workflows` + activate against live prod (API key deny-ruled from Claude). |
| operator machine → prod Supabase | Migrations + the re-stamp cred's UPDATE grant on `documents` run against the prod DB. |
| public internet → prod webhooks | The Dashboard/Suggest webhook family is unauthenticated today (carried R-02-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information Disclosure | secrets in the runbook / in tracked scripts | mitigate | Runbook references creds BY NAME only; Task-1 tweaks read cred ids from env/flags at deploy time (no secret is committed); no token/JWT/connection-string in any file this plan touches. |
| T-08-06 | Elevation of Privilege / least privilege | the re-stamp Postgres credential | mitigate | Dedicated "Postgres" cred via Session pooler 5432 scoped to the `UPDATE documents` grant the re-stamp needs; pre-flight probe proves the grant without over-provisioning. |
| T-08-07 | Spoofing / unauthenticated webhook | Dashboard/Suggest webhook family on prod | transfer (flagged) | Header-auth hardening (R-02-01 + dashboard pre-prod note) is a REQUIRED pre-real-traffic checklist item in the runbook; not a copy blocker because prod stays dormant with no live traffic this phase. |
| T-08-08 | Tampering / wrong-account bind | build-suggest-replies cred resolution on Cloud | mitigate | Task-1 override makes the prod deploy bind the real recreated-by-name cred ids explicitly instead of silently falling back to dev ids; "fail loudly, never guess" preserved. |
| T-08-09 | Denial of Service / blast radius | accidental bot-clone activation on prod | mitigate | Both bot templates imported INACTIVE (shared webhook path); runbook creates/activates NO clone; a final "prod dormant" confirmation gate. |
</threat_model>

<verification>
- Task-1 automated chain returns OK: default `verify-telegram-parity.py` passes, `--dir` is in its `--help`, and both cred-id overrides are in `build-suggest-replies.py --help`.
- No committed workflow JSON changed: `git status --porcelain Tools/n8n/workflows/` is empty.
- `08-PROD-REPLICATION.md` Task-2 grep chain returns OK (12-workflow scope, INACTIVE invariant, cred-by-name, 5432, both scripts, Supabase migration + UPDATE grant, header-auth flag, dormant).
- No secret values in any file this plan touches.
</verification>

<success_criteria>
- The runbook is an idempotent-safe, ordered one-shot that lands every dev workflow change on prod without activating a bot clone, verifiable via the `verify-telegram-parity.py --dir` go/no-go.
- The deployer + verifier are prod-targetable with zero change to dev behavior.
- The header-auth follow-up is flagged (not implemented — deferred per CONTEXT); no secrets committed.
- The plan is `autonomous: false`; the prod deploy is the owner gate (Task 3).
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-02-SUMMARY.md`.
</output>
