---
phase: 08-device-uat-milestone-closeout
plan: 18
type: execute
wave: 8
depends_on: []
files_modified:
  - Assets/Scripts/UI/EmptyStateView.cs
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Tapping «Создать бота» on the Telegram-channel empty state OPENS the Add-Bot overlay (AddBotPanel) — it no longer does nothing on device"
    - "The Add-Bot form opens with Telegram PRESELECTED from the Telegram channel (08-14's preselect preserved), WhatsApp preselected from WhatsApp"
    - "The WhatsApp create-bot CTA behaviour is byte-identical to before"
  artifacts:
    - path: "Assets/Scripts/UI/EmptyStateView.cs"
      provides: "A create-bot CTA that actually fires on the Telegram channel (root cause fixed per runtime evidence)"
      contains: "OpenCreateBotFlow"
  key_links:
    - from: "EmptyStateView «Создать бота» tap on the Telegram channel"
      to: "AddBotPanel.Open() with Telegram preselected"
      via: "OpenCreateBotFlow → StartNewBot() → AddBotPanel.Instance.Open() (no raycast/CanvasGroup/exception block)"
      pattern: "OpenCreateBotFlow"
---

<objective>
Close **D12 (medium, RE-FAIL round 3)**: the Telegram empty-state «Создать бота» CTA does NOTHING on device (owner: *"nothing happens when pressing the button"*).

**DIAGNOSIS-FIRST, RUNTIME EVIDENCE REQUIRED — static reading has been wrong here once already.** Round 2 (08-14) diagnosed the CTA statically as "opens with WhatsApp preselected" and changed ONLY the preselect line; the owner re-failed it, so the button is genuinely INERT, not merely mis-preselected. 08-14 looked ONLY at `EmptyStateView.cs`. This round MUST look WIDER — the runtime/scene factors that a single-file read misses.

**Already refuted by scene inspection (do not re-chase):** there is exactly ONE `EmptyStateView` instance in Main.unity (no per-channel duplicate with an unwired handler), and `AddBotPanel.Instance` lazily resolves include-inactive (`FindFirstObjectByType(Include)`), so it is not null even if Screen_New never activated. The visual proves `ApplyChannelAccent` ran (the card is Telegram-blue + Telegram logo) ⇒ `ConfigureForReason` ran and wired `OpenCreateBotFlow` to the button. So the listener IS attached — the tap is either not REACHING the button, or the handler runs but its effect is swallowed.

Purpose: an owner on the Telegram channel's empty state can tap «Создать бота» and get the Add-Bot overlay (Telegram preselected).

Constraint: WhatsApp CTA byte-identical. Single-file change (`EmptyStateView.cs`) unless the runtime evidence points elsewhere (then the fix goes where the evidence lands — record why). Device confirmation rides the 08-21 checkpoint.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.planning/phases/08-device-uat-milestone-closeout/08-14-SUMMARY.md
@.claude/skills/unity-ui-builder/SKILL.md

<interfaces>
<!-- Extracted from the codebase. The executor works from these. -->

// EmptyStateView.cs (ONE instance in Main.unity)
//   ConfigureForReason(NoBotsExist): primaryButton.onClick.RemoveAllListeners(); += OpenCreateBotFlow; then ApplyChannelAccent()
//   Show(): canvasGroup.alpha=1; interactable=true; blocksRaycasts=true
//   Hide(): alpha=0; interactable=false; blocksRaycasts=false; _lastReason=null
//   OnDisable(): primaryButton.onClick.RemoveAllListeners(); _lastReason=null   // re-wire on next enable
//   OpenCreateBotFlow():
//     BotsPage botsPage = FindFirstObjectByType<BotsPage>(Include); botsPage?.StartNewBot();
//     int platform = (ChatManager.ActiveChannel == Telegram) ? 2 : 1;  Manager.Instance?.SelectPlatform(platform);
//   NOTE: EmptyStateView does NOT subscribe to OnActiveChannelChanged (only OnEmptyState/OnActiveBotChanged/OnChatAdded)

// BotsPage.StartNewBot()  (BotsPage.cs:49-54)
//   switch to Bots tab, then AddBotPanel.Instance?.Open();
// AddBotPanel.Instance — lazily FindFirstObjectByType(Include) → never null once the object exists in-scene
// AddBotPanel.Open() — EnsureInit() (_rt, _rootCanvas via GetComponentInParent<Canvas>(true)); SetActive(true) + slide

// SIBLINGS under Screen_Whatsapp/ChatsPanel (z-order matters for raycast):
//   EmptyState (this), SyncingState (SyncingView — opaque bg Image, raycastTarget=true, Hide() clears blocksRaycasts),
//   ChatListSyncIndicator (pill — blocksRaycasts=false, informational)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the D12 diagnosis — runtime/scene evidence, not another single-file read</name>
  <files>Assets/Scripts/UI/EmptyStateView.cs (temporary, guarded instrumentation only — removed or kept per verdict)</files>
  <read_first>
    - Assets/Scripts/UI/EmptyStateView.cs — Show/Hide/OnEnable/OnDisable/ConfigureForReason/ApplyChannelAccent/OpenCreateBotFlow (full)
    - Assets/Scripts/Main/BotsPage.cs — StartNewBot (the tab switch + AddBotPanel.Open call)
    - Assets/Scripts/Main/AddBotPanel.cs — Instance lifecycle + Open() (EnsureInit, _rootCanvas resolution, SetActive+slide)
    - Assets/Scripts/Main/BottomTabManager.cs — SwitchTab (does the tab switch throw or no-op from the empty-state context?)
    - Assets/Scenes/Main.unity — the Screen_Whatsapp/ChatsPanel subtree: enumerate CanvasGroup components on ChatsPanel and its ancestors (Screen_Whatsapp, the canvas) and their `m_Interactable` / `m_BlocksRaycasts`; and the sibling z-order under ChatsPanel (any ACTIVE raycastTarget overlay above EmptyState). Use grep on the scene by the relevant fileIDs/component types.
  </read_first>
  <action>
Re-diagnose WIDE. For each candidate, gather concrete evidence and record a VERDICT in the SUMMARY. Add guarded instrumentation so the tap path is observable (the owner/re-verify can confirm on device or in the Editor Game view).

**Instrumentation (temporary, guarded):** add `Debug.Log` markers, guarded so they never ship noise — at `OpenCreateBotFlow` ENTRY (`[D12] OpenCreateBotFlow ch={channel}`), after `StartNewBot()` returns, and around `Manager.SelectPlatform`. Wrap in `#if UNITY_EDITOR` OR a `[D12]`-tagged conditional so they are trivially removable. The ENTRY log is the pivot: **if it fires on tap, the handler runs (cause is downstream — StartNewBot/Open/SwitchTab); if it does NOT fire, the tap never reaches the button (cause is occlusion or a parent CanvasGroup).**

**C1 — raycast occlusion by a sibling overlay.** Under Screen_Whatsapp/ChatsPanel, is any sibling rendered ABOVE EmptyState active with `raycastTarget`/`blocksRaycasts` true while NoBotsExist shows? Prime suspect: `SyncingState` (opaque bg Image, raycastTarget=true). Confirm `SyncingView.Hide()` clears `blocksRaycasts` AND alpha in the NoBotsExist state (no sync running with 0 bots), and confirm the pill is `blocksRaycasts=false`. Evidence: read the sibling list + each overlay's Hide state.

**C2 — a parent CanvasGroup gating the button (STRONGEST remaining, and exactly what 08-14 missed).** Walk the ancestor chain EmptyState → ChatsPanel → Screen_Whatsapp → Canvas. Is there a CanvasGroup with `interactable=false` or `blocksRaycasts=false` that, on the Telegram-channel state, would swallow the tap even though EmptyState's own CanvasGroup is interactable? (A channel-switch or screen-activation path may leave a parent group non-interactable.) Evidence: enumerate ancestor CanvasGroups + their flags from Main.unity; note any that could gate the button.

**C3 — handler runs but the effect is swallowed.** If the ENTRY log fires (or would), trace `StartNewBot → SwitchTab(BotsTabIndex) → AddBotPanel.Instance.Open()`: does `SwitchTab` throw or early-return from the empty-state context? Does `Open()` hit a null `_rootCanvas` (GetComponentInParent<Canvas>(true) on an inactive Screen_New) and throw? Is any exception swallowed upstream (a try/catch or an event dispatcher)? Evidence: read the SwitchTab + Open paths for a throw/no-op on this specific navigation state.

**C4 — the empty state is stale/not re-themed on a channel switch.** EmptyStateView does NOT subscribe to `OnActiveChannelChanged`. Confirm whether, when the owner switches to Telegram on the 0-bots state, `ConfigureForReason` (hence the button re-wire) actually re-runs — if the button was wired under a DIFFERENT reason/channel and never re-wired for the visible Telegram card, the tap would no-op. (The blue theming suggests it DID re-run, but confirm the exact trigger that re-runs ConfigureForReason on the 0-bots channel-switch path.)

Record the confirmed cause + the evidence. Route to Task 2: C1/C2 → clear the occlusion / parent-group gate; C3 → fix the swallowed exception / null-canvas / SwitchTab no-op; C4 → ensure ConfigureForReason (re-wire) runs for the visible Telegram empty state.
  </action>
  <acceptance_criteria>
    - The SUMMARY records a VERDICT (confirmed/refuted + evidence) for C1, C2, C3, C4 and names the ONE cause the fix targets.
    - The SUMMARY explicitly states whether the `[D12] OpenCreateBotFlow` ENTRY log fires on tap (handler-runs vs tap-blocked) — the pivot that split the diagnosis. If the executor cannot literally tap (no Play-mode/device access in this session), state the verdict as PREDICTED from the C1–C4 scene-YAML + code evidence, explicitly caveated as unconfirmed pending the 08-21 device pass — do not present an inferred verdict as observed.
    - Ancestor CanvasGroup flags (EmptyState→ChatsPanel→Screen_Whatsapp→Canvas) are enumerated from Main.unity with their interactable/blocksRaycasts values.
    - Any instrumentation added is guarded (`#if UNITY_EDITOR` or a removable `[D12]` conditional) — `grep -n "\[D12\]" Assets/Scripts/UI/EmptyStateView.cs` shows only guarded lines.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "\[D12\]\|#if UNITY_EDITOR" Assets/Scripts/UI/EmptyStateView.cs</automated>
  </verify>
  <done>The inert-CTA cause is pinned with runtime/scene evidence (not a single-file guess); the fix target is named; instrumentation is guarded.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the inert CTA per the verdict; preserve the 08-14 Telegram preselect + WhatsApp parity</name>
  <files>Assets/Scripts/UI/EmptyStateView.cs (or the file the evidence lands on — record why if not EmptyStateView)</files>
  <read_first>
    - The Task 1 VERDICT in the SUMMARY (routes the fix)
    - Assets/Scripts/UI/EmptyStateView.cs — the current OpenCreateBotFlow (08-14's channel-aware preselect: `platform = ActiveChannel==Telegram ? 2 : 1`)
    - Whatever file the verdict routes to (AddBotPanel.cs / BottomTabManager.cs / the scene overlay), so the fix matches its conventions
  </read_first>
  <action>
Implement the fix the Task 1 verdict routes to. Keep 08-14's channel-aware preselect intact (Telegram=2 on the Telegram channel, WhatsApp=1 otherwise) and the WhatsApp CTA byte-identical.

- **C1/C2 (tap blocked):** clear the block. If a sibling overlay occludes, ensure it is fully non-blocking in the NoBotsExist state (alpha 0 + raycastTarget/blocksRaycasts off). If a parent CanvasGroup gates the button, set/restore `interactable=true; blocksRaycasts=true` on the correct group for the empty-state path (code-side; a scene tweak only if unavoidable — then use a builder + orchestrator-assisted scene stamp per the scene-mutation rule, commit Main.unity immediately).
- **C3 (swallowed effect):** fix the null `_rootCanvas` / `SwitchTab` no-op / swallowed exception so `AddBotPanel.Open()` actually runs from the empty-state context; null-guard defensively.
- **C4 (stale re-wire):** ensure `ConfigureForReason` re-runs (re-wiring `OpenCreateBotFlow`) for the visible Telegram empty state — e.g., subscribe EmptyStateView to `OnActiveChannelChanged` to re-configure the current reason on a channel switch (mirror how it already catches up in OnEnable), so the visible card's button is always freshly wired.

Remove the temporary instrumentation from Task 1 UNLESS a guarded `#if UNITY_EDITOR` marker is worth keeping for the 08-21 device confirmation (state the choice). Commit atomically (`fix(08-18): …`); if a new `.cs`/scene stamp is involved, follow the meta/scene-commit conventions. Stage ONLY the named files.
  </action>
  <acceptance_criteria>
    - `grep -n "SelectPlatform" Assets/Scripts/UI/EmptyStateView.cs` still shows the channel-aware preselect (Telegram=2 branch preserved from 08-14; no hardcoded `SelectPlatform(1)`).
    - The routed fix is present and grep-verifiable (e.g., an `OnActiveChannelChanged` subscription for C4; a CanvasGroup interactable restore for C2; a null-guard/await fix for C3) — the SUMMARY cites the exact line.
    - WhatsApp CTA byte-identical: `git diff` shows no change to the WhatsApp preselect (still resolves platform 1) and no change to any WhatsApp-only path.
    - No leftover UNGUARDED debug logs: `grep -n "Debug.Log(\"\[D12\]" Assets/Scripts/UI/EmptyStateView.cs` returns only `#if UNITY_EDITOR`-guarded lines (or none).
    - EditMode suite green FRESH (Assembly-CSharp.dll recompiled after the edit): total stays 1124 unless a pure seam was extracted (+N; state arithmetic). MonoBehaviour glue needs no new test.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "SelectPlatform\|OnActiveChannelChanged\|interactable" Assets/Scripts/UI/EmptyStateView.cs; then run the EditMode suite (Editor-open bridge or Tools/run-tests-headless.sh) — green FRESH.</automated>
  </verify>
  <done>Tapping «Создать бота» on the Telegram channel opens the Add-Bot overlay with Telegram preselected (confirmed on device at 08-21); WhatsApp CTA byte-identical; suite green FRESH.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner tap → UI navigation | Local UI event only; no network, no secrets, no external input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-18-01 | Elevation/UX | EmptyStateView create-bot CTA | mitigate | Fix only restores the intended open-overlay behaviour; WhatsApp path resolves platform 1 byte-identical (no new capability, no cross-channel leak). |
| T-08-18-02 | Info disclosure | D12 instrumentation logs | accept | Guarded `#if UNITY_EDITOR` / `[D12]` markers, no secrets/PII; removed or Editor-only before ship. |
| T-08-18-03 | Tampering | Any scene CanvasGroup/overlay tweak (if C1/C2) | accept | If a scene edit is unavoidable it goes through a builder + orchestrator-assisted stamp, committed immediately; refs rewired via SerializedObject (no silent serialized-ref loss). |
</threat_model>

<verification>
- Task 1 pins the inert-CTA cause with runtime/scene evidence (CanvasGroup enumeration + ENTRY-log pivot), not another single-file guess.
- Task 2's fix is grep-verifiable and preserves 08-14's Telegram preselect + WhatsApp parity.
- EditMode suite green FRESH.
</verification>

<success_criteria>
On the Telegram channel with no bots, «Создать бота» opens the Add-Bot form with Telegram preselected; on WhatsApp it still preselects WhatsApp (byte-identical). Verified on device at 08-21.
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-18-SUMMARY.md` including the C1–C4 verdict table, the ENTRY-log pivot result, and the enumerated ancestor CanvasGroup flags.
</output>
