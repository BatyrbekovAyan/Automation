---
phase: 08-device-uat-milestone-closeout
plan: 20
type: execute
wave: 9
depends_on: [17, 19]
files_modified:
  - Assets/Scenes/Main.unity
  - Assets/Scripts/Main/ChatManager.cs
  - Assets/Scripts/UI/ChatListSyncIndicator.cs
  - Assets/Scripts/Chat/ChatListSyncIndicatorGate.cs
  - Assets/Editor/ChatListSyncIndicatorBuilder.cs
  - Assets/Tests/Editor/Chat/ChatListSyncIndicatorGateTests.cs
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "The «Синхронизация…» pill no longer exists anywhere — no scene object, no runtime script, no builder, no gate, no tests"
    - "The Telegram chat list shows NO pill (the WhatsApp-parity cover from 08-19 is the sync affordance instead); WhatsApp is byte-identical"
    - "The duplicate-sync guard still works — _chatListSyncing (used by RefreshActiveBotChats) is PRESERVED; only the pill-only events/getter are removed"
  artifacts:
    - path: "Assets/Scripts/Main/ChatManager.cs"
      provides: "Pill event plumbing removed (OnChatListSyncStart/End + IsChatListSyncing) while _chatListSyncing + its duplicate-sync guard stay"
      contains: "_chatListSyncing"
  key_links:
    - from: "RefreshActiveBotChats duplicate-sync guard"
      to: "_chatListSyncing field (PRESERVED)"
      via: "if (_chatListSyncing) return; // collapse duplicate syncs"
      pattern: "_chatListSyncing"
---

<objective>
Close **D13b (owner-approved round 3)** — half (b) of D13: REMOVE the D9 «Синхронизация…» pill entirely. Owner decision (08-16): *"Cover only, remove pill."* The pill (shipped 08-09, gated 08-12) is superseded by the WhatsApp-parity cover built in 08-19; the owner questioned why it exists and why WhatsApp has no pill — the answer is parity via the cover, so the pill goes.

**Remove ALL of it:**
1. The runtime script `Assets/Scripts/UI/ChatListSyncIndicator.cs` (+ .meta)
2. The pure gate `Assets/Scripts/Chat/ChatListSyncIndicatorGate.cs` (+ .meta)
3. The editor builder `Assets/Editor/ChatListSyncIndicatorBuilder.cs` (+ .meta)
4. Its tests `Assets/Tests/Editor/Chat/ChatListSyncIndicatorGateTests.cs` (+ .meta)
5. The scene object `ChatListSyncIndicator` under `Screen_Whatsapp/ChatsPanel` in Main.unity
6. The pill-ONLY ChatManager plumbing: the `OnChatListSyncStart` / `OnChatListSyncEnd` events + their invocations, and the `IsChatListSyncing` getter.

**CRITICAL — grep-confirmed consumer boundary (do NOT over-delete):** the `_chatListSyncing` FIELD is NOT pill-only. It backs the duplicate-sync guard in `RefreshActiveBotChats` (`ChatManager.BotState.cs:311 → if (_chatListSyncing) return; // collapse duplicate syncs`) and is reset on switch (BotState.cs:130, Channel.cs:80, PrivacyClear.cs:83). **KEEP the field, its `= true`/`= false` assignments, and every reset.** Only the two `Action` events + the two `?.Invoke()` lines + the public `IsChatListSyncing` getter are pill-only (grep confirms `ChatListSyncIndicator` is their sole subscriber). Verify the grep yourself before deleting.

Constraint: WhatsApp byte-identical (the pill was Telegram-display-gated; removal touches no WhatsApp behaviour). Sequenced AFTER 08-19 (cover exists first) and 08-17 (shared ChatManager.cs). Device confirmation rides 08-21.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.planning/phases/08-device-uat-milestone-closeout/08-16-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. -->

// PILL-ONLY plumbing to REMOVE — ChatManager.cs
//   public event Action OnChatListSyncStart;   // :132   (sole subscriber: ChatListSyncIndicator)
//   public event Action OnChatListSyncEnd;      // :135   (sole subscriber: ChatListSyncIndicator)
//   public bool IsChatListSyncing => _chatListSyncing;   // :450  (sole reader: ChatListSyncIndicator)
//   SyncAllChats(): _chatListSyncing = true; OnChatListSyncStart?.Invoke(); ... finally { _chatListSyncing = false; OnChatListSyncEnd?.Invoke(); }  // :454-455 / :490-491

// KEEP — _chatListSyncing field + duplicate-sync guard (NOT pill-only)
//   private bool _chatListSyncing;                                   // ChatManager.cs:443
//   RefreshActiveBotChats(): if (_chatListSyncing) return;           // ChatManager.BotState.cs:311  ← the real consumer
//   resets: ChatManager.BotState.cs:130, ChatManager.Channel.cs:80, ChatManager.PrivacyClear.cs:83
//   → after removal, keep: `_chatListSyncing = true;` at SyncAllChats start and `_chatListSyncing = false;` in its finally
//     (just DROP the two OnChatListSync*?.Invoke() lines). Update the field's docstring to drop the pill reference.

// SCENE OBJECT + BUILDER — remove
//   ChatListSyncIndicatorBuilder: [MenuItem("Tools/Chat List Sync Indicator/Build")] + StampHeadless()
//     const ScreenName="Screen_Whatsapp"; ChatsPanelName="ChatsPanel"; IndicatorName="ChatListSyncIndicator"
//     helper DestroyAllByName(chatsPanel, IndicatorName)  ← reuse for a Remove pass
//   Object in scene: Screen_Whatsapp/ChatsPanel/ChatListSyncIndicator (last child)

// TESTS — ChatListSyncIndicatorGateTests.cs = exactly 6 [Test] methods (delete the file).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip the pill scene object from Main.unity (scene mutation, commit immediately)</name>
  <files>Assets/Editor/ChatListSyncIndicatorBuilder.cs (add a Remove entry), Assets/Scenes/Main.unity</files>
  <read_first>
    - Assets/Editor/ChatListSyncIndicatorBuilder.cs — StampHeadless (opens Main.unity, builds, saves) + the DestroyAllByName helper + the Screen/ChatsPanel/Indicator name constants
    - .planning/phases/08-device-uat-milestone-closeout/08-09-SUMMARY.md — the pattern used to STAMP the pill (scene-stamp checkpoint / run-editor-builder; commit immediately; GUID/name grep verify no sibling clobber)
  </read_first>
  <action>
Remove the `ChatListSyncIndicator` GameObject from `Screen_Whatsapp/ChatsPanel` via an idempotent editor pass (delete-only — DestroyAllByName is already in the builder). Add a removal entry to the builder that runs the delete + saves the scene:
  - `[MenuItem("Tools/Chat List Sync Indicator/Remove")]` + a `RemoveHeadless()` (mirror `StampHeadless`: `EditorSceneManager.OpenScene("Assets/Scenes/Main.unity")` → find `Screen_Whatsapp/ChatsPanel` → `DestroyAllByName(chatsPanel, IndicatorName)` → `EditorSceneManager.SaveScene(scene)`). Reuse the existing helper + constants — no new object built.
Run the removal (executors lack mcp-unity — use the sanctioned scene-mutation path):
  - **Editor CLOSED:** `Tools/run-editor-builder.sh ChatListSyncIndicatorBuilder.RemoveHeadless` (executor runs via Bash).
  - **Editor OPEN (headless refuses on the project lock):** mark this an ORCHESTRATOR-ASSISTED scene-stamp checkpoint — the orchestrator runs `execute_menu_item "Tools/Chat List Sync Indicator/Remove"` + `save_scene` via mcp-unity.
Then **commit Main.unity IMMEDIATELY** (parallel-session scene-clobber risk). Verify the object is gone and no sibling was clobbered: grep Main.unity for the `ChatListSyncIndicator` name (0 hits) and confirm `SyncingView` / `EmptyStateView` / `ChannelSwitcherView` each still resolve to exactly 1 instance (GUID grep). Stage ONLY Main.unity + the builder edit. (The builder file itself is deleted in Task 2, AFTER its Remove entry has done its job.)
  </action>
  <acceptance_criteria>
    - `grep -c "ChatListSyncIndicator" Assets/Scenes/Main.unity` returns 0 (the GameObject name is gone from the scene).
    - Sibling overlays intact: the SyncingView GUID (51b4bed05d93347a9b00290a7b9685e1), EmptyStateView GUID (90d6c66e8d0084967b8066e220ac89af), and ChannelSwitcherView each appear exactly once in Main.unity (no clobber).
    - Main.unity committed in its own commit immediately after the stamp (`git log -1 --name-only` shows Main.unity + the builder Remove edit, nothing unrelated — no `git add -A`).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "ChatListSyncIndicator" Assets/Scenes/Main.unity; grep -c "51b4bed05d93347a9b00290a7b9685e1" Assets/Scenes/Main.unity</automated>
  </verify>
  <done>The pill scene object is removed from Main.unity, committed immediately, no sibling clobbered.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the pill files + strip the pill-only ChatManager plumbing (preserve _chatListSyncing)</name>
  <files>Assets/Scripts/Main/ChatManager.cs, Assets/Scripts/UI/ChatListSyncIndicator.cs (delete), Assets/Scripts/Chat/ChatListSyncIndicatorGate.cs (delete), Assets/Editor/ChatListSyncIndicatorBuilder.cs (delete), Assets/Tests/Editor/Chat/ChatListSyncIndicatorGateTests.cs (delete)</files>
  <read_first>
    - Assets/Scripts/Main/ChatManager.cs — the pill events (:132/:135), the invocations in SyncAllChats (:454-455 start, :490-491 finally), the IsChatListSyncing getter (:450), and the _chatListSyncing field + docstring (:438-450)
    - Assets/Scripts/Main/ChatManager.BotState.cs — the duplicate-sync guard `if (_chatListSyncing) return;` (:311) — CONFIRM it stays and still compiles
    - Assets/Scripts/Main/ChatManager.Channel.cs:80 + Assets/Scripts/Main/ChatManager.PrivacyClear.cs:83 + ChatManager.BotState.cs:130 — the `_chatListSyncing = false` resets (all KEPT)
  </read_first>
  <action>
FIRST re-run the consumer grep and paste it into the SUMMARY (evidence-before-delete):
`grep -rn "OnChatListSyncStart\|OnChatListSyncEnd\|IsChatListSyncing" Assets --include="*.cs"` — confirm the ONLY consumer is `ChatListSyncIndicator.cs` (which is being deleted). If any OTHER consumer appears, STOP and re-plan (do not delete an event something else uses).

Then:
1. **Delete the four files** (+ their `.meta`): `ChatListSyncIndicator.cs`, `ChatListSyncIndicatorGate.cs`, `ChatListSyncIndicatorBuilder.cs`, `ChatListSyncIndicatorGateTests.cs`. Use `git rm` for the `.cs` and the `.meta`.
2. **ChatManager.cs surgery (pill-only):**
   - Delete the `OnChatListSyncStart` (:132) + `OnChatListSyncEnd` (:135) event declarations and their XML doc block.
   - Delete `OnChatListSyncStart?.Invoke();` (:455) and `OnChatListSyncEnd?.Invoke();` (:491) — but KEEP `_chatListSyncing = true;` (:454) and `_chatListSyncing = false;` (:490) exactly.
   - Delete the `public bool IsChatListSyncing => _chatListSyncing;` getter (:450) + its XML doc.
   - Update the `_chatListSyncing` field docstring (:438-443) to drop the pill/ChatListSyncIndicator reference; keep the duplicate-sync-guard description.
   - Do NOT touch `RefreshActiveBotChats` (BotState.cs:311), the resets (BotState.cs:130, Channel.cs:80, PrivacyClear.cs:83), or any WhatsApp/SyncingView code.
3. **Baseline arithmetic:** the deleted `ChatListSyncIndicatorGateTests.cs` = 6 `[Test]` methods. New EditMode baseline = 1124 − 6 = **1118** (no new tests — this is deletion + surgical removal). State this explicitly in the SUMMARY; if the pre-count differs, recount and re-derive.
Commit the code + deletions atomically (`chore(08-20): remove D9 sync pill (superseded by TG cover)`), then a follow-up `chore(08-20): drop pill .meta` if any `.meta` deletions land separately (established convention). Stage ONLY the named files (`git rm` the deletions; never `git add -A`).
  </action>
  <acceptance_criteria>
    - The four pill files are gone: `ls Assets/Scripts/UI/ChatListSyncIndicator.cs Assets/Scripts/Chat/ChatListSyncIndicatorGate.cs Assets/Editor/ChatListSyncIndicatorBuilder.cs Assets/Tests/Editor/Chat/ChatListSyncIndicatorGateTests.cs 2>&1` reports all "No such file" (and their .meta too).
    - Pill plumbing gone from ChatManager.cs: `grep -c "OnChatListSyncStart\|OnChatListSyncEnd\|IsChatListSyncing" Assets/Scripts/Main/ChatManager.cs` returns 0.
    - `_chatListSyncing` PRESERVED: `grep -n "_chatListSyncing" Assets/Scripts/Main/ChatManager.cs` shows the field + the `= true`/`= false` assignments still present; `grep -n "if (_chatListSyncing) return" Assets/Scripts/Main/ChatManager.BotState.cs` still returns the duplicate-sync guard.
    - No dangling references anywhere: `grep -rn "ChatListSyncIndicator" Assets --include="*.cs"` returns 0 hits.
    - EditMode suite compiles + green FRESH at **1118/1118** (1124 − 6 deleted gate tests), Assembly-CSharp.dll recompiled after the edit. Arithmetic stated in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "OnChatListSyncStart\|OnChatListSyncEnd\|IsChatListSyncing\|ChatListSyncIndicator" Assets/Scripts/Main/ChatManager.cs; grep -rn "ChatListSyncIndicator" Assets --include="*.cs" | wc -l; grep -n "if (_chatListSyncing) return" Assets/Scripts/Main/ChatManager.BotState.cs; then EditMode suite green FRESH at 1118.</automated>
  </verify>
  <done>All pill code + tests + the scene object are gone; _chatListSyncing + its duplicate-sync guard preserved; no dangling refs; WhatsApp byte-identical; suite green FRESH at 1118.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Deletion-only change on a client-side display widget; no network, secrets, or data flow added or removed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-20-01 | Tampering/regression | _chatListSyncing duplicate-sync guard | mitigate | Explicitly PRESERVE the field + its guard (RefreshActiveBotChats) + resets; only the pill-only events/getter are deleted (grep-confirmed sole consumer before deleting). |
| T-08-20-02 | Repudiation/parity | WhatsApp sync behaviour | accept | The pill was Telegram-display-gated and drove nothing on WhatsApp; removal changes no WhatsApp code (SyncingView untouched). |
| T-08-20-03 | DoS/scene integrity | Main.unity scene stamp | mitigate | Idempotent delete-only pass + immediate commit + GUID/name grep verify (SyncingView/EmptyStateView/ChannelSwitcherView each still 1) — no sibling clobber (parallel-session risk). |
</threat_model>

<verification>
- Pill scene object removed (grep 0 in Main.unity), committed immediately, no sibling clobbered.
- Four pill files deleted; ChatManager pill plumbing removed; `_chatListSyncing` + duplicate-sync guard preserved.
- No dangling `ChatListSyncIndicator` references; EditMode suite green FRESH at 1118 (1124 − 6).
</verification>

<success_criteria>
The «Синхронизация…» pill is gone everywhere (scene + scripts + gate + builder + tests + events + getter); the Telegram list shows no pill (the 08-19 cover is the sync affordance); WhatsApp byte-identical; suite green FRESH at 1118. Verified on device at 08-21.
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-20-SUMMARY.md` including the consumer-boundary grep (sole subscriber was ChatListSyncIndicator), the preserved-`_chatListSyncing` evidence, and the 1124 − 6 = 1118 baseline arithmetic.
</output>
