---
phase: 08-device-uat-milestone-closeout
plan: 14
type: execute
wave: 5
depends_on: []
files_modified:
  - Assets/Scripts/UI/EmptyStateView.cs
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Tapping the create-bot CTA on the Telegram empty state opens the Add-Bot overlay (AddBotPanel) — it no longer does nothing"
    - "The Add-Bot form opens with Telegram PRESELECTED when launched from the Telegram channel, WhatsApp preselected from the WhatsApp channel"
    - "The WhatsApp create-bot CTA behaviour is byte-identical to before"
  artifacts:
    - path: "Assets/Scripts/UI/EmptyStateView.cs"
      provides: "Channel-aware create-bot CTA (OpenCreateBotFlow preselects platform by ActiveChannel)"
      contains: "SelectPlatform"
  key_links:
    - from: "EmptyStateView create-bot CTA on the Telegram channel"
      to: "AddBotPanel open with Telegram preselected"
      via: "StartNewBot() + Manager.SelectPlatform(2) gated on ActiveChannel"
      pattern: "SelectPlatform"
---

<objective>
Close **D12 (medium, NEW)**: the Telegram empty-state create-bot CTA does NOTHING. Expected (owner): the SAME flow as the WhatsApp CTA, but with Telegram PRESELECTED in the add-bot form.

**Confirmed from code:** the only create-bot CTA is the `NoBotsExist` reason («Создать бота»), wired to `EmptyStateView.OpenCreateBotFlow`, which calls `BotsPage.StartNewBot()` then HARDCODES `Manager.Instance.SelectPlatform(1)` (1 = WhatsApp; 2 = Telegram, 3 = Both — confirmed from Manager.cs:272-274). So on the Telegram channel the CTA either does nothing the owner expects (opens with WhatsApp preselected) or is genuinely inert. The brief flags 05-10/05-12's Telegram empty-state branches as a possible unwired-handler source — Task 1 confirms the exact no-op cause.

Purpose: an owner viewing the Telegram channel's empty state can create a bot with Telegram already chosen — parity with the WhatsApp CTA.

Constraint: WhatsApp CTA byte-identical (the WhatsApp channel still preselects platform 1). Single-file change (`EmptyStateView.cs`), no scene mutation. Device confirmation rides the 08-16 checkpoint.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.claude/skills/unity-ui-builder/SKILL.md

<interfaces>
<!-- Extracted from the codebase. -->

// EmptyStateView.cs
//   ConfigureForReason(NoBotsExist): primaryButton.onClick += OpenCreateBotFlow  (channel-agnostic wiring)
//   OpenCreateBotFlow(): FindFirstObjectByType<BotsPage>(Include)?.StartNewBot(); Manager.Instance?.SelectPlatform(1);  // ← hardcoded WA
//   ApplyChannelAccent() runs at the tail of ConfigureForReason (recolors only; does not unwire)
//   OnDisable(): primaryButton.onClick.RemoveAllListeners(); _lastReason = null;   // re-wires on next enable

// BotsPage.StartNewBot(): switch to Bots tab (if needed) + AddBotPanel.Instance?.Open()
// AddBotPanel.Instance — resolves include-inactive (works before Screen_New activates)
// Manager.SelectPlatform(int mode): 1=WhatsApp, 2=Telegram, 3=Both  (Manager.cs:1057)
// ChatManager.Instance.ActiveChannel : ChatChannel  (WhatsApp | Telegram)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (D12): diagnose the no-op — confirm the reason, channel, and wiring path</name>
  <read_first>
    - Assets/Scripts/UI/EmptyStateView.cs — full: `ConfigureForReason` (all 3 reasons), `OpenCreateBotFlow`, `ApplyChannelAccent`, `OnEnable`/`OnDisable` wiring, `Show`/`Hide` (CanvasGroup interactable/blocksRaycasts).
    - Assets/Scripts/Main/BotsPage.cs — `StartNewBot` (does it open the overlay unconditionally?).
    - Assets/Scripts/Main/AddBotPanel.cs — `Instance` resolution + `Open()` (does it work when launched from the Telegram channel / Screen_Whatsapp active?).
    - Assets/Scripts/Main/ChatManager.BotState.cs (lines ~170-220) — which `EmptyStateReason` fires on the Telegram channel with no bots vs a bot-with-no-Telegram (confirm the create-bot CTA's reason is reachable on the TG channel).
    - Assets/Scripts/Main/Manager.cs (lines 1057-1089, 272-274) — `SelectPlatform` (mode 1/2/3) + how it's wired to the option buttons.
  </read_first>
  <action>
    Record (in the SUMMARY) the confirmed answers:
    1. Which `EmptyStateReason` surfaces the «Создать бота» CTA when the owner is on the Telegram channel? (Confirm `NoBotsExist` is reachable on the TG channel, and that its handler is `OpenCreateBotFlow`.)
    2. Is `OpenCreateBotFlow` actually invoked on tap on the TG channel — i.e. is the button wired + interactable (CanvasGroup `blocksRaycasts`/`interactable` true after `Show()`; the listener present after `ConfigureForReason`)? Check whether any TG-specific branch (05-10/05-12 accent path) leaves it unwired or non-interactive.
    3. Does `StartNewBot()` + `AddBotPanel.Instance?.Open()` open the overlay from the Telegram-channel context (Screen_Whatsapp active)? Does `SelectPlatform(1)` run and apply?
    Conclude whether the "does nothing" is (i) a genuine inert handler (unwired / non-interactive), or (ii) it DOES open but with WhatsApp preselected (owner reads "wrong platform" as "nothing"). State which, with the exact code evidence, so Task 2 fixes the real cause.
  </action>
  <acceptance_criteria>
    - The SUMMARY names the exact reason that surfaces the CTA on the TG channel and whether the handler is wired + interactable on that channel.
    - The SUMMARY states the confirmed no-op cause (inert handler vs opens-with-WhatsApp), with the code lines cited.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "OpenCreateBotFlow\|SelectPlatform\|NoBotsExist" Assets/Scripts/UI/EmptyStateView.cs (evidence the sites were located) ; full EditMode suite green (baseline, unchanged)</automated>
  </verify>
  <done>The real reason the TG create-bot CTA appears to do nothing is confirmed and the fix specified.</done>
</task>

<task type="auto">
  <name>Task 2 (D12): channel-aware create-bot CTA (Telegram preselected from the Telegram channel)</name>
  <read_first>
    - Assets/Scripts/UI/EmptyStateView.cs — `OpenCreateBotFlow` (the site to change) + the `ConfigureForReason(NoBotsExist)` wiring.
    - The Task-1 SUMMARY — the confirmed no-op cause + specified fix.
    - Assets/Scripts/Main/Manager.cs (SelectPlatform) — confirm mode 2 = Telegram.
  </read_first>
  <action>
    In `EmptyStateView.OpenCreateBotFlow`, replace the hardcoded `Manager.Instance.SelectPlatform(1)` with a channel-aware preselect:
    ```csharp
    // Preselect the platform for the channel the empty state surfaced on: Telegram (2) from the
    // Telegram channel, WhatsApp (1) otherwise. StartNewBot switched to the form synchronously,
    // so the page is active and SelectPlatform's UI updates apply.
    int platform = (ChatManager.Instance != null
                    && ChatManager.Instance.ActiveChannel == ChatChannel.Telegram) ? 2 : 1;
    if (Manager.Instance != null) Manager.Instance.SelectPlatform(platform);
    ```
    Keep `BotsPage.StartNewBot()` exactly as-is (it opens the overlay). If Task 1 found a GENUINE inert-handler cause (unwired for the TG-surfaced reason, or `Show()` not making the button interactable on the TG path), ALSO fix that (e.g. ensure `ConfigureForReason(NoBotsExist)` wires `OpenCreateBotFlow` and the button is interactable on the TG channel) — implement per the confirmed cause. Do NOT change the WhatsApp path (WhatsApp still resolves to `platform = 1`, byte-identical). No scene change.

    Guard: `ChatChannel` is available to `EmptyStateView` (already used by `ApplyChannelAccent`), so the enum reference compiles.
  </action>
  <acceptance_criteria>
    - `grep -q "SelectPlatform(platform)" Assets/Scripts/UI/EmptyStateView.cs` AND `grep -q "ActiveChannel == ChatChannel.Telegram" Assets/Scripts/UI/EmptyStateView.cs` in `OpenCreateBotFlow`
    - `grep -c "SelectPlatform(1)" Assets/Scripts/UI/EmptyStateView.cs` returns 0 (the hardcoded WA preselect is gone)
    - `grep -q "StartNewBot" Assets/Scripts/UI/EmptyStateView.cs` (the overlay-open call is preserved)
    - Full EditMode suite green (≥1093) — no MonoBehaviour glue regressions.
  </acceptance_criteria>
  <verify>
    <automated>In-Editor bridge OR Tools/run-tests-headless.sh (full suite) — 0 failed</automated>
  </verify>
  <done>The Telegram create-bot CTA opens the Add-Bot overlay with Telegram preselected; WhatsApp CTA byte-identical. Device confirm rides 08-16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Empty-state CTA tap → Add-Bot overlay | UI action crossing into the bot-creation flow; must open the correct preselected platform |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14-01 | Tampering | changing OpenCreateBotFlow regresses the WhatsApp CTA | mitigate | WhatsApp channel still resolves `platform = 1` (byte-identical); only the Telegram channel changes to 2. |
| T-08-14-02 | Denial of Service | a null ChatManager/Manager crashing the CTA | accept | Every access null-guarded (`ChatManager.Instance != null`, `Manager.Instance != null`); no-op if unavailable. |
</threat_model>

<verification>
- EditMode suite green (≥1093); grep acceptance passes; WhatsApp preselect path unchanged.
- Device re-verify (08-16): on the Telegram channel with no bots, «Создать бота» opens the Add-Bot form with Telegram preselected; on WhatsApp it still preselects WhatsApp.
</verification>

<success_criteria>
- The Telegram empty-state create-bot CTA opens the Add-Bot overlay with Telegram preselected; WhatsApp byte-identical.
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-14-SUMMARY.md` with the confirmed no-op cause, the channel-aware preselect change, and any inert-handler fix applied.
</output>
