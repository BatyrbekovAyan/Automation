---
phase: 08-device-uat-milestone-closeout
plan: 15
type: execute
wave: 6
depends_on: [11]
files_modified:
  - Assets/Scripts/Main/ChatManager.cs
  - Assets/Scripts/Chat/MediaDownloadFailure.cs
  - Assets/Tests/Editor/Chat/MediaDownloadFailureTests.cs
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "A failed message/media/download logs actionable device evidence: message id, HTTP status, classified reason, and a SIZE-CAPPED response snippet (never a full payload)"
    - "A transient download failure is retried ONCE within the strictly-serial queue (no concurrent request added) before surfacing as a retryable error"
    - "A failure surfaces as the existing tap-to-retry error card, never an infinite spinner; the serial media queue invariant is preserved"
  artifacts:
    - path: "Assets/Scripts/Chat/MediaDownloadFailure.cs"
      provides: "Pure classifier (network/timeout, HTTP error, no-link-in-response) + capped log formatter"
      contains: "Classify"
    - path: "Assets/Tests/Editor/Chat/MediaDownloadFailureTests.cs"
      provides: "Classifier + capped-formatter coverage"
      contains: "Classify"
  key_links:
    - from: "DownloadMediaRoutine failure branches"
      to: "capped device log + one serial retry + onFailure error card"
      via: "MediaDownloadFailure.Classify / FormatLog"
      pattern: "MediaDownloadFailure"
---

<objective>
Close **D11 (medium, NEW)**: SOME video files never download (incl. GIFs and video notes); the owner suspects a Wappi/tapi server-side cause. **INSTRUMENT FIRST** so the next device pass produces actionable evidence, then add the justified client-side resilience.

**Confirmed from code:** `ChatManager.DownloadMediaRoutine` (ChatManager.cs:1987) has THREE silent failure exits that call `onFailure?.Invoke()` with NO diagnostics: (1) HTTP `!Success` (line 2001-2004), (2) a 200 whose body has neither `file_link` nor `file_b64` (line 2015), (3) a JSON parse exception (line 2017). So a device failure gives the owner nothing to act on. The download queue is STRICTLY SERIAL by design (Wappi cross-serves concurrent requests — memory `wappi-media-download-crossing`); the fix must NEVER add a concurrent request. 08-REVIEW IN-03 notes a pre-existing full-payload `response.txt` dump in `SyncAllChats` — do NOT replicate that pattern; scope any new logging tightly (size-capped, no file write).

Purpose: the device pass captures WHY specific videos fail (expired s3 link? size cap? media type? HTTP status?), and transient failures get one graceful retry + a clean error instead of a silent dead bubble.

Constraint: the serial media-download queue is preserved (no parallelization); WhatsApp + Telegram share this path and both benefit; no full-payload logging. The DEEPER server-side diagnosis rides the 08-16 checkpoint (owner captures the new logs on device + files a Wappi ticket if server-side).
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.planning/phases/08-device-uat-milestone-closeout/08-REVIEW.md
@.claude/rules/networking.md
@.claude/skills/chat-data-flow/SKILL.md

<interfaces>
<!-- Extracted from the codebase. -->

// ChatManager.cs — the serial media-download worker (DO NOT parallelize)
private readonly Queue<(string messageId, Action<string> onSuccess, Action onFailure)> _mediaDownloadQueue;
IEnumerator DrainMediaDownloadQueue()  // one DownloadMediaRoutine at a time
IEnumerator DownloadMediaRoutine(string messageId, Action<string> onSuccess, Action onFailure) {
    // GET WappiEndpoints.Sync(ActiveChannel, "message/media/download?profile_id=..&message_id=..")
    // www.timeout = 30 ; yield SendWebRequest();
    // if (!Success)                          { onFailure?.Invoke(); yield break; }   // (1) SILENT
    // parse: file_link → onSuccess(link) ; else file_b64 → onSuccess("base64://"+b64) ; else onFailure()  // (2) SILENT
    // catch { onFailure?.Invoke(); }                                                  // (3) SILENT
}

// www.responseCode (long HTTP status), www.result (Success/ConnectionError/ProtocolError/DataProcessingError),
// www.error (string), www.downloadHandler.text (JSON body — CAP before logging)

// Pure-seam precedent: Assets/Scripts/Chat/ (OpenChatLivePollGate etc.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (D11): instrument the three failure exits with capped, actionable diagnostics</name>
  <read_first>
    - Assets/Scripts/Main/ChatManager.cs (lines 1946-2018) — `DownloadMediaForMessage`, `DrainMediaDownloadQueue`, `ClearMediaDownloadQueue`, `DownloadMediaRoutine` (the three `onFailure` exits + the success parse).
    - Assets/Scripts/Main/ChatManager.cs (lines 466-471, 632-635) — the pre-existing `response.txt` full dumps (IN-03) — the anti-pattern to AVOID (no file write, no full payload).
    - Assets/Scripts/Chat/WappiEndpoints.cs — the download URL shape (channel-aware).
    - .claude/rules/networking.md — the logging convention (status code + context).
    - Assets/Tests/Editor/Chat/ — a nearby pure-classifier test for style.
  </read_first>
  <action>
    Create `Assets/Scripts/Chat/MediaDownloadFailure.cs` (pure, UnityEngine-free):
    - `public enum MediaDownloadFailureKind { NetworkOrTimeout, HttpError, NoLinkInResponse, ParseError }`
    - `public static MediaDownloadFailureKind Classify(bool resultIsSuccess, long httpStatus, bool hasFileLink, bool hasFileB64)` — `!resultIsSuccess && httpStatus==0` ⇒ NetworkOrTimeout; `!resultIsSuccess` ⇒ HttpError; success but `!hasFileLink && !hasFileB64` ⇒ NoLinkInResponse; else the caller does not call this.
    - `public const int MaxSnippet = 256;`
    - `public static string Snippet(string body)` — first `MaxSnippet` chars of `body` (null-safe, single-line: strip newlines) — NEVER the full body.
    - `public static string FormatLog(string messageId, long httpStatus, MediaDownloadFailureKind kind, string bodySnippet)` — one compact line, e.g. `"[MediaDownload] FAIL id={messageId} http={httpStatus} kind={kind} body={bodySnippet}"`.

    Wire into `DownloadMediaRoutine` at each of the three failure exits — a SIZE-CAPPED `Debug.LogWarning(MediaDownloadFailure.FormatLog(messageId, www.responseCode, kind, MediaDownloadFailure.Snippet(www.downloadHandler?.text)))` BEFORE the `onFailure?.Invoke()`:
    - Exit (1) HTTP `!Success`: `Classify(false, www.responseCode, false, false)`.
    - Exit (2) empty link/b64: `Classify(true, www.responseCode, false, false)` ⇒ NoLinkInResponse.
    - Exit (3) parse catch: log `ParseError` with the capped snippet.
    This is DEVICE-VISIBLE (logcat) but capped — no `File.WriteAllText`, no full payload. Do NOT touch the pre-existing `response.txt` dumps.

    Tests in NEW `MediaDownloadFailureTests.cs`: `Classify(false,0,..)==NetworkOrTimeout`; `Classify(false,404,..)==HttpError`; `Classify(true,200,false,false)==NoLinkInResponse`; `Snippet` caps at 256 + strips newlines + null-safe; `FormatLog` contains id/http/kind and NOT more than `MaxSnippet` of body.
  </action>
  <acceptance_criteria>
    - `grep -q "public static MediaDownloadFailureKind Classify" Assets/Scripts/Chat/MediaDownloadFailure.cs` AND `grep -q "MaxSnippet = 256" Assets/Scripts/Chat/MediaDownloadFailure.cs`
    - `grep -q "using UnityEngine" Assets/Scripts/Chat/MediaDownloadFailure.cs` returns NOTHING (pure)
    - `grep -c "MediaDownloadFailure" Assets/Scripts/Main/ChatManager.cs` returns ≥ 3 (all three exits instrumented)
    - The added logging does NOT write a file: `grep -n "WriteAllText" ` shows no NEW occurrence inside `DownloadMediaRoutine` (the only dumps stay the pre-existing SyncAllChats ones).
    - `grep -c "\[Test\]" Assets/Tests/Editor/Chat/MediaDownloadFailureTests.cs` returns ≥ 5
  </acceptance_criteria>
  <verify>
    <automated>In-Editor bridge OR Tools/run-tests-headless.sh "MediaDownloadFailure" — all pass; full suite green</automated>
  </verify>
  <done>Every media-download failure logs a capped, actionable line (id + http status + reason + snippet) with no full-payload dump; the device pass can now show WHY videos fail.</done>
</task>

<task type="auto">
  <name>Task 2 (D11): one serial-safe retry on transient failure + clean error surfacing</name>
  <read_first>
    - Assets/Scripts/Main/ChatManager.cs (lines 1966-2018) — `DrainMediaDownloadQueue` (the serial worker) + `DownloadMediaRoutine`.
    - Assets/Scripts/Chat/MediaDownloadFailure.cs — the `Classify` kinds from Task 1 (retry only transient kinds).
    - The consumer of `onFailure` (the bubble's retry card) — grep `DownloadMediaForMessage` callers to confirm `onFailure` already renders a tap-to-retry state (no infinite spinner).
    - memory note: `wappi-media-download-crossing` (serial queue is mandatory — a retry must NOT be a concurrent request).
  </read_first>
  <action>
    Add a BOUNDED, SERIAL-SAFE retry INSIDE `DownloadMediaRoutine` (never a second concurrent request — the drain worker stays blocked on this coroutine, so re-issuing here keeps strict seriality):
    - On a TRANSIENT failure only (`MediaDownloadFailure.Classify(...)` == `NetworkOrTimeout`, or `HttpError` with `www.responseCode >= 500`), retry the SAME request ONCE after a short `yield return new WaitForSeconds(1.5f)` backoff. A permanent failure (`NoLinkInResponse`, HTTP 4xx, `ParseError`) does NOT retry — it surfaces immediately (likely the server-side cause the owner suspects; the Task-1 log captures it).
    - After the single retry still fails, log the capped line (Task 1 format) and call `onFailure?.Invoke()` exactly once — the existing tap-to-retry error card renders (the `www.timeout = 30` already prevents an infinite spinner). Preserve the `using UnityWebRequest` disposal (a retry uses a fresh `using` request).
    - Keep `DrainMediaDownloadQueue` one-at-a-time; do NOT add any `StartCoroutine` that would run a download in parallel. Confirm `ClearMediaDownloadQueue` on bot/channel switch still cancels cleanly (the retry's `WaitForSeconds` is killed by `StopAllCoroutines`).

    DEVIATION RULE: this is conservative resilience pending device evidence. If the 08-16 device capture shows a SPECIFIC server cause (e.g. expired s3 `file_link` on a 200, or a size cap), a follow-up may add a targeted handler (e.g. re-request by id) — do NOT speculatively build that here.
  </action>
  <acceptance_criteria>
    - `DownloadMediaRoutine` retries at most ONCE (read the method: a single retry guard, e.g. a `bool retried` or an attempt counter capped at 2 total attempts) and only for transient kinds.
    - `grep -c "StartCoroutine" ` inside `DrainMediaDownloadQueue`/`DownloadMediaRoutine` shows NO new concurrent download start (the retry is inline `yield return`, not a parallel coroutine).
    - `grep -qi "WaitForSeconds" Assets/Scripts/Main/ChatManager.cs` in the retry path (bounded backoff).
    - `onFailure` is invoked exactly once per exhausted request (no double-invoke across the retry) — verify by reading the control flow.
    - Full EditMode suite green (≥1093 + Task-1 tests).
  </acceptance_criteria>
  <verify>
    <automated>In-Editor bridge OR Tools/run-tests-headless.sh (full suite) — 0 failed; MediaDownloadFailure tests pass</automated>
  </verify>
  <done>Transient media-download failures get one serial-safe retry then a clean retryable error; the serial-queue invariant is intact; permanent failures surface immediately with a captured log. Server-side diagnosis rides 08-16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tapi/Wappi `message/media/download` response → media bubble | Untrusted/failed responses (bad link, wrong file, HTTP error) must fail safe, not silently or by crossing |
| Diagnostic logging → device log | Must not leak a full media payload or write a plaintext artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15-01 | Information Disclosure | logging the download response | mitigate | `MediaDownloadFailure.Snippet` caps at 256 chars, single-line, no file write; do NOT touch/extend the pre-existing `response.txt` dumps (IN-03). |
| T-08-15-02 | Tampering | a retry introducing a concurrent request (Wappi file crossing) | mitigate | Retry is inline within the serial `DownloadMediaRoutine` (worker stays blocked); no new parallel `StartCoroutine`; `ClearMediaDownloadQueue` still cancels on switch. |
| T-08-15-03 | Denial of Service | unbounded retries hanging the queue | mitigate | At most ONE retry, transient-only, with `timeout=30` + a 1.5s backoff; permanent failures surface immediately. |
</threat_model>

<verification>
- EditMode suite green (≥1093) incl. `MediaDownloadFailureTests`.
- grep confirms: three instrumented exits, no new file write, no new concurrent download, one bounded retry.
- Device re-verify (08-16): reproduce a failing video/GIF/note → the logcat shows a capped `[MediaDownload] FAIL …` line (id + http + kind); the bubble shows a retryable error, not a dead spinner. Owner captures the logs for the server-side call.
</verification>

<success_criteria>
- Failing media downloads are diagnosable on device (capped logs) and transient ones self-retry once; the serial queue is preserved; no full-payload logging.
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-15-SUMMARY.md` with the instrumentation added, the retry policy, and the EXACT device capture ask for 08-16 (reproduce a failing video/GIF/note, copy the `[MediaDownload] FAIL …` lines).
</output>
