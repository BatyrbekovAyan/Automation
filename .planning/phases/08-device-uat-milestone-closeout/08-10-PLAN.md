---
phase: 08-device-uat-milestone-closeout
plan: 10
type: execute
wave: 4
depends_on: [04, 05, 06, 07, 08, 09]
files_modified: []
autonomous: false
gap_closure: true
requirements: []

must_haves:
  truths:
    - "The owner re-verifies every D1–D9 fix on ONE device build in a single ordered pass"
    - "Each re-verify item maps back to its exact 08-DEVICE-UAT.md runbook anchor and defect id"
    - "A FAIL spins a further gap-closure round; an all-PASS flips Gate A to PASS"
  artifacts:
    - path: ".planning/phases/08-device-uat-milestone-closeout/08-10-SUMMARY.md"
      provides: "Recorded device re-verify verdicts + Gate A disposition"
      contains: "Gate A"
  key_links:
    - from: "08-10 re-verify verdicts"
      to: "08-DEVICE-UAT.md Defects D1–D9 + Overall line"
      via: "owner records PASS/FAIL per item"
      pattern: "D1"
---

<objective>
The consolidated owner-run device RE-VERIFY of the six gap-closure plans (08-04..08-09) on ONE build, one pass — do NOT scatter per-plan device gates. This plan closes the loop opened by Gate A (08-DEVICE-UAT.md, Overall = ISSUES): every defect D1–D9 gets re-tested on a real device build, and the result dispositions Gate A.

Purpose: a single, unambiguous device pass that confirms all nine defects are fixed (or files any residual as a further gap). The autonomous fixes cannot be device-verified by Claude (no device build, no live WhatsApp/Telegram infra, no dev-n8n session in this environment) — running this pass is the owner gate.

Output: `08-10-SUMMARY.md` recording each verdict + the Gate A disposition; on all-PASS, `08-DEVICE-UAT.md` Overall flips PASS and the milestone-close mechanics (Gate B → Gate C) proceed. On any FAIL, that item spins its own `/gsd-plan-phase 08 --gaps` round.

Constraint: this is a `checkpoint:human-verify` gate — no app code changes here. Groups H (live «Вместе» + RAG) need the shared dev-n8n session + cloudflared tunnel (run `Tools/n8n/rotate-tunnel.py` first); prod bagkz stays dormant.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.planning/phases/08-device-uat-milestone-closeout/08-04-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-05-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-06-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-07-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-08-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-09-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Owner device re-verify of D1–D9 on one build</name>
  <action>Owner-run gate (not executor work): build ONE device build with 08-04..08-09 merged, then re-verify each D1–D9 item below against its 08-DEVICE-UAT.md anchor and record PASS/FAIL/N/A. Do not tick on the owner's behalf.</action>
  <what-built>
    Six gap-closure plans landed autonomously and are EditMode-green:
    - 08-04 (D5): open-chat live poll — incoming renders in the open chat + «Вместе» refresh + fresh suggestions payload, both channels.
    - 08-05 (D7): TG service-dialog dedup (`ChatIdFormat.CanonicalKey`) + cross-channel cache isolation.
    - 08-06 (D1+D2): Telegram allowed-reaction set (`TelegramReactionCatalog`) + clean 400 revert + removal tombstone.
    - 08-07 (D3): incoming video-note bubble-free transparency + duration-badge rounded-corner refresh.
    - 08-08 (D4+D6): SwipeToDelete lifecycle null-guard (no NRE) + TG swipe affordance removed.
    - 08-09 (D8+D9): RU empty-state copy + Telegram chat-list sync indicator.
    Build ONE device build (Android primary) off the current tree with all six merged. Read each plan's SUMMARY for the confirmed root cause + what to look for.
  </what-built>
  <how-to-verify>
    Prepare: a real device build, an authorized dev Telegram profile (incl. the 2FA account), and — for the live «Вместе»/RAG item only — dev n8n at localhost:5678 + a cloudflared tunnel with `Tools/n8n/rotate-tunnel.py` run first. Prod bagkz stays dormant. Record each verdict against its 08-DEVICE-UAT.md anchor:

    1. **D5 — live incoming (BOTH channels)** [08-DEVICE-UAT I.1 #3 / I.2 #6 / H2]:
       Open a chat, keep it open, have someone send a message → it renders WITHIN ~one refresh cycle without re-entering, on BOTH WhatsApp and Telegram. With «Вместе» open: the cards refresh on that incoming, and (dev-n8n up, RAG-seeded TG bot) a suggestion is relevant to the newest incoming AND grounded in the bot's catalog (re-tests H2 relevance + RAG together). Also confirm the draft-protection half of I.1 #3 (typing a draft, an incoming refreshes cards but does NOT overwrite the draft).
    2. **D7 — TG service dialog** [Extra #3 / CHAT-11]: the Telegram service dialog (login codes / 777000) shows exactly ONE row on Telegram (no logo-avatar + silhouette duplicate) and does NOT appear on the WhatsApp list. Confirm no real WhatsApp chat disappeared.
    3. **D1 — TG reactions add** [B9a]: long-press a Telegram message, tap several quick-bar emoji and several "+" picker emoji → they succeed (no HTTP 400 REACTION_INVALID on normal picks). WhatsApp reaction picker/bar unchanged.
    4. **D2 — TG reaction remove** [B9b / B13]: add a reaction, then remove it (in-app and via the Telegram app) → it clears in-app and STAYS cleared across a refresh cycle (no resurrection).
    5. **D3a — badge corners** [B5]: send/receive a кружок → the duration badge's left/right corners are fully rounded (not sharp).
    6. **D3b — incoming note float** [E1]: in the new Telegram bot, an INCOMING video note floats bubble-free (no white/coloured bubble behind the circle).
    7. **D4 — no TG swipe** [B12 / F8]: swipe a Telegram chat row → NO delete affordance slides in. Then confirm a WhatsApp row still swipe-deletes.
    8. **D6 — bot-creation NRE** [Extra #1]: create a bot → auto-return to the Bots page → NO NullReferenceException (watch logcat/console).
    9. **D8 — RU copy** [F9]: the empty-state screens (create-bot, WhatsApp-not-connected, Telegram-not-connected) read in Russian.
    10. **D9 — TG sync indicator** [Extra #2/O1]: opening the Telegram chat list shows a sync/loading indicator during its initial load/refresh (WhatsApp-parity cue).
    11. **(optional) B7 — static webp sticker** [B7]: if a static `image/webp` sticker is at hand, confirm it renders via the existing Sticker path (was N/A at Gate A).

    For each: record PASS / FAIL / N/A. On any FAIL, add/append the row in 08-DEVICE-UAT.md's Defects table with the anchor and run `/gsd-plan-phase 08 --gaps` for that item. On all-PASS, set 08-DEVICE-UAT.md Overall = PASS (Gate A closes) and re-aggregate the re-deferred I.3 #10 (01-VERIFICATION sign-off).
    Reminder (carried from Gate A): deactivate the dev test clone once the window closes (bot-activation policy — clones run against real contacts).
  </how-to-verify>
  <resume-signal>Reply "all pass" (Gate A → PASS, proceed to Gate B prod replication), or list the failing item numbers (each spins a gap-closure plan).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner device build → live WhatsApp/Telegram + dev-n8n | The only environment where these client fixes can be exercised end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10-01 | Repudiation | verdicts recorded incorrectly / on the owner's behalf | mitigate | Owner-run gate; each verdict maps to a named 08-DEVICE-UAT anchor; ticks are transcribed from the owner's stated results only. |
| T-08-10-02 | Elevation | an active bot clone left running against real contacts | mitigate | Explicit reminder to deactivate the dev test clone after the window (bot-activation policy); prod bagkz stays dormant. |
</threat_model>

<verification>
- All six fix plans are EditMode-green and merged into the one device build before the pass.
- Each of the 10 (+1 optional) items has a recorded verdict tied to its 08-DEVICE-UAT.md anchor.
- Gate A disposition (PASS or gaps-filed) recorded in 08-10-SUMMARY.md and 08-DEVICE-UAT.md Overall.
</verification>

<success_criteria>
- One device build, one ordered pass, every D1–D9 re-verified.
- All-PASS → Gate A PASS → Gate B/C proceed; any FAIL → its own gap-closure plan.
</success_criteria>

<output>
After the owner records results, create `.planning/phases/08-device-uat-milestone-closeout/08-10-SUMMARY.md` with the per-item verdicts, the Gate A disposition, and any newly filed defects.
</output>
