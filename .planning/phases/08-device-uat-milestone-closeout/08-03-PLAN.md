---
phase: 08-device-uat-milestone-closeout
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - .planning/phases/08-device-uat-milestone-closeout/08-MILESTONE-CLOSE.md
autonomous: false
requirements: []   # closeout mechanics — owns no REQ-ID; rolls carried items (SUPPRESS-01) forward, marks PROD-01 validated once 08-02 executed
user_setup: []

must_haves:
  truths:
    - "The close checklist names the two blocking gates (08-01 device UAT PASS, 08-02 prod copy executed OR explicitly deferred) and the exact mechanics to mark v1.1 complete (`/gsd-complete-milestone`), so the owner can close the milestone in the right order without guessing."
    - "Carried-forward open items roll forward explicitly: SUPPRESS-01 (server-side «Вместе» suppression) → v1.2 Phase 9; any re-deferred v1.0 UAT scenario → STATE Deferred Items; v2 polish stays in backlog."
    - "The v1.1 Active requirements that are now validated are enumerated for the PROJECT.md Active→Validated move, and the pre-close artifact-audit disposition for the still-open UAT gates is spelled out."
  artifacts:
    - path: ".planning/phases/08-device-uat-milestone-closeout/08-MILESTONE-CLOSE.md"
      provides: "Gated milestone-close checklist + carried-forward roll-forward + Active→Validated list, pointing at /gsd-complete-milestone"
      contains: "gsd-complete-milestone"
  key_links:
    - from: "08-MILESTONE-CLOSE.md gate A"
      to: "08-DEVICE-UAT.md Overall = PASS"
      via: "blocking-gate reference"
      pattern: "08-DEVICE-UAT"
    - from: "08-MILESTONE-CLOSE.md gate B"
      to: "08-PROD-REPLICATION.md executed or deferred"
      via: "blocking-gate reference"
      pattern: "08-PROD-REPLICATION"
    - from: "08-MILESTONE-CLOSE.md roll-forward"
      to: "SUPPRESS-01 → v1.2 Phase 9"
      via: "explicit carry-forward"
      pattern: "SUPPRESS-01"
---

<objective>
Author `08-MILESTONE-CLOSE.md`: the gated checklist + mechanics that let the owner formally close milestone v1.1 Telegram Parity once the two device/deploy gates are green. This is close PREP — it does NOT flip the milestone itself; the owner runs `/gsd-complete-milestone` after the gates pass. It exists so the close is done in the correct order, with every carried item rolled forward explicitly and nothing silently dropped.

Purpose: v1.1 shipped 5 code-complete phases; closing it correctly means (a) confirming the two owner sign-offs, (b) running the canonical close mechanics, and (c) rolling the deliberately-deferred items forward. A written, gated checklist prevents a premature or lossy close.

Output: `08-MILESTONE-CLOSE.md`. Writing it is autonomous; CONFIRMING the gates + running the close is the owner gate (Task 2).

Read first: `08-CONTEXT.md §Decisions → "08-03"` (+ §Deferred Ideas), `.claude/get-shit-done/workflows/complete-milestone.md` (the mechanics this checklist points at), and `PROJECT.md` §Requirements (the Active bullets that move to Validated).
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONES.md
@.planning/phases/08-device-uat-milestone-closeout/08-CONTEXT.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/complete-milestone.md

<constraints>
- DOC-ONLY. Do NOT flip PROJECT.md Active→Validated, archive phases, edit MILESTONES.md/ROADMAP, or run `/gsd-complete-milestone` yourself — those are the owner's post-sign-off actions (the checklist DESCRIBES them; the `/gsd-complete-milestone` command DOES them). This plan only produces the checklist.
- Reference the sibling deliverables by their real filenames (`08-DEVICE-UAT.md`, `08-PROD-REPLICATION.md`) — they exist by this wave (depends_on 01, 02).
- No secrets in the doc.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the gated 08-MILESTONE-CLOSE.md checklist</name>
  <files>.planning/phases/08-device-uat-milestone-closeout/08-MILESTONE-CLOSE.md</files>
  <action>
Create `08-MILESTONE-CLOSE.md` with these sections:

**Blocking gates (must be green before close):**
- **Gate A — Device UAT:** `08-DEVICE-UAT.md` Overall = PASS (or every FAIL closed by a gap-closure plan). ☐
- **Gate B — Prod replication:** `08-PROD-REPLICATION.md` executed (go/no-go GREEN) OR explicitly deferred by the owner with a recorded reason. ☐
Note that both gates are owner-run and this checklist cannot proceed until both are dispositioned.

**Pre-close artifact-audit disposition:** `/gsd-complete-milestone` runs `audit-open` first; the still-open UAT gate docs (04/05/06/07-HUMAN-UAT, 05/01-VERIFICATION `human_needed`) WILL surface. State the disposition rule: mark each resolved once 08-01 records its PASS for that surface; **acknowledge-and-defer** the residual live-server-only items (with reasons) rather than blocking — the acknowledge path writes them to STATE.md Deferred Items + notes the count in the MILESTONES.md entry.

**Active → Validated (PROJECT.md):** enumerate the v1.1 Active bullets that become Validated on close (Telegram chat client at parity; in-screen channel switcher; Telegram bots converse via tapi template; «Вместе» suggestions on Telegram; Telegram 2FA authorize; «Сводка» includes Telegram; tapi live-shape capture). Mark **PROD-01** (prod bagkz replication) Validated IF Gate B executed, else carry it forward.

**Carried-forward (roll forward explicitly — do NOT drop):**
- **SUPPRESS-01** — server-side «Вместе» suppression → **v1.2 Phase 9** (already on the ROADMAP; confirm it survives the ROADMAP reorg).
- Any **re-deferred v1.0 UAT scenario** from 08-DEVICE-UAT group I (01/02-HUMAN-UAT, 01-VERIFICATION) → STATE.md Deferred Items with its reason.
- **v2 polish backlog** (real `.tgs` Lottie animation, incoming-reaction chat-list preview, per-channel «Вместе» default) + the standing v1.0 deferrals (FB-01, FB-02, POL-01) → stay in backlog.
- The **webhook header-auth hardening** flagged in 08-PROD-REPLICATION → carry as a pre-real-prod-traffic item.

**Close mechanics (owner runs after gates green):** point at `/gsd-complete-milestone` and summarize what it does (pre-close audit → PROJECT.md full evolution review + Active→Validated → ROADMAP reorg with milestone grouping → `gsd-sdk query milestone.complete "v1.1" --name "Telegram Parity"` archival → RETROSPECTIVE.md update → git tag `v1.1`). Do NOT duplicate the mechanics — reference the workflow file.

**Owner result block:** ☐ Gate A PASS ☐ Gate B PASS/DEFERRED ☐ `/gsd-complete-milestone` run ☐ v1.1 tagged; plus a Notes area.
  </action>
  <verify>
    <automated>D=.planning/phases/08-device-uat-milestone-closeout/08-MILESTONE-CLOSE.md; test -f "$D" && grep -q "08-DEVICE-UAT" "$D" && grep -q "08-PROD-REPLICATION" "$D" && grep -q "gsd-complete-milestone" "$D" && grep -q "SUPPRESS-01" "$D" && grep -qi "Validated" "$D" && grep -q "v1.1" "$D" && grep -qi "defer" "$D" && echo OK</automated>
  </verify>
  <done>`08-MILESTONE-CLOSE.md` exists with both blocking gates referencing the sibling runbooks, the pre-close audit disposition, the Active→Validated list, the explicit SUPPRESS-01 / re-deferred-v1.0 / v2-polish roll-forward, and a pointer to `/gsd-complete-milestone` (mechanics not duplicated).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner confirms both gates then runs the milestone close</name>
  <action>Owner-run gate (not executor work): confirm Gate A (device UAT PASS) + Gate B (prod copy done/deferred), then `/clear` and run `/gsd-complete-milestone` for v1.1. Claude does not flip the milestone.</action>
  <what-built>The gated milestone-close checklist `08-MILESTONE-CLOSE.md`.</what-built>
  <how-to-verify>
1. Confirm Gate A (`08-DEVICE-UAT.md` Overall = PASS, or all FAILs closed by gap-closure plans) and Gate B (`08-PROD-REPLICATION.md` executed GREEN, or explicitly deferred with a reason).
2. If both gates are dispositioned, run `/clear` then `/gsd-complete-milestone` (v1.1 "Telegram Parity"). Let it run the pre-close audit, PROJECT.md evolution (Active→Validated), ROADMAP reorg, archival, retrospective, and the `v1.1` git tag.
3. Verify SUPPRESS-01 survived into v1.2 Phase 9 and any re-deferred v1.0 UAT landed in STATE Deferred Items.
  </how-to-verify>
  <resume-signal>Type "approved" once v1.1 is closed (or state which gate is still open / deferred).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning doc → repo | The close checklist is a committed markdown artifact; no runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Information Disclosure | `08-MILESTONE-CLOSE.md` content | mitigate | No secrets; references gates + requirement IDs + the close workflow by name only. |
| T-08-11 | Tampering / lossy close | dropping a deferred item at milestone boundary | mitigate | Roll-forward section enumerates SUPPRESS-01, re-deferred v1.0 UAT, and v2 polish explicitly; the checklist gates the close on both sign-offs so nothing closes prematurely. |
| T-08-12 | Repudiation | untracked close state | accept | The owner result block + `/gsd-complete-milestone`'s own git tag + MILESTONES.md entry provide the durable record; no new surface here. |
</threat_model>

<verification>
- `08-MILESTONE-CLOSE.md` exists; Task-1 grep chain returns OK (both gates, close mechanics pointer, SUPPRESS-01 roll-forward, Validated list, defer disposition).
- No planning file mutated beyond creating the checklist: `git status --porcelain` shows only the new doc (+ this plan's SUMMARY) for this plan.
</verification>

<success_criteria>
- The checklist gates the v1.1 close on the two owner sign-offs and points at the canonical `/gsd-complete-milestone` mechanics without duplicating them.
- Every deferred/carried item rolls forward explicitly; the Active→Validated set is enumerated.
- The plan is `autonomous: false`; the actual close is the owner gate (Task 2).
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-03-SUMMARY.md`.
</output>
