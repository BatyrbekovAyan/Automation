---
phase: 08-device-uat-milestone-closeout
plan: 13
type: execute
wave: 5
depends_on: []
files_modified:
  - Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json
  - Assets/Scripts/Chat/N8nSuggestionsProvider.cs
  - Assets/Tests/Editor/Chat/SuggestRepliesPayloadTests.cs
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "The WhatsApp «Вместе» path builds a payload that is channel-symmetric with the working Telegram path (same fresh transcript, correct botWaId, channel=whatsapp)"
    - "The canonical Suggest_Replies workflow's WhatsApp RAG/prompt branch grounds and prompts the LLM equivalently to the Telegram branch (no dropped RAG output, no wrong id filter)"
    - "verify-telegram-parity.py exits 0 against the edited canonical workflow"
  artifacts:
    - path: "Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json"
      provides: "Fixed WhatsApp-branch grounding/prompt so WA suggestions track the newest incoming"
      contains: "Retrieve RAG"
  key_links:
    - from: "WhatsApp branch (If skipRag? false → Retrieve RAG [botWaId] → Assemble → LLM)"
      to: "a grounded, relevant WhatsApp suggestion"
      via: "the located WA-branch fix (connection / filter / prompt)"
      pattern: "botWaId"
---

<objective>
Close **D10 (medium, NEW — split from D5/H2)**: on device, «Вместе» suggestions are RELEVANT on Telegram but IRRELEVANT on WhatsApp. Live refresh + draft protection PASS on both channels, and D5 (live-incoming render) is RESOLVED on both — so the transcript reaching the payload is fresh on both. The Telegram path is the working reference; diff the WhatsApp path against it end-to-end.

**DIAGNOSIS-FIRST.** The split has two candidate loci:
- **Client** (`N8nSuggestionsProvider.BuildPayloadJson`): the payload is built by ONE method for both channels — `botWaId = whatsappWorkflowId` ALWAYS, `channel = "whatsapp"|"telegram"` enum-derived, `profileId` channel-resolved, `messages`/`lastIncomingText` from the SAME `TryGetRecentMessages` live cache. Reading the code, the WA and TG inputs to the LLM (transcript + queryText) are identical — so the client is very likely symmetric. Task 1 CONFIRMS this (or finds a WA-only gap, e.g. `lastIncomingText` not populated on the WA incoming trigger).
- **Server** (`Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json`): the workflow branches — `Prep` computes `queryText` (lastIncoming or newest client msg) + `skipRag`; `If skipRag?` false → `Retrieve RAG` (filter `botWaId == $json.botWaId`); `If channel TG?` → `Retrieve RAG TG` (filter `botTgId`); both feed `Assemble` → `LLM`. The likely WA defect (Task 1 pins the exact one): (a) the WA `Retrieve RAG` output is not wired into `Assemble` (WA gets empty `ragChunks` while TG does), (b) the WA bot's chunks are stamped under a different id than `whatsappWorkflowId` so the `botWaId` filter returns nothing, (c) the branch ordering routes WA past `Assemble`, or (d) `Assemble`/`LLM` uses `queryText`/`messages` only on the TG path.

Purpose: WhatsApp suggestions track the newest incoming (and ground in the bot's catalog) exactly like Telegram.

Constraint: Telegram byte-identical (do not regress the working TG branch). The canonical JSON edit + `verify-telegram-parity.py` are autonomous; DEPLOYING to dev n8n (localhost:5678, owner-run) + the live relevance/RAG re-test are owner-gated and ride the 08-16 checkpoint. secrets.json is deny-ruled — no live calls here. Prod bagkz stays dormant.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.planning/phases/08-device-uat-milestone-closeout/08-04-SUMMARY.md
@Tools/n8n/README.md
@.claude/skills/unity-api-integration/SKILL.md

<interfaces>
<!-- Extracted from the codebase + workflow. -->

// N8nSuggestionsProvider.BuildPayloadJson (pure) — ONE builder both channels
//   profileId = isTelegram ? telegramProfileId : whatsappProfileId
//   botWaId   = whatsappWorkflowId  (ALWAYS)      botTgId = telegramWorkflowId
//   channel   = isTelegram ? "telegram" : "whatsapp"
//   messages  = ToWireMessages(recentMessages)   lastIncomingText = req.lastIncomingText
// Run(): msgs from cm.TryGetRecentMessages(req.chatId, 12, out msgs)  ← same live cache both channels

// Suggest_Replies.json nodes: Webhook → Prep → If invalid? → If skipRag? →(false) Retrieve RAG(botWaId filter)
//   ; If channel TG? →(true) Retrieve RAG TG(botTgId filter) ; → Assemble(ragChunks + messages) → LLM → Validate → Respond
// Prep: queryText = lastIncomingText || newest client message ; wfId = channel==tg? botTgId : botWaId ; skipRag = wfId ''/-1 || !queryText
// Retrieve RAG    filter: metadata botWaId = {{ $json.botWaId }}
// Retrieve RAG TG filter: metadata botTgId = {{ $json.botTgId }}

// verify-telegram-parity.py — the go/no-go gate; must exit 0 against the edited workflow
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (D10): diagnose — confirm client symmetry + locate the WhatsApp-branch defect</name>
  <read_first>
    - Assets/Scripts/Chat/N8nSuggestionsProvider.cs — `Run` + `BuildPayloadJson` + `ToWireMessages`; confirm WA vs TG inputs to the LLM (transcript + queryText) are identical, and check where `req.lastIncomingText` / `req.steerTowardText` are POPULATED (is the WA incoming-trigger path setting `lastIncomingText`?).
    - Assets/Scripts/Chat/SuggestionsController.cs — how a SuggestionRequest is built on an incoming (does WA set `lastIncomingText`?).
    - Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json — read the FULL `nodes` AND `connections` graph. Trace the WA path (Prep → If skipRag? [false] → Retrieve RAG → ? → Assemble → LLM) vs the TG path (→ If channel TG? [true] → Retrieve RAG TG → Assemble). Read the `Assemble` jsCode (how `ragChunks` + `messages` build the prompt) and the two RAG-node filters.
    - Tools/n8n/verify-telegram-parity.py — what parity it checks (so the fix keeps it green).
    - Tools/n8n/README.md — the canonical-file / import-by-id conventions.
  </read_first>
  <action>
    Produce a written diagnosis (in the SUMMARY) that answers, with evidence:
    1. Is the CLIENT payload channel-symmetric? Assert BuildPayloadJson feeds the WA path the same `messages` + `queryText`-driving fields as the TG path, and that `lastIncomingText` is populated identically on both channels' incoming triggers. If a WA-only client gap exists, name it.
    2. In the workflow `connections`: does `Retrieve RAG` (WA) feed `Assemble` the same way `Retrieve RAG TG` does? Trace node-by-node and state whether the WA branch's RAG output reaches `Assemble` (or is dropped / mis-ordered).
    3. Do the two RAG filters use the correct id? WA filters `botWaId`; confirm the WA bot's chunks are expected under `whatsappWorkflowId` (the id the client always sends as `botWaId`). If the WA branch filters a wrong/empty id, name it.
    4. Does `Assemble`/`LLM` use `queryText` + `messages` on BOTH paths (so relevance to the newest incoming isn't TG-only)?

    Conclude with the EXACT locus: `client:<field>` or `workflow:<node>.<field/connection>`, and the precise change Task 2 must make. If the diagnosis contradicts all four hypotheses, state the actual finding and the corrected fix.
  </action>
  <acceptance_criteria>
    - The SUMMARY names the exact defect locus (a specific node+field/connection in the workflow, or a specific client field) with the connections/jsCode evidence quoted.
    - The SUMMARY explicitly states whether the client payload is symmetric (WA vs TG transcript/queryText inputs).
    - A grep confirms the workflow was actually read: `grep -c "Retrieve RAG" Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json` ≥ 2 and the diagnosis cites the `connections` for `Retrieve RAG`.
  </acceptance_criteria>
  <verify>
    <automated>python3 Tools/n8n/verify-telegram-parity.py Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json ; echo "exit $?" (baseline — must be 0 before editing)</automated>
  </verify>
  <done>The WhatsApp-branch (or client) defect is located exactly, with the fix specified.</done>
</task>

<task type="auto">
  <name>Task 2 (D10): fix the located WhatsApp-branch defect in the canonical workflow (+ client if found)</name>
  <read_first>
    - Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json — the node/connection Task 1 located.
    - The Task-1 SUMMARY — the exact locus + specified change.
    - Assets/Tests/Editor/Chat/SuggestRepliesPayloadTests.cs — the payload-test style, if a client fix needs a test.
  </read_first>
  <action>
    Apply the fix Task 1 located, per confirmed cause (deviation rule: if Task 1 found a different cause, implement THAT and note it):
    - If **workflow (a) dropped WA RAG connection**: wire `Retrieve RAG` into `Assemble` mirroring the TG branch's connection so the WA path supplies `ragChunks` to `Assemble`.
    - If **workflow (b) wrong/empty id filter**: correct the WA `Retrieve RAG` metadata filter to `botWaId == {{ $json.botWaId }}` (or the exact id key Task 1 identified), matching how chunks are stamped.
    - If **workflow (c) branch ordering** or **(d) prompt uses queryText/messages TG-only**: correct the `connections` / `Assemble` jsCode so the WA path assembles the transcript + queryText into the prompt identically to TG.
    - If **client gap**: set `lastIncomingText` (or the missing field) on the WhatsApp incoming trigger in the controller/provider so the WA payload carries the newest incoming, and add/extend a `SuggestRepliesPayloadTests.cs` case asserting WA parity.

    Keep the Telegram branch byte-identical (the working reference). Preserve node ids and the `{name, nodes, connections, settings}` shape (the canonical file is imported by literal id — do NOT rename or restructure beyond the targeted fix). Do NOT deploy or call any live n8n/secrets here.
  </action>
  <acceptance_criteria>
    - `python3 Tools/n8n/verify-telegram-parity.py Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json` exits 0.
    - `python3 -c "import json;json.load(open('Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json'))"` succeeds (valid JSON).
    - The Telegram branch is unchanged: `grep -c "Retrieve RAG TG" Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json` and the `botTgId` filter are byte-identical to before (verify by diffing the TG nodes).
    - If a client fix was made: `grep -c "\[Test\]" Assets/Tests/Editor/Chat/SuggestRepliesPayloadTests.cs` increased and the full EditMode suite is green; otherwise `N8nSuggestionsProvider.cs` is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>python3 Tools/n8n/verify-telegram-parity.py Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json ; echo "exit $?" — must be 0 (+ EditMode suite green if a client test was added)</automated>
  </verify>
  <done>The WhatsApp branch grounds/prompts equivalently to Telegram; parity verifier green; Telegram unchanged. Live dev-n8n deploy + relevance re-test rides 08-16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client payload → Suggest_Replies webhook | Untrusted client fields drive the WA vs TG RAG branch + prompt |
| Suggest_Replies WA RAG filter → Supabase `documents` | The `botWaId` filter scopes a per-bot RAG read (must match the stamping key) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13-01 | Tampering | editing the workflow regresses the working Telegram branch | mitigate | Keep TG nodes/filters byte-identical; `verify-telegram-parity.py` gate must exit 0; diff the TG nodes before/after. |
| T-08-13-02 | Information Disclosure | WA RAG filter matching the wrong bot's chunks (cross-bot leak) | mitigate | Preserve the single-key `botWaId` filter invariant (RAG scoping architecture); never broaden the filter. |
| T-08-13-03 | Spoofing | deploying against prod/live n8n from this plan | accept | No live calls here — canonical file edit + offline verifier only; deploy is the owner-gated 08-16 step; prod dormant. |
</threat_model>

<verification>
- `verify-telegram-parity.py` exits 0 against the edited canonical workflow; JSON valid; TG branch unchanged.
- If a client fix was made, EditMode suite green incl. the new payload parity test.
- Device re-verify (08-16, dev-n8n up): WhatsApp «Вместе» suggestions track the newest incoming and ground in the catalog, matching Telegram.
</verification>

<success_criteria>
- WhatsApp suggestions are relevant to the last incoming (and RAG-grounded), parity with Telegram; Telegram unchanged; verifier green.
</success_criteria>

<output>
After completion, create `.planning/phases/08-device-uat-milestone-closeout/08-13-SUMMARY.md` with the confirmed locus (client vs workflow node/field), the exact change made, the verifier result, and the live re-test ask for 08-16 (deploy the edited workflow to dev n8n, then re-test WA relevance + RAG together).
</output>
