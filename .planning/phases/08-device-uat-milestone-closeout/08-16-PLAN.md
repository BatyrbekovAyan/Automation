---
phase: 08-device-uat-milestone-closeout
plan: 16
type: execute
wave: 7
depends_on: [11, 12, 13, 14, 15]
files_modified: []
autonomous: false
gap_closure: true
requirements: []

must_haves:
  truths:
    - "The owner re-verifies every round-2 defect (D2, D9, D10, D11, D12) on ONE device build in a single ordered pass"
    - "Each re-verify item maps back to its exact 08-DEVICE-UAT.md anchor + defect id, plus the diagnosis captures the round-2 plans requested (D2 echo hex, D11 failure logs)"
    - "A FAIL spins a further gap-closure round; an all-PASS flips Gate A to PASS and re-aggregates I.3 #10"
  artifacts:
    - path: ".planning/phases/08-device-uat-milestone-closeout/08-16-SUMMARY.md"
      provides: "Recorded round-2 device re-verify verdicts + Gate A disposition + captured diagnosis evidence"
      contains: "Gate A"
  key_links:
    - from: "08-16 re-verify verdicts"
      to: "08-DEVICE-UAT.md Defects D2/D9/D10/D11/D12 + Overall line"
      via: "owner records PASS/FAIL per item"
      pattern: "D2"
---

<objective>
The consolidated owner-run device RE-VERIFY of the round-2 gap-closure plans (08-11..08-15) on ONE build, one pass — do NOT scatter per-plan device gates (mirror 08-10). This closes the loop reopened by the 08-10 re-verify (Gate A still ISSUES): the five open defects **D2 (refined), D9, D10, D11, D12** get re-tested on a real device build, and the result dispositions Gate A.

Purpose: a single, unambiguous device pass that confirms the five defects are fixed (or files any residual as a further gap) AND captures the device-only evidence the diagnosis plans asked for (D2's live echo hex, D11's failure logs). The autonomous fixes cannot be device-verified by Claude (no device build, no live WhatsApp/Telegram infra, no dev-n8n session here) — running this pass is the owner gate.

Output: `08-16-SUMMARY.md` recording each verdict + the Gate A disposition; on all-PASS, `08-DEVICE-UAT.md` Overall flips PASS, I.3 #10 (01-VERIFICATION sign-off) re-aggregates, and the milestone-close mechanics (Gate B prod replication → Gate C close) proceed. On any FAIL, that item spins its own `/gsd-plan-phase 08 --gaps` round.

Constraint: `checkpoint:human-verify` — no app code changes here. D10's live re-test needs the shared dev-n8n session (localhost:5678 + cloudflared tunnel, `Tools/n8n/rotate-tunnel.py` first) with the EDITED `Suggest_Replies` workflow imported by literal id; prod bagkz stays dormant; bot clones stay inactive except during the test window.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md
@.planning/phases/08-device-uat-milestone-closeout/08-10-SUMMARY.md
@.planning/phases/08-device-uat-milestone-closeout/08-11-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-12-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-13-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-14-PLAN.md
@.planning/phases/08-device-uat-milestone-closeout/08-15-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Owner device re-verify of D2, D9, D10, D11, D12 on one build</name>
  <read_first>
    - .planning/phases/08-device-uat-milestone-closeout/08-DEVICE-UAT.md — the Defects table anchors (D2, D9, D10, D11, D12) + Overall line to update.
    - Each round-2 SUMMARY (08-11..08-15) — the confirmed root cause, what to look for, and the exact device capture ask.
  </read_first>
  <action>Owner-run gate (not executor work): build ONE device build with 08-11..08-15 merged, then re-verify each item below against its 08-DEVICE-UAT.md anchor and record PASS/FAIL/N/A. Do not tick on the owner's behalf.</action>
  <what-built>
    Five round-2 gap-closure plans landed autonomously and are EditMode-green:
    - 08-11 (D2): reaction-identity fix — a shared canonical-emoji normalizer (tapi echoes base `❤` vs the app's `❤️`) at every equality/dedup seam + a persisted owner TG user-id + a same-emoji fold so an own reaction reads as ONE reaction.
    - 08-12 (D9): sync-pill visibility — a minimum-visible-duration gate + a switch-to-Telegram-while-syncing show, so a fast Telegram list load still shows the «Синхронизация…» cue.
    - 08-13 (D10): WhatsApp «Вместе» relevance — the located WhatsApp-branch fix in the canonical `Suggest_Replies` workflow (+ any client fix); Telegram unchanged.
    - 08-14 (D12): Telegram create-bot CTA — `OpenCreateBotFlow` now preselects the platform by ActiveChannel (Telegram on the Telegram channel).
    - 08-15 (D11): media-download instrumentation — capped `[MediaDownload] FAIL …` logs at every failure exit + one serial-safe transient retry.
    Build ONE device build (Android primary) off the current tree with all five merged. Read each plan's SUMMARY for the confirmed root cause + what to look for.
  </what-built>
  <how-to-verify>
    Prepare: a real device build, an authorized dev Telegram profile, and — for the D10 live item only — dev n8n at localhost:5678 + a cloudflared tunnel (`Tools/n8n/rotate-tunnel.py` first) with the EDITED `Tools/n8n/workflows/9PTyYcelRQI7bGDb-Suggest_Replies.json` re-imported by literal id (see 08-13 SUMMARY). Prod bagkz stays dormant. Record each verdict against its 08-DEVICE-UAT.md anchor:

    1. **D2 — TG reaction identity** [B9 / B13]: on Telegram — (a) add ONE reaction to a message → the pill shows ONE glyph with NO «2» count; (b) change your reaction (👍→❤️) → exactly ONE pill (the new emoji), no stale second pill; (c) add ❤️ where a heart already exists → ONE heart, not two different heart glyphs; (d) remove your reaction → it clears and STAYS cleared across a refresh. WhatsApp reactions unchanged. **CAPTURE:** in the device log (logcat), record the 08-11 Editor/diagnostic line showing the echoed reaction's hex codepoints + `user_id` vs the learned own id (confirms the live echo form) — paste into the SUMMARY.
    2. **D9 — TG sync indicator** [Extra #2 / O1]: open a Telegram-authed bot's chat list (and pull-to-refresh / switch to Telegram) → the «Синхронизация…» pill is VISIBLE during the sync (even on a fast/cached load) and hides cleanly after; it never sticks after a bot/channel switch. WhatsApp shows NO pill.
    3. **D10 — WA «Вместе» relevance** [H2, WhatsApp half] (needs dev-n8n up with the edited workflow): open a WhatsApp chat on an authed bot, «Вместе» ON → the 4 suggestions are RELEVANT to the newest incoming message and RAG-grounded in the bot's catalog, matching the Telegram path. Re-test relevance AND RAG grounding together.
    4. **D11 — media downloads** [B-group media]: reproduce a failing video / GIF / video note → confirm (a) it now shows a retryable error card (not a dead spinner), and (b) **CAPTURE** the logcat `[MediaDownload] FAIL id=… http=… kind=… body=…` line(s) into the SUMMARY (these drive the server-side call / any Wappi ticket). Note whether a transient failure recovered on the one retry.
    5. **D12 — TG create-bot CTA** [F-group]: on the Telegram channel with the create-bot empty state showing, tap «Создать бота» → the Add-Bot form opens with Telegram PRESELECTED; repeat from the WhatsApp channel → WhatsApp preselected.

    For each: record PASS / FAIL / N/A. On any FAIL, add/append the row in 08-DEVICE-UAT.md's Defects table with the anchor and run `/gsd-plan-phase 08 --gaps` for that item. On all-PASS: set 08-DEVICE-UAT.md Overall = PASS (Gate A closes), and re-aggregate the re-deferred **I.3 #10** (01-VERIFICATION formal sign-off — I.1 #3 is already device-confirmed; it was held only on D5/D10's WhatsApp-suggestion relevance, which D10 now closes).
    Carried reminders: **deactivate the dev test clone** once the window closes (G6, bot-activation policy — clones run against real contacts); prod bagkz untouched.
  </how-to-verify>
  <resume-signal>Reply "all pass" (Gate A → PASS, re-aggregate I.3 #10, proceed to Gate B prod replication), or list the failing item numbers (each spins a gap-closure plan). Paste the D2 echo-hex + D11 failure logs regardless of verdict.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner device build → live WhatsApp/Telegram + dev-n8n | The only environment where these client + workflow fixes can be exercised end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16-01 | Repudiation | verdicts recorded incorrectly / on the owner's behalf | mitigate | Owner-run gate; each verdict maps to a named 08-DEVICE-UAT anchor; ticks transcribed from the owner's stated results only. |
| T-08-16-02 | Elevation | an active bot clone left running against real contacts | mitigate | Explicit reminder to deactivate the dev test clone after the window (bot-activation policy); prod bagkz stays dormant. |
| T-08-16-03 | Tampering | testing D10 against prod instead of dev n8n | mitigate | D10 live re-test uses dev n8n (localhost:5678) with the edited canonical workflow; prod bagkz is not touched until the Gate B replication step. |
</threat_model>

<verification>
- All five fix plans are EditMode-green (and D10's `verify-telegram-parity.py` green) and merged into the one device build before the pass.
- Each of the 5 items has a recorded verdict tied to its 08-DEVICE-UAT.md anchor; the D2 echo-hex + D11 failure logs are captured.
- Gate A disposition (PASS or gaps-filed) recorded in 08-16-SUMMARY.md and 08-DEVICE-UAT.md Overall; I.3 #10 re-aggregated on all-PASS.
</verification>

<success_criteria>
- One device build, one ordered pass, every round-2 defect (D2/D9/D10/D11/D12) re-verified with evidence captured.
- All-PASS → Gate A PASS → I.3 #10 re-aggregated → Gate B/C proceed; any FAIL → its own gap-closure plan.
</success_criteria>

<output>
After the owner records results, create `.planning/phases/08-device-uat-milestone-closeout/08-16-SUMMARY.md` with the per-item verdicts, the captured D2 echo-hex + D11 failure logs, the Gate A disposition, and any newly filed defects.
</output>
