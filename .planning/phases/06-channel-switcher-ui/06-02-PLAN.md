---
phase: 06-channel-switcher-ui
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - Assets/Editor/ChannelSwitcherBuilder.cs
  - Assets/Editor/NavRestructureBuilder.cs
  - Tools/run-editor-builder.sh
  - Assets/Scenes/Main.unity
  - .planning/phases/06-channel-switcher-ui/06-HUMAN-UAT.md
autonomous: true
requirements: [SWITCH-01, SWITCH-04]
user_setup: []

must_haves:
  truths:
    - "A WhatsApp | Telegram segmented pill exists under Screen_Whatsapp/ChatsPanel/TopBar/CenterZone, styled like the neighbouring ModeToggle (null sprite + RoundedCorners, header font, ~72u track)"
    - "The pill's ChannelSwitcherView serialized refs are stamped by the builder (SerializedObject), so tapping a chip switches the active bot's channel"
    - "The Telegram bottom-nav tab is gone: BottomTabManager.tabs has exactly 4 entries and tab 0's label reads «Чаты»"
    - "The Screen_Telegram GameObject and the TelegramTab bottom-bar root are deleted from Main.unity; NavRestructureBuilder.ReorderScreens no longer lists Screen_Telegram"
    - "The headless builder run (Tools/run-editor-builder.sh) succeeds, the scene is committed immediately, and the EditMode suite stays green"
  artifacts:
    - path: "Assets/Editor/ChannelSwitcherBuilder.cs"
      provides: "Idempotent switcher builder + nav restructure, headless entry"
      contains: "BuildHeadless"
      min_lines: 120
    - path: "Tools/run-editor-builder.sh"
      provides: "Editor-closed headless -executeMethod runner"
      contains: "executeMethod"
    - path: "Assets/Scenes/Main.unity"
      provides: "ChannelSwitcher pill under CenterZone; Screen_Telegram removed; 4-tab bar; «Чаты» label"
      contains: "ChannelSwitcher"
    - path: ".planning/phases/06-channel-switcher-ui/06-HUMAN-UAT.md"
      provides: "Open owner visual-pass gate"
      contains: "1080"
  key_links:
    - from: "Assets/Editor/ChannelSwitcherBuilder.cs"
      to: "ChannelSwitcherView serialized refs"
      via: "SerializedObject SetRef"
      pattern: "SerializedObject"
    - from: "Assets/Editor/ChannelSwitcherBuilder.cs"
      to: "BottomTabManager.tabs"
      via: "guarded DeleteArrayElementAtIndex(1) when tabs[1] is the Telegram tab"
      pattern: "tabs"
    - from: "Assets/Editor/NavRestructureBuilder.cs"
      to: "ReorderScreens order list"
      via: "Screen_Telegram removed from the array"
      pattern: "ReorderScreens"
---

<objective>
Build the scene half of the channel switcher headlessly: a `ChannelSwitcherBuilder` that (1) builds the WhatsApp|Telegram segmented pill into `Screen_Whatsapp/ChatsPanel/TopBar/CenterZone` and stamps the 06-01 `ChannelSwitcherView` refs, and (2) performs the nav restructure — remove the Telegram bottom tab, relabel tab 0 «Чаты», delete `Screen_Telegram` + its `TelegramTab` root, and prune `NavRestructureBuilder.ReorderScreens`. Then a `Tools/run-editor-builder.sh` runner EXECUTES the build (Editor closed), we assert the scene payload by grep, commit the scene immediately, and re-run the EditMode suite. A `06-HUMAN-UAT.md` records the owner's visual pass as an open gate.

Purpose: SWITCH-01 (observable TopBar switcher wired to the binder) and SWITCH-04 (Telegram tab + Screen_Telegram removed; tab 0 = «Чаты»).
Output: 2 editor files, 1 runner script, Main.unity mutated + committed, 1 open UAT doc.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-channel-switcher-ui/06-CONTEXT.md
@.planning/research/telegram-parity/ui-scaffolding.md

# MANDATORY skill reads before any builder/scene work:
@.claude/skills/unity-ui-builder/SKILL.md
@.claude/skills/mobile-app-ui-design/SKILL.md

# Builder + rewire precedents — READ FIRST, copy their idioms VERBATIM:
@Assets/Editor/ReplyModeToggleBuilder.cs
@Assets/Editor/NavRestructureBuilder.cs
@Tools/run-tests-headless.sh

<scene_targets>
<!-- Exact object paths + fileIDs from ui-scaffolding.md. Use these directly. -->
- Switcher home: `Screen_Whatsapp/ChatsPanel/TopBar/CenterZone` — INACTIVE, 360×140, anchors 0.5/0.5, pos y=-60, holds one unused `Title` TMP. Builder ACTIVATES CenterZone, deactivates/removes the `Title`, builds the pill there. LeftZone (BotSwitcherTitle) and RightZone (ModeToggle + NewChatButton) are UNTOUCHED.
- Telegram tab: `BottomTabManager.tabs[1]` (tabName "Telegram", tabRoot "TelegramTab" active=1, screenPanel line 135599). Tab order today: 0 Whatsapp, 1 Telegram, 2 Сводка, 3 Bots, 4 Profile; defaultTabIndex=0.
- Screen_Telegram: GameObject fileID 163358610 — bare inactive pink placeholder, zero children. Referenced only by its own blocks, ScreenContainer child list (line 73307), and tabs[1].screenPanel.
- Header font GUID (SF Pro Text, used across the bar): `a2b0b38b6764047da9250bcff1b0f432`.
- ModeToggle measured values to mirror (from ReplyModeToggleBuilder): track sizeDelta ~280×72, RoundedCorners radius 36 (track) / 28 (thumb) via ResolveRoundedType/AddRounded, label 28f, characterSpacing -2, enableWordWrapping false. Selected-fill white thumb pattern.
- Brand accents (read exact RGBA from BotSwitcherRowView.cs): WhatsApp `waConnectedBg`/`waConnectedLabel` green family; Telegram bg (0.902,0.945,0.984) / label (0.094,0.373,0.647); disconnected alpha 0.35. Selected Telegram accent = #2AABEE.
</scene_targets>

<interfaces>
From Assets/Scripts/UI/ChannelSwitcherView.cs (06-01) — serialized fields the builder must stamp:
```csharp
[SerializeField] private Button waChipButton, tgChipButton;
[SerializeField] private Image  waChipFill,  tgChipFill;
[SerializeField] private TextMeshProUGUI waLabel, tgLabel;
[SerializeField] private Image  waChipIcon, tgChipIcon;   // optional
```
SerializedObject SetRef idiom (copy from ReplyModeToggleBuilder.SetRef): `so.FindProperty(name).objectReferenceValue = value; so.ApplyModifiedPropertiesWithoutUndo();`
RoundedCorners resolution: RoundedCorners lives in its OWN UPM assembly — use the `ResolveRoundedType()`/`AddRounded()`/`RefreshRounded()` AppDomain-scan idiom from ReplyModeToggleBuilder (project memory: roundedcorners-assembly). Bake corners after `Canvas.ForceUpdateCanvases()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ChannelSwitcherBuilder + run-editor-builder.sh + ReorderScreens prune</name>
  <files>Assets/Editor/ChannelSwitcherBuilder.cs, Assets/Editor/NavRestructureBuilder.cs, Tools/run-editor-builder.sh</files>
  <action>
    Author the builder + runner (do NOT execute yet — execution is Task 2). Wrap all editor code in `#if UNITY_EDITOR / #endif` (editor-scripts rule).

    (A) `Assets/Editor/ChannelSwitcherBuilder.cs` — a public static class with:
      - `[MenuItem("Tools/Channel Switcher/Build")] public static void Build()` — calls `BuildInternal()`, `MarkSceneDirty`, logs the completion line, reminds to save.
      - `public static void BuildHeadless()` — parameterless, `-executeMethod` target: `var scene = EditorSceneManager.OpenScene("Assets/Scenes/Main.unity"); BuildInternal(); EditorSceneManager.SaveScene(scene); Debug.Log("[ChannelSwitcherBuilder] Headless build + save complete: switcher pill + nav restructure.");` (mirror NavRestructureBuilder.BuildHeadless exactly). The final log line is the success sentinel the runner greps.
      - `BuildInternal()` does BOTH the pill build and the nav restructure in one pass (Claude's discretion per CONTEXT allows folding nav restructure here rather than a separate NavRestructureBuilder method):

      Pill build:
      1. Find `Screen_Whatsapp` include-inactive (copy `FindByNameIncludeInactive` from ReplyModeToggleBuilder), then `ChatsPanel/TopBar/CenterZone`. Throw a clear error if any path segment is missing.
      2. `centerZone.gameObject.SetActive(true)`. Deactivate/destroy the unused `Title` child (`DestroyAllByName(centerZone, "Title")`).
      3. Idempotent: `DestroyAllByName(centerZone, "ChannelSwitcher")` before building.
      4. Build a `ChannelSwitcher` root (Image+Button track) under CenterZone sized to fit the 360×140 slot (a ~ 336×72 pill centered), then two chip labels «WhatsApp» and «Telegram» and a moving/selected fill per chip — MIRROR ReplyModeToggleBuilder's track/thumb/label construction, radius, header font (GUID above), characterSpacing -2, no word-wrap, null sprite + `AddRounded`. Selected chip = brand fill visible; unselected = transparent. Load the header font via the GUID (copy `LoadHeaderFont`). Keep new TopBar child graphics Maskable-default (project memory: bubble-graphics-must-be-Maskable — CenterZone isn't under a RectMask2D, but leave Maskable=true so a future mask doesn't cull them).
      5. `var binder = root.AddComponent<ChannelSwitcherView>();` then stamp every serialized ref via `SetRef(so, "waChipButton", ...)` etc. (SetRef idiom from ReplyModeToggleBuilder). Stamp icon refs only if you built icons; otherwise skip (SetRef logs a warning for null props — acceptable).
      6. Bake corners: `Canvas.ForceUpdateCanvases();` then `RefreshRounded(...)` each rounded component.

      Nav restructure (same BuildInternal, after the pill):
      7. `var tabManager = FindFirstObjectByType<BottomTabManager>(FindObjectsInactive.Include);` throw if null. `var so = new SerializedObject(tabManager); var tabs = so.FindProperty("tabs");`.
      8. IDEMPOTENCY GUARD (critical — re-runs must not delete the wrong tab): only remove index 1 when it is verifiably the Telegram tab. Read `tabs.GetArrayElementAtIndex(1)`: check `tabName == "Telegram"` OR its `screenPanel` object's name == "Screen_Telegram". If the guard fails (already restructured — array is 4 long / tabs[1] is Сводка), SKIP removal and log "[ChannelSwitcherBuilder] Telegram tab already removed — skipping". If it matches: capture `tabRoot` (the "TelegramTab" GameObject) and `screenPanel` (Screen_Telegram) object refs BEFORE deletion, then `tabs.DeleteArrayElementAtIndex(1); so.ApplyModifiedPropertiesWithoutUndo();` and `DestroyImmediate` both captured GameObjects (guard each for null).
      9. Relabel tab 0 to «Чаты»: `var tab0 = tabs.GetArrayElementAtIndex(0);` set `tab0.tabName = "Чаты"` (inspector clarity) and set the scene TMP text — read `tab0.labelText` (TextMeshProUGUI), then via its own SerializedObject set `m_text = "Чаты"` and `SetDirty` (copy the label-text idiom from NavRestructureBuilder.RewriteNewTabToDashboard L390-404). Apply.
      10. `MarkSceneDirty`.

    (B) `Assets/Editor/NavRestructureBuilder.cs` — edit ONLY the `ReorderScreens` order array (L419-429): remove the `"Screen_Telegram"` entry so future runs don't warn about a missing screen. Do not touch anything else in that file (its hardcoded tabs[2]/tabs[3] indices are a one-time already-run migration — leave them, they are not re-run).

    (C) `Tools/run-editor-builder.sh` — mirror `run-tests-headless.sh` conventions:
      - Resolve project root + Unity binary from `ProjectSettings/ProjectVersion.txt` (env `UNITY` override wins).
      - Lock guard: REFUSE with a clear message + exit 2 if a non-batch Unity GUI process holds this project (copy the `pgrep`/`grep -iF -- "-projectpath"` guard verbatim — the Editor is CLOSED for this run; batch can't take the lock while the Editor holds it).
      - Run: `"${UNITY}" -batchmode -nographics -projectPath "${PROJECT}" -executeMethod ChannelSwitcherBuilder.BuildHeadless -quit -logFile "${OUT_DIR}/builder.log"` (note: -quit IS correct for -executeMethod; unlike -runTests). Output dir `Tools/test-output/` (gitignored, NOT under Temp/).
      - Verdict: Unity's exit code alone is NOT authoritative — grep `builder.log` for the sentinel `[ChannelSwitcherBuilder] Headless build + save complete`. Exit 0 only if the sentinel is present AND exit code is 0; else print last 40 log lines and exit 1/2.
  </action>
  <verify>
    <automated>test -f Assets/Editor/ChannelSwitcherBuilder.cs && test -x Tools/run-editor-builder.sh && grep -q "BuildHeadless" Assets/Editor/ChannelSwitcherBuilder.cs && grep -q "executeMethod" Tools/run-editor-builder.sh && ! grep -q "Screen_Telegram" Assets/Editor/NavRestructureBuilder.cs && echo OK</automated>
  </verify>
  <done>Builder + runner + ReorderScreens prune authored; idempotency guard present; runner refuses when Editor open and greps the success sentinel; NavRestructureBuilder no longer lists Screen_Telegram. NOT yet executed.</done>
</task>

<task type="auto">
  <name>Task 2: Execute headless build, assert scene, commit immediately, re-run suite</name>
  <files>Assets/Scenes/Main.unity</files>
  <action>
    Execute the build headlessly and lock in the scene mutation. This is an AGENT-run step (Editor is closed; the runner refuses if it is open) — NOT a human gate.
    1. `chmod +x Tools/run-editor-builder.sh` then run `Tools/run-editor-builder.sh`. Require exit 0 + the success sentinel in `Tools/test-output/builder.log`. If it refuses because the Editor is open, STOP and surface that (the owner must close the Editor).
    2. Structural scene asserts against `Assets/Scenes/Main.unity` (grep — memory: verify payload by GUID/name grep):
       - `grep -c "m_Name: ChannelSwitcher" Assets/Scenes/Main.unity` ≥ 1 (pill root exists under CenterZone).
       - `grep -c "m_Name: CenterZone" Assets/Scenes/Main.unity` ≥ 1 and its `m_IsActive` is now 1 (activated).
       - `! grep -q "m_Name: Screen_Telegram" Assets/Scenes/Main.unity` (screen deleted) AND `! grep -q "163358610" Assets/Scenes/Main.unity` (fileID gone).
       - Tab 0 label reads «Чаты» (grep for the `Чаты` string; confirm it is on the tab-0 label TMP).
       - Confirm the ChannelSwitcherView component block is present with its serialized refs populated (non-zero fileIDs on waChipButton/tgChipButton/waChipFill/tgChipFill/waLabel/tgLabel).
       - Sanity: the `tabs` array now has 4 entries (spot-check: Whatsapp, Сводка, Bots, Profile remain).
       If ANY assert fails, do NOT commit — report the mismatch (the builder mutated the wrong object or the guard misfired).
    3. Commit the scene IMMEDIATELY (project memory: parallel-scene-clobber — uncommitted scene mutations get clobbered by parallel saves). Stage `Assets/Scenes/Main.unity`, the new `Assets/Editor/ChannelSwitcherBuilder.cs` + `.meta`, `Assets/Editor/ChannelSwitcherBuilder.cs.meta`, `Tools/run-editor-builder.sh`, and the `Assets/Editor/NavRestructureBuilder.cs` edit; commit per the Unity execution-loop per-task consent. (The 06-01 runtime files were already committed in wave 1.)
    4. Re-run `Tools/run-tests-headless.sh` — the builder must not have broken compilation or any test. Suite must stay GREEN (≥ 891 baseline). NOTE: the 06-01 `BotsTabIndex==2` change now matches the freshly-restructured 4-tab scene — confirm `TabIndexShiftTests` green.
  </action>
  <verify>
    <automated>grep -q "m_Name: ChannelSwitcher" Assets/Scenes/Main.unity && ! grep -q "163358610" Assets/Scenes/Main.unity && Tools/run-tests-headless.sh</automated>
  </verify>
  <done>Headless build succeeded (sentinel present); all structural grep asserts pass; Main.unity committed immediately with the new editor files; EditMode suite green with the 4-tab scene.</done>
</task>

<task type="auto">
  <name>Task 3: Write 06-HUMAN-UAT.md open visual-pass gate</name>
  <files>.planning/phases/06-channel-switcher-ui/06-HUMAN-UAT.md</files>
  <action>
    Visual/device polish cannot be verified headless — record it as an OPEN owner gate, same pattern as 03/04-HUMAN-UAT.md (the phase closes on this later; do not block execution on it). Write `.planning/phases/06-channel-switcher-ui/06-HUMAN-UAT.md` with:
    - Header: phase, purpose (owner Editor Game-view pass at 1080×2400 + device feel), status PENDING.
    - A numbered checklist the owner runs in the Unity Game view at 1080×2400:
      1. The WhatsApp|Telegram pill sits in the TopBar CenterZone and matches the ModeToggle styling (corner rounding, font, height, selected-fill).
      2. Switching chips swaps the chat list with the full reset choreography — no crossed lists, no flicker (SWITCH-01).
      3. An unconnected channel's chip reads clearly MUTED but is still tappable; tapping it shows that channel's connect empty state (SWITCH-02) — test on a WhatsApp-only bot AND a Telegram-only bot.
      4. A Telegram-only bot auto-selects Telegram on open, with the WhatsApp chip muted (SWITCH-03).
      5. The bottom tab bar shows exactly 4 tabs and tab 0 reads «Чаты»; tapping Сводка/Bots/Profile lands on the correct screen (tab-index shift, SWITCH-04).
      6. Last-used channel persists across an app restart (SWITCH-03).
    - A "Deferred polish follow-ups (record owner decision)" section carrying CONTEXT.md §Deferred:
      - Per-row swipe-delete affordance hiding on Telegram (05-03 guard already makes the network call a safe no-op; visual affordance removal needs ChatItemView/prefab surgery).
      - RU-localization sweep of any English empty-state copy (IN-09).
    - A resume line: owner records PASS / issues, phase closes on PASS.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-channel-switcher-ui/06-HUMAN-UAT.md && grep -q "1080" .planning/phases/06-channel-switcher-ui/06-HUMAN-UAT.md && echo OK</automated>
  </verify>
  <done>06-HUMAN-UAT.md written with the 6-point 1080×2400 checklist + deferred-polish section, left OPEN for the owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Builder → Main.unity | The headless builder mutates scene objects (adds a pill, deletes a screen, edits the tabs array) — wrong-object edits corrupt navigation |
| tabs array edit → navigation | Removing tabs[1] shifts every downstream tab index |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | Builder deleting/rewiring the wrong scene object | mitigate | Idempotent delete-and-rebuild by EXACT name (`DestroyAllByName`); post-build grep asserts (ChannelSwitcher present, Screen_Telegram + fileID 163358610 absent, 4-tab array, «Чаты» label); commit only when all asserts pass; immediate commit + name/GUID grep |
| T-06-05 | Denial of Service | Tab-index shift breaks SwitchTab targets | mitigate | 06-01 sets BotsTabIndex=2 + TabIndexShiftTests; all consumers use constants; defaultTabIndex=0 still lands on Screen_Whatsapp; re-run suite green against the 4-tab scene |
| T-06-06 | Tampering | Re-running the builder deletes the wrong tab (Dashboard now at index 1) | mitigate | Removal is guarded: only delete tabs[1] when its tabName=="Telegram" / screenPanel=="Screen_Telegram"; skip when already restructured; Screen_Telegram delete guarded for absence |
</threat_model>

<verification>
- `Tools/run-editor-builder.sh` exits 0 with the `[ChannelSwitcherBuilder] Headless build + save complete` sentinel in builder.log.
- Main.unity: `m_Name: ChannelSwitcher` present under an active CenterZone; `Screen_Telegram`/fileID 163358610 absent; tabs array = 4 entries; tab-0 label «Чаты».
- ChannelSwitcherView refs stamped (non-zero fileIDs).
- `Tools/run-tests-headless.sh` GREEN (≥ 891 baseline); TabIndexShiftTests green.
- Scene committed immediately after a passing build.
- `06-HUMAN-UAT.md` exists as an open gate.
</verification>

<success_criteria>
- Switcher pill built into CenterZone and wired to ChannelSwitcherView (SWITCH-01 observable).
- Telegram tab + Screen_Telegram removed; tab 0 reads «Чаты» (SWITCH-04).
- Builder is idempotent + re-run-safe (guarded tab removal); runner refuses when the Editor is open.
- Scene committed immediately; EditMode suite green; owner UAT gate open.
</success_criteria>

<output>
After completion, create `.planning/phases/06-channel-switcher-ui/06-02-SUMMARY.md`.
</output>
