---
phase: 09-semi-auto-suppression
plan: 04
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - Tools/n8n/workflows/Set_Reply_Mode.json
  - Tools/n8n/build-set-reply-mode.py
  - Tools/n8n/README.md
  - Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json
  - Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json
autonomous: false
requirements: [SUP-01, SUP-02, SUP-03, SUP-04, SUP-05]
must_haves:
  truths:
    - "reply_mode_flags exists on the dev Supabase DB the gate reads (cred 1H5xlpFSESU4w6JH) — a probe row round-trips"
    - "POST /webhook/SetReplyMode upserts default+override rows and rejects a malformed body with no partial write"
    - "runData shows a suppressed 1:1 chat dead-ends after Suppressed? (reply nodes absent); a non-suppressed chat runs the reply path"
    - "A freshly created bot's cloned workflow contains Read Reply Mode + Suppressed? after the group-chat If"
  artifacts:
    - path: "Tools/n8n/workflows/*-Set_Reply_Mode.json"
      provides: "the id-prefixed canonical export after first deploy"
      contains: "SetReplyMode"
  key_links:
    - from: "dev Supabase (cred 1H5xlpFSESU4w6JH)"
      to: "reply_mode_flags"
      via: "applied DDL"
      pattern: "reply_mode_flags"
    - from: "Create orchestrator clone"
      to: "gate nodes"
      via: "Get Sample Workflow -> Create Workflow (verbatim template clone)"
      pattern: "Read Reply Mode"
---

<objective>
Owner-run live bring-up + structural verification of the server side (all live-DB/n8n work — `secrets.json` is deny-ruled for Claude, dev n8n + tunnel + owner required). This plan starts with the **[BLOCKING] DDL apply** (SUP-01 live) that gates every live-DB check below, then deploys the Set Reply Mode workflow and runs the curl matrix (SUP-02/04), then redeploys both gated templates and confirms the fail-closed branch via runData + verifies a fresh bot inherits the gate (SUP-03/05). Behavioral end-to-end (customer sends a message) is the separate final gate 09-05.

Purpose: The structural JSON/SQL/EditMode work (09-01..03) cannot prove the flag round-trips against the live DB — that requires the applied schema + a running dev n8n. Verification would false-positive on an unapplied schema.
Output: applied dev schema, deployed + id-finalized Set Reply Mode workflow, gated templates live on dev, recorded curl + runData verdicts.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Design + research + the artifacts produced upstream
@docs/superpowers/specs/2026-07-13-semi-auto-suppression-flag-design.md
@.planning/phases/09-semi-auto-suppression/09-RESEARCH.md
@Tools/n8n/supabase/2026-07-19-reply-mode-flags.sql
@Tools/n8n/build-set-reply-mode.py
@Tools/n8n/build-suggest-replies.py

<environment_notes>
- `secrets.json` (n8nAPIKey, DB creds) is DENY-RULED for Claude — the owner runs the deployer + DDL + curl. Dev n8n at localhost:5678; tunnel via Tools/n8n/rotate-tunnel.py.
- TWO creds are named "Postgres": apply the DDL through and bind the gate to `1H5xlpFSESU4w6JH` (bot-template Chat Memory), NOT `vvRrFiEXzLVqKjOx` (Dashboard/RAG). Both target the same Supabase DB (A3) but be explicit.
- Prod bagkz stays DORMANT — do NOT target it. The gate folds into the future one-shot bulk copy (SUP-05).
- INACTIVE-clone constraint: a per-bot template clone may be ACTIVE only during a test window, then DEACTIVATED in the same step (real contacts).
- runData introspection pattern: 02-03-SUMMARY (execution introspection); structural verifier precedent: Tools/n8n/verify-telegram-parity.py.
</environment_notes>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply reply_mode_flags DDL to dev Supabase (SUP-01 live)</name>
  <files>(no repo file — live DB mutation)</files>
  <read_first>
    - Tools/n8n/supabase/2026-07-19-reply-mode-flags.sql (the exact DDL to run)
    - .planning/phases/09-semi-auto-suppression/09-RESEARCH.md (Open Question 2 — the [BLOCKING] apply mechanism; Pitfall 2 — apply through the gate's own cred)
  </read_first>
  <what-built>
Claude authored the `reply_mode_flags` DDL (09-01). It is NOT yet on the live database. The gate (09-02) and the Set Reply Mode webhook (09-01) both read/write this table — every live check below false-positives until the table exists on the DB the gate reads.
  </what-built>
  <action>
Owner applies `Tools/n8n/supabase/2026-07-19-reply-mode-flags.sql` to the dev Supabase Postgres **through the bot-template Chat Memory credential `1H5xlpFSESU4w6JH`** (a one-off n8n Postgres `executeQuery` node on that cred, OR the Supabase SQL editor on that project — the Supabase MCP is read-only so it can't apply DDL). Then round-trip a probe row to prove visibility:
```sql
insert into reply_mode_flags (profile_id, chat_id, suppressed) values ('probe','*', true)
  on conflict (profile_id, chat_id) do update set suppressed = excluded.suppressed;
select suppressed from reply_mode_flags where profile_id='probe' and chat_id='*';   -- expect: true
delete from reply_mode_flags where profile_id='probe';
```
  </action>
  <how-to-verify>
1. The `insert` succeeds (table exists, columns match).
2. The `select` returns `true`.
3. `delete` removes the probe row (leave the table empty).
Run all three through cred `1H5xlpFSESU4w6JH` specifically (the gate's cred) — not the Dashboard/RAG cred.
  </how-to-verify>
  <verify>Owner-run: probe insert -> select returns `true` -> delete, all via cred 1H5xlpFSESU4w6JH.</verify>
  <acceptance_criteria>
    - `reply_mode_flags` exists on the dev Supabase DB behind cred `1H5xlpFSESU4w6JH`
    - The probe `select` returns `true`
    - The probe row is deleted (table left empty)
    - RLS is enabled (a Data-API read with the anon key returns no rows / permission denied — default-deny confirmed)
  </acceptance_criteria>
  <done>The table is live on the gate's DB and a probe row round-trips. This unblocks Tasks 2-3 and plan 09-05.</done>
  <resume-signal>Type "applied" (or paste the probe select output). If the insert fails, report the error — the DDL or the cred target is wrong.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy Set Reply Mode + finalize id/export + curl matrix (SUP-02, SUP-04)</name>
  <files>Tools/n8n/workflows/Set_Reply_Mode.json, Tools/n8n/build-set-reply-mode.py, Tools/n8n/README.md</files>
  <read_first>
    - Tools/n8n/build-set-reply-mode.py (the deployer authored in 09-01; --dry-run / --deploy / --export ID OUT / --postgres-cred)
    - Tools/n8n/workflows/Set_Reply_Mode.json (the provisional canonical to deploy)
    - .planning/phases/09-semi-auto-suppression/09-CONTEXT.md (Specific Ideas — the n8n curl matrix scenarios)
  </read_first>
  <what-built>
The Set Reply Mode workflow JSON + REST deployer exist (09-01) but were never deployed. This gate deploys it to dev, finalizes its n8n-assigned id (the committed file is renamed to `<id>-Set_Reply_Mode.json`), and runs the curl matrix that proves upsert + precedence + malformed-rejection.
  </what-built>
  <action>
Owner, with dev n8n up (`localhost:5678`) and the n8n API key available:
1. Deploy (create + activate — this is a SHARED always-active workflow, activate-on-deploy is by design): `python3 Tools/n8n/build-set-reply-mode.py --postgres-cred 1H5xlpFSESU4w6JH`. Note the returned workflow id.
2. Export the canonical with the real id: `python3 Tools/n8n/build-set-reply-mode.py --export <id> Tools/n8n/workflows/<id>-Set_Reply_Mode.json`, then `git rm` the provisional `Set_Reply_Mode.json`, update the deployer's `CANONICAL` constant + the README line to the id-prefixed name.
3. Run the curl matrix:
```bash
BASE=http://localhost:5678
# (a) default '*' row
curl -s -X POST $BASE/webhook/SetReplyMode -H 'Content-Type: application/json' -d '{"profileIds":["probeP"],"chatId":"*","suppressed":true}'
# (b) per-chat override row
curl -s -X POST $BASE/webhook/SetReplyMode -H 'Content-Type: application/json' -d '{"profileIds":["probeP"],"chatId":"7701@c.us","suppressed":false}'
# (c) malformed (missing profileIds + suppressed)
curl -s -X POST $BASE/webhook/SetReplyMode -H 'Content-Type: application/json' -d '{"chatId":"*"}'
```
4. Confirm precedence directly (the gate's resolve query) against `probeP`:
```sql
select coalesce((select suppressed from reply_mode_flags where profile_id='probeP' and chat_id in ('7701@c.us','*') order by (chat_id='*') limit 1), false);  -- expect: false (override beats '*')
select coalesce((select suppressed from reply_mode_flags where profile_id='probeP' and chat_id in ('9999@c.us','*') order by (chat_id='*') limit 1), false);  -- expect: true  (falls back to '*')
select coalesce((select suppressed from reply_mode_flags where profile_id='nobody' and chat_id in ('x','*') order by (chat_id='*') limit 1), false);           -- expect: false (absence -> reply)
```
5. `delete from reply_mode_flags where profile_id='probeP';` (clean up), then commit the finalized files.
  </action>
  <how-to-verify>
(a) and (b) return `{"success":true,"written":1}`; (c) returns `{"success":false,"error":"bad_request"}` and writes NO row (Validate -> If invalid? -> Respond before the Upsert). Precedence selects return `false`, `true`, `false` in order. Two rows existed for `probeP` before cleanup.
  </how-to-verify>
  <verify>Owner-run curl matrix + precedence SQL; expected outputs listed in acceptance_criteria.</verify>
  <acceptance_criteria>
    - Deploy returns a workflow id; the committed canonical is renamed to `Tools/n8n/workflows/<id>-Set_Reply_Mode.json`; the provisional `Set_Reply_Mode.json` is removed; `CANONICAL` + README point at the id-prefixed name
    - curl (a) -> `{"success":true,"written":1}`; (b) -> `{"success":true,"written":1}`; (c) -> `{"success":false,"error":"bad_request"}`
    - After (c), `select count(*) from reply_mode_flags where profile_id is null` style check confirms NO partial row from the malformed call (only probeP's 2 rows exist)
    - Precedence SQL returns `false` (override wins), `true` (default fallback), `false` (absence)
    - probeP rows deleted; the Set Reply Mode workflow is ACTIVE on dev (shared workflow)
  </acceptance_criteria>
  <done>The webhook upserts default+override rows, rejects malformed bodies with no partial write, and the locked resolve query proves per-chat-beats-default + absence->reply.</done>
  <resume-signal>Type "curl-matrix pass" (paste the 3 curl responses + 3 precedence results), or report which case failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Redeploy both gated templates + runData structural verify + fresh-bot propagation (SUP-03, SUP-04, SUP-05)</name>
  <files>Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json, Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json</files>
  <read_first>
    - Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json + 4VN3gsFaC2HUYmcc-Telegram_Bot.json (the gated templates from 09-02)
    - .planning/phases/09-semi-auto-suppression/09-RESEARCH.md (Pitfall 4 / A1 — confirm the Suppressed? branch via runData; Pitfall 6 — recreate existing dev clones)
    - Tools/n8n/workflows/XuvOp7TxOImOAmlj-CreateWhatsappWorkflow.json (Get Sample Workflow -> Create Workflow clone path — why a fresh bot inherits the gate)
  </read_first>
  <what-built>
The gate lives in both committed templates (09-02) but the dev instance still runs the pre-gate template on existing clones. This gate redeploys the templates, confirms via runData that the Suppressed? branch actually dead-ends a suppressed 1:1 chat (RESEARCH A1: the boolean may surface as a string), and confirms a freshly created bot inherits the gate through the Create orchestrators' verbatim clone (SUP-05).
  </what-built>
  <action>
Owner, dev n8n + tunnel up:
1. Redeploy the two edited templates to dev (REST update, mirroring the deployer pattern). Recreate any EXISTING dev bot clone so it carries the gate (Pitfall 6) — clones are frozen copies of the old gate-less template.
2. runData structural verify (per template; the clone is ACTIVE only during the window, DEACTIVATE it immediately after):
   - Seed a suppression row via the Set Reply Mode webhook for the test bot's profile id + the test 1:1 chat id (`suppressed:true`), send a test message, and confirm the execution `runData` shows `Read Reply Mode` + `Suppressed?` present and the reply-path nodes (`Input type`, `Mark Read`, agent, send) ABSENT after `Suppressed?` (message stays unread).
   - Set `suppressed:false` (or delete the row -> absence), send again, confirm `runData` runs the full reply path.
   - If runData shows `$json.suppressed` as a STRING (branch taken wrong), apply the A1 fallback: cast `suppressed::boolean` in the Read Reply Mode query, redeploy, re-verify. Record the outcome.
   - DEACTIVATE the clone.
3. Fresh-bot propagation (SUP-05): create a throwaway bot via the app (or drive the Create orchestrator on dev), fetch the created workflow JSON via the n8n REST API, and grep the node names — confirm `Read Reply Mode` + `Suppressed?` exist wired after the group-chat `If`. If ABSENT, the orchestrator's "Get Sample Workflow" is fetching a stale template id — fix the fetch/id, do NOT hand-edit clones. Deactivate/delete the throwaway.
  </action>
  <how-to-verify>
Suppressed run: runData contains `Read Reply Mode` + `Suppressed?`, and `Input type`/`Mark Read`/send are absent (no reply, stays unread). Not-suppressed run: full reply path present. Fresh-bot workflow JSON contains both gate node names after the `If`. Every activated clone is deactivated before you finish.
  </how-to-verify>
  <verify>Owner-run runData introspection (suppressed vs not) on both channels + a REST grep of a freshly cloned workflow.</verify>
  <acceptance_criteria>
    - Both templates redeployed to dev; existing dev clone(s) recreated to carry the gate
    - Suppressed 1:1 message: runData shows `Read Reply Mode`+`Suppressed?` and NO `Input type`/`Mark Read`/send after `Suppressed?` (dead-end, stays unread) — on BOTH WhatsApp and Telegram
    - Non-suppressed (or absent-row) message: runData runs the full existing reply path
    - If the boolean surfaced as a string, the `suppressed::boolean` A1 fallback was applied + re-verified (record which)
    - A freshly created bot's cloned workflow JSON contains `Read Reply Mode` + `Suppressed?` after the group-chat `If` (SUP-05)
    - Every clone activated for testing was DEACTIVATED afterward (real-contacts constraint)
  </acceptance_criteria>
  <done>Both live templates fail-closed on a suppressed 1:1 chat (verified by runData), a fresh bot inherits the gate, and no test clone is left active.</done>
  <resume-signal>Type "gate verified" (note the A1 boolean outcome + whether a fresh bot inherited the gate), or report the failing branch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner tooling -> dev Supabase (cred 1H5xlpFSESU4w6JH) | DDL + probe rows applied through the gate's own cred |
| internet -> /webhook/SetReplyMode (dev, unauthenticated) | curl matrix exercises the live write path |
| incoming message -> gated bot template (dev clone) | runData confirms fail-closed suppression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Tampering | live /webhook/SetReplyMode (unauthenticated) | accept | R-02-01 accepted-risk posture (dev only this phase; prod bagkz dormant). Curl matrix confirms the Validate node rejects malformed bodies before any write |
| T-09-13 | Repudiation / Safety | live gate fail-closed | mitigate | runData confirms a suppressed chat dead-ends and a Postgres error would halt (no continueOnFail); the table lives on the gate's own cred so it is always visible |
| T-09-14 | Elevation of Privilege | test clone left ACTIVE | mitigate | Clone ACTIVE only during the runData window, DEACTIVATED in the same task (real contacts) — acceptance requires it |
| T-09-15 | Information Disclosure | reply_mode_flags via anon key | mitigate | Task 1 confirms default-deny RLS (anon Data-API read denied) before any live write |
</threat_model>

<verification>
- [BLOCKING] DDL applied on cred 1H5xlpFSESU4w6JH; probe row round-trips; RLS default-deny confirmed. THIS TASK GATES the curl matrix + runData below.
- Set Reply Mode deployed + id-finalized; curl matrix (upsert default+override, malformed->clean error, no partial write); precedence SQL (override>default, absence->reply).
- Both templates redeployed + runData confirms fail-closed suppression on both channels; fresh bot inherits the gate; all test clones deactivated.
</verification>

<success_criteria>
- Schema live on the gate's DB (SUP-01) with default-deny RLS.
- Webhook upsert + precedence + malformed-rejection proven live (SUP-02, SUP-04).
- Both gated templates fail-closed by runData; fresh bot inherits the gate (SUP-03, SUP-05).
- No dev clone left active.
</success_criteria>

<output>
After completion, create `.planning/phases/09-semi-auto-suppression/09-04-SUMMARY.md` recording the curl/runData/propagation verdicts + the A1 boolean outcome.
</output>
