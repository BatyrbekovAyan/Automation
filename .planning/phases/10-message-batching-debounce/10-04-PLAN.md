---
phase: 10-message-batching-debounce
plan: 04
type: execute
wave: 3
depends_on: [10-02, 10-03]
files_modified:
  - .planning/phases/10-message-batching-debounce/10-HUMAN-UAT.md
autonomous: false
requirements: [BATCH-01, BATCH-02, BATCH-03]
must_haves:
  truths:
    - "On a device, two+ text fragments to an auto-reply bot produce exactly ONE combined reply on BOTH channels"
    - "A single complete message still gets one reply after the window; the humanizer pauses feel unchanged"
    - "In a «Вместе» chat, rapid incoming fragments refresh the suggestion cards ONCE (coalesced), while manual refresh and card-pick still respond immediately"
    - "A semi-auto chat still skips the whole reply path (Phase-9 gate before the debounce — no wait, no reply)"
  artifacts:
    - path: ".planning/phases/10-message-batching-debounce/10-HUMAN-UAT.md"
      provides: "the owner e2e runbook + recorded verdicts (auto combine + suggestions coalesce, both channels)"
      contains: "фрагмент"
  key_links:
    - from: "multi-fragment customer message"
      to: "one combined reply / one coalesced card refresh"
      via: "server debounce+combine (10-01/10-03) + client debounce (10-02)"
      pattern: "ONE"
---

<objective>
The phase-closing HUMAN-UAT gate: the owner exercises the whole batching feature end-to-end on ONE device build across both channels — the auto-reply combine (multi-fragment -> one reply) AND the client-side suggestions coalesce (rapid incoming -> one card refresh) — plus the composition guarantee that a semi-auto chat still skips the path (Phase-9 gate before the debounce). This is the only verification Claude cannot run: it needs the client debounce (10-02) in a device build, the live dev n8n templates (10-03), a tunnel, and real WhatsApp/Telegram profiles. Structural + runData checks already passed in 10-03; this is behavioral confirmation of the user-observable result.

Purpose: ROADMAP Phase 10 success criteria 1-4 are user-observable (one reply arrives; single message still replies; suggestions coalesce; semi-auto skips) and can only be confirmed with real fragments on a real device.
Output: `.planning/phases/10-message-batching-debounce/10-HUMAN-UAT.md` with recorded pass/fail verdicts; on all-PASS the phase closes.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Design (Testing section = the e2e scenarios) + CONTEXT worked example
@docs/superpowers/specs/2026-07-15-message-batching-debounce-design.md
@.planning/phases/10-message-batching-debounce/10-CONTEXT.md
@.planning/phases/10-message-batching-debounce/10-03-SUMMARY.md

# The Phase-9 closing-gate precedent (HUMAN-UAT runbook shape to mirror)
@.planning/phases/09-semi-auto-suppression/09-05-PLAN.md

<environment_notes>
- USER-ASSISTED: dev n8n (localhost:5678) + tunnel (Tools/n8n/rotate-tunnel.py) + a real authorized WhatsApp profile AND a real authorized Telegram profile + a device build carrying the 10-02 client debounce. `secrets.json` deny-ruled for Claude.
- The per-bot reply-workflow clone must be ACTIVE only during the test window and DEACTIVATED immediately after (real contacts). Confirm the 10-03 test clones were already deactivated.
- Prod bagkz stays DORMANT — this phase folds into the future one-shot bulk copy.
- The debounce adds ~window-length latency (~8s) to EVERY auto-reply — this is the accepted cost; the runbook says so, so a "slow reply" is not logged as a defect.
- The humanizer pauses (Pause Before Reading / Reading Pause / Typing Pause) are UNCHANGED — confirm they still feel natural.
</environment_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 10-HUMAN-UAT.md owner e2e runbook</name>
  <files>.planning/phases/10-message-batching-debounce/10-HUMAN-UAT.md</files>
  <read_first>
    - docs/superpowers/specs/2026-07-15-message-batching-debounce-design.md (Testing section — the auto curl matrix + the client 3-rapid->1 scenario)
    - .planning/phases/10-message-batching-debounce/10-CONTEXT.md (Specific Ideas — the «есть колодки» / «на камри 70?» worked example)
    - .planning/phases/09-semi-auto-suppression/09-05-PLAN.md (the HUMAN-UAT runbook shape: pre-flight + post-run blocks, per-scenario EXPECT + PASS/FAIL, verdict table, final disposition line)
  </read_first>
  <action>
Write `.planning/phases/10-message-batching-debounce/10-HUMAN-UAT.md` as an owner-run checklist. Include a pre-flight block (dev n8n up, tunnel up, both templates redeployed with the debounce per 10-03, the test bot's reply-workflow clone ACTIVATED for the window, real WhatsApp + Telegram profiles authed, the device build carrying the 10-02 client debounce installed) and a post-run block (DEACTIVATE the clone; prod stays dormant). State the accepted ~8s per-reply latency up front so a slow reply is not logged as a defect. Each scenario has an explicit EXPECT and a PASS/FAIL line:

1. **WhatsApp multi-fragment combine** — the customer sends «есть колодки» then «на камри 70?» ~1s apart to an AUTO-mode bot. EXPECT: exactly ONE bot reply arrives (after ~8s), and it answers BOTH fragments (grounded in the combined text) — not two replies, not an answer to only the first fragment.
2. **WhatsApp single message** — the customer sends one complete message. EXPECT: exactly one reply after the window; the humanizer read/type pauses still feel natural (unchanged).
3. **Telegram multi-fragment combine** — repeat scenario 1 on a Telegram-authed bot. EXPECT: same — one combined reply.
4. **Suggestions coalesce («Вместе»)** — open a «Вместе» chat; have the customer send 2-3 rapid fragments. EXPECT: the suggestion cards refresh ONCE (after ~2.5s), grounded in the latest/combined incoming — not a flicker of N refreshes. Sub-checks: tapping the manual refresh mid-burst responds IMMEDIATELY; tapping a card re-clusters IMMEDIATELY (neither is delayed by the debounce). Do this on BOTH channels.
5. **Composition sanity (semi-auto skips the path)** — a chat in «Вместе» receives a multi-fragment message. EXPECT: NO auto-reply and the chat stays unread (the Phase-9 suppression gate runs BEFORE the debounce — the semi-auto chat skips the whole path, no wait), while the suggestions panel still populates (coalesced per scenario 4). Also confirm the «Бот работает/пауза» activation switch is untouched.

Add a verdict table (scenario | channel | expected | result | notes) and a final disposition line (ALL PASS -> phase closes -> `/gsd-secure-phase 10`; any FAIL -> gap round).
  </action>
  <verify>
    <automated>test -f .planning/phases/10-message-batching-debounce/10-HUMAN-UAT.md && grep -qi "фрагмент\|fragment" .planning/phases/10-message-batching-debounce/10-HUMAN-UAT.md && grep -qi "Telegram" .planning/phases/10-message-batching-debounce/10-HUMAN-UAT.md && grep -qi "Вместе" .planning/phases/10-message-batching-debounce/10-HUMAN-UAT.md && grep -qiE "коалес|coalesc|один раз|ONE" .planning/phases/10-message-batching-debounce/10-HUMAN-UAT.md && echo UAT_DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/10-message-batching-debounce/10-HUMAN-UAT.md` exists
    - It contains all 5 scenarios, each with an explicit EXPECT and a PASS/FAIL field
    - Scenario 4 includes the manual-refresh-immediate AND card-pick-immediate sub-checks (not debounced)
    - Scenario 5 includes the "semi-auto chat skips the path / stays unread" AND "«Бот работает/пауза» untouched" sub-checks
    - It names BOTH channels (WhatsApp + Telegram), the ~8s accepted latency note, and the auto-combine + suggestions-coalesce halves
    - It has a pre-flight (clone ACTIVE for window; device build with the client debounce) + post-run (clone DEACTIVATED; prod dormant) block
  </acceptance_criteria>
  <done>The owner runbook exists with all 5 scenarios (auto combine + suggestions coalesce + composition), expected results, the immediate-action sub-checks, and the clone activate/deactivate discipline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner runs the batching e2e on one build (BATCH-01, BATCH-02, BATCH-03)</name>
  <files>.planning/phases/10-message-batching-debounce/10-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/10-message-batching-debounce/10-HUMAN-UAT.md (the runbook authored in Task 1)
    - .planning/phases/10-message-batching-debounce/10-03-SUMMARY.md (confirm the live server side + runData matrix passed before behavioral e2e)
  </read_first>
  <what-built>
Everything: the debounce+combine stage in both bot templates (live, runData-verified, 10-03) and the Unity client debounce that coalesces rapid incoming suggestions (10-02, in this build). This gate confirms the user-observable behavior a customer + owner would experience.
  </what-built>
  <action>
Owner installs the build (with the 10-02 client debounce), brings dev n8n + tunnel up, activates the test bot's reply-workflow clone for the window, and runs all 5 scenarios in `10-HUMAN-UAT.md` on BOTH channels. Fill in each PASS/FAIL + notes. Key observations:
- Scenario 1/3: exactly ONE combined reply arrives (not two, not first-only) on WhatsApp and Telegram.
- Scenario 2: a single message still replies once; the humanizer pauses feel unchanged.
- Scenario 4: the suggestion cards refresh ONCE on a rapid burst (coalesced), while manual refresh + card-pick still respond immediately — on both channels.
- Scenario 5: a «Вместе» chat with a multi-fragment message gets NO auto-reply and stays unread (the suppression gate runs before the debounce), suggestions still populate, and the «Бот работает/пауза» switch is untouched.
If the ~8s window feels too long/short for perceived single-message latency, note the preferred value (the tuning knob lives in `apply-message-batching.py`'s `Debounce Wait amount` + the client `WindowSeconds`); a tuning change re-enters via a small gap plan, not this gate. DEACTIVATE the clone when done.
  </action>
  <how-to-verify>
Scenario 1/3: one combined reply on both channels. Scenario 2: one reply, natural pauses. Scenario 4: one coalesced card refresh + immediate manual/card actions. Scenario 5: semi-auto chat not replied + stays unread + suggestions populate + activation switch works. All 5 PASS -> Gate closes.
  </how-to-verify>
  <verify>Owner-run on-device e2e across both channels; verdicts recorded in 10-HUMAN-UAT.md.</verify>
  <acceptance_criteria>
    - All 5 scenarios recorded PASS in `10-HUMAN-UAT.md` (or gaps filed with the failing scenario + observed behavior)
    - Scenario 1 AND 3 confirm exactly ONE combined reply (auto combine) on WhatsApp and Telegram respectively
    - Scenario 4 confirms the coalesced single card refresh AND that manual refresh + card-pick stay immediate (BATCH-03)
    - Scenario 5 confirms the semi-auto chat skips the path (no reply, stays unread, suggestions still populate) AND the untouched «Бот работает/пауза» switch
    - The test reply-workflow clone was DEACTIVATED after the window (real-contacts constraint); prod bagkz untouched
  </acceptance_criteria>
  <done>The owner confirms multi-fragment -> one combined reply and rapid-incoming -> one coalesced suggestion refresh on both channels, with the semi-auto gate still short-circuiting the path; on all-PASS the phase is behaviorally complete and ready for `/gsd-secure-phase 10`.</done>
  <resume-signal>Type "UAT pass" (or list failing scenarios + observed behavior). Confirm the test clone is deactivated.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real multi-fragment customer message -> live debounce bot (dev clone, active for the window) | the combine behavior is exercised with real contacts |
| owner device build -> live suggestions provider | the client debounce coalesce is exercised end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Elevation / Safety | test reply-workflow clone left ACTIVE | mitigate | Clone ACTIVE only for the e2e window, DEACTIVATED in the same run (real contacts) — acceptance requires it; pre-flight/post-run blocks enforce it |
| T-10-04-02 | Tampering / ordering | a semi-auto chat waiting/replying (gate-vs-debounce order) | mitigate | Scenario 5 directly observes that a «Вместе» multi-fragment chat gets no reply and stays unread (the Phase-9 gate runs before the debounce — the path is skipped, no wait); suggestions still populate |
| T-10-04-03 | Tampering | prod bagkz accidental target | accept/mitigate | Prod stays DORMANT; all e2e is dev-only; the debounce folds into the future one-shot bulk copy |
</threat_model>

<verification>
- 10-HUMAN-UAT.md authored with all 5 scenarios + immediate-action + composition sub-checks (Claude-verifiable grep).
- Owner e2e: multi-fragment -> one combined reply (both channels); single message -> one reply; suggestions coalesce (one refresh, immediate manual/card); semi-auto chat skips the path + stays unread; activation switch untouched; test clone deactivated (owner-recorded).
</verification>

<success_criteria>
- The owner confirms behaviorally: multi-fragment -> ONE combined reply on WhatsApp and Telegram (BATCH-01/02); rapid incoming -> ONE coalesced suggestion refresh with immediate manual/card actions (BATCH-03); a single message still replies once; a semi-auto chat still skips the path (ROADMAP Phase 10 success criteria 1-4).
- No dev clone left active; prod dormant.
</success_criteria>

<output>
After completion, create `.planning/phases/10-message-batching-debounce/10-04-SUMMARY.md` with the recorded UAT verdicts and the phase disposition.
</output>
