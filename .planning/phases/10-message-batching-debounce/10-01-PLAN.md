---
phase: 10-message-batching-debounce
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Tools/n8n/apply-message-batching.py
  - Tools/n8n/verify-message-batching.py
  - Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json
  - Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json
  - Tools/n8n/README.md
autonomous: true
requirements: [BATCH-01, BATCH-02]
must_haves:
  truths:
    - "Both bot templates carry a 4-node debounce stage (Debounce Wait -> Fetch Recent -> Latest+Combine -> Is Latest?) spliced onto the Suppressed? FALSE branch, before Input type"
    - "The Latest+Combine Code node re-emits the webhook body so Input type / Download Audio / Text keep resolving $json.body"
    - "Fetch Recent GETs messages/get with limit only (NO mark_all) so the chat is not marked read during the wait"
    - "Is Latest? dead-ends aborted fragments (main[0]) and proceeds the winner to Input type (main[1]); the debounce sits AFTER the Phase-9 Suppressed? gate"
  artifacts:
    - path: "Tools/n8n/apply-message-batching.py"
      provides: "idempotent by-node-name splice of the 4 debounce nodes into BOTH templates"
      contains: "Latest+Combine"
    - path: "Tools/n8n/verify-message-batching.py"
      provides: "structural verifier asserting the splice landed identically in both templates + the fail-safe invariants"
      contains: "combinedText"
    - path: "Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json"
      provides: "WhatsApp template with the debounce stage on api/sync"
      contains: "Debounce Wait"
    - path: "Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json"
      provides: "Telegram template with the debounce stage on tapi/sync"
      contains: "Debounce Wait"
  key_links:
    - from: "Suppressed? (main[1], not-suppressed)"
      to: "Debounce Wait"
      via: "rewired connection (was -> Input type)"
      pattern: "Debounce Wait"
    - from: "Latest+Combine Code node"
      to: "Input type"
      via: "re-emitted body + Is Latest? main[1] -> Input type"
      pattern: "combinedText"
    - from: "Fetch Recent"
      to: "messages/get"
      via: "httpRequest GET, cred EuhhqAaV56DpoqAN, limit only, no mark_all"
      pattern: "messages/get"
---

<objective>
Splice the pre-generation debounce+combine stage into BOTH bot templates (BATCH-01, BATCH-02) via one idempotent by-node-name Python migration. Four new nodes — `Debounce Wait` (~8s in-memory pause) -> `Fetch Recent` (messages/get) -> `Latest+Combine` (Code: is-latest + combine, re-emits body) -> `Is Latest?` (If: dead-end aborted fragments) — are inserted on the Phase-9 `Suppressed?` node's FALSE branch (`main[1]`), before `Input type`. Only the last fragment's execution proceeds; earlier fragments dead-end. Pure structural authoring — no live n8n/DB. Live deploy + runData verification is 10-03 (owner gate).

Purpose: This is the whole auto-reply half of the phase — a multi-fragment customer message becomes ONE combined reply. Without the splice there is nothing to deploy.
Output: `Tools/n8n/apply-message-batching.py` (new), `Tools/n8n/verify-message-batching.py` (new), both `*-Bot.json` edited in place, README bump.
</objective>

<execution_context>
@/Users/ayan/Projects/Automation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayan/Projects/Automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Canonical design + research + patterns (READ before authoring — the Code body + splice coords live here)
@docs/superpowers/specs/2026-07-15-message-batching-debounce-design.md
@.planning/phases/10-message-batching-debounce/10-RESEARCH.md
@.planning/phases/10-message-batching-debounce/10-PATTERNS.md
@.planning/phases/10-message-batching-debounce/10-CONTEXT.md

# Migration + verifier analogs to copy structurally
@Tools/n8n/apply-rag-fixes.py
@Tools/n8n/verify-telegram-parity.py

# The two templates being edited (both structurally identical at the splice point)
@Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json
@Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json

<interfaces>
<!-- VERIFIED against the live committed JSON this session (2026-07-20). Do NOT re-derive. -->

CURRENT wiring (IDENTICAL in both templates, post Phase-9 splice):
  'If':              main[0] -> ['Read Reply Mode']            # group-chat filter (from==chatId); groups dead-end (no main[1])
  'Read Reply Mode': main[0] -> ['Suppressed?']               # Phase-9 Postgres gate
  'Suppressed?':     main    -> [ [], [ {node:'Input type'} ] ]  # main[0] TRUE(suppressed)=dead-end, main[1] FALSE=Input type  <-- SPLICE HERE
  'Input type':      main[0] -> ['Text']  main[1] -> ['Download Audio']  main[2..4] -> ['Ask to Send Text']
  'Text':            main[0] -> ['AI Agent']

Text set node CURRENT value (both templates, node 'Text', n8n-nodes-base.set tv 3.4):
  assignments[0].value = "={{ $('Webhook').item.json.body.messages[0].body }}"

Node-shape sources to clone (verified typeVersions):
  Wait:        'Reading Pause'  n8n-nodes-base.wait        tv 1.1   params {"amount": <expr|seconds>}
  httpRequest: 'Mark Read'      n8n-nodes-base.httpRequest tv 4.2   cred httpHeaderAuth id EuhhqAaV56DpoqAN name "WappiAuthToken"
                 WA Mark Read url = https://wappi.pro/api/sync/message/mark/read   (base = https://wappi.pro/api/sync/)
                 TG Mark Read url = https://wappi.pro/tapi/sync/message/mark/read  (base = https://wappi.pro/tapi/sync/)
  Code:        'Count Input Words'  n8n-nodes-base.code    tv 2     jsCode uses $('Webhook').first().json  (NOT .item)
  If:          'Suppressed?'    n8n-nodes-base.if          tv 2.2   boolean condition, operation:"true", singleValue:true, typeValidation:"loose"

CRITICAL data-flow fact (VERIFIED): 'Input type' reads "={{ $json.body.messages[0].type }}" and 'Download Audio'
reads $json.body.messages[0].file_link — BARE $json.body. An HTTP fetch + Code node between Suppressed? and
Input type REPLACES the item, so $json.body evaporates unless Latest+Combine re-emits it (Pitfall 1, non-negotiable).

Channel divergence (VERIFIED): TG 'Input type' text rule already matches type=="chat" OR type=="text" (combinator "or").
So the Latest+Combine `isText = type==='chat' || type==='text'` body is channel-agnostic — ONE Code body works in BOTH.
Only the Fetch Recent base URL differs (api/sync vs tapi/sync) — derive it per template from that template's Mark Read url.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author + run apply-message-batching.py (splice 4 nodes into BOTH templates) (BATCH-01, BATCH-02)</name>
  <files>Tools/n8n/apply-message-batching.py, Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json, Tools/n8n/workflows/4VN3gsFaC2HUYmcc-Telegram_Bot.json</files>
  <read_first>
    - Tools/n8n/apply-rag-fixes.py (COPY the skeleton: `load`/`save` with indent=2 ensure_ascii=False no-trailing-newline, `find(nodes, name=/type_suffix=)`, stable `uuid5` node ids keyed off `wf["id"]+"-<name>"`, `find(...) is None` idempotency guard, the `for fname in BOT_IDS` both-template loop — lines 14-56, 113-122, 168-172)
    - Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json (the Mark Read node shape/cred, the Reading Pause Wait shape, a Code node shape, the Suppressed? If shape, the Text set node, and the connections block)
    - .planning/phases/10-message-batching-debounce/10-RESEARCH.md (Code Examples section — the VERBATIM Latest+Combine Code body + Fetch Recent + Debounce Wait shapes; Pitfalls 1-5)
    - .planning/phases/10-message-batching-debounce/10-PATTERNS.md (apply-message-batching.py section — the splice-specific rewire note)
  </read_first>
  <action>
Create `Tools/n8n/apply-message-batching.py` copying `apply-rag-fixes.py`'s skeleton (`import json, os, uuid`; same `REPO`/`WF`/`BOT_IDS = ("4wYitz5ek30SVNlT-WhatsApp_Bot.json", "4VN3gsFaC2HUYmcc-Telegram_Bot.json")`; same `load`/`save`/`find`; same `for fname in BOT_IDS: wf=load; splice(wf); save` main loop). Add a module docstring naming it "the message-batching / debounce splice (Phase 10, BATCH-01/02)".

Write a `splice(wf)` function that is IDEMPOTENT (re-running is a no-op) and, per template, does ALL of the following:

**(1) Derive the channel base** from this template's own Mark Read node so WhatsApp gets `api/sync` and Telegram gets `tapi/sync`:
```python
mark_read = find(wf["nodes"], name="Mark Read")
base = mark_read["parameters"]["url"].rsplit("message/mark/read", 1)[0]   # -> https://wappi.pro/{api|tapi}/sync/
```

**(2) Add the 4 nodes** (guard each with `find(nodes, name=...) is None`; stable id = `str(uuid.uuid5(uuid.NAMESPACE_DNS, wf["id"] + "-<nodename>"))`; position each offset below `Suppressed?`'s position so the graph stays readable):

- **`Debounce Wait`** — `n8n-nodes-base.wait`, `typeVersion: 1.1`, `parameters: {"amount": 8}` (< 65s -> in-memory resume; 8s per CONTEXT, single tunable).
- **`Fetch Recent`** — `n8n-nodes-base.httpRequest`, `typeVersion: 4.2`. Params (clone Mark Read's auth shape, drop mark_all, GET on messages/get):
  ```python
  {
    "method": "GET",
    "url": base + "messages/get",
    "authentication": "genericCredentialType",
    "genericAuthType": "httpHeaderAuth",
    "sendQuery": True,
    "queryParameters": {"parameters": [
      {"name": "profile_id", "value": "={{ $('Webhook').item.json.body.messages[0].profile_id }}"},
      {"name": "chat_id",    "value": "={{ $('Webhook').item.json.body.messages[0].chatId }}"},
      {"name": "limit",      "value": "15"}
    ]},
    "options": {}
  }
  ```
  credentials block: `{"httpHeaderAuth": {"id": "EuhhqAaV56DpoqAN", "name": "WappiAuthToken"}}`. NO `mark_all` param (Pitfall 5 — marking read during the wait would defeat the humanizer Mark Read).
- **`Latest+Combine`** — `n8n-nodes-base.code`, `typeVersion: 2`, `parameters.jsCode` = the VERBATIM channel-agnostic body (re-emits body — Pitfall 1; `.first()` not `.item` — paired-item safety across Wait+HTTP; sort by `time` desc — Pitfall 3; `chat||text` — Pitfall 4):
  ```javascript
  const wh = $('Webhook').first().json;
  const triggeringId = wh.body.messages[0].id;

  const fetched = ($json.messages || []).slice();
  fetched.sort((a, b) => (b.time || 0) - (a.time || 0));

  const isText = (m) => m && (m.type === 'chat' || m.type === 'text');
  const incoming = fetched.filter(m => m && m.fromMe === false);
  const newestIncoming = incoming[0];

  let abort = false;
  let combinedText = null;

  if (!newestIncoming || newestIncoming.id !== triggeringId) {
    abort = true;
  } else if (isText(newestIncoming)) {
    const parts = [];
    for (const m of fetched) {
      if (m.fromMe === true) break;
      if (!isText(m)) break;
      parts.push(typeof m.body === 'string' ? m.body : '');
    }
    parts.reverse();
    combinedText = parts.join('\n');
  }

  return [{ json: { ...wh, abort, combinedText } }];
  ```
- **`Is Latest?`** — `n8n-nodes-base.if`, `typeVersion: 2.2`. Clone the `Suppressed?` If node's params but read `={{ $json.abort }}`: one boolean condition, `operator: {type:"boolean", operation:"true", singleValue:true}`, `options.typeValidation:"loose"`, `combinator:"and"`.

**(3) Rewire the connections** (overwrite idiom — `apply-rag-fixes.py:109-110`):
```python
conns = wf["connections"]
conns["Suppressed?"]  = {"main": [[], [{"node": "Debounce Wait", "type": "main", "index": 0}]]}   # main[0] dead-end kept; main[1] now -> Debounce Wait
conns["Debounce Wait"]  = {"main": [[{"node": "Fetch Recent",   "type": "main", "index": 0}]]}
conns["Fetch Recent"]   = {"main": [[{"node": "Latest+Combine", "type": "main", "index": 0}]]}
conns["Latest+Combine"] = {"main": [[{"node": "Is Latest?",     "type": "main", "index": 0}]]}
conns["Is Latest?"]     = {"main": [[], [{"node": "Input type", "type": "main", "index": 0}]]}    # main[0] TRUE(abort)=dead-end, main[1] FALSE=Input type
```
Leave `If`, `Read Reply Mode`, `Input type`, `Text`->`AI Agent`, and everything downstream UNTOUCHED.

**(4) Edit the `Text` set node value** to inject combinedText with the single-message fallback. The fallback reads bare `$json.body...` (NOT `$('Webhook').item...`): `Latest+Combine` re-emits `body` onto the current item, so this matches how `Input type`/`Download Audio` read `$json.body` — and the `.item` back-reference across the inserted Wait+HTTP+Code nodes is fragile paired-item resolution. (It is dead code on the winning text path anyway — combinedText is always set there.)
```python
find(wf["nodes"], name="Text")["parameters"]["assignments"]["assignments"][0]["value"] = \
  "={{ $json.combinedText ?? $json.body.messages[0].body }}"
```

Then RUN it: `python3 Tools/n8n/apply-message-batching.py`. Run it a SECOND time to prove idempotency (git diff must be empty on the second run). Both templates must remain valid JSON with the 4 new nodes and the rewired connections.
  </action>
  <verify>
    <automated>python3 Tools/n8n/apply-message-batching.py && python3 -c "
import json
for f in ['4wYitz5ek30SVNlT-WhatsApp_Bot.json','4VN3gsFaC2HUYmcc-Telegram_Bot.json']:
    wf=json.load(open('Tools/n8n/workflows/'+f)); names={n['name'] for n in wf['nodes']}; c=wf['connections']
    assert {'Debounce Wait','Fetch Recent','Latest+Combine','Is Latest?'} <= names, f
    assert c['Suppressed?']['main'][1][0]['node']=='Debounce Wait', f
    assert c['Is Latest?']['main'][1][0]['node']=='Input type', f
    assert c['Is Latest?']['main'][0]==[], f
    print('SPLICE_OK', f)
"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile Tools/n8n/apply-message-batching.py` exits 0
    - Both templates are valid JSON (`json.load` exits 0) and both contain nodes named `Debounce Wait`, `Fetch Recent`, `Latest+Combine`, `Is Latest?`
    - `grep -c '"amount": 8' Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json` >= 1 (Debounce Wait 8s) AND same for the Telegram file
    - WhatsApp `Fetch Recent` url is `https://wappi.pro/api/sync/messages/get`; Telegram is `https://wappi.pro/tapi/sync/messages/get` (grep each file)
    - Neither `Fetch Recent` node carries a `mark_all` param: `grep -A15 "messages/get" <file>` shows no `mark_all` in that node (Pitfall 5)
    - The `Latest+Combine` jsCode contains `$('Webhook').first().json` (NOT `.item`), `fetched.sort`, `m.type === 'chat' || m.type === 'text'`, and `return [{ json: { ...wh, abort, combinedText } }]` (body re-emit) — in BOTH files
    - Connections: `Suppressed?` main[1] -> `Debounce Wait`; `Debounce Wait` -> `Fetch Recent` -> `Latest+Combine` -> `Is Latest?`; `Is Latest?` main[1] -> `Input type`, main[0] == `[]` (dead-end) — in BOTH files (the python assert in `<verify>` proves this)
    - The `Text` node value is `={{ $json.combinedText ?? $json.body.messages[0].body }}` in BOTH files (bare `$json.body` fallback — the Code node re-emits body onto the current item)
    - Running the migration a second time produces an EMPTY `git diff` on both templates (idempotent)
    - `git diff` on each template touches ONLY the 4 new nodes + the 5 rewired connection entries + the Text value (nothing downstream of `Input type`)
  </acceptance_criteria>
  <done>Both templates carry the byte-identical debounce stage on the Suppressed? FALSE branch; the Code node re-emits body; Fetch Recent has no mark_all; the migration is idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: Structural verifier + README bump (BATCH-01, BATCH-02)</name>
  <files>Tools/n8n/verify-message-batching.py, Tools/n8n/README.md</files>
  <read_first>
    - Tools/n8n/verify-telegram-parity.py (COPY the structure: argparse `--dir` default = committed workflows/, `assert_that(cond, reason)` helper, per-file asserts, exit 0 + "ALL ... PASSED" / exit 1 + "FAIL: <reason>")
    - Tools/n8n/workflows/4wYitz5ek30SVNlT-WhatsApp_Bot.json (the spliced result from Task 1 — the shape the verifier asserts)
    - Tools/n8n/README.md (the `13 workflows the app actually depends on` line + the template descriptions to extend)
  </read_first>
  <action>
**(a) `Tools/n8n/verify-message-batching.py`** — copy `verify-telegram-parity.py`'s skeleton (argparse `--dir` overriding the default committed `workflows/` dir so the SAME asserts can gate a prod re-export; `assert_that`/fail helpers; exit 0 with "ALL BATCHING ASSERTS PASSED", exit 1 with "BATCHING FAIL: <reason>"). For EACH of the two bot templates assert the fail-safe invariants (these are exactly the subtle correctness properties a UI round-trip or a bad edit would silently drop):

1. Nodes `Debounce Wait`, `Fetch Recent`, `Latest+Combine`, `Is Latest?` all exist.
2. `Debounce Wait` is `n8n-nodes-base.wait` with `amount == 8`.
3. `Fetch Recent` is a GET on `<base>messages/get`, binds cred id `EuhhqAaV56DpoqAN`, and its query params are exactly `{profile_id, chat_id, limit}` with NO `mark_all` (Pitfall 5).
4. `Latest+Combine` jsCode contains `$('Webhook').first().json` (not `.item`), a `time` sort, the `chat || text` test, and re-emits body (`...wh` / `...` spread with `abort` + `combinedText`) — Pitfalls 1/3/4.
5. Connections: `Suppressed?` main[1] -> `Debounce Wait`; the 3-node chain; `Is Latest?` main[1] -> `Input type` AND main[0] is `[]` (aborted fragment dead-ends). This proves the debounce sits AFTER the Phase-9 gate (BATCH ordering) and only the winner proceeds.
6. The `Text` node value equals `={{ $json.combinedText ?? $('Webhook').item.json.body.messages[0].body }}`.
7. Cross-template identity: the `Latest+Combine` jsCode is byte-identical between the two templates (channel-agnostic), and the `Fetch Recent` differs ONLY in the base (`api/sync` vs `tapi/sync`).

Run it: `python3 Tools/n8n/verify-message-batching.py` -> must exit 0 / print "ALL BATCHING ASSERTS PASSED".

**(b) `Tools/n8n/README.md`** — the workflow COUNT is unchanged (no new canonical workflow — this edits two existing templates). Add a short note to the WhatsApp_Bot / Telegram_Bot descriptions: "Phase 10: a pre-generation `Debounce Wait -> Fetch Recent -> Latest+Combine -> Is Latest?` stage on the `Suppressed?` FALSE branch coalesces multi-fragment messages into one reply; re-run `apply-message-batching.py` after any template re-import; `verify-message-batching.py` gates the splice." Do NOT bump the `13 workflows` count.
  </action>
  <verify>
    <automated>python3 Tools/n8n/verify-message-batching.py && grep -qi "Debounce Wait\|message-batching\|apply-message-batching" Tools/n8n/README.md && echo VERIFY_README_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m py_compile Tools/n8n/verify-message-batching.py` exits 0
    - `python3 Tools/n8n/verify-message-batching.py` exits 0 and prints "ALL BATCHING ASSERTS PASSED"
    - The verifier asserts (grep the script): the 4 node names, `amount`==8, `messages/get` GET with cred `EuhhqAaV56DpoqAN`, absence of `mark_all` in Fetch Recent, the body re-emit in the Code node, the `Suppressed? main[1] -> Debounce Wait` and `Is Latest? main[1] -> Input type` + `main[0]==[]` connections, and the Text combinedText fallback
    - The verifier supports `--dir` (grep `--dir` / `argparse`) so a prod re-export can be gated later
    - `Tools/n8n/README.md` mentions the debounce splice + `apply-message-batching.py` AND still reads `13 workflows` (count NOT bumped — no new canonical workflow)
    - Deliberately corrupting a template (e.g. adding `mark_all` to Fetch Recent) makes the verifier exit 1 — restore afterward (spot-check, note in SUMMARY)
  </acceptance_criteria>
  <done>The structural verifier greens on both spliced templates and would catch a dropped body re-emit / added mark_all / broken rewire; README documents the splice; workflow count unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| incoming customer message (wappi/tapi Webhook) -> Debounce stage | untrusted `messages[0]` fragments enter the fetch + combine |
| bot template -> Wappi/tapi messages/get | Fetch Recent reads recent messages with the existing WappiAuthToken cred |
| combined customer text -> AI Agent | concatenated fragments feed the LLM input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | combined text as prompt-injection | accept | No NEW injection channel — the AI Agent already receives customer text today; combine only concatenates the same fragments the LLM would have seen sequentially (RESEARCH V5 partial). The Suggest/agent prompt hardening is unchanged |
| T-10-01-02 | Information Disclosure / side-effect | Fetch Recent marking the chat read early | mitigate | Fetch Recent uses `profile_id`+`chat_id`+`limit` ONLY — NO `mark_all`; the chat is marked read only by the deliberate downstream humanizer `Mark Read` after the reply decision (Pitfall 5). Verifier asserts absence of mark_all |
| T-10-01-03 | Tampering / ordering | suppression-bypass if debounce spliced before the gate | mitigate | The stage is spliced on `Suppressed?` `main[1]` (FALSE/not-suppressed) — a semi-auto chat still dead-ends at `Suppressed?` main[0] and never enters the wait/fetch. Verifier asserts the `Suppressed? -> Debounce Wait` edge (BATCH sits AFTER the Phase-9 gate) |
| T-10-01-04 | Denial of Service | rapid-fragment flood -> many concurrent waiting executions | accept | Each fragment aborts after one fetch; sub-65s waits stay in memory (no DB growth); bounded by the dev-only instance. Prod bagkz dormant |
</threat_model>

<verification>
- `apply-message-batching.py` compiles, runs, and is idempotent; both templates gain the 4-node stage on `Suppressed?` main[1] with the exact rewire.
- The Code node re-emits body (`...wh`), sorts by time, treats chat||text as text, and uses `.first()`; Fetch Recent has no mark_all; base is api/sync (WA) vs tapi/sync (TG).
- `verify-message-batching.py` greens and would catch a dropped invariant.
- README documents the splice; workflow count unchanged.
- NOTE: live redeploy + runData abort-vs-combine proof are 10-03 (owner gate) — NOT this plan.
</verification>

<success_criteria>
- Both bot templates carry the identical debounce+combine stage on the not-suppressed branch, before `Input type` (BATCH-01).
- One `messages/get` fetch (limit only, no mark_all) drives both is-latest and combine; the Code node re-emits body and is channel-agnostic (BATCH-02).
- The migration is idempotent and the structural verifier is green.
</success_criteria>

<output>
After completion, create `.planning/phases/10-message-batching-debounce/10-01-SUMMARY.md`.
</output>
